# Change Log

## 当前进展

- 已继续推进 Lua 5.4 no-base `tonumber()` 的 JIT recorder：新增逗号常量 locale 回归，覆盖 `C` numeric locale 下 `tonumber("3,4") == nil` 仍可记录 trace，以及本机存在逗号小数点 locale 时 `tonumber("3,4") == 3.4` 也可记录 trace。旧实现先红于 “Lua 5.4 tonumber comma constant C-locale recorder did not record IR_CALLS”，因为 recorder 遇到逗号常量直接 NYI。
- `src/lj_ffrecord.c` 现在把逗号常量交给 runtime scanner guard，而不是在记录期折叠；`src/lj_strscan.c` / `src/lj_strscan.h` / `src/lj_ircall.h` 新增 side-effect `numtype` / `tonum` helper，让 trace 每次按当前 numeric locale 判断和取值。locale 从 C 切到逗号小数或反向切换时会先侧退/重录，避免复用旧 locale 的 nil 或 numeric 结果。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `number_pack_helpers` 已加入 C locale 下逗号常量 nil 负载，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke；`assert_records_single_ir_trace()` 同步把 hotexit 阈值提高到 10，避免 loop-exit side trace 造成偶发假阴性，同时仍要求单次隔离运行恰好一条目标 trace。
- 已继续推进 Lua 5.4 `math.random` 的 JIT recorder：新增字符串整数区间回归，覆盖 `math.random(low_s, up_s)` 和 `math.random(single_s)` 的动态 string 参数仍按 `luaL_checkinteger()` 转换，并要求同一 seed 下 JIT on 热循环结果等于 JIT off 解释器。旧实现先红于 “Lua 5.4 math.random string interval recorder did not record IR_CALLS”，因为 random recorder 只接受 integer/number 参数。
- `src/lj_ffrecord.c` 的 `recff_lua54_random_intref()` 现在复用 `lj_strscan_tocheckintok54` / `lj_strscan_tocheckint54`，先 guard 动态字符串仍可转当前 integer，再调用 xoshiro256** side-effect helper。guard 失败时侧退出解释器，避免 trace 在参数错误前提前消耗 PRNG 状态。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `random_helpers` 已扩展字符串整数双参数/单参数区间，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 no-base `tonumber()` 的 JIT recorder：新增普通 invalid 动态字符串回归，要求 `"not-a-number"` / `"123abc"` 这类非 LuaJIT 扩展数字也能在单条 trace 内通过分类 helper guard 返回 `nil`。旧实现先红于 “Lua 5.4 tonumber no-base generic invalid recorder recorded fragmented traces”，因为 recorder 在发出分类 helper 后仍 abort，形成碎片 trace。
- `src/lj_ffrecord.c` 现在对 no-base 动态字符串分类结果 `fmt == 0` 也记录 guard 并返回 `nil`；如果后续字符串、locale 或 grammar 变化让分类变为合法 integer/float 或稳定拒绝项，会侧退回解释器或重录，避免把旧 nil 结果错误复用。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `number_pack_helpers` 已扩展普通 invalid 字符串路径，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 `tonumber(str, base)` 的 JIT recorder：新增动态 base 回归，覆盖显式 base 为动态 number、decimal string 和 hex string 时仍按 `luaL_checkinteger()` 转换，再执行 explicit-base integer scanner。旧实现先红于 “Lua 5.4 tonumber explicit-base dynamic base recorder did not record IR_CALLN”，因为 recorder 只接受常量 base。
- `src/lj_ffrecord.c` 新增 Lua 5.4 base 转换 TRef helper：动态 number base 通过 guarded `IR_CONV` 保留整数表示校验，动态 string base 通过 `lj_strscan_tocheckintok54` / `lj_strscan_tocheckint54` guard 后取整数值，并在进入 scanner 前 guard `2..36` 范围；`src/lj_strscan.c` / `src/lj_strscan.h` / `src/lj_ircall.h` 暴露相应 helper。base 或字符串后续变成非法值时侧退出解释器，保留官方错误路径。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `number_pack_helpers` 已扩展 dynamic base number/string 路径，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 `tonumber(str, base)` 的 JIT recorder：新增动态字符串 + 常量显式 base 回归，覆盖 `tonumber(hex, 16)` 成功返回 integer，以及 `0x` 前缀和 fraction 字符串在显式 base 下返回 `nil`。旧实现先红于 “Lua 5.4 tonumber explicit-base dynamic string recorder did not record IR_CALLN”，因为显式 base 动态字符串 scanner 仍退解释器。
- `src/lj_strscan.c` / `src/lj_strscan.h` 现在暴露 explicit-base scanner 的 JIT helper：一个 helper 判断字符串在给定 base 下是否可转整数，另一个在 guard 成功后返回当前 32 位 integer 表面结果；`src/lj_ffrecord.c` 对常量/动态 base 录入 helper call，并按当前样本 guard 成功/失败分支。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `number_pack_helpers` 已扩展 explicit-base 动态字符串成功/失败路径，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 `tonumber()` 的 JIT recorder：新增 no-base 动态字符串回归，要求 float 字符串热路径在 trace 内出现 `IR_STRTO`，integer 字符串保持 `math.type(...) == "integer"`，`nan` / `inf` / `0b` 这类 LuaJIT 扩展数字字符串仍返回 `nil`。旧实现先红于 “Lua 5.4 tonumber no-base recorder did not record IR_STRTO”，因为 Lua 5.4 模式下 `tonumber()` recorder 整体 NYI。
- `src/lj_strscan.c` / `src/lj_strscan.h` 现在暴露 Lua 5.4 数字字符串分类 helper 和 explicit-base scanner helper；`src/lj_ffrecord.c` 的 no-base `tonumber()` recorder 会保留 number passthrough 的 integer/float subtype，动态 float 字符串用 `IR_STRTO`，动态 integer 字符串用 guarded int helper，稳定拒绝的 LuaJIT-only 数字扩展和普通 invalid 字符串都用 guard 后返回 `nil`。`tonumber(str, base)` 的动态字符串已通过 helper call 记录成功/失败分支，动态 base 的 number/string 整数转换也已可记录；逗号 locale 常量后续已通过 runtime side-effect scanner helper 补齐。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `number_pack_helpers` 已扩展 no-base `tonumber()` float/integer/reject 三类负载，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 `math.random` 的 JIT recorder：在已有 `math.random()`、`math.random(0)` 和常量整数区间基础上，新增动态整数区间 `math.random(m, n)` / `math.random(n)` 热循环回归，要求 trace 内出现 `IR_CALLS` 且同一 seed 下序列等于 JIT off 解释器。旧实现先红于 “Lua 5.4 math.random dynamic interval recorder did not record IR_CALLS”，因为动态区间仍直接 NYI。
- `src/lj_ffrecord.c` 现在会把当前运行值可精确转为当前 `lua_Integer` 的动态 integer/number/string 参数录成 guarded integer ref，并在调用 xoshiro256** helper 前加 `up == 0`、`up >= 1`、`low <= up` 守卫；守卫失败先侧退回解释器，避免 trace 提前消费 PRNG 状态并保持官方参数错误顺序。更复杂动态转换仍退解释器，完整 64 位 integer 仍归入整数批次。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 的 `random_helpers` 已扩展到常量区间、动态双参数区间、动态单参数区间、full integer sample 和 float sample，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 locale 字符串有序比较的 JIT 支持：新增回归固定包含内嵌 NUL 的字符串在热循环中执行 `<` / `<=` / `>` / `>=` 时必须记录 trace。旧实现先红于 “Lua 5.4 locale string ordered comparison did not record a trace”，因为 recorder 为避免旧 bytewise `lj_str_cmp()` 语义而永久退出。
- `src/lj_ircall.h` 现在暴露 `IRCALL_lj_str_cmp_locale`，`src/lj_record.c` 在 Lua 5.4 模式下用同一个 `lj_str_cmp_locale()` 结果决定 guard 方向，并在 trace 内记录 side-effect call，让运行期比较始终按当前 C collate locale 和内嵌 NUL 分段语义执行，也避免跨 `os.setlocale()` 错误 CSE。
- 本功能点验证：新增用例先红后绿，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 新增 `string_order_helpers`，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 的 200 upvalue JIT 支持：新增回归构造 150 个 upvalue 的闭包，并在热循环中读写第 150 个 mutable upvalue，要求 `test/lua54_jit_regress.lua` 必须生成 trace。旧实现先红，`IR_UREFO` 把 upvalue index 左移 8 位后在 index >= 128 时撞到 IR literal 上限，只能永久退出解释器。
- `src/lj_ir.h` 现在为 `UREFO/UREFC` 定义统一编码宏，把 alias disambiguation hash 从 8 bit 收缩到 7 bit，保证 Lua 5.4 的 0..199 upvalue index 都能编码在 `< REF_BIAS` 的 literal 空间内；`src/lj_record.c`、各后端 asm、CSE/alias 分析和 `jit.dump` 已统一改用该编码。
- 本功能点验证：新增用例先在 `test/lua54_jit_regress.lua` 红于 “Lua 5.4 high-index upvalue load/store did not record a trace”；修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat test`、`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 已通过。`test/lua54_perf.lua` 新增 `many_upvalue_helpers`，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续推进 Lua 5.4 长字符串 JIT 语义：新增回归固定同一热循环轮换 80 组“不同 GC 对象但字节相同”的运行期长字符串时，`p[1] == p[2]` 必须全部为真。旧实现先红，JIT recorder 只 guard 第一次看到的两个长字符串对象，后续换成同内容新对象时会侧退并把计数跑错。
- `src/lj_record.c` 现在在 Lua 5.4 模式下遇到运行期长字符串 equality 时，录入 `lj_str_equal` helper 调用做 bytewise compare，并只 guard 当前相等/不等结果；`src/lj_ircall.h` 同步暴露 `IRCALL_lj_str_equal`。这样保留长字符串非内化对象身份，同时避免 trace 把字节相等误简化成对象身份。
- 本功能点验证：新增用例先在 `test/lua54_jit_regress.lua` 红于 generic long-string equality 断言；修复后 `cmd /c build.bat smoke54` 已通过，`test/lua54_perf.lua` 已新增 `long_string_equality` 负载并在 `.\src\luajit.exe test\lua54_perf.lua jit_on` / `jit_off` 下通过，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧字符串库方法语法在 tail-position 的参数错误表面：新增 smoke 固定 `return s:byte({})` 必须带源码调用点并报 `bad argument #1 to 'byte' (number expected, got table)`，`return s:find({})` 必须报 `bad argument #1 to 'find' (string expected, got table)`。旧实现先红，`CALLT` 擦掉方法调用帧后会把隐藏 self 算进公开参数，退回 `string.byte` / `string.find` 的第 2 参数错误。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 识别现在直接覆盖最终 callee 的 `TGETS/TGETV` table fetch，并识别方法语法生成的隐藏 self `MOV`，因此 `s:byte(...)` / `s:find(...)` 这类静态 string wrapper 方法调用在 tail-position 也会保留源码方法帧；普通未知 table 字段调用仍保持 tail call。
- 本功能点验证：新增用例先在 `test/lua54_jit_regress.lua` 中红于字符串方法 tail-call 错误名/参数编号断言；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。`test/lua54_perf.lua` 已把 `string_tail_byte_method_bad` / `string_tail_find_method_bad` 纳入 string helper，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 coroutine 库 tail-position 参数错误函数名：新增 smoke 固定 `return coroutine.status(nil)`、`return coroutine.isyieldable(nil)`、`return coroutine.create(nil)`、`return coroutine.wrap(nil)` 必须分别报到 `status` / `isyieldable` / `create` / `wrap`。旧实现先红，当前构建在 tail call 下退回 `coroutine.status` 等 fallback 名；官方 Lua 5.4.8 输出短字段名并带源码调用点。
- `src/lj_parse.c` 的 Lua 5.4 coroutine wrapper no-tail 识别现在覆盖 `status`、`isyieldable`、`create`、`wrap`，与既有 `resume` / `close` 共用同一 table-fetch 校验；只在静态确认 callee 来自 `coroutine.xxx` 时保留调用帧，不扩大到动态 `coroutine[name]` 或未知 table 字段。
- 本功能点验证：新增用例先在 `test/lua54_jit_regress.lua` 中红于 coroutine tail wrapper 错误名断言；修复后直接探针与 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 一致，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。`test/lua54_perf.lua` 已把这 4 个 tail wrapper 错误名纳入 coroutine helper，覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 table-field alias 作为 RHS 时的静态来源传播：新增 smoke 固定 `base.inner = alias_table; local holder = { alias = base.inner }; holder.alias.m = math.abs` 以及 `holder.alias = base.inner; holder.alias.n = math.abs` 后，`return alias_table.m/n(true)` 和 `return holder.alias.m/n(true)` 都必须报字段名 `m/n`。旧实现先红，因为 RHS `base.inner` 会先落到临时寄存器，source 解析从最终 `TSET*` store 开始时看不到前一条 `TGETS`。
- `src/lj_parse.c` 现在用 `lua54_store_value_table_source()` 统一解析 store RHS 的 table source：active local 仍直接按 local source 处理，非 active 临时寄存器则从 store 前一条字节码回溯，覆盖 `holder.alias = base.inner`、构造器字段 `alias = base.inner` 和同类 global/table-field alias 写入。该逻辑仍只接收前序 parser 已静态证明的 table-field alias，不把任意运行期字段读取推断成 table source。
- 本功能点验证：修复前当前构建对 `holder.alias.m(true)` / `holder.alias.n(true)` 输出 `bad argument #1 to 'math.abs'`，官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出字段名；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，`test/lua54_jit_regress.lua` 与 `test/lua54_perf.lua` 已同步纳入 JIT on/off 性能/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧标准库函数经 table 构造器字段 alias 后的 tail-position 错误定位：新增 smoke 固定 `local holder = { inner = alias_table }; holder.inner.l = math.abs` 后，`return alias_table.l(true)` 和 `return holder.inner.l(true)` 都必须报字段名 `l`。旧实现先红，`test/lua54_jit_regress.lua` 在新增断言处失败，因为构造器字段写入发生在 `holder` 成为 active local 之前，普通 table-field alias owner 检查看不到这个新 local。
- `src/lj_parse.c` 现在会在确认新 local 来自 table 构造器后，回扫该初始化区间里的静态 `TSETS/TSETV` 字段写入，并用 pending table-field alias 记录 `inner -> alias_table`。这样构造器字段 alias 能复用上一轮 table-field table alias 传播，同时只接受静态字符串字段和可证明的 table source，动态 key 仍不被吸收。
- 本功能点验证：修复前当前构建对 `holder.inner.l(true)` 输出 `bad argument #1 to 'math.abs'`，官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出字段名 `l`；修复后当前构建与官方一致。`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步把构造器字段 alias 纳入 JIT on/off 性能/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧标准库函数经静态 table-field table alias 链后的 tail-position 错误定位：新增 smoke 固定 `holder.inner = alias_table; holder.inner.k = math.abs` 后，`return alias_table.k(true)` 和 `return holder.inner.k(true)` 都必须报字段名 `k`。旧实现先红，因为 parser 只记录 local/global table alias，没有记录“某个 table 字段当前静态指向另一个 table source”，`holder.inner.f = helper` 和后续读取都会断链并退回 `math.abs`。
- `src/lj_parse.c` 现在新增 table-field table alias 记录，能在当前解析路径里把 `holder.inner = alias_table` 传播给后续 `holder.inner.k = math.abs`、`return alias_table.k(...)` 和 `return holder.inner.k(...)`。该记录只在 container table、field 名和 RHS table source 都可静态证明时生效；同一字段被写入非 table 或源 table 变量被重赋值时会清除 alias，避免动态重绑定污染调用点元数据。
- 本功能点验证：修复前当前构建对 `holder.inner.k(true)` 输出 `bad argument #1 to 'math.abs'`，官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出字段名 `k`；修复后直接探针与官方一致，且 `holder.inner` 被重写成新表后 `return alias_table.k(true)` 与官方同样变成 `attempt to call a nil value (field 'k')`，证明旧 alias 已清除。`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步纳入 JIT on/off 性能/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧标准库函数经静态 global table alias 后的 tail-position 错误定位：新增 smoke 固定 `lua54_table_global_alias = alias_table; lua54_table_global_alias.h = math.abs` 后 `return alias_table.h(true)` 必须报 `bad argument #1 to 'h'`，以及 `alias_table.j = math.abs; lua54_table_global_alias = alias_table; return lua54_table_global_alias.j(true)` 必须报字段名 `j`。旧实现先红，因为 global 只记录函数 alias，不记录“全局名当前静态指向某个 table source”，后续 table field no-tail 标记和读取都断链。
- `src/lj_parse.c` 现在为同一解析路径内可静态证明的 global table alias 记录 table source，并在 `global_table.field = helper` 或 `return global_table.field(...)` 这类形态中复用 table-field no-tail 标记。字段名解析同时覆盖大 chunk 常量表超过 `TGETS/TSETS` 8 位范围后的 `KSTR + TGETV/TSETV` 字节码形态，但只扫描当前 LHS 片段，避免把 `local k = "f"; t[k] = ...` 这类动态 key 误标为静态字段。
- 本功能点验证：新增用例先确认当前构建在完整 `smoke.lua` 中仍生成 `CALLT` 并退回 `math.abs`，官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 和修复后当前构建均输出字段名 `h/j`；额外大常量表探针确认 `KSTR + TGETV/TSETV` 路径已覆盖，动态 key 探针仍不被本轮静态规则吸收。`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步把 global table alias 纳入热路径，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧标准库函数经静态 table 字段 alias 后的 tail-position 错误定位：新增 smoke 固定 `alias_table.f = math.abs` 后 `return alias_table.f(true)` 必须报 `bad argument #1 to 'f'`。旧实现先红，因为只有 local/upvalue/global alias 会打 no-tail 标记，table 字段 alias 仍生成 `BC_CALLT`，错误名退回 `math.abs` 且丢失源码调用点。
- `src/lj_parse.c` 现在记录“可静态定位的 table 变量 + const 字段名”来自已知 Lua 5.4 helper 的事实，并在同一 table/field 后续 tail-position 调用时保留普通 `CALL`；标记会沿 upvalue/source var 传播到外层 parser 状态，覆盖 setup 子函数写入、后续 sibling/闭包调用的形态。普通未标记的 table 字段仍保持 tailcall，复杂重绑定和跨 chunk 动态传播仍留给后续 VM frame metadata 批次。
- 本功能点验证：新增用例先确认当前构建输出 `math.abs` 而官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出字段名 `f`；修复后直接探针与官方一致，且未标记 `t.f()` 仍能看到 `CALLT`。`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步把 table-field alias 纳入热路径，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧标准库函数经跨函数 setup 写入 global alias 后的 tail-position 错误定位：新增 smoke 固定 `setup()` 内执行 `lua54_global_abs_alias = math.abs` 后，后续 `return lua54_global_abs_alias(true)` 必须报 `bad argument #1 to 'lua54_global_abs_alias'`。旧实现先红，因为 no-tail 标记只存在于 setup 子函数的 `FuncState`，sibling 函数仍生成 `BC_CALLT`，错误名退回 `math.abs`。
- `src/lj_parse.c` 的 global no-tail 标记现在会同步到外层 parser 状态，让同一 chunk 内后续编译的 sibling 函数也能识别已静态证明的标准库/iterator global alias；普通未标记的未知全局函数仍保持 tailcall。该处理覆盖当前解析路径可见的 setup 函数赋值，不尝试解决跨 chunk/module 的运行期动态重绑定。
- 本功能点验证：新增用例先确认当前构建输出 `math.abs` 而官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出 `lua54_global_abs_alias`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步把 `lua54_jit_global_abs_alias` / `lua54_perf_global_abs_alias` 纳入热路径，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `io.lines()` / `file:lines()` iterator 的全局 alias tail-position 错误定位：新增 smoke 固定 `lua54_lines_global_it = iter; return lua54_lines_global_it()` 以及闭包内同名全局调用都必须报 `bad argument #2 to 'lua54_lines_global_it'`。旧实现先红，`BC_CALLT` 擦掉 wrapper 帧后只能退成 `bad argument #2 to '?'`。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 标记现在会记录本解析路径中“静态证明来自 no-tail helper/iterator 的全局字段名”，并在后续 `_ENV.name()` tail-position 调用命中该字段时保留普通 `CALL`；同时支持从已标记 local 写入全局，例如 `local it = io.lines(...); global_it = it`。该记录只对当前解析可见的 `_ENV` / `_G` 字段生效，不把任意未知全局函数都禁用 tailcall。
- 本功能点验证：新增用例先确认当前构建输出 `#2/?` 而官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出 `#2/lua54_lines_global_it`；修复后直接探针与官方一致，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步把 global iterator alias 纳入 IO helper 热路径，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `io.lines()` / `file:lines()` iterator 在 tail-position 经简单别名调用时的错误定位：新增 smoke 固定 `local it2 = iter; return it2()` 报 `bad argument #2 to 'it2'`，闭包 upvalue 形态 `return it3()` 报 `bad argument #2 to 'it3'`。旧实现先红，虽然直接 `local it = io.lines(...); return it()` 已能保留调用点，但再经过 `BC_MOV` 或 `BC_UGET` 后标记丢失，错误名退成 `?`。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 标记现在会沿简单 local alias 和 upvalue alias 传播：`lua54_slot_helper_init()` 在识别最终 tail-position callee 时会继续追踪 `BC_MOV` / `BC_UGET` 源头，只对已经带有 no-tail 标记的 iterator/helper 来源生效，不扩大到任意 table/global 函数调用。
- 本功能点验证：新增用例先确认当前构建输出 `#2/?` 而官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出 `#2/it2` / `#2/it3`；修复后直接探针与官方一致，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。更动态的跨函数传播仍作为 debug frame metadata / notail 标记批次的剩余边界记录在 `TODO.md`。
- 已继续按官方 Lua 5.4.8 对照收紧 `io.lines()` / `file:lines()` iterator 被局部变量接收后在 tail-position 调用的错误定位：新增 smoke 固定 `local it = io.lines(fname, {}); return it()` 和 `local it = f:lines({}); return it()` 都必须保留源码调用点，并报 `bad argument #2 to 'it'`。旧实现先红，`BC_CALLT` 擦掉 wrapper 帧后只能退成裸 `bad argument #2 to '?'`。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 标记现在能识别局部变量直接由 `...lines(...)` 调用结果初始化的 iterator alias；后续 `return it()` 会保留普通调用帧，让 `io_file_read` 的 Lua 5.4 参数错误可以恢复局部名和位置。该处理覆盖 `io.lines` 和文件对象 `:lines` 的直接局部接收形态，不改变普通 I/O 读取和默认 LuaJIT 构建。
- 本功能点验证：新增 tail 用例先确认当前构建输出 `#2/?` 而官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出带源码位置的 `#2/it`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 的 IO helper 继续以 tail-position `return it()` 覆盖该错误边界，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `io.lines()` / `file:lines()` iterator 的 read option 参数错误表面：新增 smoke 固定 `io.lines(fname, {})` 和 `f:lines({})` 的迭代器在非 tail 调用点下报 `bad argument #2 to 'it' (string expected, got table)`。旧实现先红，报成 `bad argument #1 to 'read'`，既漏掉 iterator 调用名，也把隐藏 file 参数从公开参数编号里扣掉了。
- `src/lib_io.c` 的 Lua 5.4 `io_file_read` 现在把“栈上起始槽位”和“公开参数编号偏移”拆开处理：普通 `io.read` / `file:read` 保持原编号，`io.lines` / `file:lines` iterator 则把 upvalue 中复制出来的 read option 映射为公开参数 #2；这样保留官方 `io_readline` 迭代函数的诊断表面，同时不影响文件方法的 hidden self 规则。
- 本功能点验证：新增探针先确认当前构建输出 `#1/read` 而官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 输出 `#2/it`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，直接探针确认当前构建与官方对 `io.lines(fname,{})` / `f:lines({})` 的非 tail iterator 错误文本一致。`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步把该错误边界纳入 IO helper 热路径，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.unpack()` 初始位置 `0` 的语义：新增 smoke 固定 `string.unpack("b", "abc", 0)` 返回 `97, 2`、`string.unpack("c1", "abc", 0)` 返回 `"a", 2`，并固定空串 `string.unpack("b", "", 0)` 进入 `data string too short`，不能先报第 3 参数初始位置越界。旧实现先红，因为把非负初始位置都直接当 1-based 位置，导致 `0` 被判成越界。
- `src/lib_string.c` 的 Lua 5.4 `string.unpack` 初始位置归一化现在按官方 `posrelatI` 语义处理：`0` 与 `1` 都映射到首字节，只有负数才按字符串末尾相对计算；已有正索引、负索引、alignment、短数据和格式解析路径不变。
- 本功能点验证：新增用例后最小断言先失败于 `bad argument #3 to 'unpack' (initial position out of string)`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `b/c1 pos=0`、空串短数据、`pos=1` 和 `pos=-1` 输出一致。`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已同步覆盖成功和错误边界，完整 perf 包含 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.gmatch()` iterator 的 debug upvalue 形态：新增 smoke 固定 `debug.getinfo(iter, "u").nups == 3`，前两个 upvalue 是 source string / pattern，第三个 upvalue 是 userdata 状态，第 4 个 upvalue 不存在；同时保留 iterator 正常产出和结束返回 0 个值。旧实现先红，额外把 pos/last 拆成两个内部 TValue upvalue，`nups == 4`，且第 4 个槽位可能把未完整初始化的内部状态泄露给 `debug.getupvalue()`。
- `src/lib_string.c` 的 Lua 5.4 `string.gmatch` 现在改用官方同形态的 3-upvalue closure：前两个 upvalue 保活字符串和 pattern，第三个 userdata 保存当前扫描位置和上次匹配结束位置；空匹配推进、`init` 参数和 `string.gmatch(..., init)` JIT 回归语义保持不变。
- 本功能点验证：新增用例后 `cmd /c build.bat smoke54` 已通过，确认官方矩阵和已有 gmatch 空匹配/init 回归不受影响；`cmd /c build.bat test` 已通过，完整 perf 中既有 `string_helpers` 覆盖 `string.gmatch` 热路径，JIT on 三档显式 `jit.opt` profile 和 JIT off 均通过。直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `nups=3`、第 4 upvalue 不存在和迭代结果一致。
- 已继续按官方 Lua 5.4.8 对照收紧 `os.execute()` 的 errno 隔离：新增 smoke 先用 `os.remove("__unlikely_lua54_missing_file__")` 制造 stale errno，再执行不存在命令，固定返回必须是 `nil, "exit", status`，不能被旧 errno 污染成 `nil, "No such file or directory", 2`。旧实现先红，正是因为 `system()` 前没有清 errno。
- `src/lib_os.c` 的 `os.execute` 现在按官方 `loslib.c` 在调用 `system()` 前设置 `errno = 0`，让 `luaL_execresult()` 只根据本次 `system()` 的真实结果决定是系统错误还是普通 exit tuple；`os.execute()` 无参数的 shell-availability 返回和参数错误路径不变。
- 本功能点验证：新增状态化探针确认旧实现失败；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 stale errno 后的不存在命令都返回 `3 nil exit 1`。该边界涉及真实 shell 调用，只放入 smoke，不放入 JIT/perf 热循环；完整 perf 矩阵仍已覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧标准库 C 闭包的 debug upvalue 可见性：新增 smoke 固定 `print` / `pairs` / `ipairs` 的 `debug.getupvalue(fn, 1)` 和 `debug.setupvalue(fn, 1, ...)` 返回 0 个值、`debug.upvalueid(fn, 1)` 返回单个 `nil`，并覆盖 JIT 热路径和 perf helper。旧实现先红，会把 LuaJIT 内部 cached `tostring` / `next` / `ipairs_aux` upvalue 暴露出来。
- `src/lib_debug.c` 现在只对 `FF_print` / `FF_pairs` 这类标准库入口隐藏内部 C upvalue；`string.gmatch()` 返回的 runtime C iterator 仍保留官方可见的 unnamed upvalue，避免破坏官方 `closure.lua`。`src/lib_base.c` 的 Lua 5.4 `ipairs` 改用 registry 保存稳定 auxiliary function，不再把它作为公开 `ipairs` C closure upvalue，同时保持多次 `ipairs({})` 返回同一个 auxiliary function。
- 本功能点验证：新增最小探针先确认旧实现失败；第一次实现因过宽隐藏所有 fast C upvalue 触发官方 `closure.lua` 的 `string.gmatch` iterator upvalue 断言，已回到根因并收窄隐藏范围。最终 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `print` / `pairs` / `ipairs` 隐藏、`gmatch` iterator 可见和 `ipairs` auxiliary 稳定性一致；完整 perf 中新增边界已覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.pack()` 缺少待打包参数时的错误文本：新增 smoke 固定 `i1` / `f` 缺数值时报 `number expected, got nil`，`c1` / `z` / `s1` 缺字符串时报 `string expected, got nil`，并覆盖 JIT 热错误路径和 perf helper。旧实现先红，因为 `luaL_buffinit()` 推入的内部 buffer 占位对象污染了缺参槽位，错误显示成 `got light userdata`。
- `src/lib_string.c` 的 `string.pack` 现在保存 `luaL_buffinit()` 前的原始实参数量，并在每个格式项真正消费 pack 值前先做缺值检查；显式传入 `nil` 仍走普通类型校验，source 字段调用和局部 alias 调用名继续与官方一致。
- 本功能点验证：新增用例先使 `cmd /c build.bat smoke54` 失败于 `test/lua54_jit_regress.lua:1271`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，且直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 direct/source/alias 的缺值和显式 nil 错误文本一致。完整 perf 中新增边界已覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.gsub()` replacement 参数错误文本：新增 smoke 固定缺第 3 参数时报 `string/function/table expected, got no value`，显式 `nil` 报 `got nil`，非法 boolean replacement 报 `got boolean`，并覆盖 direct `pcall(string.gsub, ...)`、源码字段调用和局部 alias 调用名。旧实现先红，只报 `string/function/table expected`，丢失 no-value / nil / boolean 细节。
- `src/lib_string.c` 的 Lua 5.4 `string.gsub` replacement 校验现在改走已有命名类型错误 helper，复用统一调用点名恢复和 `lj_meta_objtypename()` 类型文本；number/string/function/table 的合法 replacement 路径以及 nil/false 作为 table/function replacement 返回值保留原语义不变。
- 本功能点验证：新增用例先由直接探针确认旧实现失败；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，且直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 direct/source/alias 的缺参、显式 nil 和 boolean replacement 错误文本一致。完整 perf 中新增边界已覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `math.fmod()` / `os.difftime()` 在 no-value / nil 边界下的参数检查顺序：新增 smoke 固定 `math.fmod()` 和 `math.fmod(nil)` 必须先报第 2 个除数缺失，而 `math.fmod(nil, 1)` 仍报第 1 个参数为 nil；`os.difftime()` / `os.difftime(nil)` 必须先报第 1 个时间参数，`os.difftime(1)` 仍报第 2 个参数缺失。旧实现先红，两个函数在缺参时都因为检查顺序和 C 实参求值顺序与官方相反而报错到错误参数。
- `src/lib_math.c` 的 Lua 5.4 `math.fmod` fallback 现在先检查除数参数再检查被除数，保持 PUC Lua 的缺参诊断顺序；`src/lib_os.c` 的 `os.difftime` 改用局部 `time_t` 临时变量顺序检查两个参数，避免直接把检查调用放进 `difftime()` 实参列表后受 C 未定义求值顺序影响。
- 本功能点验证：新增用例先由直接探针确认旧实现失败；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，且直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 direct/source `math.fmod` 和 `os.difftime` 缺参/nil 边界的错误参数序号一致。完整 perf 中新增边界已覆盖 JIT on 三档显式 `jit.opt` profile 和 JIT off。
- 已继续按官方 Lua 5.4.8 对照收紧 `ipairs` 辅助迭代器耗尽时的返回形态：新增 smoke 固定 `ipairs({})` 的稳定 auxiliary function 在空表和 `math.maxinteger` wraparound 后缺失元素时都必须返回单个 `nil`，并把同一边界加入 JIT 回归和 perf helper。旧实现先红，返回 0 个值。
- `src/lib_base.c` 的 Lua 5.4 `ipairs` auxiliary 现在在 `lua_gettable()` 得到 nil 时返回 1 个值；命中元素仍返回 `index, value`，普通 for 循环表面不变，但返回数量和官方一致。
- 本功能点验证：`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `ipairs({})` 空表 auxiliary 和 `math.maxinteger` wraparound 后的缺失元素都返回 `1 nil`。
- 已继续按官方 Lua 5.4.8 对照收紧 `next({})` 的返回形态：新增 smoke 固定空表迭代结束必须返回单个 `nil`，并把同一边界加入 JIT 回归和 perf helper。旧实现先红，`next({})` 是 0 个返回值，普通 `next({}) == nil` 无法暴露差异。
- `src/lib_base.c` 的 Lua 5.4 `next` 兼容实现现在在 `lua_next()` 遍历耗尽后显式压入 `nil` 并返回 1；隐藏 `_G._ENV` 的内部兼容逻辑不变，默认 LuaJIT 5.1 fast `next` 路径不受影响。
- 本功能点验证：`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `next({})` 都返回 `1 nil`。
- 已继续按官方 Lua 5.4.8 对照收紧 `debug.gethook()` 的无 hook 返回形态和 `debug.sethook(function, "", 0)` 边界：新增 smoke 固定 main thread 和指定 coroutine 无 hook 时只返回单个 `nil`，空 mask 且 count 为 0 会真正关闭 hook；count-only hook `debug.sethook(fn, "", 1)` 仍保留 `function, "", 1`。旧实现先红，会返回 `nil, "", 0`，并把空 mask/count 0 的函数留在 hook registry。
- `src/lib_debug.c` 现在在 Lua 5.4 `debug.sethook` 中按 `mask == 0` 选择 NULL hook 并删除 registry 记录；`debug.gethook` 看到 NULL hook 时直接返回单个 `nil`，只有 active hook 才继续返回 mask 和 count。
- 本功能点验证：`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `debug.gethook()`、`debug.gethook(co)`、`debug.sethook(fn, "", 0)` 后的 `gethook` 都返回 `1 nil`，而 `debug.sethook(fn, "", 1)` 仍返回 `function, "", 1`。
- 已继续按官方 Lua 5.4.8 对照收紧 `coroutine.close()` 成功路径返回值数量：新增 smoke 固定 suspended、fresh、dead 和二次 close 成功时都只返回单个 `true`，并把同一边界加入 JIT 回归和 perf helper。旧外层 Lua wrapper 的直接探针表现为 `true, nil`，会多返回一个 nil。
- `src/lib_base.c` 的 Lua 5.4 `coroutine.close` wrapper 现在只在失败路径返回 `false, err`；成功路径清理 `resume_errors` 后直接返回单个 `true`，保留底层 C close 和错误替换语义不变。
- 本功能点验证：`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；直接探针确认当前构建与官方 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对 `dead` / `fresh` coroutine 成功 close 都返回 `1 true`。
- 已继续按官方 Lua 5.4.8 对照收紧 `debug.upvalueid()` 的缺失 upvalue 返回形态：新增 smoke 固定 0、负数和越界 index 都必须返回单个 `nil`，并把 `debug.upvalueid(fn, 0)` 加入 JIT 回归和 perf helper。旧实现先红，`debug.upvalueid(function() end, 0)` 是 0 个返回值，`== nil` 这类旧断言无法暴露差异。
- `src/lib_debug.c` 的 Lua 5.4 `debug.upvalueid` 查询路径现在对 `n <= 0` 和越界 index 显式 `lua_pushnil()` 后返回 1；`debug.upvaluejoin` 仍保留 mutating API 的越界报错语义。
- 本功能点验证：新增用例先失败于 `test/lua54_jit_regress.lua:828`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 当前矩阵、JIT 回归、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `package.searchpath()` 的空 path 错误片段：新增 smoke 固定 `package.searchpath(name, "")` 必须返回 `nil, "no file ''"`，并把同一边界加入 `test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 的 package helper 热路径。旧实现先红，只返回空错误字符串。
- `src/lib_package.c` 的 Lua 5.4 `searchpath()` 现在把空 path 也当作一个空模板迭代一次；这和前导、重复、尾部 `;` 的空模板规则一致，避免 `require()` / `package.searchpath()` 组装缺失模块错误时丢掉官方可见的 `no file ''` 片段。
- 本功能点验证：新增用例先失败于 `test/lua54_jit_regress.lua:879`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 当前矩阵、JIT 回归、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已补强本轮 `string.format` 改动的 JIT/perf 覆盖：`test/lua54_jit_regress.lua` 现在固定裸 `%a` / `%A` 在热循环中可记录 trace 并输出 `0x1.8p+0` / `0X1.8P+0`，同时固定 `%#a` modifier 错误和 `%#i` 缺实参 no-value 优先级不会生成错误 trace。`test/lua54_perf.lua` 的 `string_helpers` 也把 `string.format("%a", 1.5)` 纳入 JIT on/off 时间和内存窗口。
- 本 JIT/perf 覆盖验证：`.\src\luajit.exe test\lua54_jit_regress.lua` 通过；第一次跑 `test\lua54_perf.lua` 暴露 `string_helpers` 期望值未随新增 `%a` 成功计数同步，修正为 `string_n * 202` 后，`.\src\luajit.exe test\lua54_perf.lua jit_on` 和 `jit_off` 均通过。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.format()` 缺实参与格式错误的优先级：新增 smoke 固定 `string.format("%")`、`"%."` 等 malformed 格式、`%#i` / `%F` 这类非法规格/未知转换、以及 too-long `%999...s` 在没有第 2 个实参时都必须先报 `bad argument #2 to 'string.format' (no value)`。旧实现先红，直接报 invalid conversion 或 invalid format。
- `src/lj_strfmt.c` 的 Lua 5.4 formatter 现在在 `STRFMT_ERR` 分支和已解析 conversion 分支都先按下一个格式实参做 no-value 检查；只有实参存在时，才继续报告 malformed/too-long/invalid conversion 本身。这个顺序匹配 PUC Lua 的 `string.format` 参数检查表面，同时保留已有 source/alias 调用名恢复。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:2159`；第一次修复只覆盖 `STRFMT_ERR`，复跑暴露 `%#i` 仍先报 invalid spec，随后把 `strfmt_checkconv_lua54()` 移到 no-value 检查之后；最终 `cmd /c build.bat smoke54` 已通过。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.format("%a/%A")` modifier 边界：新增 smoke 固定 `%#a`、`%#A`、`%10a`、`%.3a`、`%+a` 必须报 `modifiers for format '%a'/'%A' not implemented`，同时保留裸 `%a` / `%A` 的十六进制浮点输出。旧实现先红，会接受这些 modifier 并格式化成功。
- `src/lj_strfmt.c` 的 Lua 5.4 formatter 校验现在对 hexadecimal float 转换单独判断 flag、width 和 precision；只要 `%a/%A` 携带任何 modifier 就按官方错误返回。默认 `%a/%A` 没有 modifier 时继续复用现有十六进制浮点输出路径。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:2152`，错误值为旧输出 `0x1.8p+0`；修复后 `cmd /c build.bat smoke54` 已通过，官方 `strings.lua` 覆盖到 `modifiers for format '%a' not available` 分支。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.format()` tail-position formatter 错误定位：新增 smoke 固定 `return string.format("%" .. string.rep("9", 21) .. "s", "x")` 这类动态拼接格式串必须带 `test/smoke.lua:` 调用点前缀，local alias `local dynfmt = string.format; return dynfmt(...)` 同样覆盖。旧实现先红，只返回裸 `invalid format (too long)`。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 识别窗口从只适合相邻调用的 6 条 bytecode 扩到有界 32 条；动态参数中的嵌套调用会把 `string.format` callee fetch 和最终 `CALL` 拉开距离，扩大窗口后仍只跟踪同一个 call base 并在覆盖时停止，避免泛化禁用普通尾调用。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:2189`；修复后 direct/alias 探针都输出 `(command line):1: invalid format (too long)`，`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 smoke/C API/header、官方 Lua 5.4.8 当前矩阵、JIT 回归、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off 性能/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.format()` 的格式规格长度阈值：新增 smoke 固定 21 位 width（`%999...s`）和 19 位 precision（`%1.999...s`）这类总长度达到 23 字节的格式项必须报 `invalid format (too long)`。旧实现先红，仍按旧 parser 的 32 字节上限误报 `invalid conversion specification`。
- `src/lj_strfmt.c` 的 Lua 5.4 格式解析现在按官方 `MAX_FORMAT - 10` 规则判断过长格式项；本地 `strfmt_speclen_lua54()` 统计包含开头 `%`，所以从总长 23 字节开始报 too long。普通成功解析路径也复用同一阈值，避免大量 `0` flag 这类可解析但过长的格式项漏过。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:2152`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 smoke/C API/header、官方 Lua 5.4.8 当前矩阵、JIT 回归、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off 性能/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `warn()` 的参数错误调用点名：新增 smoke 固定 direct `pcall(warn)`、`pcall(warn, nil)`、`pcall(warn, true)` 和带 `__tostring` table 的错误都必须报 `bad argument #1 to 'warn'`，同时确认源码调用、tail-position 调用和局部 alias 仍分别报 `warn` / `f`。旧实现先红，direct `pcall(warn, ...)` 只能报 `to '?'`。
- `src/lib_base.c` 的 Lua 5.4 `warn()` 现在复用 base 库命名字符串参数校验，保留 number 到 string 的官方转换，继续拒绝 nil/boolean/table，并避免参数校验失败前输出 warning 片段。`TODO.md` 已同步修正 `warn()` 对 nil / `__tostring` table 的状态说明。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:3852`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 smoke/C API/header、官方 Lua 5.4.8 当前矩阵、JIT 回归、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off 性能/内存 smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.pack` / `string.unpack` / `string.packsize` 的格式数字解析边界：新增 smoke 固定 `!0` / `!17` 必须报 `integral size ... out of limits [1,16]`，`s0` / `s17` / 超长 `s999...` 在 `packsize` 中也必须先做 integral size 校验，`i2147483647` 按官方只消费到安全 int 边界后报 `integral size (214748364)`，`c9999999999` 则留下溢出数字作为下一格式项并报 `invalid format option '9'`。旧实现先红，`!0` / `!17` 仍报 LuaJIT 自定义的 `maximum alignment`，`s0` / `s17` 被过早归为 `variable-length format`，超长数字还会被整体消费成 `invalid format` 或过大的 size。
- `src/lib_string.c` 的 Lua 5.4 pack 格式读取现在模拟官方 `getnum()` / `getnumlimit()` 的 int 边界行为：数字超过安全累积范围时停止继续吞数字，让后续解析看到剩余格式字符；`!` 的 max alignment 和 `s` 的长度前缀统一复用 integral size 限制，`c` 的超长尺寸保持官方“剩余数字成为非法格式项”的行为。顺带补强 coroutine `status/create/wrap/isyieldable` 显式 `nil` 的源码调用点和局部 alias 回归，确认已被现有调用点名实现覆盖。
- 本功能点验证：新增 pack/parser 用例先失败于 `test/smoke.lua:2191`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 smoke/C API/header、官方 Lua 5.4.8 当前矩阵、JIT 回归、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off 性能/内存 smoke。
- 平台矩阵本轮复核：`cmd /c build.bat platformpc` 已通过 PC x64 default 与 Lua 5.4 compat 双构建、AMD64 PE header 检查和 smoke；`cmd /c build.bat platformandroid` 已通过 Android ARM64 Lua 5.4 compat 静态 artifact 构建和 AArch64 检查，设备 smoke 因当前无在线 adb 设备 SKIP；`cmd /c build.bat platformprobe` 仍显示 iOS 需要 macOS/Xcode 主机，Emscripten 已识别 `emcc 4.0.3` 但停在 LuaJIT 缺 wasm/wasm64 VM 后端的既有阻塞。TODO 已按这次实测状态更新。
- 已继续按官方 Lua 5.4.8 对照收紧 `collectgarbage()` 多余参数规则：新增 smoke 固定默认 `collectgarbage(nil, true)` 以及 `"count"` / `"collect"` / `"stop"` / `"restart"` / `"isrunning"` 携带多余参数时必须忽略这些参数；`step` / `setpause` / `setstepmul` 和 `generational` / `incremental` 仍只校验官方定义的可选整数参数。旧实现先红，所有 option 都统一校验第 2 参数，`collectgarbage("count", true)` 会错误报第 2 参数 boolean。
- `src/lib_base.c` 的 Lua 5.4 `collectgarbage` 分支现在只对 `step` / `setpause` / `setstepmul` 读取并校验第 2 个整数参数；无调参 GC 命令按官方忽略多余实参。`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 同步把 `count/isrunning` 携带多余参数加入热路径，避免 JIT/trace 或性能 smoke 漏掉该兼容面。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:530`；修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵、JIT 回归和 VM 后端静态/DynASM 门禁。
- 已继续按官方 Lua 5.4.8 对照收紧 traceback 无名 C frame 输出：新增 smoke 固定 `debug.traceback(nil)` 末尾的未知 C frame 必须显示 `[C]: in ?`，并且不能泄露 LuaJIT 旧格式的原始函数指针 `[C]: at 0x...`。旧实现先红，仍暴露 C 函数地址。
- `src/lj_debug.c` 的 Lua 5.4 `luaL_traceback()` 分支现在对无名 C frame 使用官方 unknown-name 文本 `in ?`；默认 LuaJIT 5.1 路径仍保留旧的 pointer traceback 形态。C API smoke 同步补强 `luaL_checkudata()` 的 Lua 5.4 类型错误覆盖，固定 number/light userdata/带 `__name` table 以及源码全局/局部 alias 调用点名。
- 本功能点验证：新增 traceback 用例先失败于 `test/smoke.lua:2606`；修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和 VM 后端静态/DynASM 门禁。
- 已继续按官方 Lua 5.4.8 对照收紧文件对象 method 漏 self 的参数错误：新增 smoke 固定 `io.stdin.close/read/flush/seek/lines()` 这类点号调用必须报 `bad argument #1 to 'method' (FILE* expected, got no value)`，`io.stdin.write("x")` / `io.stdin.setvbuf("no")` 必须报 `got string`，且 tail-position 下仍带源码位置。旧实现先红，方法名只能显示为 `?`，`close` 缺 self 还只报裸 `got no value`。
- `src/lib_io.c` 新增 Lua 5.4 文件 method 专用 self 校验入口，`close/read/write/flush/seek/setvbuf/lines` 先按公开 method 名检查 `FILE*`，再复用原有读写/seek/setvbuf 参数路径；`src/lj_parse.c` 同步识别 `io.stdin/stdout/stderr.<file-method>` 的 tail-position 调用并保留用户帧，避免 `BC_CALLT` 擦掉 Lua 5.4 错误位置。非 Lua 5.4 构建仍使用旧 `io_tofile` 路径。
- 本功能点验证：新增用例先失败于 `test/smoke.lua:3848`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。额外探针确认 `return io.stdin.close()` / `return io.stdin.setvbuf("no")` 都带源码前缀和短 method 名；完整 perf 中 `io_helpers` JIT on 三档约 0.244-0.254s，JIT off 约 0.059s，未出现保留内存增长。
- 已继续按官方 Lua 5.4.8 对照收紧 Windows 下 `os.date()` 的格式边界：新增 smoke 固定 `%c` / `!%c` 与官方可执行一样规整为 `%x %X` / `!%x %X`，并固定 Windows 扩展转换 `%#c` / `%#x` / `%#d` 可用，`%#X` 仍按官方报 invalid conversion。旧实现先红，`%c` 直接暴露当前 CRT 的英文完整日期，`%#*` 两字符转换解析也因为 `|` 分组处理错误而被拒绝。
- `src/lib_os.c` 的 Lua 5.4 `os.date` option parser 现在按 PUC Lua 的 `LUA_STRFTIMEOPTIONS` 分组规则推进 `|` 分段，避免把 Windows `||#c#x...` 误解析成三字符转换；Windows 下单项 `%c` 会显式交给 `%x %X`，让当前构建和本机官方 Lua 5.4.8 可执行保持同一短日期/时间表面。invalid conversion 也改走命名参数错误 helper，因此 direct `pcall(os.date, "%Q", 0)` 报 `os.date` 而不是 `?`。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 `%c == %x %X`、`!%c == !%x %X`、`%#c/%#x/%#d` 成功、`%#X/%Q` 报 `os.date`，且 JIT on 多 `jit.opt` profile 和 JIT off 性能/内存 smoke 均通过。
- 已继续按官方 Lua 5.4.8 对照收紧 `setmetatable()` 的第 2 参数边界：新增 smoke 固定 `setmetatable({})` 必须报 `bad argument #2 ... got no value`，而 `setmetatable({}, nil)` 仍成功清除 metatable；direct `pcall(setmetatable, {})`、源码 `return setmetatable({})` 和局部 alias `local f = setmetatable; return f({})` 都覆盖对应错误名。旧实现先红，缺失第 2 参数会被当成显式 `nil` 并错误成功。
- `src/lib_base.c` 的 Lua 5.4 `setmetatable` 入口现在先判断第 2 参数是否真实存在，缺参走命名 `nil or table` 参数错误；只有调用者显式传入 `nil` 时才按 Lua 5.4 清除 metatable。该路径继续复用已有 callsite 名称 helper，所以 tail/source/alias 错误名保持对齐。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 direct/source/alias 缺第 2 参数报 `got no value`，显式 nil 返回成功，且 JIT on 多 `jit.opt` profile 和 JIT off 性能/内存 smoke 均通过。
- 已继续按官方 Lua 5.4.8 对照收紧 `xpcall()` 缺 message handler 的参数错误：新增 smoke 固定 direct `pcall(xpcall, f)`、源码 `return xpcall(f)` 和局部 alias `local g = xpcall; return g(f)` 都必须报 `got no value`，而显式传入 `nil` 仍报 `got nil`。旧实现先红，Lua 5.4 兼容 wrapper 的固定形参会把缺失的第 2 参数补成 `nil`。
- `src/lib_base.c` 的 Lua 5.4 `xpcall` wrapper 改为 vararg 入口，先用 `select("#", ...)` 保存真实实参数量；缺第 2 参数时只把第 1 参数转发到底层 raw `xpcall`，显式 `nil` 时才转发 `nil`。这样继续保留原有 `xpcall(f, f)` 的 `error in error handling` 防递归逻辑，同时不再丢失 no-value / nil 区分。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 direct/source/alias 三种调用下缺 handler 报 `got no value`，显式 `nil` 报 `got nil`，且 JIT on 多 `jit.opt` profile 和 JIT off 性能/内存 smoke 均通过。
- 已继续按官方 Lua 5.4.8 对照收紧 base/global 公开入口在 tail-position 的参数错误调用点：新增 smoke 覆盖 `rawget/rawset/rawequal/rawlen/next/pairs/ipairs/getmetatable/setmetatable/tonumber/select/assert` 的 `return ...` 形态必须带源码位置和公开函数名，并覆盖 `local f = rawget/next/tonumber/select/assert; return f(...)` 必须报到 alias `f`。旧实现先红，tail call 会擦掉调用帧，base 库自身的硬编码函数名也会让 alias 退回 `rawget` / `next` / `tonumber` 等真实入口名。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 识别新增真实 `_ENV` base/global 入口匹配，直接全局字段调用和由这些入口初始化的局部/upvalue alias 都会保留用户调用帧；识别仍要求表来源是 `_ENV` upvalue，所以普通自定义表字段调用不会被扩大禁用尾调用。`src/lib_base.c` 的 Lua 5.4 参数错误入口改用统一 `lj_debug_callname54()`，direct `pcall(cfunc, ...)` 保留公开 fallback，源码字段调用和 alias 恢复 source-level 名字。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 `return rawget()` 报 `rawget` 并带调用点，`local f = rawget/next/tonumber/select/assert; return f(...)` 分别报 `f`，且 JIT on 多 `jit.opt` profile 和 JIT off 性能/内存 smoke 均通过。
- 已继续按官方 Lua 5.4.8 对照成批收紧标准库公开入口的 tail-position 错误调用点：新增 smoke 覆盖 `math.random/randomseed/fmod/min/max`、`os.exit`、`io.open/input/read`、`debug.getinfo`、`utf8.len`、`package.searchpath` 的源码字段调用和局部别名在 `return ...` 位置仍要报到短字段名或 alias，并带源码位置；direct `pcall(math.random/randomseed, ...)` 则固定保留完整 `math.xxx` fallback。旧实现先红，尾调用会擦掉调用帧，且部分错误只能 fallback 到完整库名。
- `src/lj_debug.c` 新增通用 Lua 5.4 callsite 名称 helper，统一处理 direct `pcall(lib.fn, ...)`、源码字段调用和局部/upvalue alias；`math` / `os` / `io` / `debug` / `utf8` / `package` 的参数错误入口改用该 helper。`src/lj_parse.c` 的 no-tail 识别同步扩展为“字段名 + 实际标准库表”组合匹配，避免 `len` / `type` / `close` 这类跨库重名只命中第一个库。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 direct `pcall(math.random, true)` 报 `math.random`，而 `return math.random(true)` / `return os.exit("x")` / `return utf8.len(true)` / `return package.searchpath(...)` 分别报短字段名并带调用点，局部 alias 报 `f`。
- 已继续按官方 Lua 5.4.8 对照收紧 base loader 错误定位：新增 smoke 固定 `return load(reader)` 的 reader 非字符串返回错误必须带源码调用点前缀，`return dofile(true)` 的参数错误必须指向用户代码而不是内部 Lua wrapper。旧实现先红，tail call 会擦掉 `load` 调用帧，`dofile` 则泄露 `[string "local load = ..."]`。
- `src/lib_base.c` 现在在 Lua 5.4 `load()` reader 分支预先保存用户调用点，用于 reader 类型错误的返回消息；`dofile` 的内部加载 helper 单独跳过 Lua wrapper 帧构造参数错误。`src/lj_parse.c` 同步把 `load` / `loadfile` / `dofile` 作为已知 loader 入口保留 caller frame，覆盖直接调用和由这些入口初始化的局部/upvalue alias。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 tail 和非 tail 的 `load(reader)` 都带调用点，`dofile(true)` 不再暴露内部 wrapper。
- 已继续按官方 Lua 5.4.8 对照收紧 string 库公开入口的源码调用点名：新增 smoke 固定 `string.dump(true)` / `return string.dump(true)` 报到 `dump`，`local f = string.dump; f(true)` / `return f(true)` 报到 `f`，并覆盖 `string.pack` / `string.unpack` / `string.packsize` 的源码字段和 tail-position 错误名。旧实现先红，源码调用和 alias 都回退到 `string.dump` / `string.pack`。
- `src/lib_string.c` 的 Lua 5.4 string 参数错误名 helper 现在优先使用真实 field/local/upvalue/global 调用点名，只有 direct `pcall(string.xxx, ...)` 这类没有 Lua 调用表达式的入口才保留完整 `string.xxx` fallback；`src/lj_parse.c` 同步新增精准 `string.xxx` wrapper 识别，让 tail-position 和由 `string.xxx` 初始化的局部/upvalue alias 保留 caller frame。普通 `t.pack()` 仍保持 `CALLT`。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外探针确认 direct `pcall(string.dump, true)` 仍报 `string.dump`，`return string.pack(nil)` / `return f(nil)` 报 `pack` / `f`，而 `return t.pack(nil)` 仍是 `CALLT`。
- 已继续按官方 Lua 5.4.8 对照收紧 `coroutine.resume()` / `coroutine.close()` 在 tail-position Lua wrapper 中的调用点名：新增 smoke 固定 `return coroutine.resume()` / `return coroutine.close()` 报到短字段名 `resume` / `close`，`local f = coroutine.resume; return f()` 和重新赋值后的局部别名 `f = coroutine.close; return f()` 报到 `f`。旧实现先红，`BC_CALLT` 会在 wrapper 抛错前擦掉用户调用帧，导致 fallback 到 `coroutine.resume` / `coroutine.close`。
- `src/lj_parse.c` 的 Lua 5.4 no-tail 标记现在能识别真实来自 `_ENV.coroutine.resume/close` 的字段取值，并把普通赋值 `f = coroutine.resume` 重新标记到局部/upvalue alias；识别时会回看实际 table 临时寄存器，所以普通 `t.resume()` 仍保持 `CALLT`，不会广泛禁用所有 `.resume` / `.close` 尾调用。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；额外字节码探针确认 `return coroutine.resume()` / `return f()` 变为普通 `CALL`，而 `return t.resume()` 仍是 `CALLT`。
- 已继续按官方 Lua 5.4.8 对照收紧 `coroutine.resume()` / `coroutine.close()` 外层 Lua wrapper 的参数错误：新增 smoke 固定 direct `pcall(coroutine.resume)` / `pcall(coroutine.close)` 缺参报 `got no value`，普通源码调用报到 `resume` / `close`，局部别名 `f()` 报到 `f`，显式 `nil` 仍报 `got nil`。旧实现先红，wrapper 会把缺参变成传给底层 C fast function 的 `nil`，并从 wrapper 内部泄露位置。
- `src/lib_base.c` 新增私有 `lj_cf_coroutine_argerror54` 供 Lua 5.4 coroutine wrapper 使用；wrapper 现在用 vararg 形态保留真实参数个数，先做线程参数检查，再进入递归深度控制、resume error 记账和底层 C fast function。错误位置会跳过 wrapper 指向用户调用点，类型名仍复用 C 侧 `__name` 逻辑。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和 VM 后端静态/DynASM 门禁。
- 已继续按官方 Lua 5.4.8 对照收紧 coroutine 库普通源码调用和局部别名调用的参数错误名：新增 smoke 固定 `coroutine.status(true)` / `coroutine.create(true)` / `coroutine.wrap(true)` / `coroutine.isyieldable(true)` 分别报到 `status` / `create` / `wrap` / `isyieldable`，局部 `f = coroutine.xxx; f(true)` 报到 `f`。旧实现先红，所有这些路径都只能 fallback 到 `coroutine.xxx`。
- `src/lib_base.c` 新增 Lua 5.4 callsite 名称 helper；coroutine 的 `status` / `create` / `wrap` / `isyieldable` 参数类型错误现在会在有源码调用点时恢复短字段名或局部别名，没有可用调用点时继续保留 direct `pcall(coroutine.xxx, ...)` 的完整 fallback 名。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和 VM 后端静态/DynASM 门禁。
- 已继续按官方 Lua 5.4.8 对照修正 `table.sort()` comparator 校验时机：新增 smoke 固定 `table.sort({}, true)` 和 `table.sort({1}, true)` 必须成功，只有真正需要比较的 `table.sort({2, 1}, true)` 才报 `bad argument #2 to 'table.sort' (function expected, got boolean)`。旧实现先红，空表也会过早拒绝非函数 comparator。
- `src/lib_table.c` 的 Lua 5.4 compat 分支现在只在排序长度大于 1 时校验 comparator；这保留了两元素以上排序的官方参数错误，同时让空表/单元素表对第 2 参数保持 Lua 5.4 的宽松行为。默认 LuaJIT 5.1 路径不变。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和 VM 后端静态/DynASM 门禁。
- 已继续按官方 Lua 5.4.8 对照收紧 debug 库元表和 uservalue 入口：新增 smoke 固定 `debug.getmetatable()` 缺参报 `bad argument #1 to 'debug.getmetatable' (value expected)`，`debug.setmetatable(value, true/"x")` 报到 `debug.setmetatable` 且包含 `nil or table expected, got boolean/string`；同时固定 `debug.getuservalue(nonuserdata)` 返回 `nil`，但 bad index 仍先报第 2 参数，`debug.setuservalue(value, value, badindex)` 先报第 3 参数。旧实现先红，元表入口从内部 helper 泄露为 `?`，uservalue 则过早报第 1 参数 userdata。
- `src/lib_debug.c` 的 Lua 5.4 compat 分支现在让 `debug.getmetatable` 使用命名 value 检查；`debug.setmetatable` 对第二参数使用命名 `nil or table` 检查，显式 `nil` 仍合法，缺参和错误类型按官方函数名和类型文本报错。`debug.getuservalue` 改为先校验可选 slot index，再对非 userdata 返回 `nil`；`debug.setuservalue` 也先校验第 3 个 slot index，再检查 userdata 和待设置 value。默认 LuaJIT 5.1/debug 旧表面不变。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 smoke/C API/header、官方 Lua 5.4.8 当前矩阵、VM 后端静态/DynASM 门禁、standalone 回归，以及 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 IO 库入口错误名：新增 smoke 固定 `io.type()` 缺参报 `bad argument #1 to 'io.type' (value expected)`，`io.input(true)` / `io.output(true)` / `io.close(true)` 报到对应 `io.xxx` 且保留 `FILE* expected`，`io.open` / `io.popen` / `io.lines` 的字符串参数和 mode 参数错误报到完整函数名，`io.read({})` / `io.write(true)` 报到 `io.read` / `io.write`。旧实现先红，多数路径从内部 helper 泄露为 `?` 或短名 `read` / `write`。
- `src/lib_io.c` 新增 Lua 5.4 命名入口参数检查 helper；`io.input` / `io.output` 仍接受文件句柄或可转字符串的路径，其它对象按官方 `FILE*` 期望报错；`io.open` / `io.popen` / `io.lines` 使用命名 string 检查并保留 number-to-string 转换；函数入口 `io.read` / `io.write` 和文件方法 `read` / `write` 现在分别保留各自官方错误名。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 C API/header、官方 Lua 5.4.8 当前矩阵、VM 后端静态/DynASM 门禁，以及 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `os.exit()` 的参数错误归属：新增 smoke 固定 `os.exit("x")` / `os.exit({})` 报到 `os.exit`，`os.exit(1.5)` 报无整数表示。旧实现先红，仍从旧 `lj_lib_optint` 泄露为 `bad argument #1 to '?'`。
- `src/lib_os.c` 新增 Lua 5.4 命名可选整数检查，用于 `os.exit` status 参数；boolean status 仍按官方映射到 success/failure，第二个 close 参数继续保持 Lua 5.4 truthy 语义，不额外做类型检查。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 C API/header、官方 Lua 5.4.8 当前矩阵、VM 后端静态/DynASM 门禁，以及 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `print()`：新增 standalone smoke 固定 `tostring=nil` 时 `print(setmetatable(..., __tostring))` 仍使用内部 `luaL_tolstring()` 输出，且全局 `tostring` 被改成返回 table 后 `print("RAW54")` 仍正常输出。旧实现先红，会沿 Lua 5.1 行为调用可变全局 `tostring` 并报 `attempt to call a nil value`。
- `src/lib_base.c` 的 Lua 5.4 compat `print()` 分支现在逐参数调用内部 `luaL_tolstring()`，因此遵守 `__tostring` 元方法但不受 `_G.tostring` 重绑定影响；默认 LuaJIT 5.1 构建继续保留旧的全局 `tostring` 快捷路径和数值输出 shortcut。
- 本功能点验证：新增 standalone 用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 C API/header、官方 Lua 5.4.8 当前矩阵、VM 后端静态/DynASM 门禁、standalone 行为，以及 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `collectgarbage("generational")` / `"incremental"` 的可选调参参数：新增 smoke 覆盖 `generational(minormul, majormul)`、`incremental(pause, stepmul, stepsize)` 的合法整数/字符串整数、额外参数忽略、fraction/boolean 参数错误，以及 `__gc` finalizer 内非法可选参数仍必须先报错。旧实现先红，所有 mode 可选参数都会被忽略。
- `src/lib_base.c` 新增 Lua 5.4 命名可选整数校验 helper；GC mode shim 切换前会先校验第 2/3 个参数，`incremental` 额外校验第 4 个参数，合法时仍只记录模式并返回旧模式，底层 GC 仍明确保持 LuaJIT collector。finalizer 内合法调用继续返回 `nil`，非法可选参数不再被 reentry nil 吞掉。
- `test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已把 GC mode 可选参数加入热循环和性能/内存 smoke，避免 JIT helper 热路径再次绕过校验。本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat lua54perf` 和 `cmd /c build.bat test` 均通过，覆盖默认构建、Lua 5.4 C API/header、官方 5.4 当前矩阵、JIT on 多 `jit.opt` profile 和 JIT off。
- 已继续补强 Lua 5.4 long-string table key 的 JIT store 表面：`test/lua54_jit_regress.lua` 新增 existing-slot GC 值写入、new-key 数值写入和 new-key GC 值写入热循环回归；GC 值路径固定 trace 必须包含 `IR_TBAR`。旧实现先红，existing GC store 没有进入带 barrier 的安全路径，new-key store 则完全不录 trace。
- `src/lj_record.c` 的 Lua 5.4 long-string store 现在在 GC64 下可把 GC TValue 编成带 tag 的 raw slot bits；已有 key 继续用 `lj_tab_getstr()` 做 bytewise lookup，缺失 key 改用 `lj_tab_setstr()` 统一处理 rehash 和 key barrier，随后 raw-store value 并在 GC 值写入后发出 `IR_TBAR`。非 GC64 raw store 仍保持解释器退避。
- `test/lua54_perf.lua` 已同步加入 `long_string_table_gc_store`、`long_string_table_new_store` 和 `long_string_table_new_gc_store` 负载，覆盖 JIT on 多 `jit.opt` profile 和 JIT off 下的 existing/new long-string store 性能/内存窗口。本功能点验证：新增用例先失败、修复后 `cmd /c build.bat lua54` 通过；随后单独 `test/lua54_perf.lua jit_on` / `jit_off` 均通过。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.format()` 参数错误的源级调用名：新增 smoke 固定 direct `pcall(string.format, "%d", true)` 仍报 `string.format`，源码字段调用 `return string.format("%d", true)` 报 `format`，局部 alias `local f = string.format; return f(...)` 报 `f`。旧实现先红，字段调用和 alias 都被硬编码成 `string.format`。
- `src/lj_strfmt.c` 的 Lua 5.4 formatter 参数错误 helper 现在通过 debug callsite 恢复 field/local/upvalue 调用名，没有源级调用表达式时才回退到 `string.format`；同时继续复用同一 helper 处理 no-value、无整数表示、`%q` 无字面量形式和 `%s` NUL 等参数错误。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和 VM 后端静态/DynASM 门禁。
- 已继续收紧 `string.format()` 在 tail-position Lua wrapper 里的 formatter 错误定位：新增 smoke 固定 `local function f() return string.format("%#p", {}) end; pcall(f)` 必须带 `test/smoke.lua:` 调用点前缀。旧实现先红，虽然错误文本已对齐，但 `BC_CALLT` 在错误发生前替换掉 Lua wrapper frame，导致只剩裸 `invalid conversion specification: '%#p'`。
- `src/lj_parse.c` 的 Lua 5.4 no-tail helper 入口表新增 `format`，让 tail-position `string.format()` 保留 caller frame；`src/lj_strfmt.c` 的 `luaL_error()` formatter 错误路径因此能按官方 Lua 5.4 显示 Lua 调用点位置。该改动只作用于 Lua 5.4 compat 编译路径。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和 VM 后端静态/DynASM 门禁。
- 已继续按官方 Lua 5.4.8 对照收紧 `string.format()` 非法格式规格错误：新增 smoke 要求 `%3.1p` / `%#p` 报 `invalid conversion specification: '...'`，同时固定 `%I` / `%U` / `%F` 这类未知转换仍报 `invalid conversion '...' to 'format'`。旧实现先红，合法转换但规格非法的 `%p` 仍走旧 LuaJIT 文本 `invalid conversion '...' to 'format'`。
- `src/lj_strfmt.c` 的 Lua 5.4 formatter 错误入口现在区分“未知转换”和“合法转换的非法规格”；非法 flag/precision/width 组合走 Lua 5.4 的 `invalid conversion specification` 文本，未知转换和 `%F` 保留官方 `invalid conversion ... to 'format'` 文本。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵、VM 后端静态/DynASM 门禁，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照收紧 `tonumber()` 显式 base 范围错误：新增 smoke 要求 `tonumber("10", 1)` 和 `tonumber("10", 37)` 都报 `bad argument #2 to 'tonumber' (base out of range)`。旧实现先红，错误仍从内部参数检查显示为 `?`。
- `src/lib_base.c` 的 Lua 5.4 `tonumber()` explicit-base 路径现在在 base 越界时走命名参数错误 helper；fraction base、boolean base、缺省 base 和默认 LuaJIT 构建表面保持原有路径。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 对照补 `require()` 非参数错误的调用点位置：新增 smoke 要求 `return require("__missing__")` 组装出的 module-not-found 错误，以及 `package.searchers = 1; return require(...)` 错误，都带 `[string ...]:line:` 前缀。旧实现先红，只返回裸 `module ... not found` / `'package.searchers' must be a table`。
- `src/lib_package.c` 的 Lua 5.4 `require()` 运行期错误现在通过 caller-context error 抛出；`src/lj_parse.c` 同步把 `require()` 加入 Lua 5.4 禁用尾调用的已知库入口，避免 `BC_CALLT` 在错误发生前替换掉 Lua 调用帧。这保留了官方调用点诊断，同时只影响 Lua 5.4 compat 分支的 `return require(...)`。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续补齐 package 库相邻入口：新增 smoke 要求 `package.loadlib(nil, "x")`、`package.loadlib({}, "x")`、`package.loadlib("x", nil)`、`package.loadlib("x", {})` 都按官方 Lua 5.4 报到 `package.loadlib`，旧实现先红，仍显示内部 `?`。
- `src/lib_package.c` 的 Lua 5.4 `package.loadlib` 入口现在复用命名 string 参数检查，和 `require` / `package.searchpath` 保持同一错误名与 number-to-string 转换表面；默认非 Lua 5.4 构建仍保留原 `luaL_checkstring` 路径。
- 本功能点验证：新增 `package.loadlib` 用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续用官方 Lua 5.4.8 对照收紧 package/require 入口参数错误名：新增 smoke 要求 `require({})` / `require(true)` 报到 `require`，`package.searchpath` 的第 1-4 个 string 参数类型错误都报到 `package.searchpath`。旧实现先红，所有这些基础参数错误都从内部 C 入口显示为 `?`。
- `src/lib_package.c` 现在在 Lua 5.4 分支下为 public `require` 和 `package.searchpath` 使用命名 string 参数检查，支持 number 到 string 的既有转换、`nil` 可选参数默认值，以及 `__name` 类型文本；默认非 Lua 5.4 构建继续走原 `luaL_checkstring` / `luaL_optstring` 路径。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续用官方 Lua 5.4.8 对照收紧 `string.pack()` / `string.unpack()` / `string.packsize()` 的错误归属：新增 smoke 要求基础缺参、`c` 缺 size、`z` 含 NUL、`X` next option、alignment 非 2 的幂、`packsize("z")`、`unpack("z")` unfinished string、`unpack("s1", "")` / 短数据和 `pack("b", 1.2)` 都报到完整官方函数名 `string.pack` / `string.unpack` / `string.packsize`。旧实现先红，仍显示短名 `pack` / `unpack` / `packsize`，部分 data-too-short 路径还会显示 `?`。
- `src/lib_string.c` 现在让 `string.pack*` 共享完整 `fname` 进入格式、参数、整数/浮点写入和 data-too-short 校验路径；`string_callname54()` 也保留 `string.xxx` 库字段调用的完整 fallback，避免 direct `pcall(string.pack, ...)` 被 debug table key 缩短成 `pack`。默认非 Lua 5.4 构建路径保持旧参数检查。
- 本功能点验证：新增用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续从官方 Lua 5.4.8 `lua.c` 补 standalone `-e` / `-l` 缺参数边界：`-e -v` 必须报告 `'-e' needs argument`，`-l -e` 必须报告 `'-l' needs argument`，不能把后续选项当作 chunk 或 module 名。新增用例先红，旧实现分别报 `unexpected symbol near '-'` 和 `module '-e' not found`。
- `src/luajit.c` 的 Lua 5.4 `collectargs()` 现在会在 `-e` / `-l` 使用分离参数时拒绝以 `-` 开头的下一个 argv，并把错误归属到原选项；默认 LuaJIT 构建和 `-j` 扩展参数表面保持不变。
- 本功能点验证：新增 standalone missing-argument 用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续从官方 Lua 5.4.8 `lua.c` / `testes/main.lua` 补 standalone `-W` 顺序语义用例：`-e 'warn(...)' -W` 必须保持无输出，`-W -e 'warn(...)'` 必须输出 warning，`-e hidden -W -e visible` 只能输出后一个 warning。旧实现先红于 `-e ... -W`，因为 warning 在执行所有命令行 chunk 前被提前开启。
- `src/luajit.c` 现在把 Lua 5.4 `-W` 放到 `runargs()` 中按参数顺序执行，和官方 `lua.c` 一样只影响后续 `-e` / `-l` / 脚本执行；`pmain()` 不再因 `FLAGS_WARNING` 提前打开 warning，默认 LuaJIT 构建表面不变。
- 本功能点验证：新增 standalone `-W` 顺序用例先失败、修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已复核 `build.bat` 的 GNU make 并发上限：当前机器 24 个物理核、32 个逻辑线程，默认、`BUILD_JOBS=999`、裸 `-j`、`-j999`、`-j 999` 的 dry-run 都被压到 `-j12`；显式降低并发的 `-j2` / `-j 2` 仍保留为 `-j2`。
- 已继续推进 Lua 5.4 运行期长字符串 table key 的 JIT 表面：`test/lua54_jit_regress.lua` 先把同内容但不同 GC 对象的长字符串 table load 从“不得记录 trace”改为“必须记录 trace 并按 bytewise key 命中”，旧实现先红于 `long-string table load did not record a trace`。
- `src/lj_record.c` 现在对 Lua 5.4 long-string table load 使用 side-effect `lj_tab_getstr()` helper 做运行期 bytewise lookup，避免旧 `HREF/HREFK` 的短字符串 intern 身份假设；命中后 guard 返回 slot 非空和 raw TValue bits，再复用当前值专门化 trace。long-string table existing-slot store 也已在 GC64 TValue 布局下记录非 GC 数值/boolean 写入：每轮先做 bytewise lookup 并 guard slot 非空，再 raw-store compact TValue bits；新 key 插入、GC 值写入和非 GC64 布局仍保持 NYI，等待后续 NEWREF / barrier 专用方案。`test/lua54_jit_regress.lua` 已覆盖 existing-slot store 必须录 trace、新 key store 仍不得录 trace；`test/lua54_perf.lua` 同步新增 `long_string_table` lookup/store 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 性能/内存 smoke。
- 本轮验证：`cmd /c build.bat smoke54`、`cmd /c build.bat lua54perf` 和 `cmd /c build.bat test` 均已通过；perf 输出中 `long_string_table_store` 在 JIT on 三档显式 `jit.opt` profile 和 JIT off 下均进入时间/内存门禁。
- 已继续补齐 Lua 5.4 `string.format("%s", number)` 的 JIT recorder：`test/lua54_jit_regress.lua` 先新增 `string.format("%s", i)`、`string.format("%s", i + 0.5)` 和带宽度 `string.format("%6s", i)` 的热循环 IR 断言，要求记录到 `IR_TOSTR`，避免外层循环 trace 掩盖 formatter 仍 NYI 的问题。
- `src/lj_ffrecord.c` 的 `%s` formatter 现在在 Lua 5.4 兼容构建下对 number TRef 先发出 `IR_TOSTR`，再复用既有 string formatter 路径；这样 integer/float subtype 文本和宽度格式都可记录 trace，`__tostring` 对象和非 number 的复杂 `%s` 表面仍留在解释器以保留运行期校验。`test/lua54_perf.lua` 已把 `string.format("%s", 1.0)` 和 `string.format("%6s", 12)` 纳入 `string_helpers` 的 JIT on 多 `jit.opt` profile 与 JIT off 性能/内存 smoke。
- 本功能点验证：`cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke；`string_helpers` 在 JIT on 三档和 JIT off 下均通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补 standalone `-l` 语义用例：`Makefile` 新增 `-l mod` 必须把 `require(mod)` 返回值写入 `_G.mod`、连续 `-l mod -lother` 的后续模块能看到前一个全局、`-l g=mod` 只写指定全局，以及 `-l mod-v2` 写入截断后的 `_G.mod` 而不写 `_G["mod-v2"]`。旧实现先红于 `_G.lua54_loption_mod` 为 nil。
- `src/luajit.c` 的 Lua 5.4 `dolibrary()` 现在按官方 `globname[=modname]` 规则执行：成功 `require()` 后把首个返回值写入目标全局名，显式 `g=mod` 只改变全局目标，没有显式 `=` 时会保留完整模块名给 `require()`，但用 `LUA_IGMARK` 截断全局名中的版本后缀。默认 LuaJIT 构建继续保留旧 `-l` 表面。
- 本功能点验证：新增 standalone `-l` 用例先失败、修复后 `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/header gate、官方 Lua 5.4.8 当前矩阵，以及 JIT on 三档显式 `jit.opt` profile 和 JIT off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 收紧位运算错误诊断：`test/smoke.lua` 新增常量 string、local string/boolean/table、unary `~`、以及带 `__name` 的 table 参与 bitwise 的错误文本回归，要求能保留 `(constant '3')` / `(local 'x')` 等源级操作数名字。旧实现先红于 `test/smoke.lua:1236`，只报类型而缺少 operand source。
- `src/lib_jit.c` 现在复用 Lua 5.4 lowered helper 的操作数来源恢复逻辑；对于 helper 调用覆盖掉原寄存器名字的常量操作数，会在调用点前做有界 `MOV` / `KSTR` 回扫，所以非 number 位运算错误也能追加 source suffix，并继续保留 `__name` 类型名。修复后 `cmd /c build.bat smoke54` 已通过。
- 已继续按官方 Lua 5.4.8 收紧 `string.format("%s")` 的 metamethod 表面：`test/smoke.lua` 新增 `__tostring` 返回字符串、无 `__tostring` 时使用 `__name` 前缀、以及 `__tostring` 返回 table 必须报 `'__tostring' must return a string` 的回归。旧实现先红，会把 table 返回值继续格式化成 `table: 0x...`。
- `src/lj_strfmt.c` 的 Lua 5.4 `%s` 路径现在在调用 `__tostring` 后校验返回值，只接受 string/number，其它类型按 `luaL_tolstring()` 表面抛错；默认 LuaJIT 构建不变。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，完整测试仍覆盖 JIT on/off perf/memory smoke。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补交互模式裸表达式用例：`Makefile` 新增 `printf '10\n' | ./src/luajit -e '_PROMPT="" _PROMPT2=""' -i` 必须输出独立行 `10`，以及 `print=nil` 时必须报告 `error calling 'print'`。旧实现先红，仍把交互输入 `10` 当普通 chunk 解析并报 `unexpected symbol near '10'`。
- `src/luajit.c` 的 Lua 5.4 REPL 读入路径现在先尝试把首行编译为 `return <line>;`，失败后再按普通 statement / 多行 continuation 编译；这对齐官方 `lua.c` 的 `addreturn()` / `multiline()` 行为，让裸表达式结果可进入后续 `print` 输出路径。默认 LuaJIT 构建保留原有 REPL 行为。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补交互模式多行 statement 和 long string continuation 用例：`Makefile` 新增 `(6*2-6) -- ===` / `a =` / `10` / `print(a)` / `a` 必须输出 `6` 和两行 `10`，以及 `a = [[b\nc\nd\ne]]` 后 `=a` 必须输出完整 long string。旧实现先红，`incomplete()` 提前弹出 `<eof>` 错误导致后续拼接栈位错误，表现为 `unexpected symbol near <eof>`、多余 `nil` 和 long string 语法错误。
- `src/luajit.c` 的 Lua 5.4 `incomplete()` 现在按官方 `lua.c` 语义在识别 `<eof>` incomplete 时保留错误对象，由 multiline 拼接路径在压入 continuation 行后移除准确栈位；默认 LuaJIT 构建继续使用旧的弹栈行为。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补交互提示符非字符串边界：`Makefile` 新增 `_PROMPT=setmetatable({}, {__tostring=function() ... end})`，要求三次 prompt 输出 `123`，不能退回默认 `> > >`。旧实现先红，`write_prompt()` 只用 `lua_tostring()`，不会触发 `__tostring`。
- `src/luajit.c` 的 Lua 5.4 `write_prompt()` 现在对非 `nil` `_PROMPT` / `_PROMPT2` 调用 `luaL_tolstring()`，因此 table/userdata prompt 可按官方通过 `__tostring` 决定提示符；默认 LuaJIT 构建保留旧 prompt 路径。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补交互 continuation 被 EOF 打断的错误文本：`Makefile` 新增 `printf 'a.\n' | ./src/luajit -i`，要求 stderr 包含 `<name> expected near <eof>` 且不能出现 `'<name>' expected`。旧实现先红，仍把 `<name>` token 包在额外引号里。
- `src/lj_parse.c` / `src/lj_errmsg.h` 的 Lua 5.4 parser 路径现在对 `TK_name` expected 错误使用官方 `<name> expected` 文本；其它 token 仍沿旧 `LUA_QS " expected"` 路径，默认 LuaJIT 构建不变。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补全 `LUA_PATH` 中 `;;` 替换默认路径的首尾分隔符边界：`Makefile` 新增 `LUA_PATH=';'`、`';;'`、`';;b'`、`'a;;'`、`'a;b;;c'` 的 standalone 回归。旧实现先红于 `LUA_PATH=';;'`，会把替换后的路径错误保留为首尾都有 `;`。
- `src/lib_package.c` 的 Lua 5.4 path 初始化现在按官方 `loadlib.c` 算法只替换第一处 `;;`，并且仅当用户提供真实前缀/后缀时才在默认路径两侧补分隔符；`LUA_PATH=';'` 这种没有 `;;` 的值继续原样保留。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补 `lua -e a` 语法错误边界：`test/smoke.lua` 新增 `load("a")` 必须报 `syntax error near <eof>` 的 parser 回归，`Makefile` 同步新增 standalone `./src/luajit -e a` 必须失败且输出包含 `syntax error`。旧实现先红，仍沿 LuaJIT 旧 assignment parser 报 `'=' expected near <eof>`。
- `src/lj_parse.c` 的 Lua 5.4 兼容路径现在在 prefix expression 语句不是函数调用、且后续 token 也不是 `=` 或 `,` 时直接报通用 syntax error，不再让裸名字语句落入 assignment 的 `lex_check('=')` 旧错误表面；默认 LuaJIT 构建保持原路径。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补 `warn()` 参数错误边界：`Makefile` 新增 standalone 回归，固定 `warn("@on"); pcall(warn, "SHOULD NOT APPEAR", {}); warn("VISIBLE")` 只能向 stderr 输出 `Lua warning: VISIBLE`，不能把参数错误前已经输出的 warning 片段残留到下一条 warning。旧实现先红，输出 `Lua warning: SHOULD NOT APPEARVISIBLE`。
- `src/lib_base.c` 的 `warn()` 现在按 Lua 5.4 官方实现先校验所有参数并完成数字到字符串转换，确认不会抛错后才逐段调用 `lua_warning()`；这样后续参数报错时不会让默认 warning writer 留在 continuation 状态。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖新增 standalone warning 回归、官方 Lua 5.4.8 当前矩阵、默认构建 smoke/C API、Lua 5.4 C API/header gate，以及 JIT on/off perf/memory smoke。
- 已把多后端 VM DynASM 模板生成从手工验证固化为 Lua 5.4 smoke 门禁：`Makefile` 先接入尚不存在的 `test/lua54_vm_backend_dynasm.lua`，`cmd /c build.bat smoke54` 预期先红于 `cannot open test/lua54_vm_backend_dynasm.lua`，确认新增覆盖是测试先行。
- 新增 `test/lua54_vm_backend_dynasm.lua`，用 `src/host/minilua(.exe)` 调 `dynasm/dynasm.lua` 自动生成并清理 x86 dualnum、ARM hard/soft、MIPS32 LE hard-float、MIPS32 BE soft-float、MIPS64 LE hard-float、MIPS64 BE soft-float、PPC hard-float、PPC soft-float、PPC GPR64/TOC 的临时 `buildvm_arch`；同时更新 `test/lua54_vm_backend_static.lua` 注释，明确静态门禁和 DynASM 门禁仍不替代真实 artifact/runtime smoke。修复 Windows `os.execute` 路径拼接后，`test/lua54_vm_backend_dynasm.lua`、`test/lua54_vm_backend_static.lua`、`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过。
- 已继续把 `<close>` VM continuation 剩余静态后端成批补齐：`test/lua54_vm_backend_static.lua` 先把 `src/vm_mips.dasc`、`src/vm_mips64.dasc`、`src/vm_ppc.dasc` 纳入 error-unwind / C-return close continuation 静态门禁，并要求 C helper gate 扩到 `LJ_TARGET_ARM || LJ_TARGET_MIPS || LJ_TARGET_MIPS64 || LJ_TARGET_PPC || LJ_TARGET_X86 || LJ_TARGET_X64 || LJ_TARGET_ARM64`。新增门禁先红于 `src/lj_close.c` gate 仍未允许 MIPS/PPC。
- `src/lj_close.c` / `src/lj_close.h` 现在允许 MIPS/MIPS64/PPC 使用 close continuation helper；`src/vm_mips.dasc`、`src/vm_mips64.dasc`、`src/vm_ppc.dasc` 已补 C-return close、error-unwind close、`LJ_CONT_CLOSE` / `LJ_CONT_CLOSE_CFRAME` dispatch、`cont_close` / `cont_close_cframe` 和 `vm_call_close` 接线。修复后 `test/lua54_vm_backend_static.lua`、MIPS32 LE hard-float、MIPS32 BE soft-float、MIPS64 LE hard-float、MIPS64 BE soft-float、PPC hard-float、PPC soft-float、PPC GPR64/TOC DynASM 模板生成、`cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 均已通过。
- 已继续推进 `<close>` VM continuation 到 ARM 后端：`test/lua54_vm_backend_static.lua` 先把 `src/vm_arm.dasc` 纳入 error-unwind / C-return close continuation 静态门禁，并要求 C helper gate 扩到 `LJ_TARGET_ARM || LJ_TARGET_X86 || LJ_TARGET_X64 || LJ_TARGET_ARM64`。新增门禁先红于 `src/lj_close.c` gate 仍未允许 ARM。
- `src/lj_close.c` / `src/lj_close.h` 现在允许 ARM 使用 close continuation helper；`src/vm_arm.dasc` 已补 C-return close、error-unwind close、`LJ_CONT_CLOSE` / `LJ_CONT_CLOSE_CFRAME` dispatch、`cont_close` / `cont_close_cframe` 和 `vm_call_close` 接线。修复后 `test/lua54_vm_backend_static.lua` 通过；额外用 DynASM 分别生成 ARM hard-float 与 soft-float 临时 `buildvm_arch`，均通过语法验证。
- 已继续推进 `<close>` VM continuation 后端覆盖：`test/lua54_vm_backend_static.lua` 先把 `src/vm_x86.dasc` 纳入 error-unwind / C-return close continuation 静态门禁，并要求 C helper gate 扩到 `LJ_TARGET_X86 || LJ_TARGET_X64 || LJ_TARGET_ARM64`。新增门禁先红于 `src/lj_close.c` gate 仍只允许 x64/ARM64。
- `src/lj_close.c` / `src/lj_close.h` 现在允许 x86 使用 close continuation helper；`src/vm_x86.dasc` 已补 `lj_close_prepare_pcall` / `lj_close_continue_pcall`、`lj_close_prepare_cframe_pcall` / `lj_close_continue_cframe_pcall`、`LJ_CONT_CLOSE` / `LJ_CONT_CLOSE_CFRAME`、`cont_close` / `cont_close_cframe`、`vm_call_close` 和 `vm_returnc_closed` / `vm_unwind_ff_closed` 接线。修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 均已通过；额外用 `src/host/minilua.exe dynasm/dynasm.lua ... src/vm_x86.dasc` 生成 x86 临时 `buildvm_arch`，先抓到并修正 `ARG4 <- MULTRES` memory-to-memory mov，再通过 DynASM 语法验证。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 补 standalone `-v` 用例：`Makefile` 新增 `./src/luajit -v -e "print'hello'"` 必须输出包含 `Lua 5.4.8` 和 `PUC-Rio` 的官方版权版本行，并继续执行 `-e` chunk 打印 `hello`。旧实现先红，仍输出 LuaJIT 自身版本 banner。
- `src/luajit.c` 的 `print_version()` 现在在 Lua 5.4 兼容构建下输出 `LUA_COPYRIGHT`，默认 LuaJIT 构建继续输出原 LuaJIT banner。本轮新增用例先失败，修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat default` 和 `cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 当前矩阵、C API/头文件 gate，以及 JIT on/off perf/memory smoke。
- 已继续从官方 Lua 5.4.8 standalone 错误处理补负向用例：`Makefile` 在 Lua 5.4 smoke 中新增脚本级 `error({})`、`error(false)`、`error(nil)` 必须分别报告具体错误对象类型，并补 `__tostring` 返回字符串、以及官方 `main.lua` 中用 `debug.getinfo(4).currentline` 检查 message handler 栈形态的正向消息用例。旧实现先红，table/boolean 会退成 LuaJIT 旧 `(error object is not a string)`，`error(nil)` 顶层甚至不会输出消息。
- `src/luajit.c` 的 Lua 5.4 message handler 现在按官方 `lua.c` 表面处理非字符串错误对象：能字符串化的对象直接用 `__tostring` 结果，不能字符串化时生成 `(error object is a <type> value)` 并追加 traceback；`report()` 的 Lua 5.4 兜底文本也改为官方 `(error message not a string)`。默认 LuaJIT 构建保留旧错误表面。本轮新增用例先失败，修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 当前矩阵、C API/头文件 gate，以及 JIT on/off perf/memory smoke。
- 已继续从官方 Lua 5.4.8 `testes/main.lua` 抽 portable standalone 负向用例：`Makefile` 在 Lua 5.4 smoke 中新增 `-h`、`---`、`-Ex`、`-vv`、`-iv` 必须失败并报告 `unrecognized option ...`，以及缺少参数的 `-e` / `-l` 必须报告 `needs argument`。旧实现先红：多数 bad option 只打印通用 usage，`-Ex` 还会被误当成 `-E` 接受并进入交互。
- `src/luajit.c` 的参数扫描现在会记录具体坏选项；Lua 5.4 兼容构建在 usage 前输出官方式错误摘要，`-E` 也和官方一样拒绝尾随字符。默认 LuaJIT 的扩展选项表面保持不变。本轮验证：新增用例先失败，修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat default` 和 `cmd /c build.bat test` 均已通过，覆盖新增 standalone negative gate、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 当前矩阵、C API/头文件 gate，以及 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `main.lua` 补 standalone `arg` 表破坏回归：`Makefile` 新增 `printf '\n' | ./src/luajit -e 'arg = 1' -` 必须失败并报告 `'arg' is not a table`。旧实现先红，因为 `handle_script()` 在全局 `arg` 非 table 时只静默弹栈并以 0 个参数执行脚本。
- `src/luajit.c` 的 Lua 5.4 `handle_script()` 路径现在会在脚本执行前检查全局 `arg` 仍是 table；如果 `LUA_INIT` 或 `-e` 已经破坏它，就按官方抛错。默认 LuaJIT 模式继续保留旧宽松行为。补完该项后 `cmd /c build.bat test` 已通过，覆盖 default、Lua 5.4 compat、官方矩阵、C API/头文件 gate 和 JIT on/off perf/memory smoke。
- 已按本次反馈继续收紧 `build.bat` 的 GNU make 并发：默认从 Windows 检测物理 CPU 核数并取一半，保底 1；过大的 `BUILD_JOBS`、裸 `-j`、`-jN` 以及分开写的 `-j N` 都会被归一到半核上限，只有低于上限的显式值才会降低并发。当前机器 24 个物理核、32 个逻辑线程，dry-run 验证默认、`BUILD_JOBS=999`、裸 `-j`、`-j999`、`-j 999` 都输出 `-j12`，`-j2` / `-j 2` 保留为 `-j2`。
- 已继续按官方 Lua 5.4.8 `main.lua` 覆盖 `os.exit(true, true)` 的 active `<close>` 行为：`Makefile` 先新增独立脚本回归，要求 main chunk 中仍 active 的两个 to-be-closed local 在 `os.exit(..., true)` 触发 `lua_close()` 时按 LIFO 输出 `120` / `Ok`。旧实现先失败于没有任何 close 输出。
- `src/lj_state.c` 的 Lua 5.4 `lua_close()` 路径现在会在清理全局状态和 finalizer 前关闭 main thread 栈上的 active to-be-closed slot；C API smoke 也补了独立 state 上 `lua_toclose()` 后直接 `lua_close()` 必须调用 `__close(value, nil)` 的回归。
- 已继续按官方 Lua 5.4.8 `main.lua` 收紧 close-state finalizer reentry：`Makefile` 新增独立进程回归，finalizer `2` 中创建新的 table `__gc` 后再调用 `os.exit(0, true)`，官方只允许当前已排队的 finalizer `1` 继续运行，不允许新创建的 finalizer `3` 加入同一轮 state close。旧实现先失败于多输出 `3`，初版修正又暴露递归 `lua_close()` 过早退出导致少输出 `1`。
- `src/lj_state.c` / `src/lj_obj.h` 已新增 Lua 5.4 close-state 标记：首次 `lua_close()` 只分离关闭开始前已有的 finalizer 队列，后续不再重新扫描新创建的 `__gc` 对象；finalizer 中递归 `lua_close()` 时只清空当前已排队 finalizer，不再分离新对象。本轮验证：`cmd /c build.bat smoke54`、`cmd /c build.bat test` 和 `cmd /c build.bat platformpc` 均已通过，完整 perf/memory smoke 未触发门禁失败。
- 已修正 `luaL_getmetafield()` 回归预期：官方 Lua 5.4.8 `lauxlib.c` 对 metatable 字段使用 raw lookup，不会跟随 metatable 自己的 `__index`；`test/lua54_capi_smoke.c` 已把新增用例从错误的 `__index` 期望改为 `LUA_TNIL`，同时保留返回实际类型码的正向覆盖。
- 已继续按官方 Lua 5.4.8 收紧 stripped chunk 错误定位：`test/smoke.lua` 先新增 stripped dump 中直接 `error("stripped boom")` 只返回原始错误对象、不拼 `?:-1:` 的回归；旧实现先失败于新增断言，因为 `luaL_where()` 复用了 VM runtime 错误定位并把 `currentline == -1` 当作可显示行号。
- `src/lj_debug.c` / `src/lj_err.c` 已拆开 `luaL_where()` / `luaL_error()` 和 VM runtime 错误定位：Lua 5.4 的 `luaL_where()` 在 stripped chunk 行号为 `-1` 时压入空前缀，`luaL_error()` 通过该路径拼接位置；普通 VM 运算/类型等 runtime 错误仍保留官方可见的 `?:-1:` 诊断。本轮验证：新增 stripped `error()` 用例先失败，修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，且既有 stripped arithmetic 错误仍固定为 `?:-1:`；完整 perf 中 `io_helpers` JIT on 三档约 0.142-0.155s、JIT off 约 0.027s，`os_helpers` JIT on 约 0.066-0.069s、JIT off 约 0.018s，未触发内存门禁。
- 已继续按官方 Lua 5.4.8 `lauxlib.c` 收紧 `luaL_checkoption()` 默认参数语义：`test/lua54_capi_smoke.c` 先新增显式 `nil` 仍使用默认 option、显式 table 不能被默认值吞掉而必须报 `string expected` 的回归；旧实现先失败于新增 `luaL_checkoption rejects table with default`，原因是当前路径对任意不可转字符串参数都套用了 `def`。
- `src/lj_api.c` 的 Lua 5.4 分支现在只在缺参/`nil` 时使用 `luaL_checkoption()` 的默认值，显式传入 table/boolean 等不可转字符串值会走标准参数类型错误；数字仍沿 `luaL_checkstring` 路径转成字符串再匹配 option，默认 LuaJIT 构建继续保留旧路径。本轮验证：`cmd /c build.bat smoketest-capi-lua54compat` 和 `cmd /c build.bat test` 已通过，覆盖新增 C API 用例、官方 Lua 5.4.8 matrix、头文件门禁、默认构建 smoke/C API，以及 Lua 5.4 perf/memory smoke；完整 perf 中 `io_helpers` JIT on 三档约 0.143-0.153s、JIT off 约 0.026s，`os_helpers` JIT on 约 0.066-0.068s、JIT off 约 0.017s，未触发内存门禁。
- 已继续按官方 Lua 5.4.8 收紧 IO read / write / seek / setvbuf 边界：`test/smoke.lua` 先新增 `file:read(1.5)`、`file:seek("set", 1.5)` / `"1.5"` 和 `file:setvbuf("full", 1.5)` / `"1.5"` 必须报 `number has no integer representation`；`file:read("2")` / table、`file:write({})` / `io.write({})`、`file:seek("x")`、`file:setvbuf("x")` 的函数名、参数编号和类型/格式错误也按官方表面固定。旧二进制先失败于新增 smoke，原因是 read length / seek offset / setvbuf size 仍被旧路径截断或交给 C，`write` 方法错误还把 self 算进参数编号并显示 `?` 函数名。
- `src/lib_io.c` 的 Lua 5.4 分支新增严格整数 helper，`file:read(n)` length、`file:seek` offset 和 `file:setvbuf` size 现在接受整数参数并拒绝 fraction number；`seek`/`setvbuf` 仍接受字符串整数但拒绝 fraction string，不再把 Lua 5.4 的 `lua_Integer` 参数悄悄截断后传给 C；`file:write` / `io.write` 的非 string/number 参数按官方 `write` 函数名和公开参数编号报错。默认 LuaJIT 构建继续保留旧转换路径。
- 同批修正 `io.lines()` iterator 的 table option 识别：只有内部打包表带整数 `[0]` 计数时才展开为 250 个 read options；用户传入普通 table 作为 read option 时不再误当成内部打包表并默认读行，新增 smoke 固定 `io.lines(fname, {})` 迭代时会报 table 类型错误。
- 同批补强 IO helper 的 JIT/trace 和性能门禁：`test/lua54_jit_regress.lua` 新增二进制临时文件下的 `file:seek()` / `file:read("n")` / 定长 read / `file:setvbuf("no")` 热循环和 read/write/seek/setvbuf 错误热循环；`test/lua54_perf.lua` 新增 `io_helpers`，纳入显式 `jit.opt` 三档和 JIT off 的时间/内存 smoke。`cmd /c build.bat smoke54`、`cmd /c build.bat lua54perf` 和 `cmd /c build.bat test` 已通过；完整批次中 `io_helpers` JIT on 三档约 0.148-0.160s、JIT off 约 0.027s，内存增量 0-8.4KB。
- 已继续按官方 Lua 5.4.8 收紧 os 库不可表示时间结果：`test/smoke.lua` 先新增 `os.date("%Y", 2^60)` 和 `os.time{year=4000, month=1, day=1}` 必须抛 `date/time result cannot be represented in this installation` 的回归；旧实现返回 `nil`。`src/lib_os.c` 的 Lua 5.4 分支现在在 `localtime/gmtime` 失败或 `mktime` 返回 `-1` 时抛官方错误，默认 LuaJIT 构建保留旧 `nil` 表面。
- 同批把上述 date/time represented 错误加入 `test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 的 `os` 热错误路径；`os_helpers` 期望值同步从每轮 11 项扩到 12 项。
- 本轮验证：新增 represented 用例先失败于 `test/smoke.lua:3266`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 均已通过。完整 perf 中 `os_helpers` 在 JIT on 三档 profile 下约 0.064-0.068s、JIT off 约 0.017s，未出现保留内存增长。
- 已继续按官方 Lua 5.4.8 `loslib.c` 收紧 os 库时间参数：`test/smoke.lua` 先新增 `os.date("%Y", 1.5)` / 字符串 `"1.5"`、`os.difftime(2.5, 1)` / 第二参数 fraction、以及 `os.difftime(1)` 缺第二参数回归；旧实现会截断 fraction 或把缺失第二参数当 0。`src/lib_os.c` 新增 Lua 5.4 专用 `os_checktime_named54()`，时间参数现在必须有整数表示，`os.difftime` 第二参数也改为必填，同时保留字符串整数转换。
- 同批扩展 `test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 的 `os` 热错误路径，覆盖 `os.date` / `os.difftime` 的 fraction 和缺参错误；`os_helpers` 期望值同步从每轮 9 项扩到 11 项。
- 本轮验证：新增 `os.date("%Y", 1.5)` 用例先失败于 `test/smoke.lua:3262`；修复后 `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 均已通过。完整 perf 中 `os_helpers` 在 JIT on 三档 profile 下约 0.056-0.060s、JIT off 约 0.015s，未出现保留内存增长。
- 已按 Lua 5.4 数值子类型继续收紧 `os.time()`：`test/smoke.lua`、`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 先新增 2020 年 32 位范围内 timestamp 必须返回 `math.type(...) == "integer"` 的回归；旧实现先失败于 `test/smoke.lua:3285`，随后 `src/lib_os.c` 的 Lua 5.4 分支改为用 `setint64V()` 推入 `time_t`，在当前 32 位 integer subtype 可表示时返回 integer，超出当前 runtime integer 表面时仍退回 number。
- 本轮验证：`cmd /c build.bat smoke54`、`src\luajit.exe test\lua54_perf.lua jit_on`、`src\luajit.exe test\lua54_perf.lua jit_off` 和 `cmd /c build.bat test` 均已通过；`os_helpers` 在完整 perf 中 JIT on 三档 profile 下约 0.035-0.037s、JIT off 约 0.009s，未出现保留内存增长。
- 已继续补强 Lua 5.4 os helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `os.date()` 稳定本地日期与内嵌 NUL 格式、`os.difftime()`、`os.time(table)` 字段规范化回写、`os.getenv()`、`os.setlocale(nil, "time")`，以及 `os.time()` / `os.date()` 参数错误的热循环回归；`test/lua54_perf.lua` 新增 `os_helpers`，纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 本轮验证：`src\luajit.exe test\lua54_jit_regress.lua`、`src\luajit.exe test\lua54_perf.lua jit_on`、`src\luajit.exe test\lua54_perf.lua jit_off` 和 `cmd /c build.bat test` 均已通过；完整 perf 中 `os_helpers` 在 JIT on 三档 profile 下约 0.036-0.038s、JIT off 约 0.009s，保留内存均为 0KB。
- 已按本次反馈继续收紧 `build.bat` 的 GNU make 并发上限：默认并发仍按 Windows 检测到的物理 CPU 核数取一半且保底 1；裸 `-j` 会被剥离并归一到该上限，显式 `-jN` 和环境变量 `BUILD_JOBS` 现在也只能降低并发，不能再把线程数抬高超过半物理核心。
- 本轮 dry-run 验证：当前机器 24 个物理核、32 个逻辑线程，`cmd /c build.bat build -n`、`cmd /c build.bat build -j -n`、`cmd /c build.bat build -j999 -n` 和 `BUILD_JOBS=999` 都输出 `-j12`；显式降低到 `-j4` 时保持 `-j4`，`build.bat help` 的并发说明已同步。
- 已继续补强 Lua 5.4 package/require helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `require()` 已加载模块 fast path、nil-returning preload loader 首次 `:preload:` loaderdata 与后续 loaded fast path、loader 抛错不污染 `package.loaded`，以及 `package.searchpath()` 保留空模板错误片段的热循环回归；`test/lua54_perf.lua` 新增 `package_helpers`，纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续补强 Lua 5.4 GC mode shim 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `collectgarbage("generational")` / `"incremental"`、`"isrunning"`、`"step"` 字符串整数参数，以及 `collectgarbage("step", 1.2)` 整数错误路径的热循环回归；`test/lua54_perf.lua` 新增 `gc_mode_helpers`，把这些公开 GC 控制入口纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。底层仍是 LuaJIT GC，这里固定的是 Lua 5.4 兼容公开表面不能因 JIT 或 opt profile 漂移。
- 已继续补强 Lua 5.4 coroutine helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `coroutine.running()` / `status()` / `isyieldable(co)` 热循环、`coroutine.resume()` 多返回和 nil 洞、`coroutine.close()` 正常关闭 suspended coroutine、关闭 `<close>` local 时传播非字符串错误对象，以及 `coroutine.wrap()` 错误路径先关闭 active close local 的 trace 回归；`test/lua54_perf.lua` 新增 `coroutine_helpers`，把同一批控制流 helper 纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 本轮验证：`src\luajit.exe test\lua54_jit_regress.lua`、`src\luajit.exe test\lua54_perf.lua jit_on`、`src\luajit.exe test\lua54_perf.lua jit_off` 和单路 `cmd /c build.bat test` 均已通过；`coroutine_helpers` 在 JIT on 三档 profile 下约 0.012-0.013s、JIT off 约 0.004s，`gc_mode_helpers` 在 JIT on 三档 profile 下约 0.006-0.007s、JIT off 约 0.002s，`package_helpers` 在 JIT on 三档 profile 下约 0.044-0.049s、JIT off 约 0.010s，未触发内存门禁。曾并行启动 `build.bat lua54` 与 `build.bat test`，其中 `lua54` 一路因同时清理生成文件失败；该并发清理冲突已通过单独重跑 `cmd /c build.bat lua54` 和随后单路 `cmd /c build.bat test` 排除。
- 已继续按官方 Lua 5.4.8 `lauxlib.c` 收紧 `luaL_Buffer` 大缓冲生命周期：`test/lua54_capi_smoke.c` 新增自定义 allocator live-bytes 回归，C 函数先把 `luaL_Buffer` 扩到大于 `LUAL_BUFFERSIZE`，随后在 `luaL_pushresult()` 前抛错；旧实现会把直接 allocator 侧缓冲泄漏到 `lua_close()` 后，用例先按预期失败于 `luaL_Buffer large allocation is closed after error`。
- `src/lib_aux.c` 的 Lua 5.4 buffer 实现已改为官方同类的栈上 box 模型：`luaL_buffinit()` 先压 placeholder，扩容时替换成带 `__gc` / `__close` 的 userdata box 并 `lua_toclose()` 标记，`luaL_pushresult()` 正常关闭 box，错误 unwind 则由 `<close>` 释放侧缓冲。修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.066-0.068s、JIT off 约 0.007s，新增 buffer 生命周期用例没有引入内存门禁失败。
- 已继续对齐 Lua 5.4 `lua_pushfstring()` / `lua_pushvfstring()` formatter 表面：`test/lua54_capi_smoke.c` 新增 `%I`、`%U`、`%f` 和既有 `%d/%c/%s/%%` 组合回归，固定 `%I` 按 `lua_Integer` 输出、`%U` 按 UTF-8 codepoint 输出、`%f` 走 Lua 5.4 raw number-to-string 因而保留 `1.0` 的 `.0`；同一用例还覆盖非法 `%Z`、带 modifier 的 `%04d`、以及旧 LuaJIT 扩展 `%u` / `%g` 都必须抛 `invalid option ... to 'lua_pushfstring'`，而不是沿用旧 LuaJIT 的 `?` 占位或通用 printf 子集路径。`test/smoke.lua` 同步新增 `string.format("%I")` / `"%U"` 负向回归，防止 C API 专用格式漏进 Lua 层 `string.format`。
- 实现上只在 `lj_strfmt_pushvf()` 处理 Lua 5.4 专用 `%I/%U/%f`、非法转换、禁止 modifier 和限制官方转换集的错误，不扩大共享 `string.format` parser；`%U` 使用本地 UTF-8 写入 helper，`%I/%f` 复用当前 Lua 5.4 number-to-string 路径，默认 LuaJIT 构建保持旧 formatter 表面。本轮验证：新增非法格式用例先按预期失败于 `lua_pushfstring invalid format status`，新增 `%04d` 用例先失败于 `lua_pushfstring invalid modifier status/message`，新增 `%u` 用例先失败于 `lua_pushfstring invalid unsigned status`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 C API formatter 正向、错误路径、Lua 层 formatter 负向 smoke、官方 Lua 5.4.8 matrix、头文件 gate，以及 JIT on/off perf/memory smoke。本轮 perf 中 `number_string_helpers` 约 0.000-0.001s，`number_pack_helpers` JIT on 约 0.067s、JIT off 约 0.007s。
- 已继续收紧 Lua 5.4 数字转字符串子类型表面：`test/smoke.lua` 新增 `tostring(1.0)`、`"x" .. 1.0` 和 `table.concat({1.0, 2, 3.5})` 回归，`test/lua54_capi_smoke.c` 新增 `luaL_tolstring()` 的 integer / integral-float / fraction-float 覆盖；`src/lj_strfmt_num.c` / `src/lj_strfmt.c` / `src/lj_meta.c` / `src/lj_buf.c` 已让 Lua 5.4 兼容构建在 raw number-to-string、print/write、concat 和 table.concat 路径保留 float subtype 的 `.0`。
- 同批补强 JIT 回归：`test/lua54_jit_regress.lua` 新增 `tostring(1.0)`、数字拼接和 `table.concat` 数字元素的 trace 断言，要求开启 JIT 后仍保留 integer/float subtype 文本，避免 recorder 或 buffer 快路径退回旧 LuaJIT `1.0 -> "1"` 行为。
- 本轮验证：新增 Lua 用例先在旧实现上失败于 `test/smoke.lua:1404` 的 `tostring(1.0)`，修完共享转换后又暴露 `test/smoke.lua:1406` 的拼接路径仍走旧 buffer 写法；补齐 concat 直接路径并追加 JIT 回归后，`src/luajit test/lua54_jit_regress.lua`、`cmd /c build.bat smoke54`、`cmd /c build.bat lua54`、`cmd /c build.bat lua54perf` 和 `cmd /c build.bat test` 均已通过。本轮 perf 新增 `number_string_helpers`，JIT on 三档约 0.000-0.001s，JIT off 约 0.000s；`number_pack_helpers` JIT on 三档约 0.067-0.072s，JIT off 约 0.007s。
- 已继续收紧 Lua 5.4 `luaL_checkstack()` 的错误文本边界：`test/lua54_capi_smoke.c` 新增 `luaL_checkstack(L, INT_MAX, NULL)` 回归，固定 auxiliary message 为 NULL 时只报告 `stack overflow`，不能泄露旧格式化路径的 `(null)`；`src/lj_api.c` 已在 Lua 5.4 兼容构建下走无附加文本的 caller error，默认 LuaJIT 构建保持旧表面。
- 本轮验证：新增用例先在旧实现上失败于 `luaL_checkstack NULL message text`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.066-0.067s，JIT off 约 0.007s。
- 已继续收紧 Lua 5.4 `luaL_typeerror()` 的 light userdata 类型名：`test/lua54_capi_smoke.c` 新增 `luaL_checktype()` 直接路径和 `luaL_argexpected()` 宏路径的 light userdata 回归，固定没有字符串 `__name` 覆盖时错误文本必须包含 `light userdata`；`src/lib_aux.c` 已在兼容构建下补官方特判，默认 LuaJIT 构建保持旧表面。
- 本轮验证：新增 `luaL_checktype()` light userdata 覆盖已在既有实现上通过，`luaL_argexpected()` 宏路径先失败于 `luaL_argexpected light userdata name`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke；本轮 perf 中 `number_pack_helpers` JIT on 三档均约 0.068s，JIT off 约 0.007s。
- 已继续收紧 Lua 5.4 `luaL_checkversion_()` 错误文本：`test/lua54_capi_smoke.c` 新增 version mismatch 数字形态回归，固定错误中出现 `503.0` / `504.0` 这类 Lua number 文本；`src/lib_aux.c` 已在兼容构建下按 Lua 5.4 number tostring 形态格式化版本号，默认 LuaJIT 构建保留旧整数文本。
- 本轮验证：新增用例先在旧实现上失败于 `luaL_checkversion_ version numbers are Lua numbers`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.065-0.069s，JIT off 约 0.007s。
- 已继续对齐 Lua 5.4 lauxlib I/O 结果 helper 边界：`test/lua54_capi_smoke.c` 新增 `luaL_fileresult()` 在 `errno == 0` 的失败返回必须给 `"(no extra info)"`，以及 `luaL_execresult(nonzero)` 在 `errno` 有值时必须返回 system-error tuple 的回归；`src/lib_aux.c` 已按官方 Lua 5.4 收紧这两条路径，默认 LuaJIT 构建保持旧行为。
- 本轮验证：新增用例先在旧实现上失败于 `luaL_fileresult errno-zero message`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.066-0.068s，JIT off 约 0.007s。
- 已继续对齐 Lua 5.4 lauxlib ref freelist 语义：`test/lua54_capi_smoke.c` 新增 `luaL_ref()` / `luaL_unref()` 回归，固定 freed ref slot 会链到整数 `0`，同时 ref table 自己的 key `0` 不能被 auxiliary freelist 覆盖；`src/lib_aux.c` 已把 Lua 5.4 兼容构建的 freelist key 移到 `LUA_RIDX_LAST + 1`，默认 LuaJIT 构建继续保留旧 key `0` 表面。
- 本轮验证：新增用例先在旧实现上失败于 `luaL_ref freelist preserves key 0`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.067-0.070s，JIT off 约 0.007s。
- 已继续对齐 Lua 5.4 lauxlib 返回值表面：`luaL_getmetafield()` 在兼容构建下返回被压入 metafield 的实际类型码，`test/lua54_capi_smoke.c` 已把 `__name` 字符串字段固定为 `LUA_TSTRING`；默认 LuaJIT 构建继续保留旧的 1/0 返回表面。
- 本轮验证：新增类型码用例先在旧实现上失败于 `luaL_getmetafield returns field type`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、JIT on/off perf/memory smoke 和 lauxlib 类型码回归；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.068-0.069s，JIT off 约 0.007s。
- 已继续收紧 Lua 5.4 auxiliary library 兼容：`test/lua54_capi_smoke.c` 新增 `luaL_setfuncs()` 中 `{name, NULL}` 占位项用例，固定官方 Lua 5.4 行为会把该字段设为 `false`，且不会消耗共享 upvalue；`src/lib_aux.c` 已按此补 NULL placeholder 分支。
- 本轮验证：新增用例先在旧实现上失败于 `luaL_setfuncs NULL placeholder becomes false`；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate、JIT on/off perf/memory smoke 和新增 lauxlib 占位项回归。
- 已继续补强 Lua 5.4 C API `lua_toclose()` 正常 C 返回时的 yieldable `__close` 边界：`test/lua54_capi_smoke.c` 新增 C 函数返回前 `__close` 先 `coroutine.yield()` 再成功、以及 yield 后再抛错的 coroutine 恢复用例，固定返回值窗口保留、close 错误替换和 `lua_resume()` 错误公开栈只留下单个错误对象。
- 同批接入 VM continuation：新增 `LJ_CONT_CLOSE_CFRAME`，x64 / ARM64 的 `vm_returnc` 会在 return hook 和返回值搬移前通过 `lj_close_prepare_cframe_pcall()` 进入 close continuation，恢复后用 `lj_close_continue_cframe_pcall()` 继续关闭剩余 marked slot，并重新计算 C return result window。
- 已继续补强 C-return close continuation 的重入边界：`test/lua54_capi_smoke.c` 新增两个 marked slot 连续 yield、被返回值自身是 to-be-closed slot、以及内层 `__close` yield 后抛错且外层 close 接收替换错误的回归；`lj_close` 现在把原 C 返回计数保存在 continuation 隐藏栈槽，避免嵌套 `pcall(__close, ...)` 的 C-return 覆盖外层返回窗口。
- 同批修正 default / Lua 5.4 双构建生成边界：`vm_returnc_closed` 和 `cont_close_cframe` 标签在 x64 / ARM64 DynASM 中都有默认构建 fallback，避免 Lua 5.4 专用 continuation 接线让默认 LuaJIT 构建报未定义标签。
- 本轮验证：`cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 C-return close-yield 和重入回归、更新后的 x64/ARM64 静态 VM 后端门禁、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke；本轮 perf 中 `number_pack_helpers` JIT on 三档约 0.067-0.068s、JIT off 约 0.007s。
- 平台验证：`cmd /c build.bat platformandroid` 已通过 Android ARM64 Lua 5.4 compat 静态 artifact 构建，真实汇编了更新后的 `vm_arm64.dasc`；本机当前无在线 adb 设备，设备 smoke 按脚本输出 SKIP。
- 已继续补强 Lua 5.4 C API to-be-closed 边界：`test/lua54_capi_smoke.c` 新增 `lua_closethread()` / `lua_resetthread()` 关闭挂起 coroutine 时，Lua `__close` metamethod 试图 `coroutine.yield()` 的回归；该路径必须返回 `LUA_ERRRUN`、报告 `yield across` 错误、把目标 coroutine 复位到不可继续恢复的干净状态，并允许后续再次 close/reset 成功。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 C API close-yield 回归、Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate 和现有 JIT regression。
- 已继续补强 Lua 5.4 number/pack helper 的 JIT 与性能门禁：`test/lua54_jit_regress.lua` 新增 `math.type()` integer/float/nil 热循环、`tonumber(str, base)` 显式 base=10/2/16/34 热循环，以及 `string.packsize()` 固定格式和 `z` / `c` 错误格式热循环；这些路径都先用真实 recorder 探针确认会产生 trace，再写入回归。
- `test/lua54_perf.lua` 已新增 `number_pack_helpers` 负载，覆盖 `math.type`、`math.tointeger`、`math.ult`、显式 base `tonumber` 和 `string.packsize`，并纳入三档显式 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 本轮验证：`src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off`、`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过；完整 perf 批次中 `number_pack_helpers` 在 JIT on 三档 profile 下约 0.068-0.070s、JIT off 约 0.007s，未出现额外保留内存增长。
- 已复核并确认 `build.bat` 的 `make -j` 并发上限符合要求：默认按 Windows 检测到的物理 CPU 核数取一半且保底 1，裸 `-j` 会被剥离后归一到这个上限，过大的环境变量 `BUILD_JOBS` 也会被压回半核，避免测试/编译把机器线程打满。
- 本轮 dry-run 验证：当前机器 24 个物理核、32 个逻辑线程，`cmd /c build.bat build -n`、`cmd /c build.bat build -j -n` 和 `BUILD_JOBS=999` 的 dry-run 都输出 `-j12`，已从旧的半逻辑线程 `-j16` 降到半物理核心。
- 已把官方 Lua 5.4.8 `calls.lua` 的 pre-binary 段纳入 `test/lua54_official_matrix.lua`：runner 会读取官方文件并执行到 `print("testing binary chunks")` 前，覆盖 function/call、tail call、`load`/`string.dump`、upvalue 和 200 upvalue dump/load 等段；仅保留已文档化的 LuaJIT bytecode 与官方 Lua 5.4 binary chunk header 差异块作为跳过边界。
- `TODO.md` 已同步更新官方矩阵状态：`calls.lua pre-binary` 现在计入绿色矩阵，剩余 `calls.lua` 观察项只指向 binary chunk header block，不再把整份 `calls.lua` 排除。
- 本轮完整验证：`cmd /c build.bat official54` 已通过并输出 `official54: calls.lua pre-binary`；随后 `cmd /c build.bat test` 已通过，覆盖 default smoke/C API、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 gate、C++ `lua.hpp` gate，以及 JIT on/off perf/memory smoke。两个入口都使用半物理核心 `-j12`。
- 已清理 `recff_rawlen` 在 default / Lua 5.4 compat 交替构建时的生成头文件边界：`recff_rawlen` recorder 现在始终保留给可能滞后的 `lj_recdef.h` 引用，同时在当前配置未生成 `rawlen` recorder 表时用编译器 `unused` 属性压掉无效 warning；这样既不会因为上一次 Lua 5.4 生成头导致 default 增量构建缺符号，也不会在 clean default 构建里留下 `-Wunused-function` 噪声。
- 本轮验证：先用 stale Lua 5.4 `lj_recdef.h` 跑 `cmd /c build.bat build` 复现了条件包裹方案的 `recff_rawlen undeclared` 失败；修正后 `cmd /c build.bat build`、`cmd /c build.bat default`、`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，完整矩阵输出中不再出现 `recff_rawlen` warning，并继续覆盖 default、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 gate 和 JIT on/off perf/memory smoke。
- 已修正 Lua 5.4 `require()` 的 loader 失败和 nil 返回边界：兼容模式不再沿用 LuaJIT 5.1 的 `_LOADED[name]` sentinel 预写入，loader 抛错后 `package.loaded[name]` 会保持 `nil`，后续 `require()` 会重新执行 loader；loader 返回 `nil` 且没有自行写入 `_LOADED[name]` 时会按官方 Lua 5.4 写入并返回 `true`，第二次 require 只返回模块值、不再返回 loaderdata。`test/smoke.lua` 已新增这两类回归。
- 本轮验证：新增 `require()` 回归先在旧二进制上失败于第二次 require 报 `loop or previous error loading module` 和 nil loader 返回内部 sentinel；修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat lua54`、`cmd /c build.bat test` 均已通过，覆盖 Lua 5.4 smoke、官方 Lua 5.4.8 matrix、C API/头文件 gate、默认构建，以及 JIT on/off perf/memory smoke。
- 已继续补强 Lua 5.4 `table.sort` 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增数字小表排序、对象 `__lt` 排序、非法比较函数错误路径，以及 proxy table 通过 `__len` / `__index` / `__newindex` 排序的热循环回归；`test/lua54_perf.lua` 新增 `table_sort_helpers` 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 的时间/内存 smoke，避免排序兼容路径只在普通 smoke 中被覆盖。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off` 均已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 `table.sort` JIT/perf 回归、默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 gate，以及 JIT on/off perf/memory smoke。
- 已按要求收紧 `build.bat` 的 `make -j` 自动并发：默认按检测到的 CPU 物理核数取一半且保底 1；如果环境里残留了过大的 `BUILD_JOBS`，现在也会被压到这个半核上限，避免全局变量把编译线程数拉太高；命令行显式 `-jN` 仍作为一次性人工覆盖。
- 已继续补强 Lua 5.4 base value fast function 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `tostring()` primitive、`__tostring` 字符串返回、非字符串返回错误、`__name` 默认对象名、`type()`、`select("#", ...)` / `select("2", ...)`、`select(1.2, ...)` 错误路径、`assert(true, ...)` 以及 `assert(false, table)` 保留非字符串错误对象的热循环回归；`test/lua54_perf.lua` 新增 `base_value_helpers` 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 同批修正 Lua 5.4 recorder 边界：`tostring()` 遇到 `__tostring` 时不再沿用 LuaJIT 5.1 tailcall 假设跳过返回值字符串检查；`select()` recorder 在 Lua 5.4 下用整数表示规则校验运行期 index，不再把 `1.2` 截断成 `1` 录入 trace；`test/lua54_jit_regress.lua` 的 trace helper 也改成 off/flush/GC/on 隔离，避免错误路径残留影响下一条 JIT 门禁。
- 本轮验证：新增 `select(1.2, ...)` JIT 回归先暴露旧 recorder 截断和后续 trace 状态崩溃；修复后 `src/luajit test/lua54_jit_regress.lua` 连续 30 次通过，`src/luajit test/lua54_perf.lua jit_on` / `jit_off` 通过，`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 gate，以及 JIT on/off perf/memory smoke。
- 已继续补强 Lua 5.4 protected-call helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `pcall()` / `xpcall()` 成功调用、多参数传递、字符串错误、非字符串错误对象、message handler 返回对象，以及 `error(table, level)` 保留非字符串错误对象的热循环回归；`test/lua54_perf.lua` 新增 `protected_call_helpers` 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 已继续补强 Lua 5.4 base/raw fast function 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `getmetatable()` 的 `__metatable` 保护值热循环、未受保护 `setmetatable()` 热循环、受保护 `setmetatable()` 错误路径不生成错误 trace，以及 `rawlen()` / `rawget()` / `rawset()` / `rawequal()` 绕过 `__len` / `__index` / `__newindex` / `__eq` 的热循环回归；`test/lua54_perf.lua` 新增 `base_raw_helpers` 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off` 均已通过；其中 protected `setmetatable` 首轮测试暴露“错误路径不会录 trace”的合理退避，已改为 normal path 必须 trace、protected error path 必须 no-trace 的门禁。
- 已继续补强 Lua 5.4 `utf8` helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `utf8.char()` / `codepoint()` / `codes()` / `len()` / `offset()` 在 lax 扩展码点路径下的热循环回归，并覆盖 `utf8.char(0x80000000)`、`utf8.offset(..., 1.2)` 和非法 UTF-8 `utf8.len()` 的热错误/返回路径；`test/lua54_perf.lua` 新增 `utf8_helpers` 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off` 均已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，新增 `utf8_helpers` 在 JIT on 三档 profile 和 JIT off 下均满足当前时间/内存门禁，并已进入完整批处理矩阵。
- 已修正 Lua 5.4 tail-call 到 C 标准库 local/upvalue alias 时的参数错误名：新增 `local f = math.abs; return f(true)`、闭包 upvalue `local f = math.abs; return f(true)` 以及 `local f = table.concat; return f(nil)` 回归；parser 现在会给由已知标准库字段初始化的 local alias 打标记，tail-position 调用时保留 caller frame，并把该标记传播到 upvalue，避免 `BC_CALLT` 把调用点局部名 `f` 丢成 `math.abs` / `table.concat`。
- 同批收紧 `math_argname54()` 的 direct-pcall 判定：有真实源码调用点名字时优先使用 `f` / `abs`，只有 `pcall(math.xxx, bad)` 这种没有 Lua 调用点的入口才保留官方 `math.xxx` fallback；本轮验证 `cmd /c build.bat smoke54` 已通过，覆盖新增错误名回归、Lua 5.4 smoke、官方 Lua 5.4.8 当前矩阵和既有 Lua 5.4 regressions。
- 已继续补强 Lua 5.4 string helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `string.find(..., init, true)` 在热循环中接受字符串整数 init 并记录 trace 的回归，同时固定 pattern capture 路径不能被 plain-find recorder 错录，以及 `string.find(..., 1.2, true)` 热错误路径仍保留整数表示错误；`test/lua54_perf.lua` 新增 `string.find` / `string.sub` / `string.rep` / `string.format("%q")` 混合 helper 负载，纳入 JIT on 多 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off` 均已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 gate、负向编译 gate，以及新增 string helper perf/memory smoke。
- 已继续补强 Lua 5.4 pattern helper 的 JIT/trace 和性能边界：`test/lua54_jit_regress.lua` 新增 `string.gmatch(..., init)` 字符串整数 init + 空匹配推进回归，以及 `string.gsub("a b cd", " *", "-")` 空匹配 replacement 计数回归；`test/lua54_perf.lua` 的 string helper 负载同步覆盖 `gsub` / `gmatch`，避免 pattern helper 兼容路径只在功能 smoke 中出现。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off` 均已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 pattern helper JIT/perf 回归、默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 gate 和负向编译 gate。
- 已继续补强 Lua 5.4 常用 string helper 热路径：`test/lua54_jit_regress.lua` 新增 `string.match(..., init)` 字符串整数 init、`string.len`、`string.reverse`、`string.lower`、`string.upper` 的热循环回归；`test/lua54_perf.lua` 的 string helper 负载同步加入 match/case/reverse/len，扩大 JIT on/off 性能门禁覆盖。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua`、`src/luajit test/lua54_perf.lua jit_on`、`src/luajit test/lua54_perf.lua jit_off` 均已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增常用 string helper 热路径、默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 gate 和负向编译 gate。
- 已继续补强 Lua 5.4 string helper 的 JIT/trace 边界：`test/lua54_jit_regress.lua` 新增 `string.byte()` / `string.sub()` 在热循环中接受字符串整数参数并产生 trace 的回归，同时覆盖 `string.sub(..., 1.2)` 和 `string.char(256)` 的热错误路径仍分别保留 `number has no integer representation` / `value out of range`。
- 本轮验证：新增 string range/char JIT 用例修正测试期望值后，单独 `src/luajit test/lua54_jit_regress.lua` 已通过。
- 已继续补强 Lua 5.4 JIT/trace helper 回归：`test/lua54_jit_regress.lua` 新增 `string.rep(s, n, sep)` 热循环，固定 recorder 记录 trace 后仍保留 Lua 5.4 separator 语义；同时新增超大 repeat 热错误路径，要求保持解释器的确定性 `resulting string too large`，且不能录成旧 buffer/memory-error helper trace。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua` 已通过；`cmd /c build.bat lua54` 已通过，覆盖新增 JIT regression、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate 和 C++ `lua.hpp` 头文件 gate。
- 已补强 Lua 5.4 C API allocator shrink 语义用例：`test/lua54_capi_smoke.c` 现在会先用 `string.rep` 撑大 GC 临时 buffer，再让自定义 allocator 拒绝 GC 期间的 shrink，并断言拒绝的旧块超过 1MB 且 full GC 仍成功；这把此前只覆盖线程栈收缩失败的用例扩展到 `lj_buf_shrink()` 的 opportunistic tmpbuf 收缩路径。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 tmpbuf shrink allocator-failure 用例、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate 和 C++ `lua.hpp` 头文件 gate。
- 已复核 `build.bat` 的 GNU make 并行度策略：未显式指定 `BUILD_JOBS` / `-jN` 时默认按 Windows 物理 CPU 核数取一半，裸 `-j` 会被剥离并归一到这个有界默认值，避免 GNU make 无限并发；本机 24 物理核、32 逻辑线程实测 `build.bat build -n` 和 `build.bat build -j -n` 均输出 `-j12`，显式 `build.bat build -j8 -n` 仍保留人工覆盖。
- 已继续收紧 C++ wrapper `lua.hpp` 的 Lua 5.4 外部头表面：兼容模式下 `lua.hpp` 现在只包含 `lua.h` / `lauxlib.h` / `lualib.h`，不再无条件包含 `luajit.h` 泄露 `LUAJIT_VERSION` 等 LuaJIT 专有宏；默认 LuaJIT 模式仍保留历史 `lua.hpp` 包含 `luajit.h` 的行为。新增 `test/lua54_lu.hpp_header_smoke.cpp` 固定 Lua 5.4 C++ 头表面和私有 selector 不泄露，新增 `test/lua51_lu.hpp_header_smoke.cpp` 固定默认模式仍暴露 LuaJIT C++ 头表面。
- 本轮验证：`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 C++ `lua.hpp` 头文件 gate、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 gate、既有负向编译门禁，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续收紧 Lua 5.4 外部头文件私有 selector 边界：`LUAJIT_EXTERNAL_LUA54` 现在只在 `lua.h` / `lauxlib.h` / `lualib.h` 内部临时计算，用完即 `#undef`，外部模块包含任一公开头后都不会再看到该非官方宏；`test/lua54_lua_header_smoke.c`、`test/lua54_lauxlib_header_smoke.c`、`test/lua54_lualib_header_smoke.c` 和 `test/lua54_header_macro_audit.lua` 已同步加入负向门禁，先红于旧泄露后再修复。
- 本轮验证：新增不泄露门禁先按预期失败于旧 `LUAJIT_EXTERNAL_LUA54` 外露；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 `LUAJIT_EXTERNAL_LUA54` 不泄露门禁、官方/当前头文件宏名审计、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 gate、既有负向编译门禁，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 平台批次复核：`cmd /c build.bat platformpc` 已通过，default 与 Lua 5.4 compat 双构建均产出 AMD64 `0x8664` PE 并运行 smoke；`cmd /c build.bat platformprobe` 已通过当前主机探针，iOS 在 Windows 上按环境限制 SKIP，Emscripten 识别到 `emcc 4.0.3` 并继续定位为 LuaJIT 缺 wasm/wasm64 VM 后端的目标选择阻塞。
- 已继续收紧 Lua 5.4 外部 `luaconf.h` 私有宏边界：先新增 `test/lua54_luaconf_extra_reject.c` 并接入 `smoketest-capi-lua54compat`，用例先红于 `LUA_PATH_CONFIG` / `LUA_QS` / `LUAI_GCMUL` / `LUA_NUMBER_SCAN` 等 LuaJIT/Lua 5.1 私有宏泄露；随后在 `src/Makefile` 为 host/target 内部对象统一定义 `LUAJIT_INTERNAL_USE`，再把这些私有宏限制为内部构建和默认 LuaJIT 5.1 头可见，外部 Lua 5.4 模块默认不再看到。同批新增 `test/lua54_luaconf_apicheck_smoke.c`，固定显式 `LUA_USE_APICHECK` 时仍按官方 Lua 5.4 暴露 `luai_apicheck`。
- 已新增 Lua 5.4 头文件宏名级审计门禁：`test/lua54_header_macro_audit.lua` 会用 gcc 预处理官方 Lua 5.4.8 和当前外部兼容头，要求没有缺失官方公开宏名；当前额外项只允许 `lua_load` / `lua_resume` / `lua_gettable` / `lua_version` 等已知 ABI wrapper 宏和构建开关宏，避免后续误把未记录的私有宏重新暴露出去。该门禁先暴露了测试自身两个问题：Lua pattern 不支持 `|` alternation 会导致采集空集合假绿，临时输入放在 `src/` 下又会让官方侧 quoted include 误读本仓库头；两处均已修正，并加了空集合断言。
- 已修正 `build.bat` 的失败错误码传播：`:RUN` / `:RUN_PLATFORM` 现在用 delayed expansion 的 `!ERRORLEVEL!` 返回子命令状态，避免括号块内 `%ERRORLEVEL%` 提前展开导致 make 失败仍返回 0；实测缺失 make target 返回 exit code 2，裸 `-j` 仍规整为半物理核心 `-j12`。
- 本轮验证：新增 private-macro 负向门禁修复前按预期失败；修复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 gate、新增 `luaconf.h` 私有宏负向 gate、`LUA_USE_APICHECK` 正向 gate、官方/当前头文件宏名审计，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续对照官方 Lua 5.4.8 `lua.h` / `luaconf.h` 预处理宏表，确认当前外部兼容头仍会额外暴露一批 LuaJIT/Lua 5.1 私有配置宏（如 `LUA_PATH_CONFIG`、`LUA_QS`、`LUA_NUMBER_SCAN`、`LUAI_GCMUL` 等）；直接在 `luaconf.h` 或 `lua.h` 中裁剪会同时影响当前 LuaJIT 内部兼容构建，因为内部源码和外部 C 模块暂时使用同一组 `LUAJIT_ENABLE_LUA54COMPAT` 编译宏。该项已记录到 `TODO.md`，后续需先建立内部编译标记（例如让内部对象显式带 `LUAJIT_INTERNAL_USE` 或等价边界）再补负向门禁，避免再次破坏内部错误文本/配置宏。
- 本轮验证：新增私有宏负向门禁曾按预期失败于 `LUA_PATH_CONFIG` / `LUA_QS` 等泄露；直接裁剪方案又失败于内部 `LUA_QS` / `LUA_QL` 依赖。已撤回破坏性改动，恢复后 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，完整覆盖默认构建、Lua 5.4 兼容、官方矩阵、C API/头文件 gate，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `luaconf.h` 补强 include guard 静态门禁：新增 `test/lua54_luaconf_guard_smoke.c` 并接入 `smoketest-capi-lua54compat`，固定预定义 `luaconf_h` 时必须跳过整份配置头内容，不能泄露 `LUA_INT_TYPE` / `LUA_FLOAT_TYPE`、路径配置、平台配置或 `LUAI_MAXSTACK` 等内部限制宏。
- 本轮验证：`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 `luaconf_h` guard 静态门禁、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译门禁，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `lua.h` 补强 include guard 静态门禁：新增 `test/lua54_lua_guard_smoke.c` 并接入 `smoketest-capi-lua54compat`，固定预定义 `lua_h` 时必须跳过整份核心头内容，不能泄露 `LUA_VERSION`、`LUA_SIGNATURE`、`LUA_REGISTRYINDEX` 或类型 tag 宏；同轮用官方 5.4.8 预处理宏表对照确认当前外部 `LUA_` / `lua_` / `luai_` 名字无缺失。
- 本轮验证：`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 `lua_h` guard 静态门禁、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译门禁，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `lauxlib.h` 补强头文件 ABI 静态门禁：`test/lua54_lauxlib_header_smoke.c` 现在固定 `luaL_Buffer` 的 `b/size/n/L/init` 字段顺序、字段类型、`LUAI_MAXALIGN` union 成员和 `luaL_Stream` 字段类型；新增 `test/lua54_lauxlib_guard_smoke.c` 固定预定义 `lauxlib_h` 时整份 lauxlib 声明/宏都会被 include guard 跳过。
- 本轮验证：`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 lauxlib ABI/guard 静态门禁、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译门禁，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `lauxlib.h` 补强 macro-only 负向编译门禁：`lua_writestring()` / `lua_writeline()` / `lua_writestringerror()` / `lua_assert()` 现在进入 `test/lua54_capi_lauxlib_macroonly_reject.c` 和 Makefile 循环，固定这些 function-like 宏不能被外部 Lua 5.4 模块当作函数地址获取。
- 本轮验证：`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 lauxlib macro-only gate、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译门禁，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已按要求复核 `build.bat` 的 `make -j` 并发上限：未显式指定 `BUILD_JOBS` / `-jN` 时默认取 Windows 物理 CPU 核数的一半；本机 dry-run 默认 `build.bat build -n` 和裸 `build.bat build -j -n` 都输出 `-j12`，显式 `build.bat build -j8 -n` / `BUILD_JOBS=6` 仍保留人工覆盖。
- 已继续补强 Lua 5.4 JIT/trace 表库 helper 回归覆盖：`test/lua54_jit_regress.lua` 新增 `table.unpack`、`table.insert`、`table.remove` 热循环用例，固定 trace 记录后仍走 Lua 5.4 的 `__index` / `__newindex` / `__len` 和 API get/set 语义，且元方法替换后会失效/重录，避免后续优化退回旧 raw fast path。
- 已继续补强 Lua 5.4 JIT/trace string pack 回归覆盖：`test/lua54_jit_regress.lua` 新增 `string.pack(">i2")` / `string.unpack(">i2")` 热循环，以及 `pack("z", "a\0b")` / `unpack("s1", "")` 的 pcall 热错误路径，固定 trace 下仍保留 Lua 5.4 格式推进和错误文本。
- 已继续补强 Lua 5.4 `math.random` JIT/trace 回归覆盖：同一 seed 下 JIT on 热循环结果必须等于 JIT off 解释器 xoshiro256** 序列，避免旧 LuaJIT random recorder/PRNG 被重新启用；当前 recorder 已能把无参、`0` 和常量整数区间 random 调用录成 side-effect helper call，同时覆盖 `math.random(10, 5)` 的 pcall 热错误路径仍报 `interval is empty`。
- 已继续补强 Lua 5.4 math helper JIT/trace 回归覆盖：新增 `math.min` + `__lt` 元方法热循环并验证元方法替换后失效/重录，新增 `math.tointeger()` 字符串整数/fraction `nil` 热循环，以及 `math.ult()` 当前 integer 表面的无符号比较热循环。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua` 已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 兼容、官方 Lua 5.4.8 matrix、C API/头文件/负向编译 gate，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续收紧一元 `math` fast function 的源码 tail-call 错误名：Lua 5.4 parser 现在会像处理 `table.concat` 一样，把 `return math.abs(...)` / `return (math.abs)(...)` 等覆盖的一元 `math` 字段调用保留为普通 call+return，避免 `BC_CALLT` 在 C 参数检查前丢掉调用者 frame；新增回归固定这些场景按官方报短名 `abs` / `sin`，局部变量 tail-call `local f=math.abs; return f(...)` 仍记录在 TODO 的 deeper `BC_CALLT` 元数据批次。
- 已继续收紧一元 `math` fast function 的 direct-pcall 回归：`pcall(math.abs, true)` 等直接把标准库函数传入 `pcall` 的场景，现在测试只接受官方 Lua 5.4 的 `math.xxx` 错误名，不再允许短名 `xxx` 混过；局部变量 tail-call 的剩余名字差异已继续留在 `TODO.md` 的 debug frame metadata 批次。
- 已继续批量收紧一元 `math` fast function 的 Lua 5.4 参数错误归属：`abs` / `acos` / `asin` / `ceil` / `cos` / `exp` / `floor` / `log` / `modf` / `sin` / `sqrt` / `tan` 的 direct `pcall(math.xxx, bad)` 现在回退到官方 `math.xxx` 名称；源码普通字段调用的 `math.xxx(bad)` 会按 Lua 5.4 显示短字段名 `xxx`，源码 tail-call 的局部名恢复仍留在 `TODO.md`。合法参数由 Lua 5.4 C fallback 直接计算，避免纯 fast path `FFH_RETRY` 自旋。
- 已记录剩余 debug-frame 名字边界：一元 `math` fast function 的错误名仍会随入口 frame 形态在 `math.xxx` 与短名 `xxx` 间切换，局部变量 tail-call 也仍不能恢复 `f`；这些不再伪装成已完成，继续留在 TODO 的 debug frame 元数据批次。
- 已补 `math.log(x, nil)` 的 Lua 5.4 行为：第二参数显式 `nil` 现在等同缺省 base，不再报第 2 参数类型错误；非 nil 的错误 base 继续报到 `math.log`。
- 已继续收紧 `math.fmod` 的 Lua 5.4 错误归属：整数快路径除零现在报 `bad argument #2 to 'math.fmod' (zero)`，不再从 `luaL_argcheck` 泄露为 `?`；同时新增回归固定含 float 零除数仍按官方/C `fmod` 返回 NaN。
- 本轮验证：新增 `math.fmod` 和一元 `math` direct-pcall 回归先使 `cmd /c build.bat smoke54` 失败于旧 `?`/短名错误归属；中途发现 `FFH_RETRY` 用在纯 fast path 上会造成自旋，已改为 Lua 5.4 C fallback 直接计算合法参数。修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及三档显式 `jit.opt` + JIT off perf/memory smoke。
- 已按要求再次收紧 `build.bat` 默认并行度：未显式指定 `BUILD_JOBS` / `-jN` 时，现在优先按 Windows 物理 CPU 核数取一半作为 `make -j`；本机 24 物理核 dry-run 默认和裸 `-j` 均输出 `-j12`，显式 `-j8` / `BUILD_JOBS=6` 仍保持优先。
- 已继续收紧 `math.random` / `math.randomseed` 的 Lua 5.4 参数错误归属：空区间、fraction number 和 boolean 参数现在分别报到官方短函数名 `random` / `randomseed`，不再从共享整数检查 helper 泄露为 `?`。
- 本轮验证：新增 `math.random(10, 5)` 回归先使 `cmd /c build.bat smoke54` 失败于旧 `?` 错误归属；修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及 JIT on/off perf/memory smoke。
- 已继续收紧 `string.unpack` 的 Lua 5.4 运行期数据错误归属：`unpack("z", "abc")` 的 `unfinished string for format 'z'` 和 `unpack("s1", short)` 的 `data string too short` 现在都会报到官方短函数名 `unpack`，不再从内部 unpack helper 泄露为 `?`。
- 本轮验证：新增 `unpack("z")` 回归先使 `cmd /c build.bat smoke54` 失败于旧 `?` 错误归属；修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及 JIT on/off perf/memory smoke。
- 已继续收紧 Lua 5.4 `lauxlib.h` 宏表面：`luaL_argcheck()` / `luaL_argexpected()` 现在按官方 Lua 5.4 形态用 `luai_likely()` 包裹条件，避免外部模块看到的辅助库宏回退到旧 LuaJIT 直接条件展开；`test/lua54_header_static.lua` 已加入静态门禁。
- 本轮验证：新增静态门禁先失败于旧宏形态，修正后单独 `src/luajit test/lua54_header_static.lua` 通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及 JIT on/off perf/memory smoke。
- 已继续补强 Lua 5.4 JIT/trace 回归覆盖：`test/lua54_jit_regress.lua` 新增 `string.format("%p")` / `%q` 热循环记录门禁，固定 `false` / `math.huge` / `NaN` 的 Lua 5.4 输出；带宽度 `%s` 遇到内嵌 NUL 则必须退出回解释器并报 `string contains zeros`，不能被旧 recorder 错录。
- 同批新增 `table.concat` / `table.move` 元方法热循环回归：trace 记录后仍必须走 Lua 5.4 的 `__index` / `__newindex` helper 语义，且元方法替换后要触发失效/重录，避免退回旧 raw fast path。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua` 已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 JIT helper regression、默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及显式 `jit.opt` profile 下的 JIT on/off perf/memory smoke。
- 已继续收紧 `string.pack` / `string.packsize` 的 Lua 5.4 格式错误归属：`c` 缺 size、`z` 字符串含 NUL、`X` next option 非法、alignment 非 2 的幂和 `packsize("z")` 等边界现在都会报到官方短函数名 `pack` / `packsize`，`c` 缺 size 文本也对齐为 `missing size for format option 'c'`；基础缺参错误同步从 `string.pack` / `string.unpack` / `string.packsize` 改为官方 `pack` / `unpack` / `packsize`。
- 本轮验证：新增回归用例先使 `cmd /c build.bat smoke54` 失败于旧 `?`/缺 size 错误文本；修复后 `cmd /c build.bat smoke54`、`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `lua.h` 批量补强常量静态门禁：`test/lua54_lua_header_smoke.c` 现在额外固定 `LUA_MULTRET`、`LUA_OK` / `LUA_YIELD` / `LUA_ERR*`、`LUA_T*` 类型码、`LUA_OP*` 算术/比较码、`LUA_GC*` 变参 GC 操作码以及 `LUA_HOOK*` / `LUA_MASK*` debug hook 常量值。
- 已继续补强 Lua 5.4 `lua.h` 单独包含静态门禁：`test/lua54_lua_header_smoke.c` 现在用编译期检查固定 `LUA_SIGNATURE`、`LUA_VERSION*` / `LUA_RELEASE`、`LUA_VERSION_NUM` / `LUA_VERSION_RELEASE_NUM`、`LUA_MINSTACK`、`LUA_IDSIZE`、`LUA_RIDX_*`、`LUA_NUMTYPES` / `LUA_NUMTAGS` 和 `LUA_EXTRASPACE`，防止外部模块只包含 `lua.h` 时看到的官方 5.4 核心常量表面回退。
- 本轮验证：`cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 `lua.h` 常量静态门禁、默认构建、Lua 5.4 compat、官方 Lua 5.4.8 矩阵、C API/头文件 smoke、负向编译 gate，以及 JIT on/off perf/memory smoke。
- 已继续补强 Lua 5.4 C API macro-only 负向编译门禁：`lua_upvalueindex()` 现在纳入 `test/lua54_capi_macroonly_reject.c` / `Makefile`，在已有官方伪索引公式静态检查之外，额外固定它不能被外部模块当作函数地址获取。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 `UPVALUEINDEX` macro-only gate、`NUMBERTOINTEGER` macro-only gate、intcasts macro-only gate、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和既有负向编译门禁。
- 已继续补强 Lua 5.4 C API macro-only 负向编译门禁：`lua_numbertointeger()` 现在纳入 `test/lua54_capi_macroonly_reject.c` / `Makefile`，固定它作为官方 `luaconf.h` 宏可调用但不能被外部模块取函数地址。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 `NUMBERTOINTEGER` macro-only gate、intcasts macro-only gate、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和既有负向编译门禁。
- 已继续补强 `LUA_COMPAT_APIINTCASTS` macro-only 负向编译门禁：新增 `test/lua54_capi_intcasts_macroonly_reject.c` 并接入 `smoketest-capi-lua54compat`，覆盖 `lua_pushunsigned` / `lua_tounsignedx` / `lua_tounsigned` / `luaL_checkunsigned` / `luaL_optunsigned` 以及 deprecated `luaL_checkint` / `luaL_optint` / `luaL_checklong` / `luaL_optlong` 在兼容开关下只能作为宏调用，不能取得旧 LuaJIT 函数地址。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 intcasts macro-only gate、扩展后的 intcasts 隐藏 gate、`LUA_COMPAT_APIINTCASTS` 正向 smoke、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和既有负向编译门禁。
- 已继续补强 `LUA_COMPAT_APIINTCASTS` 负向编译门禁：`test/lua54_capi_intcasts_reject.c` 现在同时确认普通外部 Lua 5.4 头不会泄露 `lua_pushunsigned` / `lua_tounsignedx` / `lua_tounsigned` / `luaL_checkunsigned` / `luaL_optunsigned`，这些 deprecated unsigned cast 宏只有显式定义 `LUA_COMPAT_APIINTCASTS` 时才可见。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖扩展后的 intcasts 负向编译 gate、`LUA_COMPAT_APIINTCASTS` 正向 smoke、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和既有负向编译门禁。
- 已继续补强 Lua 5.4 C API macro-only 负向编译门禁：`test/lua54_capi_macroonly_reject.c` / `Makefile` 现在把官方宏 `lua_pushliteral()` 纳入 gate，外部 Lua 5.4 头仍可调用该宏，但不能把它当作旧 LuaJIT 函数地址获取。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 `PUSHLITERAL` 负向编译 gate、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和既有负向编译门禁。
- 已继续补强 PC 64 位平台矩阵门禁：`tools/lua54_platform_matrix.ps1 -Target pc` 现在在 default 和 Lua 5.4 compat 两次构建后都会直接读取 `src/luajit.exe` 的 PE header，确认 machine 为 AMD64 `0x8664`，避免只凭 Windows/MSYS2 宿主环境推断产物架构。
- 本轮验证：修改前 `cmd /c build.bat platformpc` 只能证明 PC 目标可构建并运行 smoke；修改后 `cmd /c build.bat platformpc` 会在结果中明确输出 `PE machine AMD64 (0x8664)`，并继续通过 default smoke 与 Lua 5.4 compat smoke。
- 已继续补强 `<close>` / VM unwind 后端静态门禁：`test/lua54_vm_backend_static.lua` 现在会固定 `LJ_CONT_CLOSE`、`lj_close_prepare_pcall`、`lj_close_continue_pcall` 这些 close-yield continuation 接口存在，并要求 x64 / ARM64 VM 后端都包含 close pcall prepare、`LJ_CONT_CLOSE` 分发、`cont_close` 标签和 close pcall continue 接线，防止已完成的两个主力后端回退。
- 本轮验证：单独 `src/luajit test/lua54_vm_backend_static.lua` 已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增静态门禁、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译门禁，以及 JIT on/off perf/memory smoke。
- 已继续推进 Emscripten 平台矩阵的可验证阻塞探针：`tools/lua54_platform_matrix.ps1 -Target emscripten` 现在探测到 `emcc` 后会实际执行 `wasm32-unknown-emscripten` 和 `wasm64-unknown-emscripten` 两个预处理探针；只有两个目标都命中 LuaJIT 自身 target-selection 的 `Architecture not supported` / `No target architecture defined` / `No support for this number mode on this architecture` 等缺 wasm VM 后端错误时才输出 SKIP，其他预处理失败会报 FAIL。
- 本轮验证：`cmd /c build.bat platformemscripten` 已通过，识别本机 `emcc 4.0.3` 后由 wasm32/wasm64 双预处理探针输出“缺 LuaJIT wasm/wasm64 target/VM backend”的明确 SKIP；PowerShell 脚本语法检查、`cmd /c build.bat platformprobe` 和 `cmd /c build.bat test` 均已通过。
- 已继续推进平台矩阵 iOS ARM64 分支：`tools/lua54_platform_matrix.ps1 -Target ios` 在 macOS/Xcode 主机上不再只是 `xcrun detected` 后 SKIP，而是会通过 `xcrun --sdk iphoneos` 查找 SDK、clang、ar、strip，执行 Lua 5.4 compat + dual-number 的 `TARGET_SYS=iOS` / `BUILDMODE=static` 构建，并用 `lipo -info src/libluajit.a` 校验静态库包含 `arm64`；当前 Windows 主机仍按真实环境限制输出 SKIP。
- 本轮验证：`powershell` 脚本语法检查已通过；`cmd /c build.bat platformios` 已在当前 Windows 主机输出预期 SKIP；`cmd /c build.bat platformprobe` 已通过，iOS 输出 Windows 主机 SKIP，Emscripten 识别本机 `emcc 4.0.3` 后继续输出缺 wasm JIT/interpreter 后端的 SKIP；追加 `cmd /c build.bat test` 已通过，确认默认构建、Lua 5.4 兼容、官方矩阵和 JIT on/off perf/memory smoke 不受平台脚本改动影响。
- 已继续补强 Lua 5.4 JIT/trace 回归覆盖：`test/lua54_jit_regress.lua` 新增 `ipairs()` + `__index` 热循环用例，固定 trace 记录后仍必须走 Lua 5.4 普通索引语义，不能退回旧 LuaJIT 只看原始数组的迭代路径。
- 本轮验证：单独 `src/luajit test/lua54_jit_regress.lua` 已通过；追加 `cmd /c build.bat lua54` 和 `cmd /c build.bat test` 均已通过，覆盖新增 JIT regression、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译门禁，以及 JIT on/off perf/memory smoke。
- 已继续补强 Lua 5.4 C API 头文件 macro-only 门禁：新增 `test/lua54_capi_macroonly_reject.c` 并接入 `smoketest-capi-lua54compat`，批量固定 `lua_getextraspace`、`lua_tonumber` / `lua_tointeger`、`lua_pushglobaltable`、`lua_insert` / `lua_remove` / `lua_replace`、`lua_newuserdata`、`lua_getuservalue` / `lua_setuservalue` 以及常用栈/类型便利入口只能按官方宏使用，外部模块不能再取到 LuaJIT 旧函数地址。
- 本轮验证：`cmd /c build.bat lua54`、`cmd /c build.bat test` 均已通过，覆盖新增 C API macro-only 负向编译 gate、默认构建、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API/头文件 smoke、既有负向编译门禁，以及 JIT on/off perf/memory smoke；`build.bat build -n` 和 `build.bat build -j -n` 均输出半物理核心 `-j12`，显式 `build.bat build -j8 -n` 保留人工并行度；`git diff --check` 仅有仓库 LF/CRLF 提示，没有 whitespace error。
- 已继续按官方 Lua 5.4.8 `lauxlib.h` 收紧 `luaL_Buffer` 公开类型形态：头文件现在先 `typedef struct luaL_Buffer luaL_Buffer;`，后续再定义 tagged `struct luaL_Buffer`，不再用定义处重复 typedef 的旧写法；字段布局保持当前 Lua 5.4 buffer ABI 不变，默认构建也保留同名 typedef/struct 可用表面。
- 本轮验证：`test/lua54_header_static.lua` 新增 `luaL_Buffer` forward typedef / tagged struct 形态门禁；修复前该门禁失败于缺少 forward typedef，修复后 `cmd /c build.bat lua54` 已通过，覆盖新静态头文件门禁、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和负向编译 gate。
- 已继续按官方 Lua 5.4.8 头文件收紧标准库模块 API 标记：`LUAMOD_API` 现在直接定义为 `LUA_API`，`lualib.h` 中标准库 `luaopen_base` / `luaopen_coroutine` / `luaopen_table` / `luaopen_io` / `luaopen_os` / `luaopen_string` / `luaopen_utf8` / `luaopen_math` / `luaopen_debug` / `luaopen_package` 均改用 `LUAMOD_API` 声明，`luaL_openlibs` 继续保留 `LUALIB_API`；LuaJIT 扩展库声明不纳入外部 5.4 标准库表面。
- 本轮验证：新增 `test/lua54_header_static.lua` 并接入 `smoketest-capi-lua54compat`；修复前该门禁失败于 `LUAMOD_API` 未直接定义为 `LUA_API`，修复后 `cmd /c build.bat lua54` 已通过，覆盖新静态头文件门禁、官方 Lua 5.4.8 matrix、C API/头文件 smoke 和负向编译 gate。
- 已继续收紧 Lua 5.4 `lualib.h` 的 include guard 语义：`lualib_h` 现在和官方头一样是真正的保护宏，外部 C 模块如果预先定义 `lualib_h` 再包含 `lualib.h`，不会再看到标准库名、`LUA_VERSUFFIX` 或 `luaopen_*` 声明；新增 `test/lua54_lualib_guard_smoke.c` 固定该行为。
- 本轮验证：新增 guard smoke 修复前失败于预定义 `lualib_h` 后仍暴露 `LUA_COLIBNAME` / `LUA_VERSUFFIX`；修复后单独 `lua54_lualib_guard_smoke.c` / `lua54_lualib_header_smoke.c` 编译通过，`cmd /c build.bat lua54`、`cmd /c build.bat test` 均已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续收紧 Lua 5.4 `luaconf.h` 的 Windows/C89 数值辅助宏形状：header smoke 现在会字符串化检查 `l_mathop(floor)` 展开为官方 `"(lua_Number)floor"`、`lua_str2number("1", 0)` 展开为官方 C89 fallback，并确认 `LUA_USE_C89` 生效时不暴露 `lua_strx2number`；修复前该用例失败于 `l_mathop` / `lua_str2number` 宏形状不一致，修复后通过。
- 本轮验证：单独 `gcc -DLUAJIT_ENABLE_LUA54COMPAT -std=c99 -I src -c test\lua54_lua_header_smoke.c` 已通过，`cmd /c build.bat lua54` 已通过，追加 `cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API/头文件 smoke、负向编译 gate，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 头文件宏列表收紧 `luaconf.h` / `lualib.h` 表面：补齐 `LUA_INT_TYPE` / `LUA_FLOAT_TYPE` 选项族、`LUA_32BITS`、`LUA_C89_NUMBERS`、`l_sprintf` / `l_mathop` / `l_floor` / `l_floatatt`、`lua_str2number` / `lua_strx2number` / `lua_pointer2str` / `lua_number2strx` / `lua_getlocaledecpoint`、`LUAI_FUNC` / `LUAI_DDEC` / `LUAI_DDEF`，以及官方 `lualib_h` include guard；当前按 `lua.h+lauxlib.h+lualib.h` 预处理对比，官方 `LUA*` / `lua*` / `LUAI*` / `LUAL*` / `l_*` 宏在兼容头中已无缺失项。
- 本轮验证：新增 header smoke 修复前失败于缺少上述 `luaconf.h` 数字配置/工具宏和 `lualib_h`；修复后单独 `lua54_lua_header_smoke.c` / `lua54_lualib_header_smoke.c` 编译通过，`cmd /c build.bat lua54`、`cmd /c build.bat default`、`cmd /c build.bat test` 均已通过，覆盖官方 Lua 5.4.8 matrix、默认 LuaJIT 5.1 构建、C API/头文件 smoke、负向编译 gate，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `luaconf.h` 补齐 Windows 平台公开宏：外部 Lua 5.4 兼容头在 Windows 下现在暴露 `LUA_USE_WINDOWS`、`LUA_USE_C89`、`LUA_DL_DLL`，并让 `lua_KContext` 在该平台宏组合下保持官方 `ptrdiff_t` 形状；这些宏只作为外部兼容表面和 C 模块条件编译依据，LuaJIT 内部平台实现仍走原有路径。
- 本轮验证：新增 header smoke 修复前失败于缺少 `LUA_USE_WINDOWS` / `LUA_USE_C89` / `LUA_DL_DLL`；修复后单独 `gcc -DLUAJIT_ENABLE_LUA54COMPAT -std=c99 -I src -c test\lua54_lua_header_smoke.c` 已通过，`cmd /c build.bat lua54`、`cmd /c build.bat default`、`cmd /c build.bat test` 均已通过，覆盖官方 Lua 5.4.8 matrix、默认 LuaJIT 5.1 构建、C API/头文件 smoke、负向编译 gate，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已继续按官方 Lua 5.4.8 `luaconf.h` 收紧外部头文件路径宏：外部 Lua 5.4 头现在暴露官方 `LUA_PATH_SEP` / `LUA_EXEC_DIR`，不再暴露 LuaJIT 旧 `LUA_PATHSEP` / `LUA_EXECDIR` 或 standalone/package 私有的 `LUA_PATH` / `LUA_CPATH` / `LUA_INIT` / `LUA_*_5_4` 环境变量名宏；内部和默认 LuaJIT 构建仍保留这些旧宏供 `lib_package.c`、standalone 和 Lua 5.1 兼容头使用。
- 本轮验证：新增 header smoke 修复前失败于缺少 `LUA_PATH_SEP` / `LUA_EXEC_DIR` 且旧宏泄露；修复后单独 `gcc -DLUAJIT_ENABLE_LUA54COMPAT -std=c99 -I src -c test\lua54_lua_header_smoke.c` 已通过，`cmd /c build.bat lua54` 已通过，覆盖官方 Lua 5.4.8 matrix、standalone 环境变量行为、头文件 smoke、C API smoke 和负向编译 gate。
- 追加验证：`cmd /c build.bat default` 已通过，确认默认 LuaJIT 5.1 兼容头和旧宏不受影响；`cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API smoke，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
- 已调整 `build.bat` 默认并行度：未显式指定 `BUILD_JOBS` / `-jN` / `JOBS=N` 时，优先通过 CIM 读取物理 CPU 核数后取一半；读不到物理核数时才回退到 `NUMBER_OF_PROCESSORS / 2`，避免把 Windows 逻辑线程数当核心数导致 `make -j` 过高。
- 已按要求采用 `Change.md` 记录修改、新功能和进展；仓库中未创建 `Modify.md`。
- 本轮验证：`cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API smoke 和 JIT on/off perf；`cmd /c build.bat platformandroid` 已通过 Android ARM64 静态 artifact 构建和设备 `R5CN30J05BT` 的 `test/smoke.lua lua54compat`；最后追加 `cmd /c build.bat lua54` 已通过并恢复 PC Lua 5.4 compat 构建。
- 已继续补齐 Lua 5.4 C continuation 错误边界：新增 `lua_callk()` callee yield 后再抛错的 C API smoke，固定未保护调用会返回 `LUA_ERRRUN`、错误对象留在协程栈顶、`nresults=0`，并且不会调用保存的 C continuation。
- 本轮验证：`cmd /c build.bat smoketest-capi-lua54compat` 已通过，覆盖新增 `lua_callk` yield-error 用例、`LUA_MULTRET` 用例、re-yield 用例、仓库 Lua 5.4 regressions、官方 Lua 5.4.8 matrix 和 C API smoke。
- 已继续推进 Lua 5.4 C continuation 批次：新增 `lua_callk()` / `lua_pcallk()` 的 `LUA_MULTRET` yielding callee 用例，固定 callee yield 后返回多个值和中间 `nil` 洞时，C continuation 能按 Lua 5.4 栈表面看到完整动态结果列表。
- 已修正 `lua_callk` / `lua_pcallk` 的 `LUA_MULTRET` continuation 结果搬移：yielded callee 返回后，动态结果窗口保留在被恢复的 C 调用帧 `L->base..L->top`，现在会在调用保存的 C continuation 前整体搬到协程公开栈底，避免 continuation 只看到空栈或错误栈窗口。
- 本轮验证：新增 `LUA_MULTRET` 用例修复前失败于 `lua_callk multret continuation stack`；修复后 `cmd /c build.bat smoketest-capi-lua54compat` 已通过。
- 已继续推进 Lua 5.4 C continuation 批次：新增默认 C API smoke 覆盖 `lua_callk()` / `lua_pcallk()` 的 continuation 内再次 `lua_yieldk()`，固定 callee 首次 yield、callee 返回后第一段 continuation re-yield、第二次 resume 进入新 continuation 并返回最终结果的三段流程。
- 本轮验证：`cmd /c build.bat smoketest-capi-lua54compat` 已通过，覆盖新增 `lua_callk` / `lua_pcallk` re-yield 用例、仓库 Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke、warning stderr gate 和 intcasts gate。
- 已继续推进 Lua 5.4 C continuation 批次：`lua_callk` / `lua_pcallk` 的 yielding callee 用例已进入默认 C API smoke，覆盖第一次 `lua_resume` yield 出 callee 值，第二次 resume 进入 C continuation 并接收 callee 返回值；`lua_pcallk` 还覆盖 callee 在 yield 后抛错时 continuation 收到 `LUA_ERRRUN` 和错误对象。
- 已补 `lua_callk` / `lua_pcallk` yielding callee 恢复：非 NULL continuation 且当前 C frame 可 yield 时，调用入口使用 resume-style VM 调用避免普通 `vm_call` 的不可 yield C frame；首次 yield 会重新挂回外层 resumable C frame 并抛出 `LUA_YIELD`，后续 `lua_resume54()` 先恢复 Lua callee，完成后再用保存的 `lua_KContext` / `lua_KFunction` 调 C continuation。固定返回数结果会在调用 continuation 前压缩到公开栈底，`lua_pcallk` 的 yield 后错误会把错误对象作为唯一参数传入 continuation。
- 本轮验证：`cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、官方 Lua 5.4.8 matrix、C API smoke 和 JIT on/off perf；`cmd /c build.bat platformandroid` 已通过 Android ARM64 静态 artifact 构建和设备 `R5CN30J05BT` 的 `test/smoke.lua lua54compat`；最后追加 `cmd /c build.bat lua54` 已通过并恢复 PC Lua 5.4 compat 构建。
- 已继续推进平台矩阵探测脚本：`tools/lua54_platform_matrix.ps1` 现在会从 `EMCC` / `EMSCRIPTEN` / `EMSDK` 以及本机 UE HTML5 engine-local emsdk 路径探测 `emcc` / `emcc.bat`，避免 Emscripten 状态被 PATH 配置误报为工具缺失；探测到工具后会输出 `emcc --version` 并继续标明当前真实阻塞是 LuaJIT 缺 wasm JIT/interpreter 后端。
- 本轮平台验证：`cmd /c build.bat platformprobe` 和 `cmd /c build.bat platformemscripten` 已能识别本机 `emcc 4.0.3` 并输出 Emscripten 目标 `SKIP`；`powershell -ExecutionPolicy Bypass -File tools\lua54_platform_matrix.ps1 -Target emscripten -RequireAll` 会因该 SKIP 返回失败，避免 RequireAll/CI 把未完成平台误判成通过。
- 已继续补强 Lua 5.4 `lualib.h` 外部头文件编译门禁：`test/lua54_lualib_header_smoke.c` 现在单独包含 `lualib.h` 时会固定 `LUAMOD_API`、官方标准库名宏、`luaopen_*` 和 `luaL_openlibs()` 的函数指针签名，同时继续确认 `LUA_GNAME` / `LUA_FILEHANDLE` / `lua_assert` 这类 lauxlib-only 表面以及 LuaJIT 扩展库名不泄露。
- 已继续补强 Lua 5.4 `lua.h` 外部头文件 ABI：`test/lua54_lua_header_smoke.c` 现在按官方 5.4 函数指针签名批量检查状态管理、栈操作、取值/设值、load/dump、continuation、warning、GC、debug hook/upvalue 和 `lua_setcstacklimit()` 等入口，防止宏 wrapper 退回 LuaJIT 旧 ABI。
- 新增 `test/lua54_lauxlib_header_smoke.c` 并纳入 `smoketest-capi-lua54compat`：单独包含 `lauxlib.h` 时固定 `LUA_GNAME` / `LUA_FILEHANDLE` / loaded/preload 表名、输出宏、`lua_assert`、`LUA_ERRFILE` / ref 常量、`LUAL_NUMSIZES` / `LUAL_BUFFERSIZE` 公式，以及常用 lauxlib 真实函数的官方 5.4 函数指针签名。
- 本轮验证：新增 `lua.h` 签名矩阵用例初次运行时暴露了测试自身把 `lua_rawequal` 写成 `lua_compare` 四参数签名的问题，修正为官方三参数签名后，`cmd /c build.bat smoketest-capi-lua54compat` 已通过，覆盖仓库 Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke、warning stderr gate、intcasts gate 和新增 `lua.h` / `lauxlib.h` / `lualib.h` 头文件表面 smoke。
- 已继续推进 Lua 5.4 C continuation 批次：新增 C API smoke 覆盖 `lua_yieldk()` continuation 内再次 `lua_yieldk()` 的三段 resume 流程，固定第一段 yield、continuation re-yield、第二段 continuation 最终返回的 `LUA_YIELD` / `LUA_OK` 状态、resume 参数、context 和结果搬移。
- 已补 `lua_yieldk()` continuation re-yield 恢复：`lua_resume54()` 调起 Lua 5.4 C continuation 时会临时标记该 continuation 为可 yield；continuation 内 `lua_yieldk()` 会把 yielded values 搬到协程结果基址、保存下一段 `lua_KContext` / `lua_KFunction`，并让本次 `lua_resume54()` 返回 `LUA_YIELD`，下一次 resume 再调用新的 continuation。
- 本轮验证：新增 re-yield 用例修复前失败于 `lua_yieldk reyield continuation yield status`；修复后 `cmd /c build.bat smoketest-capi-lua54compat` 已通过，覆盖仓库 Lua 5.4 regressions、官方 Lua 5.4.8 matrix 和 C API smoke；追加 `cmd /c build.bat default` 已通过，确认默认 LuaJIT 构建不受影响；追加 `cmd /c build.bat lua54perf` 已通过，覆盖三档固定 `jit.opt` profile 和 `jit.off()`。
- 已继续收紧 Lua 5.4 lauxlib 头文件 ABI：`test/lua54_capi_lauxlib_macroonly_reject.c` 和 Makefile 负向编译 gate 新增 `luaL_checkversion` / `luaL_intop`，固定它们在外部 Lua 5.4 头里只能作为官方宏使用，不能被外部模块按旧函数地址获取。
- 本轮验证：新增 lauxlib macro-only gate 后，`cmd /c build.bat smoketest-capi-lua54compat` 已通过；同时实测 `lua_callk` yielding callee 仍失败在第一次 resume，确认剩余 `lua_callk` / `lua_pcallk` yielding continuation 需要真正 C frame continuation，不能用 API 层嵌套 resume 硬补。
- 已继续清理 Lua 5.4 外部 lauxlib 旧兼容宏：`luaL_putchar` 现在只在默认 LuaJIT/Lua 5.1 头表面暴露，Lua 5.4 外部兼容头会隐藏它；新增 `test/lua54_capi_putchar_reject.c` 负向编译 gate 固定该边界，同时在默认 `test/lua51_capi_smoke.c` 中覆盖 `luaL_putchar` 仍可运行。
- 本轮验证：`cmd /c build.bat smoketest-capi-lua54compat` 和 `cmd /c build.bat default` 均已通过，分别确认 Lua 5.4 外部头隐藏 `luaL_putchar`、默认 LuaJIT 头保留兼容宏。
- 已继续收紧 Lua 5.4 外部 `lua.h` load ABI：外部 `lua_load` 现在映射到新的五参数 `lua_load54()` wrapper，内部仍保留 LuaJIT 旧四参数 `lua_load()` 和扩展 `lua_loadx()`；新增 `test/lua54_capi_loadx_reject.c` 负向编译 gate，固定 Lua 5.4 外部头不泄露 LuaJIT-only `lua_loadx`。
- 本轮验证：新增 `lua_loadx` 负向用例修复前可编译通过，修复后会报 `implicit declaration of function 'lua_loadx'`；`cmd /c build.bat lua54` 已通过，覆盖官方 Lua 5.4.8 matrix、C API smoke 中五参数 `lua_load` 函数指针/mode 语义，以及新增负向 gate；追加 `cmd /c build.bat default` 已通过，确认默认 LuaJIT 旧 ABI 不受影响；追加 `cmd /c build.bat lua54perf` 已通过，覆盖三档固定 `jit.opt` profile 和 `jit.off()`。
- 已修正 Makefile `test` 入口的并行安全性：`test` 不再把 default、Lua 5.4 C API 和 perf 三个会 `clean/build` 同一 `src` 目录的目标声明为并列 prerequisites，而是用 recipe 顺序调用三个子目标，避免直接 `make -j test` 时互相清理/重建导致偶发崩溃或输出交叉。
- 本轮验证：`make -j16 -n test` 已确认三个大门禁按顺序展开；`make -j16 test` 已完整通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API/官方矩阵/负向编译 gate，以及 Lua 5.4 perf/memory smoke。
- 已继续收紧 Lua 5.4 外部 `lualib.h` ABI：外部头不再声明 LuaJIT-only `luaopen_base54`；Lua 5.4 compat 构建中的导出 `luaopen_base()` 现在直接是官方单返回入口，旧的 base+coroutine 初始化逻辑改为内部 `luaopen_base_luajit()` 供 `luaL_openlibs()` 使用。
- 本轮验证：新增 `luaopen_base54` 负向分支修复前可编译通过，修复后会报 `implicit declaration of function 'luaopen_base54'`；`cmd /c build.bat lua54` 已通过，覆盖 `luaopen_base` 函数指针单返回值、官方矩阵和新增 lualib 负向 gate；追加 `cmd /c build.bat default` 已通过，确认默认 LuaJIT 构建不受影响。
- 已复查并确认 `build.bat` 的并行度策略：默认 `make` 并行数按 `NUMBER_OF_PROCESSORS / 2` 计算且最小为 1；裸 `-j` 会被剥离并归一到半核默认，避免 GNU make 无限并发；显式 `-jN` / `BUILD_JOBS` / `JOBS=N` 仍保留人工覆盖能力。本机 dry-run 验证默认和裸 `-j` 均输出 `-j16`。
- 已继续收紧 Lua 5.4 warning C API/standalone 语义：`lua_setwarnf(L, NULL, NULL)` 现在会真正禁用后续 `lua_warning()`，不会回退到默认 stderr writer；`warn()` 的所有参数都会统一走 `lua_warning()`，`@on` / `@off` 只在非 continuation 的新 warning 消息头上作为控制消息，多段 `warn("@off", "XXX", "@off")` 会按普通文本输出。
- 新增 `test/lua54_capi_warning_null_smoke.c`，并在 `smoketest-capi-lua54compat` 中用 stderr 为空的方式固定 `lua_setwarnf(NULL)` 禁用语义；Makefile warning smoke 同步覆盖多段 warning 中的 `@off` 不应被误当控制消息。
- 已加固 `test/lua54_gc_regress.lua` 的弱长字符串回归用例：fresh compat build 后弱表清理可能多需要一轮 sweep，现在用 bounded GC 等到语义固定点后再断言，仍保留“只剩 `a` 长字符串键，清除后弱表为空”的覆盖。
- 已修正 Lua 5.4 弱表长字符串 dead-key 遍历崩溃：`lj_tab_keyindex()` 在 `next(t, key)` 查找当前 key 时，对 value 已为 nil 的 hash slot 只做原始 TValue 身份匹配，不再调用会字节比较长字符串的 `lj_obj_equal()`，避免 GC 已释放的弱表 dead long-string key 被 `memcmp` 访问。
- 本轮验证：`cmd /c build.bat lua54` 已通过；`./src/luajit -joff test/lua54_gc_regress.lua` 连续 50 轮通过；默认 JIT 下 `./src/luajit test/lua54_gc_regress.lua` 连续 50 轮通过，覆盖此前偶发崩溃的弱 `kv` 长字符串 `next()` 清理路径。
- 已继续收紧 Lua 5.4 table 库参数错误函数名：`table.concat` / `insert` / `remove` / `move` / `sort` / `unpack` 的内部错误保留 `table.xxx` fallback，非 tail-call 源码调用会通过 debug slot 恢复 `concat` / 局部变量 `f` 这类调用点名称；`table.sort({1,2,3}, table.sort)` 这类内部 comparator 错误继续对齐官方 `table.sort` 文本。
- 已继续收紧 table 库 tail-call 错误函数名：Lua 5.4 兼容 parser 会把 `return table.concat(...)` / `return table.sort(...)` 等表库字段调用保留为普通 call+return，不再改写成 `BC_CALLT`，避免 C 参数检查前丢掉调用者 frame；对应错误现在可报告 `concat` / `sort`，直接 `pcall(table.concat, ...)` 仍保留 `table.concat` fallback。
- 已修正 `build.bat` 透传带空格 make 变量的参数转发：`build.bat build -n "XCFLAGS=-DLUAJIT_ENABLE_LUA54COMPAT -DLUAJIT_NUMMODE=2"` 现在会把整个 `XCFLAGS=...` 保留为一个参数传给 GNU make，不再拆成 `-D...` make 选项；裸 `-j` 半核规范化和 help 入口已复测。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 warning smoke、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke、头文件负向编译 gate，以及本轮 table 库错误函数名回归；追加验证 `cmd /c build.bat smoke54` 已通过，覆盖 table 库 tail-call 字段名回归。
- 已继续推进 Lua 5.4 C continuation 批次：`lua_yieldk()` 现在会在线程上保存 non-NULL `lua_KContext` / `lua_KFunction`，下一次 `lua_resume()` 会把 resume 参数暴露给 continuation，并把 continuation 返回值作为协程最终结果；新增 C API smoke 固定 yield 结果、resume 参数、`LUA_YIELD` status、ctx、最终返回值、continuation 抛错返回错误对象，以及 `lua_closethread()` 清理 pending continuation。`lua_callk` / `lua_pcallk` 的真实 continuation 以及 continuation 内再次 yield 仍留在 TODO。
- 本轮验证：新增 `lua_yieldk` continuation 用例修复前失败于 `lua_yieldk continuation called`，修复后 `cmd /c build.bat smoketest-capi-lua54compat` 和 `cmd /c build.bat lua54` 均已通过。
- 已补充同批次 `lua_callk()` / `lua_pcallk()` non-yielding continuation 边界：C API smoke 现在固定被调函数正常返回或 `lua_pcallk()` 普通错误返回时，non-NULL continuation 不会被调用，结果/错误对象仍按普通调用表面返回。
- 已排除一个错误实现方向：在 `lua_callk()` API 层直接嵌套 `lj_vm_resume()` 并手动 unwind 会破坏 LuaJIT C frame 链；真实 yielding `lua_callk` / `lua_pcallk` continuation 必须后续在 VM frame/continuation 调度层实现。
- 已继续补齐 Lua 5.4 C API upvalue smoke：新增 C 用例直接覆盖 `lua_getupvalue()` / `lua_setupvalue()` / `lua_upvalueid()` / `lua_upvaluejoin()`，固定 upvalue 名称和值、无效索引不压栈、upvalue id、join 后共享槽位以及 join 后通过源闭包修改能影响目标闭包。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 upvalue C API smoke、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke 和非主力 VM 后端静态检查。
- 已继续补齐 Lua 5.4 debug C API hook 查询 smoke：C API smoke 现在会在 `lua_sethook()` 设置 count hook 后检查 `lua_gethook()` / `lua_gethookmask()` / `lua_gethookcount()`，再清理 hook 并确认查询结果归零，固定 Lua 5.4 外部 `lua_sethook` void 表面下的 hook 状态查询行为。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 debug hook 查询 C API smoke、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke 和非主力 VM 后端静态检查。
- 已继续补齐 Lua 5.4 C API metatable/uservalue smoke：新增 C 用例固定 `lua_getmetatable()` 无 metatable 时返回 0 且不压栈、`lua_setmetatable()` 返回 1 并弹出 table/nil metatable、nil metatable 会移除已有 metatable；同时覆盖 `lua_getiuservalue()` / `lua_setiuservalue()` 对非 userdata 的 `LUA_TNONE` / `0` 返回和栈效果。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 metatable/uservalue C API smoke、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke 和非主力 VM 后端静态检查。
- 已继续补齐 Lua 5.4 C API GC 变参 smoke：`lua_gc(L, what, ...)` 现在有 C 用例覆盖 `LUA_GCSTOP` / `LUA_GCRESTART` / `LUA_GCCOLLECT` / `LUA_GCCOUNT` / `LUA_GCCOUNTB` / `LUA_GCSTEP` / `LUA_GCSETPAUSE` / `LUA_GCSETSTEPMUL` / `LUA_GCISRUNNING` / `LUA_GCGEN` / `LUA_GCINC`，固定 stopped 状态下 collect/step 不会隐式 restart、`isrunning` 返回布尔表面、以及 Lua 5.4 公开 GC 参数按 4 点粒度量化的旧值返回。
- 本轮验证：`cmd /c build.bat lua54` 已通过，覆盖新增 C API GC 变参用例、Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke 和非主力 VM 后端静态检查。
- 已加固 `build.bat` 的 `make -j` 并行度控制：默认继续使用 `NUMBER_OF_PROCESSORS / 2` 且保底 1；命令行传入裸 `-j` 时不再透传给 GNU make 的无限并行，而是剥离裸 `-j` 并使用半核默认；显式 `-j8` 或 `BUILD_JOBS=8` 仍作为人工覆盖。
- 已补齐 Lua 5.4 长字符串对象模型在非主力 VM 后端的 equality 退避：新增 `lj_meta_equal_lstr()` 共用 helper，并让 x86/ARM/MIPS/MIPS64/PPC 的 `ISEQV` / `ISEQS` 字符串 fast path 在 Lua 5.4 模式下退到 `lj_str_equal()` 字节比较，避免非内化长字符串只按 `GCstr` 指针身份判断；`test/lua54_vm_backend_static.lua` 同步固定这些后端必须具备 equality fallback。
- 本轮验证：`test/lua54_vm_backend_static.lua` 已通过；`cmd /c build.bat lua54` 通过，覆盖 Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke 和非主力 VM 后端静态检查；`cmd /c build.bat default` 通过，确认默认 LuaJIT 构建不受影响。
- 已继续推进 Lua 5.4 长字符串对象模型的 JIT recorder 覆盖：新增 `test/lua54_jit_regress.lua` 低 `hotloop` 回归，先确认旧 recorder 会把同内容不同对象的运行期长字符串 equality 录成成功 trace；随后在 Lua 5.4 模式下让 recorder 遇到长字符串 equality 或长字符串 table key load/store 时统一以 `NYI: long string lookup or comparison` 退出回解释器，避免 `IR_EQ` / `HREF` / `HREFK` 沿用短字符串 intern/object identity 假设。
- 已继续优化 Lua 5.4 长字符串 equality 的 JIT 路径：recorder 遇到运行期长字符串 `==` / `~=` 时不再直接 NYI，而是对当前两个长字符串对象加精确对象 guard，并把本次 bytewise 比较结果专门化进 trace；这保证语义正确，同时避免热循环里固定长字符串对象比较退回解释器。长字符串 table key lookup/store 仍保持 NYI。
- 本轮验证：`test/lua54_jit_regress.lua` 已改为要求长字符串 equality 热循环能记录 trace，而长字符串 table load/store 仍不能记录错误 trace；`cmd /c build.bat lua54` 已通过。
- 本轮验证：新增 JIT 用例修复前失败于 `long-string equality unexpectedly recorded a trace`，修复后 `test/lua54_jit_regress.lua` 通过；`cmd /c build.bat lua54` 通过，覆盖 Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke、非主力 VM 后端静态检查和本轮长字符串 JIT recorder 回归。
- 已继续推进 Lua 5.4 长字符串对象模型的非主力 VM 后端覆盖：x86/ARM/MIPS/MIPS64/PPC 的 table string-key fast path 现在会在 Lua 5.4 模式下对运行期长字符串 key 和长字符串常量 key 退回通用 lookup/set 路径，避免非内化长字符串误走只适合短字符串 intern 的 `sid` 快路径；新增 `test/lua54_vm_backend_static.lua` 并纳入 `smoketest-lua54compat`，用于固定非 x64/ARM64 后端必须具备这些退避点。长字符串 equality fast path 和 JIT 优化策略仍保留在 TODO。
- 本轮验证：新增静态用例在修复前会失败于 `src/vm_x86.dasc: missing long-string table key fast-path guards`，修复后 `test/lua54_vm_backend_static.lua` 通过；`cmd /c build.bat lua54` 通过，覆盖 Lua 5.4 regressions、官方 Lua 5.4.8 matrix、C API smoke 和新增 VM backend 静态检查。
- 已继续收紧 Lua 5.4 C 头文件整数格式 ABI：`LUA_INTEGER_FRMLEN` 改为 C99 `t` 修饰符，`LUAI_UACINT` 改为 `lua_Integer`，`lua_integer2str()` 不再通过 `int` 截断外部 C 侧 64 位 `lua_Integer` 值；C API smoke 新增 `2147483648` 格式化断言。默认 LuaJIT 构建已验证不受影响。
- 已继续推进 Lua 5.4 C 头文件整数 ABI 边界：`lua_Unsigned` 现在由 `LUA_UNSIGNED` 派生，兼容构建下 `LUA_UNSIGNED` 改为与 `lua_Integer` 同宽的 `uintptr_t`，不再把外部 5.4 头的 unsigned 整数表面截成 `unsigned int`；默认 LuaJIT 构建仍保留 `unsigned int` 兼容表面。C API smoke 新增 `lua_Unsigned` / `lua_Integer` 宽度断言和 `LUA_MAXUNSIGNED` 超 32 位检查。完整 64 位 runtime/TValue/JIT integer 语义仍在 TODO 中。
- 已继续推进平台矩阵脚本：`tools/lua54_platform_matrix.ps1` 现在支持独立 `-Target ios` / `-Target emscripten`，`probe` 只是组合调用这两个目标；`build.bat` 增加 `platform` / `platformprobe` / `platformpc` / `platformandroid` / `platformios` / `platformemscripten` 入口，方便后续把 iOS/Emscripten 工具链接入同一验证门。当前 Windows 主机上 iOS 和 Emscripten 仍按真实工具链状态输出 `SKIP`，不是完成态。
- 已修正 `build.bat` 的 Windows batch 换行格式并验证帮助入口可用；默认并行度在未显式传 `BUILD_JOBS` / `-jN` / `JOBS=N` 时按 `NUMBER_OF_PROCESSORS / 2` 传给 GNU make，例如 8 核 dry-run 输出 `-j4`，避免全核并行抢占机器。
- 已新增 `test/lua54_official_matrix.lua`、`make run-official-lua54compat` / `smoketest-official-lua54compat` 和 `build.bat official54`，把当前可执行的官方 Lua 5.4.8 `testes` 子集纳入回归门；Windows 非移植的 `main.lua`、`files.lua`/`attrib.lua` 的 `_port` 分支，以及 LuaJIT bytecode 与官方 Lua binary chunk 边界在 runner 中明确标注。
- 已修正 Lua 5.4 locale 字符串语义：字符串 `<` / `<=` 在兼容模式下走 `strcoll` 并处理内嵌 NUL 分段，pattern 字符类 `%a/%l/%u/...` 和 `string.upper/lower` 走当前 C `ctype` locale；JIT recorder 现在会通过 `lj_str_cmp_locale()` 记录 locale-sensitive 字符串有序比较，避免录旧 bytewise 比较。
- 已修正 Lua 5.4 JIT 下当前 32-bit integer 表面的溢出语义：int+int 的 `+` / `-` / `*` 在 trace 中保持 32-bit wrap，不再把 `10^10` 热循环误 widening 成 float；Lua 5.4 `tonumber()` 后续已恢复 no-base recorder 子集，并支持普通 invalid 字符串 nil guard、逗号 locale 常量 runtime guard、`tonumber(str, base)` 动态字符串和动态 base helper 分支；其它更复杂字符串扫描仍保守退解释器，避免旧 `STRTO` 路径破坏子类型和扩展数字拒绝规则。
- 已修正内部 close 调度对 debug hook 的可见性：generic-for 隐藏 closing value 的 close helper 不再在 `end` 后产生额外 line hook；close helper 调用 registry 中保存的原始 `xpcall`，避免 Lua 5.4 兼容包装函数的内部行号出现在官方 `db.lua` 的 line trace 中。
- 本轮验证：`cmd /c build.bat official54` 已通过，覆盖官方 `literals/strings/pm/math/bitwise/bwcoercion/gc/gengc/tracegc/tpack/closure/cstack/constructs/goto/db/nextvar/utf8/vararg/sort/errors/coroutine/code/api/files/attrib/locals/events/verybig/big`；runner 明确跳过 Windows shell 非移植 `main.lua` 和已文档化的 `calls.lua` binary chunk header 边界。
- 本轮继续推进 Lua 5.4 `<close>` / VM unwind 批次：x64 和 ARM64 错误展开期间的 `__close(value, err)` 已改由 VM continuation 接管，`__close` 内 `coroutine.yield()` 恢复后会继续关闭剩余 close locals，并按 Lua 5.4 规则用后续 `__close` 错误替换原错误对象。
- 已修正 `coroutine.wrap()` 错误路径：包装协程报错后会先调用 `lua_closethread()` 关闭仍 active 的 `<close>` locals，再把最终错误对象抛回调用者；同时修正 `lua_closethread()` 在 close 过程中栈扩容后的 stale base 指针和重置协程栈残留，避免 close-error 后 GC 访问旧栈内容。
- 已修正 Lua 5.4 C API `lua_resume()` 的 dead coroutine 判定：当协程已 reset/关闭且栈上没有初始函数时，会返回 `cannot resume dead coroutine`，不再继续调用空栈导致 `attempt to call a nil value`；官方 `testes/coroutine.lua` 的 5.4.1 `coroutine.wrap` reset 回归已通过。
- 已新增 Lua smoke 回归覆盖：错误展开 close-yield continuation、内部 `pcall` 不应提前关闭外层 `<close>`、`coroutine.wrap` 错误关闭、`__close` 错误替换和 close-error 后 `collectgarbage()` 稳定性。
- 已新增 C API smoke 回归覆盖：`lua_resume()` 成功结束并清空结果后，再次 resume 空栈 dead coroutine 会返回 Lua 5.4 的 dead coroutine 错误。
- 已继续推进并跑通官方 `testes/files.lua`：文件 method 缺 self 会报 `got no value`，`io.open` / `io.popen` 按 Lua 5.4 校验 mode，文件句柄 metatable 新增 GC-style `__close`，`read("n")` 改为读数值前缀后走 `lua_stringtonumber()`，`dofile` 改为可 yield 的 Lua 包装层，`io.lines` 支持 250 个 read options，并修正带 read option 的自动关闭判断，EOF 后不会在 Windows 留下文件句柄。
- 已补齐 `files.lua` 后半段相关收尾：`debug.getlocal` 会按 Lua 5.4 顺序暴露 generic-for 的 4 个隐藏 `(for state)` 值并把第 4 个映射到 closing value；`luaL_loadfilex` 支持跳过首行 `#` 注释后加载 LuaJIT binary chunk；`os.date` 按长度处理格式串中的内嵌 NUL；`os.time(table)` 严格校验整数字段并把 `mktime` 规范化结果写回原表。
- 已继续推进 Lua 5.4 长字符串对象模型批次：兼容模式下运行期长字符串不再复用同内容 `GCstr` 对象，源码长字符串 literal 仍通过 parser 内化保持 chunk 内复用；字符串 equality、table key、x64 `ISEQV` 和 x64 长字符串 table 访问慢路径已改为按字节比较，`%p` 可观察到同内容长字符串的不同对象身份。
- 已修正 `string.gsub` 的长字符串身份语义：实际替换生成同内容长字符串时会得到新对象；没有实际替换或 replacement 返回 nil/false 保留原文时复用原字符串对象，匹配官方 `pm.lua` 的 `%p` 断言。
- 已修正 Lua 5.4 scanner 的 locale 小数点路径：当当前 C locale 的 decimal point 为逗号时，`tonumber("3,4")` 可按 Lua 5.4 转换为 `3.4`，同时源码 `load("a = (3,4)")` 仍按逗号分隔表达式解析。
- 已继续推进官方 `testes/gc.lua` 批次：新增 `test/lua54_gc_regress.lua`，覆盖 table `__gc` 在 allocation loop 中必须及时触发、嵌套 table constructor 中 finalizer 对象不能被死临时槽保活、弱 `kv` 表中的长字符串 key/value 回收、以及 `__gc` finalizer 内 `collectgarbage(...)` 合法选项应返回 `nil` 且非法选项仍报错。
- 已修正 Lua 5.4 GC/bytecode 交互细节：赋值 RHS 的 call frame 临时槽、table constructor 消费后的 call 临时槽、indexed assignment 的 LHS key/RHS 临时槽会在 Lua 5.4 模式下清空，避免 conservative stack scan 把已覆盖的 finalizable table、弱表临时 key 或 weak value 保活；新登记 table finalizer 后会短期保持 allocation-driven GC 响应；弱 `kv` 表清理先判断弱 value，避免准备删除的 entry 因检查 string key 而把长字符串延后一轮回收；大临时字符串 buffer 在 full GC 后会收缩回小 buffer。
- 已修正 finalizer 内 `collectgarbage` 重入语义：合法选项完成参数校验后统一返回 `nil`，不再让 `collectgarbage("count")` 等分支在 `__gc` 回调中返回真实 GC 状态或再次驱动 collector。
- 已修正 weak table dead-key 查找：table lookup/set 会跳过 value 已为 nil 的 hash slot，不再对 GC 留下的 dead string key 做字节比较；官方 `gc.lua` 的 “will not try to compare with dead key” 路径在 JIT 默认和 `-joff` 下均通过。
- 已继续按官方 `testes/tpack.lua` 收紧 `string.pack` / `unpack` / `packsize`：`i/I` 支持 1..16 字节格式尺寸，超过当前 4 字节 Lua integer 的部分按符号/零扩展校验；新增 `J`；`X` 后的 next option 不跳过空白；`c` 必须显式带 size；格式 size 溢出报 `invalid format`；`packsize` 总长度按 `0x7fffffff` 上限报 `too large`；`unpack` 初始位置支持负索引。
- 已修正本轮官方核心矩阵的两个前置阻塞：`closure.lua` 默认 JIT 卡在弱表 GC 可见性的问题已通过 JIT recorder 对弱表读取退回解释器解决；`debug.upvalueid` 对不存在 upvalue 已按 Lua 5.4 返回 `nil`，`closure.lua` 在 JIT 默认和 `-joff` 下均通过。
- 已推进 `calls.lua` tail-call 段：`xpcall(f, f)` 的错误处理递归会直接返回 `error in error handling`，避免 Windows 原生栈被打穿；x64 `__call` metamethod 链会解析到最终函数并把中间 callable 作为隐式参数传入，官方 `__call` chain/table.pack 探针已可得到 `n == 23`。
- 已同步非 x64 VM 的 `__call` callable-chain 协议：x86、ARM64、ARM、MIPS、MIPS64、PPC 后端都会消费 `lj_meta_call()` 返回的隐式参数数量，不再硬编码只增加一个 callable 参数；Android ARM64 设备已验证 20 层 `__call` 链可得到 `table.pack(...).n == 23`。
- 已修正 Android ARM64 Lua 5.4 长字符串 fast path：`ISEQV` / `ISEQS` 对非内化长字符串会调用 `lj_str_equal()` 做字节比较，长字符串作为 table key 的 `TGET*` / `TSET*` fast path 会退回 generic byte-key lookup；设备 smoke 已越过 `<close>`/`xpcall`、单侧 `__eq` 和长字符串 table key 批次。
- 已修正 Lua 5.4 C stack 官方回归：`string.gsub` 的函数/table replacement 递归会在进入 Lua 回调前检查 C frame 深度并报 `C stack overflow`，避免 Windows 原生栈溢出；`coroutine.close` 的链式 `__close` 关闭同样增加库级递归上限，错误会按 Lua 5.4 作为 `coroutine.close` 的失败返回值传播。
- 已修正 Lua 5.4 `__call` tailcall 栈增长路径：x64 / ARM64 `vmeta_call` 会在 `lj_meta_call()` 扩栈后恢复 CALLT 标记或从 `L->top` 反推新 base，`BC_CALLT` 搬参后会归位 `L->top`；仓库 smoke 新增 100 层 callable chain + coroutine tailcall + 深递归用例，Android ARM64 设备 smoke 已覆盖。
- 已补齐 Lua 5.4 的 200 upvalue 上限：`LJ_MAX_UPVAL` 提升到 200，LuaJIT bytecode dump/load 后可遍历并 `debug.upvaluejoin` 200 个 upvalue；JIT recorder 现在也能记录 128..199 高索引 upvalue 的 load/store，避免旧 `IR_UREFO` literal 编码迫使这些热路径永久退回解释器。
- 当前官方 Lua 5.4.8 对照：`literals.lua`、`strings.lua`、`pm.lua`、`math.lua`、`bitwise.lua`、`gc.lua`、`tpack.lua`、`closure.lua`、`cstack.lua` 已在 PC x64 compat 构建下通过；`calls.lua` 已越过 tail-call timeout、100 层 `__call` tailcall 和 200 upvalue 段，当前停在已文档化的官方 Lua 5.4 binary chunk header 差异。
- 本轮验证：`cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、C API smoke 和 JIT on/off perf/memory smoke；`powershell -ExecutionPolicy Bypass -File tools\lua54_platform_matrix.ps1 -Target android` 已通过 Android ARM64 compat 构建和设备 `test/smoke.lua lua54compat`；单跑官方 `cstack.lua` 已完整 `OK`；200 upvalue dump/load/upvaluejoin probe 已通过；官方 `calls.lua` 当前推进到 binary chunk header 断言。
- 已修正默认构建生成器边界：`buildvm_lib.c` 的轻量预处理器会识别 `#if LJ_54`，x64 VM 的 `cont_close` 标签本身保持可解析但主体只在 Lua 5.4 compat 下生成，`lj_err.c` 的 close-defer unwind 访问也只在 `LJ_54` 下启用。
- 本轮验证：`cmd /c build.bat test` 已通过，覆盖默认构建、Lua 5.4 compat、C API smoke，以及固定 `jit.opt` profile 的 JIT on 和 JIT off perf/memory smoke；官方 Lua 5.4.8 `testes/gc.lua` 在 JIT 默认开启和 `-joff` 下均跑到 `OK`；官方 `testes/tpack.lua` 跑到 `OK`；`powershell -ExecutionPolicy Bypass -File tools\lua54_platform_matrix.ps1 -Target android` 已通过 Android ARM64 compat 构建和设备 `test/smoke.lua lua54compat`。
- 此前数值 for 验证：官方 Lua 5.4.8 `testes/nextvar.lua` 在 JIT 默认开启和 `-joff` 下均跑到 `OK`；单独低 hotloop 探针确认 `jit.opt.start("hotloop=1", "hotexit=1")` 不再把 float numeric for trace 窄化成 integer。
- 已新增 `test/lua54_perf.lua` 和 `make smoketest-perf-lua54compat`，性能/内存 smoke 默认固定三档 `jit.opt` profile：`3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10` 和 `0,hotloop=3,hotexit=2`，再覆盖 `jit.off()`；`build.bat test` 已纳入该 perf 目标，`build.bat lua54perf` 可单独运行。
- 已按用户提醒固定 perf guard 的 JIT optimizer 变量：测试默认会先 reset optimizer flags，再显式设置 profile，避免 `jit.opt` 默认或外部嵌入配置变化造成性能结果不可比；本地排查可用 `LUA54_PERF_JIT_OPTS` 覆盖多 profile 矩阵，旧的 `LUA54_PERF_JIT_OPT` 仍可指定单 profile；每个自定义 profile 现在必须显式写明 opt level、`hotloop` 和 `hotexit`。
- 已调整 `build.bat` 默认并行编译策略：未显式指定 `BUILD_JOBS` / `-jN` / `JOBS=N` 时，默认使用 1/2 CPU 核数 `NUMBER_OF_PROCESSORS / 2`，并保底为 1，避免 `make -j` 抢满机器影响测试和交互。
- 已继续对齐 Lua 5.4 外部头文件调用入口：`lua_call` / `lua_pcall` / `lua_yield` 只暴露官方宏表面，新增负向编译 gate 防止外部模块从 5.4 头中取到旧 LuaJIT 函数地址；真实可取地址入口仍是 `lua_callk` / `lua_pcallk` / `lua_yieldk`。
- 已按官方 Lua 5.4 allocator 语义收紧 GC 机会性 shrink：自定义 allocator 拒绝缩小线程栈或临时 buffer 时，GC 会保留原内存块并继续执行，不再把 `collectgarbage()` 变成内存错误；C API smoke 新增 shrink-fail allocator 用例覆盖该路径。逻辑尺寸必须改变的 table/parser shrink 仍保持报错路径。
- 已继续对齐 Lua 5.4 外部头文件宏语义：`lua_pushliteral()` 在外部 5.4 头中改为官方 `lua_pushstring(L, "" s)` 形态，因此包含内嵌 NUL 的 literal 会按 C 字符串截断；默认 LuaJIT 头继续保留旧的 `lua_pushlstring(sizeof)` 宏。
- 已继续收紧 Lua 5.4 deprecated intcast 头文件表面：外部 5.4 头只有显式定义 `LUA_COMPAT_APIINTCASTS` 时才暴露 `luaL_checkint` / `luaL_optint` / `luaL_checklong` / `luaL_optlong`；默认 LuaJIT 头仍保留旧宏，兼容 smoke 和负向编译 gate 均已覆盖。
- 已补 Lua 5.4 外部头负向编译 gate，固定旧 Lua 5.1 C API `lua_equal` / `lua_lessthan` / `lua_objlen` / `lua_cpcall` / `lua_getfenv` / `lua_setfenv` 不会从 5.4 `lua.h` 泄露；默认构建仍由 Lua 5.1 C API smoke 覆盖旧入口。
- 已按官方 Lua 5.4.8 头文件继续对齐 `lua_Debug` 外部布局：兼容构建新增 `srclen`，并将 `nups` / `nparams` / `isvararg` / `istailcall` / private `i_ci` 调整为 Lua 5.4 形态；C API smoke 新增编译期布局断言和运行期 `lua_getinfo(..., "S")` source length 校验，默认 LuaJIT 5.1 布局保持不变。
- 已继续对齐 Lua 5.4 `luaconf.h` 数字宏表面：兼容构建新增 `LUA_NUMBER_FRMLEN`、`LUA_INTEGER_FRMLEN`、`LUA_INTEGER_FMT`、`LUAI_UACINT`、`LUA_UNSIGNED`、`LUA_MAXUNSIGNED`、三参数 `lua_number2str()` 和 `lua_integer2str()`；同时按官方宏语义修正 `lua_numbertointeger()`， in-range fraction 会经 C cast 截断，运行时精确整数检查继续使用 `lua_tointegerx()`。
- 已按官方 `testes/math.lua` 的整数位宽断言收紧 lowered 位运算 helper：当前公开 `math.maxinteger/mininteger` 仍是 32 位时，`1 << intbits` 返回 `0`，`1 << (intbits - 1)` 返回 `math.mininteger`，位运算按 32 位 wrap/zero-fill shift 自洽，`2^40 & 1` 这类超出当前 `lua_Integer` 范围的 number 操作数会报无整数表示。
- 已修正 Lua 5.4 lowered 位运算 helper 的无整数表示错误文本：`math.huge << 1` 会通过 debug frame/slot 反推源级操作数，报 `number (field 'huge') has no integer representation`；`return jit._lua54_*` 私有 helper 不再尾调用，避免错误路径丢失调用者帧。
- 已按官方 `testes/math.lua` 继续收紧 math/number 批次：`tonumber(str, base)` 显式 base=10 不再走普通 decimal float 扫描，显式 base 会按 Lua 字符串长度拒绝内嵌 NUL；hex 整数字符串/字面量仍按当前 32 位 `lua_Unsigned` 表面回绕。
- 已补齐当前 32 位 integer 表面下官方 `testes/math.lua` 覆盖的 math 细节：float `%` 改用 `fmod`+符号修正避免大数精度丢失，`math.abs(math.mininteger)` 在 VM fast path 中回绕为 integer，`math.atan(y, x)` 支持 Lua 5.4 二参 `atan2` 语义，`math.fmod` 在双 integer 参数时返回 integer 并处理 `-1/0` 特例。
- 已将 Lua 5.4 compat 的 `math.random` / `math.randomseed` 切到官方 xoshiro256** 算法和无偏区间投影：`math.randomseed(1007)` 的首个 `random(0)`、首个 float、`randomseed()` 返回种子后复现状态、full integer/float 分布均已由官方 `testes/math.lua` 验证；JIT recorder 在 Lua 5.4 模式下不再录旧 LuaJIT PRNG，避免热循环静默切换算法。
- 已修正 Lua 5.4 dual-number JIT 常量记录：runtime float TValue 进入 trace 时保留为 KNUM，即使数值为整数，也不会把 `i + 0.0` 热循环窄化回 integer；`math.fmod(i + 0.0, j)` 等类型敏感路径已进入 smoke。
- 官方 Lua 5.4.8 `testes/math.lua` 当前在 PC x64 Lua 5.4 compat 构建下已跑到 `OK`。
- 已按官方 Lua 5.4.8 `testes/attrib.lua` 修正 `package.searchpath()` 的空模板语义：Lua 5.4 兼容模式会保留 path 中前导、重复和尾部 `;` 产生的空模板，并报告 `no file ''`；官方 `attrib.lua` 当前已跑到 `OK`。
- 已按官方 Lua 5.4.8 `testes/constructs.lua` 修正 `<close>` 赋值语义：to-be-closed local 初始化后同时按 const 处理，普通赋值和闭包内 upvalue 赋值都会在 LHS 行报 `attempt to assign to const variable`；官方 `constructs.lua` 当前已跑到 `OK`。
- 已继续按官方 Lua 5.4.8 `testes/literals.lua` 收紧 scanner：Lua 5.4 兼容模式下 `load()` 不再把首字符 `#` 当 POSIX header 跳过，标识符字符限制为 ASCII 字母/数字/下划线，并且非法 `\x` / decimal / `\u{}` escape 的 `near` 片段保留出错转义文本；`literals.lua` 当前已推进到已知长字符串非内化阻塞。
- 已按官方 Lua 5.4.8 `testes/goto.lua` 修正 label 可见性：Lua 5.4 兼容模式下内层 block 不能重复定义外层仍可见 label，但不同函数仍可复用同名 label；官方 `goto.lua` 当前已跑到 `OK`。
- 已按官方 Lua 5.4.8 `testes/db.lua` 收口 debug frame metadata 批次：line hook 现在按启用 hook 的具体 frame/line 跳过同一行剩余执行，一行函数入口会报告定义行，stripped chunk 仍触发首条 line hook 但行号参数为 `nil`。
- 已补齐 Lua 5.4 debug 名字和 transfer 边界：operator metamethod、table finalizer `__gc`、`for iterator`、hook 回调帧、普通/fast C return、`pcall` / `xpcall` 合成 C return、traceback 裁剪格式和 stripped upvalue `(no name)` 均已进入 smoke；官方 `testes/db.lua` 当前通过。
- 已继续推进 `<close>` / VM unwind 批次：parser 生成的普通 `__close` 调用现在会被 debug/traceback 识别为 `metamethod 'close'`，同时隐藏 `_lua54_closevalue` / `_lua54_packreturn` / `_lua54_unpackreturn` 等内部 helper 的 return hook 噪声，避免用户 hook 看见编译器桥接细节。
- 官方 `testes/locals.lua` 当前已越过 `__close` 错误 traceback、return hook 顺序、基础 coroutine close-yield 和 error unwind close-yield continuation；PC x64 下 JIT 默认开启与 `-joff` 均已跑到 `OK`。
- 已验证并排除一个低成本替代方案：把 `lj_close_unwind()` 的 C `lua_pcall` 改成 C `lua_call` 仍会在第三个 close-yield 处触发跨 C 边界 yield；当前实现改为在 x64 `FRAME_CONT`/`cont_dispatch` 中加入 close continuation，C helper 只负责准备 close call 和消费 pcall 结果。
- 已修正 Lua 5.4 字符串算术错误路径：VM arithmetic helper 在当前 Lua 帧补齐 `L->top` 后抛出错误，`pcall(function() return "x"+1 end)` 以及 hook 回调里的 `pcall(load(...))` 都能捕获，并保留源码位置。
- 已用用户指定的 `https://github.com/lua/lua/archive/refs/tags/v5.4.8.zip` 重新下载官方源码并和本地 `D:\p4_gl2\pristine\tools\lua\v5.4.8.zip` 核对 SHA256，确认当前对照源码一致。
- 已继续按官方 Lua 5.4.8 `lstrlib.c` / `testes/strings.lua` 成批收紧字符串库边界：`string.rep` 结果过大时稳定报 `resulting string too large`；`string.format("%p")` 支持宽度/左对齐，primitive 仍输出 `(null)`；带宽度/精度的 `%s` 会拒绝内嵌 NUL，裸 `%s` 仍保留原字节。
- 已按官方 `testes/strings.lua` 补齐 `string.format` 规格校验：超长规格报 `too long`，三位宽度/精度、非法 flag、`%F`、`%q` modifier、`%p` precision、缺少参数等边界进入 smoke。
- 已按官方 Lua 5.4 行为收紧 `tostring()`：`__tostring` 返回非 string 时会报 `'__tostring' must return a string`，不再把任意 metamethod 返回值透出。
- 已按官方 `testes/strings.lua` 修正 `table.concat` 的 `math.maxinteger` 终点边界，避免循环自增回绕后多读 `math.mininteger`。
- 已用官方 `testes/strings.lua` 继续做源码级对照：除已记录到 `TODO.md` 的长字符串非内化、真实 64 位 integer/TValue、locale-aware 字符串比较/字符分类和未启用 `testC` 块外，临时跳过这些大改项后当前字符串测试可跑到 `OK`。
- 已继续按官方 Lua 5.4.8 `testes/pm.lua` 收紧 pattern/replacement 语义：`string.gsub` / `string.gmatch` 使用 `lastmatch` 规则处理空匹配，replacement 非法 capture 会带 `%0/%1/%2`，`%x` 非法 escape 和 `%b` 缺参数错误文本已对齐。
- 已用官方 `testes/pm.lua` 做源码级对照：除已记录到 `TODO.md` 的长字符串非内化导致同内容长字符串 `%p` 身份相同外，临时跳过该大改项后当前 pattern 测试可跑到 `OK`。
- 已按官方 Lua 5.4.8 `lvm.c` / `testes/events.lua` 修正 `__eq` 查找规则：table/userdata 不同对象比较时先用左操作数 `__eq`，左侧没有再用右操作数 `__eq`，不再沿用 LuaJIT 旧的“两侧必须同一个 `__eq`”限制。
- 已扩展 Lua 5.4 compat smoke 与 JIT smoke 覆盖 `__eq` 单侧元方法、左右不同 `__eq` 的左侧优先，以及 `==` / `~=` 在热循环中的 recorder 路径；官方 `testes/events.lua` 当前可跑到 `OK`，未启用的 `testC` userdata 块按官方测试提示跳过。
- 已新增根目录 `build.bat`，默认使用 `D:\p4_gl2\pristine\ruby\Ruby33-x64\msys64\usr\bin\make.exe` 并按 1/2 `%NUMBER_OF_PROCESSORS%` 传入 `-j` 并行编译；无参数默认跑 `test`，也可直接透传 `smoketest-lua54compat`、带空格的 `XCFLAGS="..."` 等 make 参数。
- 已继续按官方 `testes/nextvar.lua` 对照收紧基础边界：常量 `for` step 为 `0` / `0.0` 时不再编译期拒绝，而是在执行时统一报 `'for' step is zero`；`ipairs()` 复用稳定辅助函数并支持当前 `math.maxinteger` 到 `math.mininteger` 的迭代器回绕；`table.remove({[0]=...})` 在长度为 0 时会读取并清除 key `0`。
- 已继续推进 Lua 5.4 数值表示批次：PC x64 兼容 smoke 改为 `LUAJIT_NUMMODE=2` dual-number 构建，lexer 会按字面量拼写保留 `1.0` / `1e0` / `0x1p0` 的 float 子类型，parser 常量缓存用内部 boxed key 避免把 `1` 和 `1.0` 合并丢失 TValue 子类型。
- 已按官方 `testes/nextvar.lua` 修正 numeric for 子类型与边界：init/step 为 float 时循环变量保持 float，init/step 为 integer 且 limit 为 float 时按步长方向取整或裁剪到当前 `math.mininteger` / `math.maxinteger`；JIT recorder 现在按 Lua 5.4 的初值/步长实际 TValue 子类型决定 integer trace，不会在 `jit.opt.start("hotloop=1")` 等低阈值配置下把 integral-looking float 循环变量重新窄化成 integer。
- 已按官方 `testes/nextvar.lua` 修正 `__pairs` 内 yield：Lua 5.4 兼容模式保留 `pairs` fast-function tailcall 路径，并把其 cached `next` upvalue 指向兼容版 `next`，因此 `__pairs` 可以在 coroutine 中 yield，同时 `pairs(_G)` 仍不枚举内部 `_ENV`；官方 `nextvar.lua` 当前在 JIT 开启和 `-joff` 下都通过。
- 已继续收紧字符串数字算术的 Lua 5.4 子类型：`"1" + 2`、`"1" + "2"`、`"5" % "2"` 和 `-"1"` 会保留 integer 结果，`"1.0" + 2` / `"1e0" + 2` 保留 float；C API `lua_arith()` 也按同样规则返回 integer/float 子类型，`lua_isinteger()` 在 dual-number 兼容构建下改为按 TValue 子类型判断。
- 已将 Lua 5.4 `%` 降级到 `_lua54_mod` helper：变量路径的 integer `% 0` 不再走旧 VM 浮点 `%` 崩溃/NaN 路径，而是报 `attempt to perform 'n%0'`；integer `%` 保留 integer 子类型，float `%` 保留 float，同时字符串操作数会先尊重显式 `__mod`，`__idiv` 也同步修正为字符串显式元方法优先于数字转换。
- 已按官方 Lua 5.4.8 `lutf8lib.c` / `testes/utf8.lua` 成批对齐 UTF-8 边界：源码 `\u{...}` 字面量接受 `0..0x7fffffff` 和 surrogate 字节序列，`utf8.codes` 迭代器按官方处理越界控制变量和 continuation byte，`utf8.codepoint` / `len` / `offset` 错误文本与边界归属收紧，`utf8.offset` 不再用 strict decode 阻断 lax 5/6 字节序列。
- 已按官方 Lua 5.4.8 `lstrlib.c` 修正 string pattern 对内嵌 NUL 的处理：`MatchState` 现在保存 pattern end 指针，`find` / `match` / `gmatch` / `gsub` 会按长度解析模式串，因此 `utf8.charpattern` 可保持官方内嵌 NUL 常量而不是改写成替代表达式。
- 已先把 Lua 5.4 lowered 位运算 helper 的原始数值路径收紧为当前 32 位公开整数自洽表面：`1 << 31 == math.mininteger`、`1 << 32 == 0`、`1 << 40 == 0`，完整 64 位 integer/TValue 仍保留在 `TODO.md`。
- 已确认官方 Lua 5.4.8 `testes/utf8.lua` 在当前 LuaJIT 兼容构建下通过。
- 已调整后续实现策略：`TODO.md` 新增按底层依赖分批推进的实施规划，后续优先按 VM unwind/`<close>`、真实 `_ENV`、64 位整数/数值表示、debug frame metadata、平台/JIT、C API/标准库收尾这些批次推进，不再逐条零散清 TODO。
- 已启动 VM unwind/`<close>` 批次的接口化实现：新增 `src/lj_close.c` / `src/lj_close.h`，统一 `__close` 查找、closable 校验和 `__close(value, err)` 调用入口，并让 `jit._lua54_checkclose`、`jit._lua54_closevalue`、`lua_toclose()`、`lua_closeslot()` 复用同一层。
- 已补 close-active 动态返回路径：`return f()` 和 `return fixed, ...` 会先保存动态返回值数量与 nil 洞，再按 LIFO 执行 `__close(value, nil)`，最后恢复原返回值；当前通过私有 pack/close/unpack 桥接实现，后续 VM unwind 接管时应替换该桥接。
- 已下载官方 Lua 5.4.8 源码到 `D:\p4_gl2\pristine\tools\lua\lua-5.4.8-src\lua-5.4.8` 作为源码级对照；当前 `<close>` 异常展开按 `luaF_close` / `luaD_closeprotected` 的错误替换语义校准。
- 已补 Lua 5.4 `<close>` error unwind 桥接：声明时记录真实 Lua local 槽位，异常展开时按栈层级 LIFO 调用 `__close(value, err)`；`pcall` / `xpcall`、`__close` 自身抛错替换错误对象，以及 generic for 第 4 个 closing value 的错误展开已进入 smoke。
- 已补 Lua 5.4 coroutine reset/close 的 `<close>` 路径：`coroutine.close`、`lua_closethread()` 和 `lua_resetthread()` 会关闭 suspended coroutine 中的 active close locals，yield 状态传 `nil` 错误对象；`__close` 抛错时返回失败和替换后的错误，再次 close 不会重复报错。
- 已补 Lua 5.4 C API `lua_toclose` 弹栈和错误展开路径：`lua_settop` / `lua_pop` 弹出 marked slot 会先调用 `__close(value, nil)`；C 函数抛错展开时会把 body error 传给 `__close(value, err)`。
- 已补 Lua 5.4 C API `lua_toclose` 正常 C 返回路径：x64/arm64 VM `returnc` 在 return hook 和搬移返回值前关闭 C frame 内 marked slot；`__close(value, nil)` 错误会替换为 C 返回错误，且 close slot 本身作为返回值时保留原值。
- 已按 Lua 5.4.8 `lapi.c` 对齐 `lua_toclose` / `lua_closeslot` 的栈顺序约束：`lua_toclose()` 只能标记高于当前 active close slot 的槽位，`lua_closeslot()` 只能关闭最后一个仍 active 的 marked slot；false/nil 被忽略后也不能绕过顺序检查。
- 已按官方 Lua 5.4.8 `lua.h` / `lapi.c` 对齐头文件和外部识别符：兼容构建现在暴露 `LUA_VERSION_RELEASE "8"`、`LUA_RELEASE "Lua 5.4.8"`、`LUA_VERSION_RELEASE_NUM 50408`、5.4.8 版权/作者文本以及 `lua_ident` 符号。
- 已按官方 Lua 5.4.8 `lua.h` / `ldebug.c` 对齐 `lua_sethook` 外部签名：兼容构建的外部头现在把 `lua_sethook` 暴露为 `void` 返回表面，默认构建和 LuaJIT 内部继续保留旧 `int` ABI。
- 已按官方 Lua 5.4.8 `lua.h` 对齐 `lua_pushglobaltable` 宏返回表面：外部兼容头现在显式返回 `void`，同时继续从 registry globals 表取全局表。
- 已按官方 Lua 5.4.8 `lua.h` 对齐 debug hook 事件宏：外部兼容头只暴露 `LUA_HOOKTAILCALL`，不再暴露旧 LuaJIT/Lua 5.1 `LUA_HOOKTAILRET`。
- 已按官方 Lua 5.4.8 `lua.h` 对齐 `lua_callk` / `lua_pcallk` / `lua_yieldk` 的外部 ABI：兼容构建现在暴露真实导出函数，外部 C 模块可以按官方签名取函数指针；当前 wrapper 覆盖 NULL-continuation 路径，真正 continuation 恢复语义仍留在 VM unwind 批次。
- 已继续按官方 Lua 5.4.8 `lua.h` 对齐外部函数指针表面：`lua_rawlen`、`lua_pushlstring`、`lua_pushstring`、`lua_gettable`、`lua_getfield`、`lua_geti`、`lua_rawget`、`lua_rawgeti`、`lua_rawgetp`、`lua_rawseti`、`lua_getglobal`、`lua_setglobal`、`lua_load`、`lua_dump` 和 `lua_resume` 现在都可按官方函数名取指针，同时仍转发到内部 `*54` 兼容入口以保留 LuaJIT 内部旧 ABI。
- 已按官方 Lua 5.4.8 `luaconf.h` 对齐 Lua 5.4 兼容构建的 `LUAL_BUFFERSIZE`：从 LuaJIT/stdio `BUFSIZ` 表面切到官方 `16 * sizeof(void*) * sizeof(lua_Number)` 公式，默认构建继续保留旧 ABI。
- 已按官方 Lua 5.4.8 `luaconf.h` / `lua.h` / `lapi.c` 对齐外部 registry/upvalue 伪索引：外部 `LUAI_IS32INT`、`LUAI_MAXSTACK`、`LUA_REGISTRYINDEX` 和 `lua_upvalueindex()` 现在使用官方公式，运行时会接受这些官方伪索引并映射回 LuaJIT 内部 registry/upvalue ABI，默认构建保持旧值。
- 已按官方 Lua 5.4.8 `luaconf.h` / `lauxlib.h` 对齐 Lua 5.4 `luaL_Buffer` 布局：兼容头现在暴露 `LUAI_MAXALIGN`，初始缓冲区字段使用官方 `init.b` union，buffer 实现同步改用该字段，默认构建不变。
- 已按官方 Lua 5.4.8 `lauxlib.h` 收紧外部宏入口表面：`luaL_argexpected`、`luaL_pushfail`、`luaL_loadfile`、`luaL_loadbuffer` 和 `luaL_prepbuffer` 在外部 5.4 头中只作为宏可调用，不再暴露可取地址的旧 LuaJIT 函数声明；默认构建和内部 ABI 保持不变。
- 已按官方 Lua 5.4.8 `luaconf.h` 对齐 continuation/context 与 likely 宏表面：兼容头现在暴露 `LUA_KCONTEXT`、`luai_likely` 和 `luai_unlikely`，`lua_KContext` 由 `LUA_KCONTEXT` 派生；内部短名 `l_likely` / `l_unlikely` 仅在 `LUA_CORE` / `LUA_LIB` 下暴露。
- 已按官方 Lua 5.4.8 `lualib.h` 收紧外部标准库头表面：单独包含 `lualib.h` 不再暴露 lauxlib-only 的 `LUA_GNAME` / `LUA_FILEHANDLE`，也不再暴露 LuaJIT 专有 `bit` / `jit` / `ffi` / `string.buffer` 库名和 open 函数；默认构建和内部 ABI 保持不变。
- 已按官方 Lua 5.4.8 `lauxlib.h` / `lualib.h` 对齐 `lua_assert` 归属：`lauxlib.h` 负责兜底暴露 `lua_assert`，外部 5.4 的 `lualib.h` 单独包含时不再暴露该 lauxlib-only 宏。
- 已继续按官方 Lua 5.4.8 `lua.h` 清理外部兼容头：`lua_open`、`lua_getregistry`、`lua_getgccount`、`lua_Chunkreader`、`lua_Chunkwriter` 和 `lua_setlevel` 不再对外暴露；默认构建 C smoke 覆盖旧兼容宏仍可用。
- 已按官方 `testes/bwcoercion.lua` 修正 Lua 5.4 lowered operator helper 的 metamethod 返回栈：`__band` / `__idiv` 等路径只返回 metamethod 第一个结果，不再把原操作数漏成额外返回值。
- 已按官方 `testes/locals.lua` 的 to-be-closed coroutine 用例推进 `<close>` yield 边界：普通块退出和 close-active `return` 路径不再在 C helper 内部调用 `__close`，而是由 parser 生成普通 Lua 调用，因此 `__close` 内 `coroutine.yield()` 后可恢复，并保留返回值数量与 `nil` 洞；error unwind / C return 等路径仍记录在 `TODO.md` 继续由 VM unwind continuation 接管。
- 已继续按官方 `testes/locals.lua` 收紧普通 close 错误边界：隐式函数结尾会先关闭根作用域 `<close>` 再执行 final return，`__close` 自身抛错可被 `pcall` 捕获；声明后移除 `__close` 时错误文本包含 `metamethod 'close'`。
- 已按官方 `error()` 行为收紧 Lua 5.4 非字符串错误对象：`error(101)` / `error(table)` 会保留原错误对象，不再因 LuaJIT 5.1 的 `lua_isstring(number)` 兼容规则被加上位置信息；同时覆盖 close 方法内部再声明 `<close>` 后抛数值错误的错误替换链。
- 已继续按官方字符串数值运算边界收紧 lowered operator helper：`local a, b = "1.0" // "2"` 这类固定多赋值不再泄漏左操作数临时槽，helper 原始数值结果和 parser 调用 base 已规整为单返回值。
- 已按官方 `assert()` 行为继续收紧 Lua 5.4 非字符串错误对象：`assert(false, 123)` / `assert(false, table)` 会保留原错误对象，只有 string 消息走字符串错误文本路径。
- 已继续收紧 Lua 5.4 兼容模式下的 JIT 工具链：启动时仍隐藏旧全局 `bit`，但把 `bit` 作为显式 preload 模块保留，确保 `jit.dump` / `-jdump` 这类 LuaJIT 自带工具可通过 `require("bit")` 运行。
- 已按官方 Lua 5.4 `_ENV` 语义推进源码 parser：main chunk 现在创建隐藏但真实的首个 `_ENV` upvalue，隐式全局访问降级为 `_ENV.name`，`debug.setupvalue(load("return x"), 1, 5)` 和 `debug.upvaluejoin` 到非 table lexical `_ENV` 后都会按 Lua 5.4 在运行时报 `_ENV` 非 table 索引错误。
- 已修正 Lua 5.4 helper 降级在真实 `_ENV` 访问下的寄存器布局：`_ENV.name` 降级为 `TGETV` 时，`//`、位运算和一元 `~` helper 调用会先物化仍待求值的操作数，避免 helper 自身的 `jit._lua54_*` 取值覆盖 `_ENV` 表寄存器。
- 已修正 Lua 5.4 debug 名字反推：大 chunk 中 `_ENV.name` 因常量索引超过 `TGETS` 范围而生成 `KSTR + TGETV` 时，`debug.getinfo(..., "n")` 仍能恢复 `xpcall` 等函数名，保持 hook transfer smoke 稳定。
- 已修复 Lua 5.4 compat 的 amalgamation 构建遗漏：`ljamalg.c` 现在包含 `lib_utf8.c`，避免 `luaopen_utf8` 在合并编译链接时缺失。
- 已完成实验性 Lua 5.4 兼容模式的阶段性实现与测试。
- 已通过 `make test` 验证默认构建和 Lua 5.4 兼容构建的 smoke 测试。
- 已继续推进 Lua 5.4 语言层属性语法：`local x <const>` / `local x <close>` 已进入测试覆盖。
- 已继续推进 Lua 5.4 运算符语法：`//`、`&`、`|`、`~`、`<<`、`>>` 已进入测试覆盖。
- 已继续补齐 Lua 5.4 基础库细节：`ipairs`、`warn()`、`math.randomseed()` 和 `math.random()` 的行为差异已进入测试覆盖。
- 已继续补齐 Lua 5.4 `_ENV` 基础语义：全局 `_ENV` 和局部 `_ENV` 名字解析已进入测试覆盖。
- 已继续补齐 Lua 5.4 `utf8.char()` 扩展编码范围和 `string.pack()` 的 `j` / `T` 格式。
- 已开始按 `TODO.md` 逐项清理剩余缺口；已完成 `__le`、`string.gmatch(init)`、`warn()` 数字参数、`math.randomseed()` 无参返回、`utf8` lax 模式，以及 `string.pack()` / `string.unpack()` / `string.packsize()` 的 alignment、`X`、`l` / `L` 格式；`//` 已补充字符串数字的算术转换路径；`debug.getinfo(..., "t")` 已支持最小 `istailcall=false` 表面。
- 已先补 Lua 5.4 C API 烟测 `test/lua54_capi_smoke.c` 和 `make smoketest-capi-lua54compat`，覆盖 `lua_arith`、`lua_compare`、`lua_len`、`lua_rotate`、`lua_stringtonumber`、`lua_numbertointeger` 以及 `luaL_*` 常用兼容入口。
- C API 烟测继续扩展到 registry 索引、`lua_getextraspace()`、官方 `lua.h` 真实函数的函数指针兼容表面、`lua_callk()` / `lua_pcallk()` / `lua_yieldk()` NULL-continuation 路径、warning 回调和 `LUA_GCGEN` / `LUA_GCINC` 模式切换表面。
- C API 烟测继续扩展到状态 allocator 表面：`lua_newstate()` 可使用自定义 allocator，`lua_getallocf()` / `lua_setallocf()` 可读写 allocator 与 userdata。
- C API 烟测继续扩展到状态/错误入口：`lua_atpanic()` 返回旧 panic handler，`lua_pushthread()` 区分主线程和 coroutine，`lua_error()` 可通过 `lua_pcall()` 捕获错误对象。
- C API 烟测继续扩展到栈/表基础入口：`lua_checkstack()`、`lua_settop()`、`lua_pushvalue()`、`lua_concat()` 和 `lua_next()`。
- C API 烟测继续扩展到类型/取值基础入口：`lua_type()`、`lua_typename()`、`lua_rawequal()`、`lua_iscfunction()`、`lua_tocfunction()`、`lua_touserdata()`、`lua_topointer()`、`lua_toboolean()` 以及 light/full userdata 判定。
- 已继续补 Lua 5.4 `<close>` 声明点语义：`local x <close>` 现在会在运行期校验非 `nil`/`false` 值必须有 `__close`，非 closable 值按官方报 `variable 'x' got a non-closable value`；完整作用域退出调度仍保留在 `TODO.md`。
- 已继续补 Lua 5.4 C API `<close>` 显式关闭表面：新增 `lua_toclose()` closable 校验和 `lua_closeslot()` 显式 `__close(value, nil)` 调用，关闭后会把槽位置为 `nil`；`lua_toclose()` 的 false/nil 值会被忽略，因此不能再用 `lua_closeslot()` 关闭；自动随作用域退出关闭仍保留在 `TODO.md`。
- 已继续补 Lua 5.4 `<close>` 普通块退出调度：自然执行到 `end` 时会按 LIFO 调用待关闭局部变量的 `__close(value, nil)`；`return` / `break` / `goto` 和 error unwind 已进入后续桥接实现。
- 已继续补 Lua 5.4 `<close>` 返回路径：`return "x", n` 这类已知返回个数，以及 `return f()` / `return fixed, ...` 这类动态多返回路径，都会先求值返回表达式，再按 LIFO 调用 `__close(value, nil)`，最后返回已求值结果；动态路径当前仍是 parser/helper 桥接，后续需要由 VM unwind 接口替换。
- 已继续补 Lua 5.4 `<close>` `break` 路径：退出最近循环前会关闭循环体和嵌套块中仍处于活动状态的 close locals，按 LIFO 调用 `__close(value, nil)`。
- 已继续补 Lua 5.4 `<close>` 已知向后 `goto` 路径：跳回当前或外层已出现的可见标签前，会按目标标签活动局部变量层级关闭被跳出的 close locals；前向/未解析 `goto` 仍记录在 `TODO.md`。
- 已继续补 Lua 5.4 `<close>` 前向 `goto` 路径：当 pending goto 在作用域结束时确认要跳出该作用域，会生成 goto 专用 close 跳板，只关闭 goto 发生时已经活跃的 close locals；同一作用域内的前向标签不会提前关闭。
- 已继续补 Lua 5.4 generic for 第 4 个 closing value：表达式第 4 个结果会进入隐藏 close slot，进入循环前校验 `__close`，自然结束、`break` 退出和错误展开时都会关闭。
- 已继续补 Lua 5.4 C API `lua_version` 头文件表面：兼容构建的外部头现在暴露 value-returning wrapper，可按官方 `lua_Number (*)(lua_State *)` 签名取函数指针；默认构建和内部 ABI 仍保留 LuaJIT 旧指针返回入口。
- 已继续打通 Lua debug 库和 C API indexed uservalue：C 创建带 declared uservalue 的 userdata 后，`debug.getuservalue()` / `debug.setuservalue()` 已能按 Lua 5.4 表面读写声明槽位。
- 已继续补 `table.concat({1,nil,3}, ",")` 的 Lua 5.4 边界：默认终点会覆盖 LuaJIT 旧长度搜索提前停在 1 的情况，从而检查到 index 2 的 nil 并报错。
- 已继续补 `lua_resetthread()` 基础 C API：可把无 `<close>` 状态的 yielded coroutine 重置回 `LUA_OK` 并清空栈。
- 已继续补 table 库 Lua 5.4 边界：`table.insert` / `table.remove` / `table.move` 的位置参数现在拒绝无整数表示的数值，`table.concat` / `table.insert` / `table.remove` 的默认长度路径会对齐带 `__len` 和中间 nil 洞数组的 Lua 5.4 表库行为。
- 已继续收紧 `table.move()` 参数检查顺序：Lua 5.4 兼容模式下先检查 `f/e/t` 整数参数，再检查源表和目标表，缺参时优先报第 2 个参数。
- 已开始把 Lua 5.4 新语义纳入 JIT smoke：当前 PC 构建会在开启 JIT 后运行 `//`、位运算和局部 `_ENV` 热循环，并确认产生 trace。
- 已继续扩展 Lua 5.4 JIT smoke：带第 4 个 closing value=`false` 的 generic `next` 热循环会在兼容构建下产生 trace，覆盖 generic for 新控制布局的 JIT 路径。
- 已继续补 Lua 5.4 C API 表面：外部兼容头中的 `lua_resume(L, from, nargs, nresults)` 已映射到 `lua_resume54()` 包装入口，并覆盖 yield/return 的结果数量。
- 已继续补 lauxlib C API smoke：`luaL_loadbufferx()` / `luaL_loadfilex()` 的 text 模式加载和 binary-only 模式拒绝 text chunk 已进入回归。
- 已继续补 lauxlib C API smoke：覆盖 `luaL_fileresult()`、`luaL_execresult()`、`luaL_newlib()`、`luaL_setfuncs()`、registered metatable/userdata helper、`luaL_traceback()` 和 `luaL_dostring()`。
- 已继续补 Lua 5.4 lauxlib 头文件宏：新增 `luaL_intop()`，按当前兼容层公开的 32 位整数范围做 unsigned wraparound，并进入 C API smoke。
- 已继续扩展 Lua 5.4 lauxlib C API smoke：覆盖 `luaL_checkoption()`、`luaL_gsub()`、`luaL_ref()` / `luaL_unref()`、`luaL_getmetafield()`、`luaL_callmeta()`、`luaL_where()`、`luaL_error()` 和 `luaL_typename()`。
- 已继续补 Lua 5.4 lauxlib 头文件宏：`LUA_GNAME` / `LUA_FILEHANDLE` 现在可由 `lauxlib.h` 单独暴露，`lualib.h` 保留保护式定义兼容旧包含顺序。
- 已继续对齐 Lua 5.4 头文件归属：`LUA_LOADED_TABLE` / `LUA_PRELOAD_TABLE` 现在由 `lauxlib.h` 暴露；新增 `lua.h` 单独包含 smoke，防止外部 Lua 5.4 `lua.h` 泄露 lauxlib-only 宏。
- 已继续补 Lua 5.4 `lualib.h` 头文件宏：新增 `LUA_VERSUFFIX`，并在 C API smoke 中确认值为 `"_5_4"`。
- 已继续补 Lua 5.4 `lauxlib.h` 输出宏：新增可覆写的 `lua_writestring` / `lua_writeline` / `lua_writestringerror`，并用 compile-only smoke 覆盖宏展开。
- 已继续补 Lua 5.4 `lauxlib.h` buffer 宏：外部兼容头中的 `luaL_prepbuffer` 现在按官方映射到 `luaL_prepbuffsize(B, LUAL_BUFFERSIZE)`，内部和默认构建保留旧函数 ABI。
- 已继续补 Lua 5.4 `lauxlib.h` buffer ABI：`luaL_pushresultsize()` / `luaL_buffinitsize()` 现在是可取函数指针的真实函数入口，默认构建继续保留 LuaJIT 旧宏表面。
- 已继续补 Lua 5.4 `lauxlib.h` 辅助宏：外部兼容头中的 `luaL_argexpected` / `luaL_pushfail` 现在按官方作为宏暴露，内部和默认构建保留旧函数 ABI。
- 已继续补 Lua 5.4 `lauxlib.h` load 宏：外部兼容头中的 `luaL_loadfile` / `luaL_loadbuffer` 现在按官方映射到 `luaL_loadfilex(..., NULL)` / `luaL_loadbufferx(..., NULL)`。
- 已继续补 Lua 5.4 `lauxlib.h` newlib 宏：外部兼容头中的 `luaL_newlib` 现在按官方先执行 `luaL_checkversion()`，默认 LuaJIT 5.1 宏保持不变。
- 已继续补 Lua 5.4 C API smoke：`lua_isyieldable()` 已覆盖主 C frame 返回 false、通过 `lua_resume()` 进入 coroutine C frame 返回 true。
- 已继续补 Lua 5.4 `lua.h` 兼容宏：新增 `LUA_NUMTAGS` 作为 `LUA_NUMTYPES` 别名，并进入 C API smoke。
- 已继续补 Lua 5.4 C API 表面：新增 `lua_closethread()` / `lua_resetthread()` 基础实现，覆盖 yielded/fresh coroutine 关闭后返回 `LUA_OK`、清空栈并恢复 OK 状态；当前也会关闭 suspended coroutine 中的 active close locals。
- 已继续扩展 Lua 5.4 C API smoke：覆盖 `lua_toclose()` 拒绝 non-closable 值、`lua_closeslot()` 关闭带 `__close` 的 table，以及 false close value 被 `lua_toclose()` 忽略后不能被 `lua_closeslot()` 关闭。
- 已继续扩展 Lua 5.4 C API smoke：覆盖 `lua_version` 作为官方 value-returning 函数指针调用并返回 `LUA_VERSION_NUM`。
- 已继续补 Lua 5.4 deprecated intcast 兼容宏：在 `LUA_COMPAT_APIINTCASTS` 下暴露 `lua_pushunsigned` / `lua_tounsignedx` / `lua_tounsigned` / `luaL_checkunsigned` / `luaL_optunsigned`，并新增单独 C smoke。
- 已继续补 Lua 5.4 外部兼容头别名：`lua_newuserdata()`、`lua_getuservalue()`、`lua_setuservalue()` 现在作为官方 slot 1 alias 宏暴露，默认 LuaJIT 5.1 ABI 不变。
- 已继续清理 Lua 5.4 外部兼容头：旧 `LUA_GLOBALSINDEX` / `LUA_ENVIRONINDEX` 伪索引和一批 Lua 5.1 API 声明已从外部 5.4 表面隐藏，`lua_pushglobaltable()` / `lua_getglobal()` / `lua_setglobal()` 改走 registry globals 包装路径。
- 已继续清理 Lua 5.4 外部 lauxlib 头：`luaL_openlib` / `luaL_register` / `luaL_pushmodule` 不再对外声明，并新增负向编译 gate；默认构建 C smoke 覆盖这些旧 API 仍可用。
- 已继续清理 Lua 5.4 外部 lauxlib 头：旧 `luaL_typerror` 不再对外声明，新增负向编译 gate；默认构建 C smoke 覆盖旧 API 仍可用。
- 已继续清理 Lua 5.4 外部 lauxlib 头：LuaJIT helper `luaL_findtable` 不再对外声明，新增负向编译 gate；默认构建 C smoke 覆盖旧 helper 仍可用。
- 已继续补 C API GC 常量覆盖：`LUA_GCCOUNTB` 已进入 smoke，验证 byte remainder 在 `0..1023` 范围内。
- 已继续补 Lua 5.4 C API `lua_gc` 变参表面：兼容构建下 `lua_gc(L, LUA_GCCOLLECT)` / `LUA_GCCOUNT` / `LUA_GCCOUNTB` 可用两参数调用，`LUA_GCGEN` / `LUA_GCINC` 使用官方参数形态；默认 LuaJIT 5.1 头仍保留旧三参数签名。
- 已继续补 Lua 5.4 getter 返回值签名：外部兼容头中的 `lua_gettable()` / `lua_getfield()` / `lua_geti()` / `lua_rawget()` / `lua_rawgeti()` / `lua_rawgetp()` 现在通过包装入口返回取到值的 Lua 类型。
- 已继续补 Lua 5.4 raw integer API 签名：外部兼容头中的 `lua_rawgeti()` / `lua_rawseti()` 索引参数现在通过 `*54` 包装入口暴露为 `lua_Integer`。
- 已继续补 Lua 5.4 栈操作宏表面：外部兼容头中的 `lua_insert()` / `lua_remove()` / `lua_replace()` 现在按官方映射到 `lua_rotate()` / `lua_copy()`，默认构建仍保留旧函数 ABI。
- 已继续补 Lua 5.4 字符串 push 返回值表面：外部兼容头中的 `lua_pushlstring()` / `lua_pushstring()` 现在通过 `*54` wrapper 返回入栈字符串指针，默认构建仍保留 LuaJIT 5.1 的 `void` 头文件 ABI。
- 已继续补 Lua 5.4 number 转换宏表面：外部兼容头中的 `lua_tonumber()` / `lua_tointeger()` 现在按官方映射到 `lua_tonumberx(..., NULL)` / `lua_tointegerx(..., NULL)`，默认构建仍保留旧函数 ABI。
- 已继续补 Lua 5.4 extraspace 宏表面：外部兼容头中的 `lua_getextraspace()` 现在作为宏映射到当前 pointer-sized `L->exdata` 兼容存储，默认构建仍保留旧函数 ABI。
- 已继续补 Lua 5.4 `lua_rawlen()` 返回值表面：外部兼容头现在通过 `lua_rawlen54()` wrapper 暴露 `lua_Unsigned` 返回类型，内部和默认构建仍保留 LuaJIT 旧 `size_t` ABI。
- 已继续补 Lua 5.4 调用入口表面：外部兼容头中的 `lua_call()` / `lua_pcall()` / `lua_yield()` 现在按官方映射到真实 `*k(..., 0, NULL)` 函数；当前 `*k` wrapper 仍不支持非 NULL continuation 恢复。
- 已更新 Lua 5.4 `math.random` 的 JIT 策略：旧的 `math.random(1, 4)` trace 回归已被官方 xoshiro256** 语义取代；当前 Lua 5.4 模式下 random recorder 已用 xoshiro 专用 helper 记录无参、`0`、常量整数区间，以及当前运行值可精确转为整数的动态 number/string 区间。
- 已继续补 Lua 5.4 C API 表面：外部兼容头中的 `lua_load(L, reader, data, chunkname, mode)` 已映射到五参数 `lua_load54()` wrapper，并覆盖 text/binary mode 行为。
- 已继续补 Lua 5.4 C API 表面：外部兼容头中的 `lua_dump(L, writer, data, strip)` 已映射到 `lua_dump54()`，并覆盖 full/stripped LuaJIT bytecode 写出和 binary mode 回读。
- 已新增平台矩阵验证脚本 `tools/lua54_platform_matrix.ps1`：当前可在 Windows/MSYS2 环境一键构建并运行 PC x64 default / Lua 5.4 compat smoke；检测到 Android NDK 时会实际构建 Android ARM64 Lua 5.4 compat 静态 artifact，并用 `llvm-readelf` 确认 AArch64；iOS/Emscripten 目标先输出工具链探测和明确 SKIP 原因。
- 已继续扩展 Android ARM64 平台矩阵：脚本检测到在线 adb 设备时会自动 push `luajit` 和 `test/smoke.lua` 到 `/data/local/tmp` 并运行 Lua 5.4 smoke；当前无在线设备时记录明确 SKIP。
- 已修正 Android ARM64 设备 smoke 的 LuaJIT 工具模块布局：矩阵脚本现在同时 push `src/jit` 到远端工作目录，确保 `require("jit.dump")` / `-jdump` 等 JIT Lua 工具在 Android 设备上和 PC 一样可用；设备 `R5CN30J05BT` 已通过 `test/smoke.lua lua54compat`。
- 已修正 Android ARM64 交叉编译中 Lua 5.4 兼容路径使用 `lj_vm_floor` 时触发的 `floor` 隐式声明警告，相关 C 文件显式包含 `<math.h>`。
- 已修正 Windows 主机交叉构建后的清理边界：`src/Makefile clean` 现在会删除无扩展名的 `luajit`，避免 Android/Linux artifact 残留导致后续本机 smoke 误执行错误架构二进制。
- 已继续补 Lua 5.4 standalone 环境变量优先级：兼容构建会优先读取 `LUA_INIT_5_4`、`LUA_PATH_5_4`、`LUA_CPATH_5_4`，再回退旧变量名。
- 已继续补 Lua 层 binary chunk smoke：`string.dump(f, strip)` 已覆盖 full/stripped 写出、`mode="b"` 回读执行，以及 binary chunk 被 text-only mode 拒绝。
- 已继续补 Lua 5.4 `load(..., env)` 的真实 upvalue 初始化语义：LuaJIT dumped chunk 回读时，带真实首个 upvalue 的函数现在会保留第 4 个 env 参数的原始值，覆盖 table / number / false / nil；未传 env 时仍默认初始化为当前全局环境。
- 已继续收紧 Lua 5.4 `string.format()` 错误表面：整数格式遇到非 number 类型、以及 `%q` 遇到 table / 带 `__tostring` 的 table 时，错误函数名现在保持为 `string.format`，不再从内部格式化 helper 泄露为 `?`。
- 已修正 Lua 5.4 `string.pack("lL")` smoke 的跨平台 ABI 假设：native `long` 在 Windows x64 为 4 字节、Android ARM64 为 8 字节，测试现在按目标 ABI 校验；Android ARM64 设备 `R5CN30J05BT` 已通过 `test/smoke.lua lua54compat`。
- 已继续补 Lua 5.4 debug hook transfer 字段：普通 Lua 函数 call hook 会报告参数 transfer 范围，return hook 会报告返回值 transfer 范围；非 hook 场景仍返回 `0, 0`。
- 已继续补 Lua 5.4 debug hook transfer 的 vararg 边界覆盖：Lua smoke 和 C API smoke 均校验 vararg 函数 call hook 只报告固定参数 transfer，return hook 报告完整返回值 transfer。
- 已继续补 Lua 5.4 debug hook transfer 的 C 函数 call 边界：C 函数在 call hook 中会按本次调用的实际参数个数报告 transfer 范围。
- 已继续补 Lua 5.4 debug frame metadata 批次的 C return transfer 接口：新增统一 C return hook dispatch 入口，x64 / ARM64 VM 的普通 C 返回路径会在结果折叠前发布 `ftransfer` / `ntransfer`，fast C 函数入口会先保留原始参数 transfer 起点。
- 已继续扩展 Lua 5.4 debug hook transfer 覆盖：Lua smoke 校验 fast C 函数 return hook transfer，C API smoke 校验注册 C 函数 call/return hook transfer；更深的 tailcall 嵌套和错误展开边界继续留在同一批次后续推进。
- 已继续补 Lua 5.4 debug frame metadata 批次的 Lua tailcall 元数据：x64 / ARM64 `BC_CALLT` 会为普通 Lua tail call 保存 frame side marker，`debug.getinfo(..., "t")`、Lua hook 和 C API hook 可报告 `istailcall=true`，并在 frame 返回时清 marker。
- 已继续扩展 Lua 5.4 tailcall 回归：Lua smoke 覆盖 `tail call` hook 文本、tailcall return transfer 和直接普通调用不误判，C API smoke 覆盖 `LUA_HOOKTAILCALL`、`lua_Debug.istailcall` 和 transfer 字段。
- 已继续补 Lua 5.4 vararg tailcall 元数据边界：debug 层会把 `FRAME_VARG` pseudo-frame 回溯到真实 Lua frame 判断 tailcall marker，vararg tailcall 的 return hook 现在也会报告 `istailcall=true` 和完整返回 transfer。
- 已继续扩展 Lua/C API tailcall 回归：Lua smoke 和 C API smoke 均覆盖 vararg tailcall 的 tail event、return event、`istailcall` 和 transfer 字段。
- 已继续补 Lua 5.4 tail-position C return 回归：Lua smoke 和 C API smoke 覆盖 Lua 函数尾位置返回 C 函数时不误报 Lua tail hook，并继续报告 C call/return transfer。
- 已继续收口 Lua 5.4 debug frame metadata 批次：`pcall` / `xpcall` 的 protected-call 合成 C return hook 现在会在 frame 被覆盖前报告 Lua 5.4 transfer 范围，成功路径会先同步可被 hook/debug API 观察的 `L->base` / `L->top` 窗口；同时补了 tailcall error unwind 后普通调用不误报 `istailcall` 的回归。
- 已补充 Lua 5.4 `os.remove()` 失败返回 smoke：对齐官方保留文件名前缀、返回系统错误文本和 errno code 的表面；`os.rename()` 继续保持不拼接源文件名。
- 已用本机 `emcc 5.0.6` 试跑 Emscripten 构建入口，当前 Makefile 在 `lj_arch.h` 阶段明确失败为 wasm 架构不受支持；该平台仍需要单独 wasm/interpreter VM 后端方案。
- 已继续收紧 Lua 5.4 GC 公开表面：兼容构建中 `collectgarbage("setstepmul", n)` 的初始旧值现在对齐 Lua 5.4 的 `100`。
- 已继续收紧严格 Lua 5.4 语法表面：`L` / `LL` / `UL` / `ULL` / `uLL`、`0b...` 和 imaginary `i` 数字字面量扩展都已进入拒绝用例。
- 已继续补 Lua 5.4 头文件/辅助库表面：新增 `LUA_VERSION_MAJOR` / `LUA_VERSION_MINOR` / `LUA_VERSION_RELEASE` / `LUA_VERSION_RELEASE_NUM` / `LUA_NUMTYPES` 宏，以及 `luaL_addgsub()`。
- 已继续收紧 `__name` 错误文本覆盖：Lua 5.4 兼容模式下 `coroutine.resume()` / `coroutine.close()` 的非线程参数会报 `thread expected, got <__name>`。
- 已继续补齐 coroutine 库 Lua 5.4 表面：`coroutine.isyieldable([co])` 支持可选 thread 参数，挂起/死亡的非主 coroutine 返回 true，非 thread 参数走 Lua 5.4 风格错误文本。
- 已完成 `__name` 元字段当前兼容闭环：覆盖 `tostring()` 前缀、参数类型错误、`type()` 不受影响、非字符串 `__name` 忽略、`luaL_newmetatable()` 和 `luaL_tolstring()` 路径。
- 已继续收紧 coroutine 库参数错误：`coroutine.create()` / `resume()` / `status()` / `wrap()` / `close()` 的基础错误会带 `coroutine.xxx` 函数名，并使用 Lua 5.4 的 `function/thread expected` 文本。
- 已继续收紧 `coroutine.yield()` 主线程错误：Lua 5.4 兼容模式下主线程直接 yield 会报 `attempt to yield from outside a coroutine`。
- 已继续补 Lua 5.4 standalone `arg` 表边界：无脚本、仅 `-e` 执行时，兼容构建的 `arg[0]` / `arg[1]` / `arg[2]` 已对齐 Lua 5.4。
- 已继续补 Lua 5.4 standalone `arg` 表覆盖：脚本文件、`-- script` 和 stdin `-` 的 `arg` 正负索引组合已按官方 Lua 5.4.8 对照进入 smoke。
- 已继续补 Lua 5.4 standalone `-i` 边界：兼容构建进入交互模式时不再额外打印 LuaJIT 的 `JIT:` 状态行，默认 LuaJIT 构建保留原输出。
- 已继续补 Lua 5.4 standalone 默认 package 搜索路径：兼容构建的 `package.path` / `package.cpath` 默认值已改用 Lua 5.4 风格的 executable-local、versioned share/lib 和当前目录 fallback。
- 已重新核对 `<const>` debug API 行为：本机 Lua 5.4.8 允许 `debug.setlocal()` / `debug.setupvalue()` / `debug.upvaluejoin()` 绕过源码级 const 限制，当前行为已补 smoke 并从 TODO 缺口中移除。
- 已继续收紧 `_ENV` 表面：Lua 5.4 兼容模式下 `rawget(_G, "_ENV")` 已按官方 Lua 5.4.8 返回 `nil`，`next(_G)` / `pairs(_G)` 也不会枚举内部兼容 `_ENV` 键，同时保留裸 `_ENV == _G` 的当前兼容访问。
- 已继续扩展 `_ENV` 回归：覆盖闭包继承局部 `_ENV`、真实 lexical `_ENV` 的 `debug.upvalueid()` / `debug.upvaluejoin()`、非 table lexical `_ENV` 的 debug 枚举和运行期索引错误。
- 已重新核对 GC 选项表面：本机 Lua 5.4.8 不接受 `collectgarbage("minor")` / `"major"`，当前 invalid option 行为已补 smoke。
- 已继续补 table `__gc` 元方法：Lua 5.4 兼容构建会在 metatable 赋值时 armed table finalizer，GC 回收时按 LIFO 调度，并将 finalizer 抛错转入 Lua 5.4 warning 通道。
- 已继续收尾 table 库 Lua 5.4 边界：显式空范围、`table.sort` comparator 错误传播、非严格 comparator 和官方允许的 always-true / always-false comparator 结果已进入 smoke。
- 已继续补弱键表 ephemeron 语义：`__mode="k"` 下 value 反向引用 key 不再保活 key，GC atomic 阶段会对弱键强值表做固定点标记；smoke 覆盖自环 key 被清理和外部强引用 key 被保留。
- 已修复 Lua 5.4 helper 字段访问的大 chunk 常量索引问题：`jit._lua54_*` helper 名称超过 `BC_TGETS` 8 位常量范围时改用 `KSTR + TGETV`，避免误读成 `generational`、`error` 等其他常量。
- 已继续补 `string.format("%p")` 的 Lua 5.4 表面：nil、boolean、number 等没有 GC 指针的值输出 `(null)`，string 等 GC 对象仍输出平台 C `%p` 文本。
- 已继续收紧数值 `for` 的 Lua 5.4 表面：常量 `0` / `0.0` step 会在加载阶段报 `'for' step is zero`，动态变量 `0` 和字符串 `"0"` step 会在进入循环前通过跨平台 helper 报同样错误。
- 已继续收紧 GC 参数边界：`collectgarbage("setpause", n)` / `("setstepmul", n)` 在 Lua 5.4 兼容模式下会把参数压到 `0..1000`，并按官方 4 点粒度向下取整。
- 已继续收紧标准库边界：`math.type()` 现在按 Lua 5.4 在缺少参数时报错，对 nil/string 等非 number 参数仍返回 nil。
- 已继续收紧 math 新入口错误文本：`math.type()` / `math.tointeger()` / `math.ult()` / `math.min()` / `math.max()` 的缺参参数错误会带 `math.xxx` 函数名，`math.ult()` 的整数表示错误也使用该函数名。
- 已继续补齐 `pairs()` 的 Lua 5.4 边界：没有 `__pairs` 时不再入口强制 table，会返回原始 `next, value, nil`，由后续 `next()` 调用决定是否报错。
- 已继续收紧标准库错误文本：Lua 5.4 兼容模式下 `assert()`、`type()`、`tostring()`、`pcall()`、`xpcall()`、`select()`、`error()`、`tonumber()`、`load()`、`loadfile()`、`next()`、`pairs()`、`ipairs()`、`getmetatable()`、`setmetatable()`、`rawget()` / `rawset()` / `rawequal()` / `rawlen()` 的基础参数错误会带实际函数名，`rawlen()` 的期望类型文本对齐为 `table or string`。
- 已继续收紧 `math.deg()` / `math.rad()` 边界：Lua 5.4 兼容模式下改用带参数检查的 C helper，缺参和错误类型会报标准参数错误，同时保留数值字符串转换。
- 已继续补齐 Lua 5.4 `math.min` / `math.max` 语义：兼容模式下不再强制 number，改用普通 `<` 比较，因此支持字符串和带 `__lt` 的对象。
- 已继续收紧 `string.format("%q", number)`：整数输出十进制，负零输出 `-0x0p+0`，NaN 输出 `(0/0)`，正负无穷输出 `1e9999` / `-1e9999`，有限浮点继续输出可读回十六进制浮点。
- 已继续收紧 `tonumber()` 的 Lua 5.4 数值文本规则：兼容模式下字符串 `inf` / `infinity` / `nan` 及大小写、符号、空白变体会返回 `nil`，不再走 LuaJIT/C 风格非有限数解析。
- 已继续收紧 `tonumber()` 的 Lua 5.4 扩展数字拒绝规则：兼容模式下 `0b` / `0B` 二进制前缀字符串及符号、空白变体会返回 `nil`，不再走 LuaJIT 扩展扫描。
- 已继续同步 Lua 5.4 通用字符串数字转换表面：`math.abs()` / `math.tointeger()` / `math.type()` 以及 `lua_isnumber()` / `lua_tonumberx()` / `lua_tointegerx()` / `luaL_checknumber()` 现在同样拒绝 `inf` / `nan` / `0b` 等 LuaJIT 扩展数字字符串。
- 已继续同步 Lua 5.4 C API 数字扫描表面：`lua_stringtonumber()` 现在同样拒绝 `inf` / `nan` / `0b` 等 LuaJIT 扩展数字字符串。
- 已继续扩展 JIT smoke：开启 JIT 后，`tonumber()` 在热循环中仍会拒绝 `inf` / `nan` / `0b` 等 LuaJIT 扫描器扩展数字字符串，并确认该路径产生 trace。
- 已继续明确 binary chunk 兼容边界：预生成的官方 Lua 5.4.8 `string.dump(function() return 54 end, true)` 会被兼容构建明确拒绝为 incompatible bytecode，当前只支持 LuaJIT bytecode。
- 已继续补齐 LuaJIT bytecode 回读的 upvalue 初始化：`load(string.dump(function() return upvalue end, true), ..., "b")` 会按 Lua 5.4 规则把第一个真实 upvalue 初始化为当前全局环境，传入第 4 个 env 参数时初始化为指定 env。
- 已继续收紧 LuaJIT stripped bytecode 的 debug upvalue 枚举：plain dump 不再误暴露伪 `_ENV`，带真实 upvalue 的 stripped dump 不再额外插入伪 `_ENV`。
- 已补充平台构建 TODO：把 Android ARM64、iOS ARM64、PC x64、Emscripten 的 64 位构建和 smoke 验证矩阵列为独立未完成项；当前已验证范围仍是本机 Windows PC 64 位 `make test`。
- 已继续补默认构建 C API 回归：新增 `test/lua51_capi_smoke.c` 和 `make smoketest-capi-default`，确认 Lua 5.4 兼容头改动不影响旧 LuaJIT 5.1 宏和 ABI 入口。
- 已继续清理 `TODO.md` 过期状态：`string.gmatch(init)`、`utf8` lax、`math.randomseed()` 无参、`string.pack` alignment/`X` 和 `_ENV` raw global 表面已按当前验证结果修正。
- 已继续收紧 string 库错误文本：Lua 5.4 兼容模式下 `string.char(256)` 的越界错误现在包含 `value out of range`，不再使用 LuaJIT 旧 `invalid value` 文本。
- 已继续收紧 utf8 库错误文本：Lua 5.4 兼容模式下 `utf8.char(0x80000000)` 这类整数可表示但超出扩展码点范围的输入会报 `value out of range`。
- 已继续收紧 `string.format()` 错误文本：Lua 5.4 兼容模式下整数格式遇到无整数表示的字符串数字时，错误会归到 `string.format`，不再显示为 `?`。
- 已继续收紧标准库错误文本：Lua 5.4 兼容模式下 `dofile()` 和 `collectgarbage()` 的 option、字符串参数、整数参数错误会带实际函数名。
- 已继续收紧 table 库错误文本：Lua 5.4 兼容模式下 `table.concat()` / `insert()` / `remove()` / `sort()` / `move()` 的表、分隔符、整数位置和 comparator 参数错误会带 `table.xxx` 函数名。
- 已继续收紧 os 库错误文本：Lua 5.4 兼容模式下 `os.date()` / `difftime()` / `execute()` / `getenv()` / `remove()` / `rename()` / `setlocale()` / `time()` 的基础参数错误会带 `os.xxx` 函数名。
- 已继续收紧 debug 库错误文本：Lua 5.4 兼容模式下 `debug.getinfo()` / `getlocal()` / `setlocal()` / `getupvalue()` / `setupvalue()` / `upvalueid()` / `upvaluejoin()` / `sethook()` / `getuservalue()` / `setuservalue()` / `setcstacklimit()` 的基础参数错误会带 `debug.xxx` 函数名。
- 已继续收紧 utf8 库错误文本：Lua 5.4 兼容模式下 `utf8.char()` / `codepoint()` / `codes()` / `len()` / `offset()` 的基础参数错误会带 `utf8.xxx` 函数名。
- 已继续收紧 string 库错误文本：Lua 5.4 兼容模式下 `string.byte()` / `char()` / `dump()` / `find()` / `format()` / `gmatch()` / `gsub()` / `len()` / `lower()` / `match()` / `rep()` / `reverse()` / `sub()` / `upper()` / `pack()` / `unpack()` / `packsize()` 的基础参数错误会带 `string.xxx` 函数名。
- 已继续补齐 `table.unpack` 的 Lua 5.4 边界：默认终点共用表库兼容长度，能返回 `{ "a", nil, "c" }` 的第三个值；显式 `i` / `j` 会拒绝无整数表示的数值。
- 已继续补齐 `table.unpack` 的非 table 边界：入口不再强制 table，默认终点先触发 length 语义，显式空范围可直接返回空结果，实际取值时再由普通索引路径报错。
- 已继续收紧 `table.concat` 的 Lua 5.4 边界：显式 `i` / `j` 会拒绝无整数表示的数值，而不是截断后继续拼接。
- 已继续收紧 `collectgarbage` 参数边界：`step` / `setpause` / `setstepmul` 的第二参数会拒绝无整数表示的 number，同时保留字符串数字的 Lua 5.4 兼容转换。
- 已继续收紧 `error(message, level)` 的 Lua 5.4 边界：level 会拒绝无整数表示的 number，而不是截断后继续抛原错误。
- 已继续收紧 `getmetatable()` 的 Lua 5.4 边界：兼容模式下缺少参数会按 value expected 报错，同时保留 `__metatable` 保护返回值。
- 已继续收紧 `select(index, ...)` 的 Lua 5.4 边界：index 会拒绝无整数表示的 number，同时保留字符串数字转换。
- 已继续收紧 debug 库的 Lua 5.4 整数边界：`debug.getinfo` / `getlocal` / `setlocal` / `getupvalue` / `setupvalue` / `upvalueid` / `upvaluejoin` / `sethook` / `traceback` / `getuservalue` / `setuservalue` / `setcstacklimit` 都会拒绝无整数表示的 number，同时保留字符串数字转换。
- 已继续收紧 string / utf8 库和 C API 的 Lua 5.4 整数边界：`string.byte` / `char` / `sub` / `rep` / `find` / `match` / `gmatch` / `gsub` / `pack` / `unpack`、`utf8.char` / `codepoint` / `len` / `offset`、`lua_tointegerx()`、`luaL_checkinteger()` 和 `luaL_optinteger()` 都已拒绝无整数表示的 number。
- 已继续补齐 table 库 Lua 5.4 长度边界：`table.sort` 的排序范围现在共用表库兼容长度，会尊重 `__len`，并在 `__len` 返回 fraction number 时按 `object length is not an integer` 报错。
- 已继续补齐 `table.move` 的 Lua 5.4 元方法边界：兼容模式下移动元素时会通过 `__index` 读取源值、通过 `__newindex` 写入目标值，而不是只做 raw table 拷贝。
- 已继续收紧 `load()` / `loadfile()` 的 Lua 5.4 mode 错误文本：text/binary chunk 与 mode 不匹配时会报告具体 chunk 类型并回显 mode。
- 已继续收紧 `require()` / `package.searchpath()` 的 Lua 5.4 错误片段格式：searcher 直接返回的错误不再带前导 `\n\t`，最终 `require()` 错误由 require 自己统一添加缩进。
- 已同步 C API smoke 中 `luaL_loadbufferx()` / `luaL_loadfilex()` / `lua_load()` 的 mode 错误文本断言，覆盖新的 Lua 5.4 文案。
- 已继续补齐 debug 信息表面：`debug.getinfo(..., "r")` 和 C API `lua_getinfo(..., "r")` 已接受 Lua 5.4 transfer-info 选项，非 hook 场景返回保守的 `ftransfer=0` / `ntransfer=0`。
- 已继续补齐 Lua 5.4 standalone 表面：兼容构建现在接受 `-W`，并会在执行 `-e` chunk 或脚本前开启 warning 输出。
- 已继续收紧 `table.sort` 的 Lua 5.4 comparator 边界：兼容模式会拒绝 `a <= b` / `a >= b` 这类非严格 comparator，并报 `invalid order function for sorting`。
- 已继续补齐 `table.sort` 的 Lua 5.4 元方法边界：兼容模式排序代理表时会通过 `__index` 读取元素、通过 `__newindex` 写回结果。
- 已继续收紧 `tonumber` 的 Lua 5.4 base 参数边界：显式 base 会拒绝无整数表示的 number，同时保留字符串数字 base 的兼容转换。
- 已继续补齐 warning 默认输出表面：`warn("@on")` 和 standalone `-W` 输出现在带 Lua 5.4 的 `Lua warning: ` 前缀。
- 已继续补 Lua 5.4 C API 头文件表面：兼容头现在声明 `lua_copy()`，C API smoke 覆盖栈槽复制行为。
- 已继续补 Lua 5.4 lauxlib 头文件表面：新增 `luaL_Stream` 类型定义，并在 C API smoke 中编译覆盖字段访问。
- 已继续补 Lua 5.4 `io.lines(filename)` 返回形态：现在会返回第 4 个 closing value，并在迭代到 EOF 后关闭文件。
- 已继续收紧 `string.format("%c", value)` 的 Lua 5.4 数值边界：fraction number 现在报“没有整数表示”，字符串数字仍可转换。
- 已继续收紧 Lua 5.4 `//` 错误文本：无元方法且无法做数值整除时会报 `attempt to idiv a '<lhs>' with a '<rhs>'`，不再泄露 `_lua54_idiv` helper，并保留 `__name` 类型名。
- 已继续收紧 Lua 5.4 位运算错误文本：`&` / `|` / `~` / `<<` / `>>` 降级 helper 遇到无整数表示的 number 会报 `number has no integer representation`，遇到 string/boolean/table 会报 bitwise operator 错误并保留 `__name` 类型名。
- 已继续补齐 table 库代理表读取语义：`table.concat` / `table.unpack` 在 Lua 5.4 兼容模式下会通过 `__index` 读取元素。
- 已继续收紧 `os.rename()` 的 Lua 5.4 错误文本边界：失败时返回原始系统错误字符串，不再拼入源文件名。
- 已继续补齐 table 库代理表读写语义：`table.insert` / `table.remove` 在 Lua 5.4 兼容模式下会通过 `__index` 移动源元素、通过 `__newindex` 写回目标槽位。
- 已继续收紧 `string.format("%q", value)` 的 Lua 5.4 字面量规则：table 等没有字面量形式的值即使带 `__tostring` 也会报错。
- 已继续收紧普通算术的字符串失败路径：Lua 5.4 兼容模式下 `"x" + 1`、`1 + "x"`、`"x" * true` 和 `-"x"` 会报具体操作名和左右操作数类型，不再只报 LuaJIT 泛化的 `perform arithmetic on string value`。
- 已继续补齐字符串算术元方法优先级：Lua 5.4 兼容模式下字符串 metatable 显式提供 `__add` / `__mul` / `__unm` 时，会优先于 `"1"` 这类字符串数字转换执行。
- 已继续扩展 JIT smoke：开启 JIT 后的字符串 `__add` 元方法优先级会在热循环中验证结果并确认产生 trace。
- 已继续扩展 JIT smoke：真实 `_ENV` upvalue 经 `debug.setupvalue` 替换为自定义 table 后，隐式全局访问可以在热循环中执行并产生 trace。

## 修改内容

- 新增 `LUAJIT_ENABLE_LUA54COMPAT` 构建开关。
- 新增内部 `LJ_54` 兼容标识。
- Lua 5.4 兼容模式会自动启用现有 Lua 5.2 兼容层作为基础。
- 默认构建保持 LuaJIT 原有 Lua 5.1 行为不变。
- 更新文档说明实验性 Lua 5.4 兼容模式、当前范围和限制。
- 新增 `src/lib_utf8.c`，并仅在 `LJ_54` 模式由 `luaL_openlibs()` 加载 `utf8` 标准库。
- 新增顶层测试目标：
  - `make smoketest`
  - `make smoketest-lua54compat`
  - `make smoketest-capi-lua54compat`
  - `make test`

## 新功能

- Lua 5.4 兼容构建中：
  - `_VERSION` 报告为 `Lua 5.4`。
  - `jit.lua54compat` 报告为 `true`。
  - 新增全局 `warn()`。
  - `warn()` 支持 `@on` / `@off` 控制 warning 输出。
  - `warn()` 输出写入 stderr。
  - warning 开关状态保存在 `global_State` 中。
  - 新增 `math.type()`，用于区分 LuaJIT 内部整数和浮点数值。
  - 新增 `math.maxinteger` / `math.mininteger`，当前对应 LuaJIT 内部 32 位整数范围。
  - 新增 `math.tointeger()`，按当前 32 位内部整数范围做严格整数转换；无法精确表示为整数时返回 `nil`。
  - 新增 `math.ult()`，按当前 32 位内部整数范围执行无符号小于比较。
  - 新增 `utf8` 标准库的基础函数：`utf8.char()`、`utf8.codepoint()`、`utf8.codes()`、`utf8.len()`、`utf8.offset()` 和 `utf8.charpattern`。
  - 新增 `debug.setcstacklimit()` 兼容入口；LuaJIT 使用自身 C 栈保护，因此当前实现为 no-op 并返回稳定有效值。
  - 新增 `coroutine.close()` 的基础兼容语义，可关闭无 `<close>` 变量的 suspended/dead 协程。
  - `package` 库在 Lua 5.4 兼容构建中只暴露 `package.searchers`，不再暴露旧别名 `package.loaders`。
  - `require()` 支持 Lua 5.4 searcher 的 loader data：传给 loader，并在首次加载时作为第二返回值返回。
  - 新增 `string.pack()`、`string.unpack()`、`string.packsize()` 的基础兼容子集，当前覆盖整数、浮点、固定长度字符串、零结尾字符串、长度前缀字符串、跳过字节和 endian 标记。
  - 新增局部变量属性语法解析：`local x <const>` 和 `local x <close>`。
  - `<const>` 局部变量会阻止后续局部赋值和闭包内上值赋值，错误在 chunk 加载/编译阶段抛出。
  - `<close>` 语法遵循 Lua 5.4 限制：同一条 local 声明中不允许多个 to-be-closed 变量。
  - `<close>` 当前先完成语法接收和属性记录；`__close` 元方法调度仍需要后续 VM 层实现。
  - 新增 Lua 5.4 兼容模式下的整除运算符 `//`。
  - 新增 Lua 5.4 兼容模式下的位运算符：`&`、`|`、二元 `~`、一元 `~`、`<<`、`>>`。
  - 新运算符当前由解析器降级为 `jit` 表中的内部辅助函数调用，避免修改 LuaJIT 字节码格式和所有 VM 后端；整数范围沿用当前兼容层的 32 位内部整数策略。
  - `ipairs()` 在 Lua 5.4 兼容模式下使用普通索引访问，支持通过 `__index` 返回迭代值。
  - `warn()` 空参数调用现在按 Lua 5.4 报错。
  - `math.randomseed(x, y)` 在 Lua 5.4 兼容模式下接受第二个种子参数并返回实际接受的两个种子值；内部 PRNG 仍沿用 LuaJIT 的单状态实现。
  - `math.random()` 保持无参数时返回 `[0, 1)` 浮点数；带整数区间参数时改为 Lua 5.4 风格的整数区间语义。
  - `math.random(0)` 在 Lua 5.4 兼容模式下返回当前 32 位兼容整数范围内的随机整数值；未启用 dual-number 的构建只能以可精确转整数的 number 形式承载该值。
  - `math.random(n)` / `math.random(m, n)` 会拒绝无整数表示的参数、空区间和超过两个参数的调用。
  - `math.randomseed()` 无参数调用现在返回从新 PRNG 状态生成的两个非零占位种子，不再返回固定 `0, 0`。
  - `warn()` 现在按 Lua 5.4 接受 number 参数并转换为字符串；boolean/table 等仍按 `string expected` 报错。
  - `utf8.len()`、`utf8.codepoint()`、`utf8.codes()` 支持 Lua 5.4 lax 参数，可解析扩展 5/6 字节 UTF-8 和 surrogate 字节序列；默认 strict 模式仍保持有效 Unicode 检查。
  - `string.gmatch(s, pattern, init)` 支持第三个起始位置参数，包括负数相对位置。
  - `<=` 在 Lua 5.4 兼容模式下不再使用 `__lt` 模拟 `__le`；缺少 `__le` 会报比较错误。
  - `//` helper 支持字符串数字的算术转换，例如 `"5" // 2`；位运算 helper 继续拒绝字符串，符合 Lua 5.4 对 bitwise 的限制。
  - `//`、`&`、`|`、二元 `~`、一元 `~`、`<<`、`>>` 的 helper 在原始数值路径不可用时会查找并调用 Lua 5.4 元方法：`__idiv`、`__band`、`__bor`、`__bxor`、`__bnot`、`__shl`、`__shr`。
  - `__name` 元字段会影响 `tostring()` 的对象类型名前缀，以及参数类型错误中的实际类型名。
  - `coroutine.resume()` / `coroutine.close()` 在 Lua 5.4 兼容模式下的非线程参数错误会使用 `__name` 参与实际类型名；默认构建继续保留 LuaJIT 旧错误文本。
  - `coroutine.isyieldable([co])` 支持 Lua 5.4 的可选 thread 参数；传入挂起、死亡或尚未启动的非主 coroutine 时返回 true，传入非 thread 参数时报 `thread expected`。
  - `luaL_newmetatable()` 在 Lua 5.4 兼容模式下会把注册类型名写入 `__name`。
  - `tonumber(s, 16)` 在 Lua 5.4 兼容模式下不再把 `0x` 前缀当成 explicit-base 16 的合法前缀；`base >= 34` 时 `x` 仍按普通数字字符处理。
  - `string.format()` 的整数格式现在拒绝没有整数表示的 number/string number；`%q` 对 number 输出 Lua 5.4 风格的可读回数值文本，`%p` 对 `nil` 输出 `(null)`，非 nil 指针使用平台 C `%p` 文本。
  - `math.type()` 在非 dual-number 构建下会把当前 32 位范围内可精确表示为整数的 number 报告为 `integer`；`math.floor()`、`math.ceil()`、`math.modf()` 的整数结果会尽量返回当前兼容整数表面。
  - `debug.getuservalue()` / `debug.setuservalue()` 在 Lua 5.4 兼容模式下不再暴露 LuaJIT userdata 内部环境表；内置 userdata 的 indexed uservalue 表面返回 `nil`。
  - `table.concat()` 未显式传入终点时会尊重 `__len` 长度元方法，再按该终点检查 nil 洞。
  - Lua 5.4 兼容模式禁用 LuaJIT 的 `0b...` 二进制数字字面量、`L` / `LL` / `UL` / `ULL` / `uLL` FFI 整数后缀和 imaginary `i` 数字字面量扩展。
  - `lua.h` / C API 新增一批 Lua 5.4 表面：`lua_Unsigned`、`LUA_MAXINTEGER`、`LUA_MININTEGER`、`lua_absindex()`、`lua_isinteger()`、`lua_rawlen()`、`lua_geti()`、`lua_seti()`、`lua_rawgetp()`、`lua_rawsetp()`、`lua_pushglobaltable()`。
  - `lua.h` / C API 继续新增 Lua 5.4 表面：`lua_arith()`、`lua_compare()`、`lua_len()`、`lua_numbertointeger()`、`lua_rotate()`、`lua_stringtonumber()` 以及 `LUA_OP*` 比较/算术常量。
  - C API 继续新增 `lua_getextraspace()`、`lua_setwarnf()`、`lua_warning()`，并补 `lua_KContext`、`lua_KFunction`、`lua_WarnFunction`、`LUA_RIDX_MAINTHREAD`、`LUA_RIDX_GLOBALS`、`LUA_RIDX_LAST`、`LUA_EXTRASPACE`、`LUA_NUMTAGS`、`LUA_GNAME`、`LUA_FILEHANDLE`、`LUA_VERSUFFIX`、`LUAMOD_API`、`LUA_LOADED_TABLE`、`LUA_PRELOAD_TABLE`、`LUA_HOOKTAILCALL`、`LUA_GCGEN`、`LUA_GCINC`。
  - C API 新增独立 `luaopen_coroutine()` 入口；`luaL_openlibs()` 仍通过同一路径注册 coroutine 库，Lua 5.4 兼容模式下会一并安装 `coroutine.close()`。
  - Lua 5.4 外部兼容头下的 `luaopen_base()` 已映射到单返回值包装；LuaJIT 内部和默认构建仍保留 base+coroutine 的旧返回约定。
  - C API 新增 indexed uservalue 表面：`lua_newuserdatauv()`、`lua_getiuservalue()`、`lua_setiuservalue()`；当前用 LuaJIT userdata 环境表保存声明数量和值，超出声明范围按 Lua 5.4 表面返回失败。
  - C API 新增 `lua_resetthread()` / `lua_closethread()` 基础兼容入口；当前覆盖无 to-be-closed 变量的 coroutine reset/close，完整 `<close>` 关闭语义仍保留在 TODO。
  - C API 新增 Lua 5.4 形态的 `lua_resume(L, from, nargs, nresults)` 外部宏和 `lua_resume54()` 包装入口；当前复用 LuaJIT 内部 2 参数 resume ABI，并把 yield/return 后的栈顶结果数写回 `nresults`。
  - Lua 5.4 外部兼容头不再暴露 `LUA_GLOBALSINDEX`、`LUA_ENVIRONINDEX`、`lua_strlen` 以及旧 `lua_equal`、`lua_lessthan`、`lua_objlen`、`lua_cpcall`、`lua_getfenv`、`lua_setfenv` 声明；LuaJIT 内部和命令行 frontend 仍通过内部标记使用旧 ABI。
  - Lua 5.4 外部兼容头不再暴露旧 lauxlib 注册入口 `luaL_openlib` / `luaL_register` / `luaL_pushmodule`；默认构建和内部库代码继续保留旧入口。
  - Lua 5.4 外部兼容头下的 `lua_pushglobaltable()`、`lua_getglobal()`、`lua_setglobal()` 已使用 registry globals 表实现，不再依赖旧 `LUA_GLOBALSINDEX`。
  - Lua 5.4 外部兼容头下的 `lua_gettable()`、`lua_getfield()`、`lua_geti()`、`lua_rawget()`、`lua_rawgeti()`、`lua_rawgetp()` 已使用 `*54` 包装入口返回取到值的 Lua 类型；LuaJIT 内部继续使用旧 `void` ABI。
  - Lua 5.4 外部兼容头下的 `lua_load()` 已使用 5 参数宏映射到 `lua_load54()` wrapper，支持传入 `mode`。
  - Lua 5.4 外部兼容头下的 `lua_dump()` 已使用 4 参数宏映射到 `lua_dump54()`，支持把 `strip` 参数转发为 LuaJIT bytecode writer 的 strip 标志；内部旧 3 参数 ABI 不变。
  - `lua.h` 新增 `LUA_VERSION_MAJOR` / `LUA_VERSION_MINOR` / `LUA_VERSION_RELEASE` / `LUA_VERSION_RELEASE_NUM` / `LUA_NUMTYPES` 宏。
  - `LUA_GCCOUNTB` 的 C API 表面已进入 smoke，当前覆盖 LuaJIT GC 总量低 10 bit 的 byte remainder 返回范围。
  - `debug.getuservalue()` / `debug.setuservalue()` 会访问 declared indexed uservalue；内置或未声明 uservalue 的 userdata 仍不暴露 LuaJIT 内部环境表。
  - `table.concat()` 对新建 list table 中间有 nil、后续仍有数组项的情况，会在 Lua 5.4 兼容模式下继续检查后续数组项对应的默认终点，和官方 Lua 5.4 对 `{1,nil,3}` 的报错行为对齐。
  - `table.insert()` / `table.remove()` / `table.move()` 在 Lua 5.4 兼容模式下使用严格整数参数检查；无整数表示的位置参数不再被 LuaJIT 旧路径截断。
  - `table.concat()` / `table.insert()` / `table.remove()` 在 Lua 5.4 兼容模式下的默认位置使用表库专用长度兼容逻辑，带 `__len` 的表和 `{1,nil,3}` 这类新建 list table 会按 Lua 5.4 参考行为检查、插入或删除；`__len` 返回无整数表示的值会报 `object length is not an integer`。
  - JIT 开启时，Lua 5.4 兼容模式下的 `//`、位运算和局部 `_ENV` 热循环已进入 smoke 回归，并在当前平台确认能产生 trace。
  - JIT 开启时，Lua 5.4 兼容模式下的 `math.random` 不再使用 LuaJIT 旧 PRNG recorder；无参、`0` 和常量整数区间会记录 xoshiro256** helper，官方 xoshiro256** 路径已由 `testes/math.lua` 和 smoke 覆盖。
  - JIT 开启时，Lua 5.4 兼容模式下的 `tonumber()` 扫描器扩展数字拒绝路径已进入 smoke 回归，并在当前平台确认能产生 trace。
  - registry 初始化会写入 `LUA_RIDX_MAINTHREAD` 和 `LUA_RIDX_GLOBALS`；新线程会复制当前线程的 pointer-sized extraspace。
  - `lua_callk()`、`lua_pcallk()`、`lua_yieldk()` 已暴露为真实函数 ABI，当前 wrapper 覆盖非 continuation 调用；真实 yield continuation 语义仍保留在 `TODO.md`。
  - `lauxlib.h` / 辅助库新增 Lua 5.4 常用表面：`LUA_GNAME`、`LUA_FILEHANDLE`、`LUA_LOADED_TABLE`、`LUA_PRELOAD_TABLE`、`lua_writestring`、`lua_writeline`、`lua_writestringerror`、`luaL_pushfail()`、`luaL_len()`、`luaL_getsubtable()`、`luaL_requiref()`、`luaL_tolstring()`、`luaL_typeerror()`、`luaL_argexpected()`、`luaL_checkversion()`、`luaL_addgsub()`、`luaL_intop()` 和 buffer API。
  - Lua 5.4 兼容模式下 `luaL_prepbuffsize()` / `luaL_buffinitsize()` 会按请求尺寸增长 buffer，不再被旧 LuaJIT 固定 `LUAL_BUFFERSIZE` 缓冲区限制；默认构建仍保留旧 LuaJIT buffer 结构。
  - `luaL_loadbufferx()` / `luaL_loadfilex()` 的 `mode` 参数路径已通过 C API smoke 覆盖；text 模式可加载源码，binary-only 模式会拒绝 text chunk。
  - `luaL_fileresult()`、`luaL_execresult()`、`luaL_newlib()`、`luaL_setfuncs()`、`luaL_newmetatable()` / `luaL_getmetatable()`、`luaL_setmetatable()`、`luaL_testudata()`、`luaL_checkudata()`、`luaL_traceback()` 和 `luaL_dostring()` 已进入 Lua 5.4 C API smoke。
  - `LUA_COMPAT_APIINTCASTS` 下的 deprecated unsigned/int/long cast 宏已进入单独 C smoke，覆盖编译可见性和基础 push/check/opt 转换。
  - `lua_Debug` 新增 Lua 5.4 字段和外部布局：`srclen`、`nparams`、`isvararg`、`istailcall`、`ftransfer`、`ntransfer`；C API `lua_getinfo(..., "Sut")` 已能读取 source length 和参数字段，普通 Lua tailcall 和 vararg tailcall 会报告 `istailcall=true`。
  - `debug.getinfo(f, "t")` 不再报 invalid option；普通 Lua tailcall frame 会返回 `istailcall=true`，非 tailcall 场景返回 `false`，更深的错误展开和 tail-position C return 边界仍在 `TODO.md` 保留。
  - 全局 `_ENV` 在 Lua 5.4 兼容模式下指向 `_G`。
  - 局部 `_ENV` 在 Lua 5.4 兼容模式下会接管未解析名字的读写，例如 `local _ENV = {x=1}; return x`。
  - `utf8.char()` 在 Lua 5.4 兼容模式下接受 `0..0x7fffffff`，可生成 5/6 字节扩展形式；`utf8.charpattern` 首字节范围同步扩展到 `\xfd`。
  - `string.pack()` / `string.unpack()` / `string.packsize()` 新增 `j` 和 `T` 格式，分别按 `lua_Integer` 和 `size_t` 的本机宽度处理。
  - `string.pack()` / `string.unpack()` / `string.packsize()` 支持 `!n` 对齐控制、`X` 对齐填充，以及 `l` / `L` 本机 long 整数格式；三条路径共享当前位置对齐规则。
  - `string.dump(f, strip)` 在 Lua 5.4 兼容模式下支持 boolean strip 参数；当前仍写出 LuaJIT bytecode，不是官方 Lua 5.4 binary chunk 格式。
  - 官方 Lua 5.4.8 binary chunk 已进入 smoke，当前兼容构建会明确拒绝该非 LuaJIT bytecode 格式。
  - LuaJIT bytecode 回读时，带真实 upvalue 的顶层 closure 会把第一个 upvalue 初始化为 `load` 选择的 env，避免回读后该 upvalue 仍为 nil。
  - LuaJIT stripped bytecode 回读后的 debug upvalue 枚举会区分“真实全局访问需要伪 `_ENV`”和“stripped plain/upvalue dump 不应额外伪造 `_ENV`”。
  - Lua 5.4 兼容构建的 GC 初始 `stepmul` 公开值为 `100`，使 `collectgarbage("setstepmul", n)` 的首次返回值和 Lua 5.4 对齐；默认构建不变。
  - standalone 初始化在 Lua 5.4 兼容构建中会优先执行 `LUA_INIT_5_4`，未设置时再回退 `LUA_INIT`。
  - package 路径初始化在 Lua 5.4 兼容构建中会优先读取 `LUA_PATH_5_4` / `LUA_CPATH_5_4`，未设置时再回退旧 `LUA_PATH` / `LUA_CPATH`。
  - standalone `arg` 表在 Lua 5.4 兼容构建中，无脚本 `-e` 执行时会以程序名作为 `arg[0]`，选项和命令字符串进入正索引；带脚本执行时仍以脚本名作为 `arg[0]`。
- 默认构建中：
  - `_VERSION` 仍为 `Lua 5.1`。
  - `jit.lua54compat` 为 `false`。
  - 不暴露 `warn()`。
  - 不暴露 `math.type()`、`math.tointeger()`、`math.ult()`、`math.maxinteger` 或 `math.mininteger`。
  - 不暴露 `utf8` 标准库。
  - 不暴露 `debug.setcstacklimit()` 或 `coroutine.close()`。
  - 继续保留 Lua 5.1 风格的 `package.loaders`。
  - 不暴露 `string.pack()`、`string.unpack()` 或 `string.packsize()`。
  - 继续拒绝 Lua 5.4 的局部变量属性语法。
  - 继续拒绝 Lua 5.4 的整除和位运算符语法。

## 测试进展

- 新增 `test/smoke.lua`，覆盖默认模式和 Lua 5.4 兼容模式。
- 覆盖 `jit` 基本元信息。
- 覆盖 Lua 5.4 兼容模式下的 `_VERSION` 和 `jit.lua54compat`。
- 覆盖 Lua 5.4 兼容模式下的 `coroutine.running()` 主线程返回值。
- 覆盖 Lua 5.4 兼容模式下 `__len` 元方法行为。
- 覆盖 `warn()` 是否按模式暴露。
- 覆盖 `warn()` 参数类型检查。
- 覆盖 `warn()` stderr 输出。
- 覆盖 `math.type()`、`math.maxinteger` 和 `math.mininteger` 是否按模式暴露及基本行为。
- 覆盖 `math.tointeger()` 的缺参报错、`nil` 返回、字符串整数、浮点整数、小数和越界值。
- 覆盖 `math.ult()` 的正数、负数无符号比较和非整数参数报错。
- 覆盖 `utf8` 标准库的有效 UTF-8 长度、码点提取、字符编码、迭代、偏移定位和非法字节检测。
- 覆盖 `debug.setcstacklimit()` 的暴露状态、返回值形态和参数类型检查。
- 覆盖 `coroutine.close()` 的 suspended 协程关闭、dead 协程关闭、关闭后 resume 失败和运行中协程报错。
- 覆盖 `package.searchers` / `package.loaders` 在默认模式与 Lua 5.4 兼容模式下的差异。
- 覆盖 `require()` 对 `package.preload` 的 `:preload:` loader data，以及自定义 searcher loader data 的传参和第二返回值。
- 覆盖 `string.pack()` / `string.unpack()` 的 signed/unsigned 1、2、4 字节整数，小端/大端，`float`/`double`，`cN` 零填充，`z`，`sN`，`x` 和位置返回。
- 覆盖 `string.packsize()` 的固定长度格式统计，以及 `z` / `sN` 变长格式报错。
- 覆盖默认构建拒绝 `local x <const>` / `local x <close>`。
- 覆盖 Lua 5.4 兼容构建接受 `local x <const>` / `local x <close>`。
- 覆盖 `<const>` 局部变量和被闭包捕获后的上值重赋值都会编译失败。
- 覆盖 `debug.setlocal()`、`debug.setupvalue()`、`debug.upvaluejoin()` 可修改 `<const>` local/upvalue 的官方兼容行为。
- 覆盖未知局部变量属性会编译失败。
- 覆盖同一条 local 声明中多个 `<close>` 变量会编译失败。
- 覆盖默认构建拒绝 `//`、`&`、`|`、`~`、`<<`、`>>`。
- 覆盖 Lua 5.4 兼容构建的 `//`、`&`、`|`、二元 `~`、一元 `~`、`<<`、`>>` 基本语义、负数整除、负位移方向反转、变量参与和嵌套表达式。
- 覆盖位运算对无整数表示的数值报错。
- 覆盖 Lua 5.4 兼容构建下 `ipairs()` 通过 `__index` 取得首个值。
- 覆盖 `math.randomseed(1, 2)` 返回两个种子。
- 覆盖 `math.random(0)` 的整型范围语义、固定区间、空区间、非整数参数和超过两个参数报错。
- 覆盖 `math.randomseed()` 无参数返回两个可转整数且不同时为零。
- 覆盖 `warn(1)` 成功、`warn(true)` 报错。
- 覆盖 `utf8.len()` / `utf8.codepoint()` / `utf8.codes()` 的 lax 参数。
- 覆盖 `string.gmatch()` 的正数和负数 `init` 参数。
- 覆盖 Lua 5.4 下 `<=` 缺少 `__le` 时不会回退 `__lt`。
- 覆盖 `"5" // 2` 和 `"5.5" // 2` 的字符串数字整除；覆盖 `"3" & 1` 仍报错。
- 覆盖 `//`、`&`、`|`、二元 `~`、一元 `~`、`<<`、`>>` 的 Lua 5.4 元方法调用和反向查找。
- 覆盖 `__name` 对 `tostring()`、`math.abs` 参数类型错误、`coroutine.resume()` / `coroutine.close()` 线程类型错误的影响。
- 覆盖 `coroutine.isyieldable([co])` 的 Lua 5.4 可选 thread 参数：主线程默认 false、coroutine 内默认 true、挂起/死亡 coroutine 参数 true，以及非 thread 参数错误文本。
- 覆盖 `coroutine.create()` / `resume()` / `status()` / `wrap()` / `close()` 的基础参数错误函数名。
- 覆盖主线程直接 `coroutine.yield()` 的 Lua 5.4 错误文本。
- 覆盖 `tonumber("0x10", 16) == nil`、`tonumber("0x10", 34)` 仍按普通数字解析，并覆盖 `inf` / `infinity` / `nan` 与 `0b` / `0B` 字符串在 Lua 5.4 兼容模式下返回 `nil`。
- 覆盖 `string.format("%d", 1.2)` 报错、`%q` number 输出和 `%p` nil 输出。
- 覆盖 `string.format("%d", "1.2")` 的 Lua 5.4 整数转换错误函数名。
- 覆盖 Lua 5.4 `//` 失败路径：string 在左右两侧和带 `__name` 的 table 都报 idiv operator 错误。
- 覆盖 Lua 5.4 位运算失败路径：fraction number 报“没有整数表示”，string/boolean/带 `__name` 的 table 报 bitwise operator 错误。
- 覆盖普通算术字符串转换边界：`"1" + "2"` 可转换，不可转换字符串参与 `+` / `*` / 一元 `-` 时按 Lua 5.4 报具体操作名和左右类型。
- 覆盖字符串算术元方法优先级：字符串 metatable 的 `__add` / `__mul` / `__unm` 会拦截 `"1" + 2`、`2 + "1"`、`"1" * 2` 和 `-"1"`。
- 覆盖当前 JIT 可用平台下，字符串 metatable 的 `__add` 优先级可以在热循环中执行并产生 trace。
- 覆盖 `string.char(256)` 的 Lua 5.4 越界错误文本。
- 覆盖 `utf8.char(0x80000000)` 的 Lua 5.4 越界错误文本。
- 覆盖 `math.floor()`、`math.ceil()`、`math.modf()` 的 Lua 5.4 整数返回表面。
- 覆盖 `math.type()` / `math.tointeger()` / `math.ult()` / `math.min()` / `math.max()` 的缺参错误函数名。
- 覆盖内置 userdata 的 indexed `debug.getuservalue` / `debug.setuservalue` 返回 `nil`。
- 覆盖 `table.concat()` 默认终点尊重 `__len` 元方法、检查 nil 洞，并拒绝无整数表示的 `__len` 返回值。
- 覆盖 `table.insert()` 的无整数表示位置、越界位置、带 `__len` 默认尾插，以及 `{1,nil,3}` 默认尾插。
- 覆盖 `table.remove()` 的无整数表示位置、越界位置、`len+1` 返回 nil、空表显式 `0`，以及 `{1,nil,3}` 默认尾删。
- 覆盖 `table.move()` 的 `f/e/t` 无整数表示报错，并保留数字字符串位置参数兼容。
- 覆盖 `table.move()` 缺参时优先报第 2 个参数，以及源表为 nil 时仍报第 1 个参数。
- 覆盖 `table.unpack()` 缺参 length 错误、nil/number 的显式空范围返回空结果，以及 nil 的实际索引错误。
- 覆盖当前 JIT 可用平台下，Lua 5.4 兼容热循环中的 `//`、位运算和局部 `_ENV` 可以在开启 JIT 后执行并产生 trace。
- 覆盖 Lua 5.4 兼容模式下 `math.random` 不再被旧 LuaJIT PRNG recorder 记录；无参、`0` 和常量整数区间可进入 trace，且 deterministic seed 和分布语义继续对齐官方 xoshiro256**。
- 覆盖当前 JIT 可用平台下，Lua 5.4 兼容热循环中的 `tonumber()` 会拒绝 LuaJIT 扫描器扩展数字字符串并产生 trace。
- 覆盖 Lua 5.4 C API 形态的 `lua_resume(L, from, nargs, nresults)`：yield 两个值和 return 两个值时都会填入正确结果数量。
- 覆盖 Lua 5.4 C API `lua_closethread()` 在无 `<close>` 状态下关闭 yielded/fresh coroutine：返回 `LUA_OK`、状态恢复 OK 且栈被清空。
- 覆盖 Lua 5.4 C API `lua_toclose()` / `lua_closeslot()` 的显式关闭表面：non-closable 校验、`__close(value, nil)` 调用、关闭后槽位为 `nil`，以及 false close value 被忽略后不能再 `lua_closeslot()`。
- 覆盖 Lua 5.4 C API `lua_version` 的 value-returning 函数指针签名，确认外部兼容头不再只依赖函数式宏解引用旧指针 ABI。
- 覆盖 Lua 5.4 `<close>` 普通块自然退出：多个待关闭局部变量按 LIFO 调用 `__close(value, nil)`，false/nil close value 不触发 `__close`。
- 覆盖 Lua 5.4 `<close>` 固定返回值退出：返回表达式先求值，随后按 LIFO 关闭 close locals，再返回原结果。
- 覆盖 Lua 5.4 `<close>` `break` 退出：嵌套块和循环体中的待关闭局部变量按 LIFO 调用 `__close(value, nil)`。
- 覆盖 Lua 5.4 `<close>` 已知向后 `goto`：从内层块跳回外层已出现标签前关闭被跳出的 close local，随后正常再次进入并在块自然结束时关闭第二个 close local。
- 覆盖 Lua 5.4 `<close>` 前向 `goto`：跳出包含 close local 的块时关闭该变量；goto 后才声明的 close local 不会被错误关闭；跳到同一作用域内标签不会提前关闭。
- 覆盖 Lua 5.4 generic for 第 4 个 closing value：循环自然结束和 `break` 都会调用 `__close(value, nil)`；第 4 个值为非 closable 时会按 `(for state)` 报错。
- 覆盖 Lua 5.4 `<close>` 错误展开：`pcall` / `xpcall` 会把错误对象传给 `__close(value, err)`，`__close` 自身抛错会替换原错误，generic for 第 4 个 closing value 在 iterator 抛错时会自动关闭。
- 覆盖 Lua 5.4 coroutine close/reset 的 `<close>` 路径：`coroutine.close` 和 `lua_closethread()` 会关闭 suspended coroutine 中的 active close locals，close 错误会按 Lua 5.4 返回失败和错误对象，且重入关闭同一个 running coroutine 会报错。
- 覆盖 Lua 5.4 C API `lua_toclose` 的弹栈/错误路径：`lua_settop` / `lua_pop` 自动关闭 marked slot，C 函数错误展开时 `__close` 收到 body error。
- 覆盖 Lua 5.4 C API `lua_toclose` 的正常 C 返回路径：C 函数 `return 0` 自动关闭 marked slot，close 错误会让 `lua_pcall` 返回错误，被关闭槽位作为返回值时仍返回原 table。
- 覆盖 Lua 5.4 C API `lua_toclose` / `lua_closeslot` 的 LIFO 顺序约束：重复标记同一槽、标记低于 active slot、nil 低于 active slot 都会报 `below or equal`，`lua_closeslot` 关闭非最后 marked slot 会报 `no variable to close`，按 LIFO 显式关闭两个槽位成功。
- 覆盖 Lua 5.4.8 版本头文件常量和识别符：`LUA_VERSION_RELEASE`、`LUA_RELEASE`、`LUA_VERSION_RELEASE_NUM`、`LUA_COPYRIGHT`、`LUA_AUTHORS` 和 `lua_ident` 均按官方 5.4.8 暴露。
- 覆盖 Lua 5.4 外部兼容头中的 `lua_sethook` 官方签名：纯 `lua.h` 编译烟测会断言其函数指针类型为 `void (*)(lua_State *, lua_Hook, int, int)`。
- 覆盖 Lua 5.4 外部兼容头中的 `lua_pushglobaltable` 官方宏表面：纯 `lua.h` 编译烟测会断言该宏表达式类型为 `void`。
- 覆盖 Lua 5.4 外部兼容头不会暴露旧 `LUA_HOOKTAILRET`，只保留官方 `LUA_HOOKTAILCALL` 表面。
- 覆盖 Lua 5.4 外部兼容头不会暴露 LuaJIT 旧兼容宏/声明：`lua_open`、`lua_getregistry`、`lua_getgccount`、`lua_Chunkreader`、`lua_Chunkwriter` 和 `lua_setlevel`；默认构建 C smoke 覆盖旧兼容宏仍可用。
- 覆盖 Lua 5.4 lowered bitwise/idiv helper 的 string metatable metamethod 返回值：`&` / `|` / `~` / unary `~` / `<<` / `>>` / `//` 在对应 metamethod 返回多值时只保留第一个结果。
- 覆盖 Lua 5.4 外部兼容头不会暴露 `LUA_GLOBALSINDEX` / `LUA_ENVIRONINDEX` / `lua_strlen`，并覆盖 `lua_pushglobaltable()` / `lua_getglobal()` / `lua_setglobal()` 的 registry globals 路径。
- 覆盖 Lua 5.4 外部兼容头暴露 `LUA_RIDX_LAST`，并确认其值等于 `LUA_RIDX_GLOBALS`。
- 覆盖 Lua 5.4 外部兼容头暴露 `LUA_EXTRASPACE`，并确认其大小与当前 pointer-sized extraspace 实现一致。
- 覆盖 Lua 5.4 外部兼容头通过 `lauxlib.h` 暴露 `LUA_GNAME` / `LUA_FILEHANDLE` / `LUA_LOADED_TABLE` / `LUA_PRELOAD_TABLE`，并确认其值分别为 `"_G"` / `"FILE*"` / `"_LOADED"` / `"_PRELOAD"`。
- 覆盖 Lua 5.4 外部兼容头单独包含 `lua.h` 时不暴露 lauxlib-only 的 `LUA_LOADED_TABLE` / `LUA_PRELOAD_TABLE`。
- 覆盖 Lua 5.4 `lualib.h` 暴露 `LUA_VERSUFFIX`，并确认其值为 `"_5_4"`。
- 覆盖 Lua 5.4 `lauxlib.h` 输出宏 `lua_writestring` / `lua_writeline` / `lua_writestringerror` 的可见性和可编译展开。
- 覆盖 Lua 5.4 `lauxlib.h` 中 `luaL_prepbuffer` 的宏可见性，以及该宏返回 buffer 后配合 `luaL_addsize()` / `luaL_pushresult()` 的基础写入结果。
- 覆盖 Lua 5.4 `lauxlib.h` 中 `luaL_argexpected` / `luaL_pushfail` 的宏可见性，并继续覆盖 `luaL_pushfail()` 的 nil 返回表面。
- 覆盖 Lua 5.4 `lauxlib.h` 中 `luaL_loadfile` / `luaL_loadbuffer` 的宏可见性和基础加载执行结果。
- 覆盖 Lua 5.4 `lauxlib.h` 中 `luaL_newlib` 会先调用 `luaL_checkversion()` 的宏形态。
- 覆盖 Lua 5.4 外部兼容头中 `lua_newuserdata()` / `lua_getuservalue()` / `lua_setuservalue()` 作为 slot 1 alias 宏的可见性和基础运行行为。
- 覆盖 `LUA_COMPAT_APIINTCASTS` 下 `lua_pushunsigned` / `lua_tounsignedx` / `lua_tounsigned` / `luaL_checkunsigned` / `luaL_optunsigned` 以及既有 int/long cast 宏。
- 覆盖 Lua 5.4 外部兼容头声明 `luaopen_coroutine()`，并确认独立打开 coroutine 库会返回包含 `create` 的库表。
- 覆盖 Lua 5.4 外部兼容头中的 `luaopen_base()` 返回 1 个 base 库表，并确认返回表包含 `assert`。
- 覆盖 `lua_stringtonumber()`、`lua_isnumber()`、`lua_tonumberx()`、`lua_tointegerx()` 和 `luaL_checknumber()` 拒绝 `inf` / `nan` / `0b` 扫描器扩展字符串。
- 覆盖默认构建 C API smoke：`LUA_GLOBALSINDEX` / `LUA_ENVIRONINDEX` / `lua_strlen` 仍可见，`lua_objlen()` / `lua_getfenv()` 仍可编译、链接、运行。
- 覆盖 Lua 5.4 外部兼容头的负向编译 gate：`luaL_openlib` / `luaL_register` / `luaL_pushmodule` 不应可编译；默认构建 smoke 覆盖这三个旧 lauxlib API 仍可用。
- 覆盖 Lua 5.4 外部兼容头的负向编译 gate：旧 `luaL_typerror` 不应可编译；默认构建 smoke 覆盖该旧 API 仍可用并保持旧错误文本。
- 覆盖 Lua 5.4 外部兼容头的负向编译 gate：LuaJIT helper `luaL_findtable` 不应可编译；默认构建 smoke 覆盖该 helper 仍可创建 registry 子表。
- 覆盖 Lua 5.4 外部兼容头中 `lua_gettable()`、`lua_getfield()`、`lua_geti()`、`lua_rawget()`、`lua_rawgeti()`、`lua_rawgetp()` 的返回类型签名和运行时返回值。
- 覆盖 Lua 5.4 外部兼容头中 `lua_rawgeti54()` / `lua_rawseti54()` 的 `lua_Integer` 索引签名。
- 覆盖 Lua 5.4 外部兼容头中 `lua_insert()` / `lua_remove()` / `lua_replace()` 的宏可见性和基础栈行为。
- 覆盖 Lua 5.4 外部兼容头中 `lua_pushlstring()` / `lua_pushstring()` / `lua_pushliteral()` 返回入栈字符串指针，以及 `lua_pushstring(NULL)` 返回 `NULL` 并压入 nil。
- 覆盖 Lua 5.4 外部兼容头中 `lua_tonumber()` / `lua_tointeger()` 的宏可见性和基础转换结果。
- 覆盖 Lua 5.4 外部兼容头中 `lua_getextraspace()` 的宏可见性，并继续覆盖主线程 extraspace 写入会复制到新线程。
- 覆盖 Lua 5.4 C API 状态 allocator 表面：`lua_newstate()` 自定义 allocator、`lua_getallocf()` 取回 allocator/userdata、`lua_setallocf()` 重新设置 allocator。
- 覆盖 Lua 5.4 C API 状态/错误表面：`lua_atpanic()` old handler、`lua_pushthread()` 主线程/协程返回值、`lua_error()` 错误传播。
- 覆盖 Lua 5.4 C API 栈/表表面：`lua_checkstack()`、`lua_settop()`、`lua_pushvalue()`、`lua_concat()` 和 `lua_next()`。
- 覆盖 Lua 5.4 C API `lua_gc()` 的变参调用形态：无额外参数的 collect/count/countb，以及带官方参数列表的 `LUA_GCGEN` / `LUA_GCINC`。
- 覆盖 Lua 5.4 外部兼容头中 `lua_call()` / `lua_pcall()` / `lua_yield()` 的宏可见性，以及 `lua_call()` / `lua_pcall()` 基础运行路径。
- 覆盖 Lua 5.4 C API `lua_isyieldable()`：主 C frame 不可 yield，`lua_resume()` 进入的 coroutine C frame 可 yield。
- 覆盖 Lua 5.4 兼容模式拒绝 `0b...` 二进制数字字面量、`L` / `LL` / `UL` / `ULL` / `uLL` 整数后缀和 imaginary `i` 数字字面量。
- 用临时 C 程序编译/链接验证 `lua_absindex`、`lua_isinteger`、`lua_rawlen`、`lua_geti` / `lua_seti`、`lua_rawgetp` / `lua_rawsetp`、`lua_pushglobaltable` 和 `luaL_newmetatable` 写入 `__name`。
- 覆盖 `luaL_loadbufferx()` / `luaL_loadfilex()` 的 `mode="t"` 和 `mode="b"` 路径。
- 覆盖 Lua 5.4 外部兼容头中 `lua_load(..., mode)` 的 `mode="t"` 和 `mode="b"` 路径。
- 覆盖 Lua 5.4 外部兼容头中 `lua_dump(..., strip)` 的 full/stripped 写出，以及 stripped LuaJIT bytecode 用 `mode="b"` 回读执行。
- 覆盖 Lua 5.4 头文件中 `LUA_VERSION_MAJOR` / `LUA_VERSION_MINOR` / `LUA_VERSION_RELEASE` / `LUA_VERSION_RELEASE_NUM` / `LUA_NUMTYPES` / `LUA_NUMTAGS` 的可见性，以及 `luaL_addgsub()` 的 buffer 替换结果。
- 覆盖 Lua 5.4 兼容模式下 `luaL_prepbuffsize()` / `luaL_buffinitsize()` 请求大于 `LUAL_BUFFERSIZE` 时的 buffer 写入和最终字符串长度。
- 覆盖 `luaL_fileresult()` 成功/失败返回形态、`luaL_execresult()` exit 返回形态、`luaL_newlib()` / `luaL_setfuncs()` 注册函数、registered metatable/userdata helper、`luaL_traceback()` 文本和 `luaL_dostring()` 结果。
- 覆盖 `luaL_intop()` 的加法、减法和 bit-and 结果，其中加减法验证当前 32 位兼容整数范围的 wraparound。
- 覆盖 `LUA_GCCOUNTB` 返回 `0..1023` 范围内的 byte remainder。
- 覆盖 `debug.getinfo(function() end, "t").istailcall == false`。
- 覆盖 `warn()` 无参数调用报错。
- 覆盖默认构建中 `_ENV` 保持 `nil`。
- 覆盖 Lua 5.4 兼容构建中 `_ENV == _G`。
- 覆盖局部 `_ENV` 接管未解析名字读取、写入和函数调用。
- 覆盖 `utf8.char(0x110000)`、`utf8.char(0x200000)`、`utf8.char(0x7fffffff)` 的扩展编码长度，以及 `0x80000000` 越界报错。
- 覆盖 `utf8.charpattern` 包含 `\xfd`。
- 覆盖 `string.packsize("jT")` 和 `<jT` 打包/解包。
- 覆盖 `string.packsize("!8bi8")`、`string.pack("!8bi8", ...)`、`string.unpack("!8bi8", ...)` 的自动对齐。
- 覆盖 `X` 格式的 padding-only 行为，以及 `l` / `L` 打包解包。
- 覆盖 Lua 层 `string.dump(f, strip)` 的 full/stripped 写出、`mode="b"` 回读执行，以及 binary chunk 被 `mode="t"` 拒绝。
- 覆盖官方 Lua 5.4.8 binary chunk 在兼容构建中被明确拒绝，记录当前只支持 LuaJIT bytecode 的边界。
- 覆盖 LuaJIT bytecode 回读带 upvalue 函数时，第一个真实 upvalue 默认初始化为 `_G`，并且会跟随 `load(..., env)` 的第 4 个 env 参数。
- 覆盖 LuaJIT stripped bytecode 回读后的 debug upvalue 枚举：plain dump 没有 upvalue，带真实 upvalue 的 dump 只暴露真实 upvalue，带全局访问的 dump 保留伪 `_ENV`。
- 覆盖 Lua 5.4 兼容构建中 `LUA_INIT_5_4` 优先于 `LUA_INIT`，`LUA_PATH_5_4` / `LUA_CPATH_5_4` 优先于旧环境变量。
- 覆盖 Lua 5.4 兼容构建中无脚本 `-e` 的 `arg[0]` / `arg[1]` / `arg[2]` 表形态。
- 覆盖 Lua 5.4 兼容构建中 `collectgarbage("setpause", n)` 初始返回 `200`、`collectgarbage("setstepmul", n)` 初始返回 `100`，并验证旧值恢复。
- 覆盖 Lua 5.4 兼容构建中 `collectgarbage("minor")` / `"major"` 仍按官方 Lua 5.4.8 报 invalid option。
- 覆盖 `pairs(1)` 返回 `next, 1, nil`，并由后续 `next()` 调用报 table 类型错误。
- 覆盖 `assert()`、`type()`、`tostring()`、`pcall()`、`xpcall()`、`select()`、`error()`、`tonumber()`、`load()`、`loadfile()`、`next()`、`pairs()`、`ipairs()`、`getmetatable()`、`setmetatable()`、`rawget()` / `rawset()` / `rawequal()` / `rawlen()` 的基础参数错误函数名，以及 `rawlen()` 的 `table or string` 期望类型文本。
- 覆盖 `math.deg()` / `math.rad()` 的缺参/错误类型参数错误，以及数值字符串转换。
- 覆盖 `luaL_checkversion_()` 对错误版本号和错误 numeric ABI 尺寸的报错路径。
- 覆盖 Lua 5.4 外部兼容头中 `lua_version(L)` 的数值返回表面。
- 覆盖 `lua_setcstacklimit()` C API shim 的查询和设置返回值。
- 覆盖 Lua 5.4 standalone `-l g=mod` 会把 `require(mod)` 的返回值写入指定全局名。
- 覆盖 Lua 5.4 standalone `-E` 会忽略 `LUA_INIT_5_4`、`LUA_PATH_5_4` 和 `LUA_CPATH_5_4`。
- 覆盖 Lua 5.4 standalone `-i` 交互启动不会输出额外 `JIT:` 状态行。
- 覆盖 Lua 5.4 兼容模式中 main chunk 的伪 `_ENV` debug upvalue、`debug.setupvalue` 替换环境，以及含真实上值闭包的 `_ENV` upvalue 排序。
- 覆盖 Lua 5.4 兼容模式中 `rawget(_G, "_ENV") == nil`，并覆盖 `next(_G)` / `pairs(_G)` 不枚举 `_ENV`，对齐官方全局表不暴露 `_ENV` 键的表面。
- 覆盖 Lua 5.4 兼容模式中真实 lexical `_ENV` 的闭包继承、`debug.upvalueid()` / `debug.upvaluejoin()` identity 合并，以及非 table lexical `_ENV` 被闭包访问时的错误表面。
- 覆盖弱键表 ephemeron 自环 key 清理，以及外部强引用 key 时 value 保留。
- 覆盖大 smoke chunk 中 Lua 5.4 数值 `for` / 运算符 helper 字段名不会因常量表超过 255 被截断。
- 覆盖 Android ARM64 Lua 5.4 error unwind close continuation：`pcall`/`xpcall` 错误展开中的 `__close` 可通过 VM continuation 继续执行，`coroutine.create(pcall)` 这类非 Lua 调用者会走 `vm_return` 分流，不再把 frame delta 当作字节码 PC。
- 覆盖 Android ARM64 fast function / ordinary C function return hook transfer：`math.sin`、`select` 等 C return hook 能暴露正确的 `ftransfer` / `ntransfer` 和返回值窗口。
- 覆盖 Android ARM64 nested tailcall marker：第二层 tailcall marker 会被保留和清理，`debug.getinfo(..., "t").istailcall` 在嵌套 tailcall 中不丢失。
- 覆盖当前 32 位 integer 表面下的跨后端 Lua 5.4 一元负号边界：ARM64/ARM/MIPS/MIPS64/PPC 的 `-(1 << (intbits - 1))` 保持整数回绕为 `math.mininteger`。
- 覆盖 Lua 5.4 perf/memory smoke 的多 `jit.opt` profile：默认跑 `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10` 和 `0,hotloop=3,hotexit=2` 三档 JIT on，再跑 JIT off；可用 `LUA54_PERF_JIT_OPTS` 扩展或覆盖 profile 矩阵，且每个自定义 profile 必须显式写明 opt level、`hotloop` 和 `hotexit`。
- 覆盖 full userdata 的 Lua 5.4 to-be-closed 语义：C 创建的 userdata 带 `__close` metatable 时，Lua `<close>` local 作用域退出会自动调用 `__close(value, nil)`，C API `lua_toclose()` 标记 userdata 后正常 C 返回也会关闭。
- 文档化 Lua 5.4 compat binary chunk 边界：`string.dump()` / `lua_dump()` 仍写 LuaJIT bytecode，`load(..., "b")` 接受 LuaJIT bytecode，官方 PUC-Rio Lua 5.4 binary chunk 会被明确拒绝为 incompatible bytecode。
- 覆盖 Lua 5.4 lauxlib 冷门宏组合：新增 C API smoke 覆盖 `luaL_checkstring()` / `luaL_optstring()`、`luaL_argcheck()`、`luaL_argexpected()`、`luaL_checkany()` / `luaL_checktype()`、`luaL_opt()`、`luaL_newlibtable()` 和 `luaL_dofile()` 的基础运行与错误路径，并补充这些官方宏的头文件可见性断言。
- 继续扩展 Lua 5.4 lauxlib C API smoke：覆盖 `luaL_optnumber()` 的 default/string/error 路径、`luaL_checkstack()`、`luaL_len()` 的 string/metatable/error 路径，以及 `luaL_addchar()` / `luaL_addlstring()` / `luaL_addstring()` / `luaL_addvalue()` buffer 组合。
- 扩展 Lua 5.4 lauxlib 宏-only 负向编译 gate：`luaL_addchar()`、`luaL_addsize()`、`luaL_buffaddr()`、`luaL_bufflen()`、`luaL_buffsub()` 不能被外部模块当作旧函数地址获取。

## 验证结果

- `make -j16 test` 已通过，覆盖本轮 `lj_tab_keyindex()` dead long-string key 修复后的默认构建、Lua 5.4 compat、官方 Lua 5.4.8 子集、C API smoke，以及 Lua 5.4 perf/memory smoke 的 JIT on/off profiles。
- `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API、Lua 5.4 compat smoke/C API，以及 Lua 5.4 perf/memory smoke。
- `cmd /c build.bat lua54` 已通过，覆盖本轮新增 lauxlib 宏组合与 buffer C API smoke。
- `powershell -ExecutionPolicy Bypass -File tools\lua54_platform_matrix.ps1 -Target android` 已通过，覆盖 Android ARM64 Lua 5.4 compat 静态构建和设备端 `test/smoke.lua lua54compat`。
- 已用本机 `D:\p4_gl2\pristine\tools\lua\lua5.4.8\lua54.exe` 对比标准库可见 API；当前 Lua 5.4 兼容构建的 `_G` 主要标准库表项与 Lua 5.4.8 一致，额外保留 LuaJIT 自身 `jit` 扩展。
- 已确认下一批主要未支持语言功能转为 `<close>` 的 C API 自动关闭/yield 边界，以及 Lua 5.4 64 位整数完整语义；这些需要 VM/字节码层继续实现。
- 本机验证时需要先把 `D:\p4_gl2\pristine\ruby\Ruby33-x64\msys64\ucrt64\bin` 前置到 `PATH`，否则顶层 `make` 能启动但 `src/Makefile` 找不到 `gcc`。
- CodeQL 检查未发现安全告警。
- 自动代码审查提出的问题已处理：
  - 避免 `warn()` 使用进程级静态开关状态。
  - 调整 C89 风格变量声明。
  - 增加 stderr 输出测试。
  - 确保 warning 输出测试会检查 `luajit` 命令退出状态。
