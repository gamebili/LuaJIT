# Lua 5.4 Compatibility TODO

本清单基于当前 `LUAJIT_ENABLE_LUA54COMPAT` 实现、`test/smoke.lua`
覆盖范围，以及和本机 `lua5.4.8` 的行为探针对比。后续继续实现时仍按
“先写用例，再补实现，再跑完整测试”的顺序推进。

## 实施规划：按底层依赖成批推进

后续不再按 TODO 条目逐个零散修补，而按下面的底层依赖分批推进。每一批
先补稳定接口和回归用例，再把依赖该接口的多个 TODO 一起收口。

1. **VM unwind / to-be-closed 批次**
   - 目标：为 `<close>` 建立 VM 级关闭接口，而不是继续堆 parser helper。
   - 覆盖：`return f()` 动态多返回、error unwind、`pcall`/`xpcall`、coroutine close、generic for closing value 错误展开、`lua_toclose` 自动随 C frame 退出关闭。
   - 接口要求：需要能保存动态返回值数量、传递错误对象给 `__close(value, err)`，并在 `__close` 自身抛错时按 Lua 5.4 规则替换/传播错误。
   - 验证面：Lua smoke + C API smoke + PC/Android ARM64；iOS/Emscripten 在平台批次补工具链后纳入。
   - 接口草案：新增统一 runtime close 层，至少提供“声明时校验 closable”“按栈层级 LIFO close”“带错误对象 close”“return 动态结果保护后 close”四类入口；当前 `_lua54_checkclose` / `_lua54_closevalue` parser helper 后续只能作为临时桥接，不能继续扩成最终方案。
   - 当前进展：已新增 `lj_close` runtime helper，先统一声明校验、`__close` 查找和 `__close(value, err)` 调用入口；PC x64 的 close-active `return f()` / `return fixed, ...` 已收敛到 VM close continuation，直接保存动态返回窗口、`MULTRES` 和 nil 洞，不再走 parser pack/close/unpack 桥；return hook 可在 `__close` 中动态启用并按 `return sethook`、`return close`、`return foo` 顺序继续收口，close 错误对象会按 Lua 5.4 替换/加位置信息；PC x64、ARM/x86、MIPS/MIPS64/PPC 静态后端和 Android ARM64 的 error unwind close-yield 已接入 VM continuation，覆盖 `pcall`/`xpcall`、`__close` 错误替换、yield 后继续关闭剩余 close locals、generic for 错误展开以及 `coroutine.create(pcall)` 这类非 Lua 调用者错误返回收尾；`coroutine.close` / `lua_closethread` / `lua_resetthread` 已能关闭 suspended coroutine 的 active close locals；`coroutine.wrap` 错误路径也会先关闭协程再抛出最终错误；C API `lua_toclose` marked slot 已接入 ARM/x86/x64/ARM64/MIPS/MIPS64/PPC VM C return continuation，其中 ARM/x86/MIPS/MIPS64/PPC 已由 `test/lua54_vm_backend_dynasm.lua` 纳入 `smoketest-lua54compat` 的 DynASM 生成门禁，正常 C 返回会在 return hook 前关闭并保留返回值槽位，`__close` 内 yield 后可恢复继续返回或传播后续 close 错误，多个 marked slot 连续 yield 和内层 close 错误继续关闭外层 slot 已进入 C API smoke；后续仍需在可用工具链上补各非本机后端 artifact/runtime smoke。
   - 批量完成标准：同一批提交应同时覆盖普通 Lua return/error、generic-for closing value、coroutine reset/close 和 C API `lua_toclose` 自动关闭路径，避免每条控制流各写一套 close 调度。

2. **真实 `_ENV` upvalue 批次**
   - 目标：减少伪 `_ENV` 对 LuaJIT 函数环境的依赖，为隐式全局访问提供可被 debug API 操作的真实 upvalue 语义。
   - 覆盖：`debug.setupvalue(load("return x"), 1, non_table)`、`debug.upvaluejoin`、dump/load 后 `_ENV` identity、stripped dump upvalue 名称差异。
   - 接口要求：明确真实 `_ENV` upvalue 与旧 `fn->c.env` 的同步边界，保留默认构建 LuaJIT 5.1 ABI 不受影响。

3. **64 位整数 / 数值表示批次**
   - 目标：统一 `lua_Integer`、TValue 数字子类型、字符串扫描、格式化、算术、bitwise、numeric for 和 JIT recorder 的整数路径。
   - 覆盖：`1 << 40`、`math.mininteger/maxinteger`、`math.type(1.0)`、`"1.0"+2`、`math.tointeger`、`math.ult`、`string.pack("j/i8/I8")`、C API integer 边界。
   - 接口要求：先决定 dual-number / 64-bit integer 表示与 JIT IR 扩展策略，再批量改库函数；不要在单个库函数里继续做 32 位补丁。
  - 当前进展：PC x64 Lua 5.4 compat 构建已切到 `LUAJIT_NUMMODE=2` dual-number；TValue 可承载 64-bit boxed integer，float 字面量、常量折叠、字符串数字转换、字符串数字算术、`%` helper、`lua_arith()`、numeric for、bitwise、`string.pack("j/J")`、`math.random(0)` 和 JIT recorder 的主要 integer 子集已按 TValue 子类型保留 integer/float 区分；C API smoke 已固定 `lua_stringtonumber()` / `lua_tointegerx()` / `luaL_checkinteger()` / `luaL_optinteger()` 对十六进制 64-bit unsigned wrap 字符串和十进制下界舍入字符串的 Lua 5.4 边界；剩余工作集中在跨平台 artifact/runtime、C API 深边界和 ABI 验证。

4. **debug frame metadata 批次**
   - 目标：在 VM frame 层保留 Lua 5.4 hook/tailcall 所需元信息。
   - 覆盖：真实 `istailcall`、C function call/return hook transfer、tail call transfer、Lua/C hook 的边界一致性。
   - 接口要求：call/return hook 的 transfer 信息应来自统一 frame/dispatch 元数据，而不是每个 hook 特判。
   - 当前进展：line hook 已按启用 hook 的具体 frame/line 做同一行跳过，call hook 内启用 line hook 不再误跳过被调函数首行；一行函数入口会按 Lua 5.4 报告定义行；hook 回调帧的 `debug.getinfo(..., "n")` / traceback 已能显示 `hook '?'`。
   - 当前进展：普通 Lua call/return、vararg return、tailcall transfer、普通 C 函数 call/return、fast C 函数 return、`pcall` / `xpcall` 合成 C return 已进入 smoke；字符串算术错误在 hook 内 protected call 下可被捕获。
  - 当前进展：metamethod/finalizer 的 debug 名字、`for iterator` 名字反推、traceback 大栈裁剪格式、stripped chunk 的 `(no name)` upvalue 和无行号 line hook `nil` 参数已按官方 Lua 5.4.8 `testes/db.lua` 收口。
   - 当前进展：stripped chunk 中 Lua 层 `error()` / C 层 `luaL_error()` 通过 `luaL_where()` 时会在 `currentline == -1` 下省略位置前缀，不再误拼 `?:-1:`；VM 运算/类型等 runtime 错误仍保留官方可见的 stripped `?:-1:` 诊断。
   - 当前进展：`luaL_traceback()` 会对同线程和目标 coroutine 栈上的 C 函数执行 `_LOADED` 反查，按 Lua 5.4 输出 `math.abs` / `table.concat` 这类限定名，不再在 `debug.traceback(co)` 中退回短名。
   - 当前进展：Android ARM64 VM 已补 fast function / ordinary C function return hook transfer，并保留第二层 tailcall marker；`math.sin` / `select` / nested tailcall 的 debug transfer 和 `istailcall` smoke 已在设备侧通过。
   - 验证状态：官方 Lua 5.4.8 `testes/db.lua` 当前已通过；该批次没有已知剩余阻塞。

5. **平台和 JIT 批次**
   - 目标：把 PC/Android/iOS/Emscripten 64 位构建矩阵变成常规验证门，并明确各平台 JIT/解释器策略。
   - 覆盖：Windows PC x64、Android ARM64、iOS ARM64、Emscripten wasm/wasm64；JIT on/off 和 Lua 5.4 compat smoke。
   - 接口要求：Emscripten 不能假设传统本机 JIT；需要单独 wasm/interpreter 后端或明确禁用 JIT 的构建路径。
   - 当前进展：新增 Lua 5.4 perf/memory smoke，默认固定三档显式 `jit.opt` profile：`3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10` 和 `0,hotloop=3,hotexit=2`，并分别覆盖 JIT on/off 下的 `pairs` / `__pairs` / `//` / `%` / string helper / hook churn；可用 `LUA54_PERF_JIT_OPTS` 覆盖本地 profiling 矩阵，且自定义 profile 必须显式写明 opt level、`hotloop` 和 `hotexit`，避免 opt 默认值变化掩盖性能回退。
   - 当前进展：PC x64、x86、ARM64、ARM、MIPS、MIPS64、PPC 的 `__call` callable-chain 已统一改为由 `lj_meta_call` 返回新增隐式参数数量，并由各 VM 后端更新 `NARGS`；PC x64 / Android ARM64 已额外覆盖 100 层 callable-chain tailcall 扩栈和 `CALLT` 保持，Android ARM64 设备 smoke 已覆盖该路径。

6. **C API / lauxlib / 标准库收尾批次**
   - 目标：在上述底层接口稳定后，统一核对 ABI、头文件宏、错误文本和冷门边界。
   - 覆盖：continuation API、allocator 缩小失败语义、旧 LuaJIT API 默认构建兼容、lauxlib 冷门宏组合、标准库逐字错误文本。
   - 接口要求：新增外部 Lua 5.4 ABI wrapper 时必须保留内部旧 ABI，避免破坏 LuaJIT 自身和默认构建。
   - 当前进展：C API smoke 已固定 `lua_gc(L, LUA_GCGEN/LUA_GCINC, ...)` 在 collector stopped 状态下只切换模式/参数，不会隐式重启自动 GC。
   - 当前进展：C API smoke 已固定 `lua_compare()` 非法 op，以及 `lua_next()` / `lua_rawget*()` / `lua_rawset*()` 的缺 key、非 table 和宽整数 key 路径在 release 构建下统一报 `invalid value`，不再只依赖 debug-only `api_check`。
   - 当前进展：C API smoke 已固定 `lua_pushvalue()` / `lua_copy()` / `lua_remove()` / `lua_insert()` / `lua_rotate()` 和 `lua_pcall()` message handler 对无效栈索引的 release 错误边界，统一报 `invalid value`。
   - 当前进展：Lua 5.4 C continuation smoke 已固定 `lua_yieldk()` continuation 返回负数或超过当前栈结果数量时，在 release 构建下统一报 `invalid value`，不再依赖 debug-only `api_check`。
   - 当前进展：C API smoke 已固定 `lua_setmetatable()` 非 table metatable、默认 ABI `lua_setfenv()` 非 table env，以及 `lua_replace(LUA_GLOBALSINDEX/LUA_ENVIRONINDEX)` 非 table value 在 release 构建下统一报 `invalid value`。

## P0：核心语义缺口

- [x] Lua 5.4 的 200 upvalue 函数上限。
  - 当前状态：`LJ_MAX_UPVAL` 已提升到 200，parser upvalue map、LuaJIT bytecode dump/load、`debug.upvaluejoin` 遍历 200 个 upvalue 的官方 `calls.lua` 段已用本地 probe 通过；超过 200 个 upvalue 仍会报 `too many upvalues`。
  - 当前进展：JIT recorder 的 `IR_UREFO` / `IR_UREFC` 编码已从“7 bit index + 8 bit hash”调整为“Lua 5.4 0..199 upvalue index + 7 bit alias hash”，所有 encoded literal 继续保持 `< REF_BIAS`。`test/lua54_jit_regress.lua` 已覆盖 150 upvalue 闭包中读写第 150 个 mutable upvalue 仍会记录 trace；`test/lua54_perf.lua` 已把同一高索引 upvalue 读写纳入 JIT on/off 性能/内存 smoke。

- [ ] `<close>` 的运行期 `__close` 调度。
  - 当前状态：已接受 `local x <close>` 语法并记录属性，声明点会按 Lua 5.4 校验非 `nil`/`false` 值必须带 `__close`；普通块自然执行到 `end`、固定返回值 `return`、`break` 退出循环、已知向后 `goto`、前向 `goto` 跳出 close local 作用域以及 generic for 第 4 个 closing value 的普通退出路径时会按 LIFO 调用 `__close(value, nil)`。
  - 当前进展：`io.lines(filename)` 已按 Lua 5.4 返回第 4 个 closing value，迭代器仍会在 EOF 时主动关闭文件；`local x <close> = 1` 已按 Lua 5.4 在运行期报 `variable 'x' got a non-closable value`，`nil` / `false` 声明会跳过校验，带 `__close` 的值可声明；C API 已补 `lua_toclose()` 的 closable 校验、只能高于当前 active close slot 的标记顺序检查，以及 `lua_closeslot()` 只能关闭最后一个 active marked slot 的检查；普通 fall-through block exit、固定返回值 `return`、`break`、已知向后 `goto`、前向 `goto` 离开作用域和 generic for 自然结束/`break` 退出仍通过 parser helper 调度 close locals；PC x64 close-active 动态多返回 `return f()` / `return fixed, ...` 已收敛到 VM close continuation，直接保存返回值数量、返回窗口、`MULTRES` 和 nil 洞；error 展开已通过 close-list/VM continuation 桥接关闭 active close locals，并覆盖 `pcall`/`xpcall`、`__close(value, err)`、`__close` 自身抛错替换错误对象和 generic for 错误展开；`coroutine.close` / `lua_closethread` / `lua_resetthread` 已能关闭 suspended coroutine 中的 active close locals，yield 状态传 `nil` 错误对象，成功路径返回单个 `true`，close 抛错时返回失败和替换后的错误；C API `lua_settop` / `lua_pop` 弹出 `lua_toclose` 标记槽位时会自动关闭，C 函数抛错展开时会把 body error 传给 `__close`；C 函数正常返回时会在 return hook 和返回值搬移前关闭 `lua_toclose` 标记槽位，`__close` 抛错会替换为返回错误，且被关闭槽位作为返回值时不会被清成 `nil`。
  - 当前进展：普通块退出和非动态 close-active `return` 路径中的 `__close` 仍可由 parser 生成普通 Lua 调用，已支持 `__close` 内 `coroutine.yield()` 后恢复继续执行，并保留 `return` 的多返回值和 `nil` 洞；PC x64 动态 `return f()` / `return fixed, values()` 在 `__close` yield 后抛错时，会丢弃已保存的动态返回值并传播 close 替换错误，`__close` yield 后动态启用 return hook 时也会在恢复最终返回前保留 nil 洞并触发目标函数 return hook。
  - 当前进展：parser 生成的普通 `__close` 调用已能在 debug/traceback 中显示 `metamethod 'close'`，并隐藏 `_lua54_closevalue` / `_lua54_unpackreturn` 等内部 helper 的 return hook 噪声；官方 `testes/locals.lua` 已越过 `__close` 错误 traceback、return hook 顺序、基础 coroutine close-yield 和错误展开期间的 close-yield continuation。
  - 当前进展：ARM/x86/x64/ARM64/MIPS/MIPS64/PPC error unwind 路径已在 `FRAME_CONT` / `cont_dispatch` 中加入 close continuation，C helper 只负责准备 `__close(value, err)` protected call 和消费 pcall 结果；`__close` 内 yield 后会恢复 continuation，继续关闭剩余 active close locals，并在后续 `__close` 抛错时替换原错误对象；ARM64 还补齐了非 Lua 调用者的 pcall 错误返回分流，避免把 frame delta 当成字节码 PC；ARM/x86/MIPS/MIPS64/PPC 已补同类 DynASM 接线并通过 `test/lua54_vm_backend_dynasm.lua` 自动模板生成验证。
  - 当前进展：`test/lua54_vm_backend_static.lua` 已补静态门禁，固定 `lj_frame.h` 中存在 `LJ_CONT_CLOSE` / `LJ_CONT_CLOSE_CFRAME`、`lj_close.c` 存在 error-unwind 和 C-return close pcall prepare/continue helper，并要求 ARM/x86/x64/ARM64/MIPS/MIPS64/PPC VM 后端都包含 `lj_close_prepare_pcall` / `lj_close_prepare_cframe_pcall`、对应 continuation dispatch id、`cont_close` / `cont_close_cframe`、`vm_call_close` 和 continue helper 接线；`test/lua54_vm_backend_dynasm.lua` 已纳入 `smoketest-lua54compat`，自动生成 x86、ARM hard/soft、MIPS32/64 LE/BE、PPC hard/soft/GPR64 TOC 的临时 `buildvm_arch` 并清理输出；这保护已完成的 VM 后端接线和 DynASM 模板语法，真实跨平台 artifact/runtime smoke 仍按平台批次补齐。
  - 当前进展：`coroutine.wrap()` 错误路径会调用 `lua_closethread()`，因此 wrapper 内的错误会先关闭 coroutine 栈上的 `<close>` locals；`lua_closethread()` 已修正 close 过程中栈扩容后的 base 指针恢复和重置协程栈残留，close-error 后执行 GC 不再访问旧栈内容；Lua 5.4 C API `lua_resume()` 已补空栈 dead coroutine 判定，避免 wrap reset 后第二次调用误报 `attempt to call a nil value`。
  - 当前进展：C API smoke 已补 `lua_closethread()` / `lua_resetthread()` 关闭挂起 coroutine 时 `__close` 试图 yield 的边界；这类 close-yield 会按 Lua 5.4 变成 `LUA_ERRRUN` / `yield across` 错误，并把目标 coroutine 清到后续 close/reset 可再次成功的状态，避免留下半关闭 continuation。
  - 当前进展：C API smoke 已补 `lua_toclose()` marked slot 在正常 C 返回时的 close-yield continuation：`__close` 先 yield 后成功会保留普通原 C 返回值继续返回，`__close` 先 yield 后抛错会替换为 close 错误，并在 `lua_resume()` 错误返回后只向 coroutine 公开单个错误对象；两个 marked slot 连续 yield、内层 close yield 后抛错且外层 close 接收替换错误也已覆盖；ARM / x86 / x64 / ARM64 / MIPS / MIPS64 / PPC VM 后端都已接入 `LJ_CONT_CLOSE_CFRAME` 静态门禁，ARM/x86/MIPS/MIPS64/PPC 额外由 `test/lua54_vm_backend_dynasm.lua` 自动验证 DynASM 生成。
  - 当前进展：`lua_toclose()` marked slot 自身作为 C 返回值且 `__close` 先 yield 的路径已在 PC x64 收敛；C API 弹栈关闭会以 offset 保存目标 top，内部 `pcall(__close, ...)` 返回后恢复到稳定 top，并在自动 GC 未停止时完成当前 GC cycle，避免后续 C-frame close-yield continuation 观察到内部 pcall 的半失效 frame 残留；`C return yielded close self` 及完整 C API smoke 已通过。
  - 当前进展：`lua_close()` 已补 main thread active `<close>` 关闭路径，`os.exit(true, true)` 会在关闭状态前按 LIFO 执行仍 active 的 main chunk to-be-closed locals；C API smoke 也覆盖了单独 state 上 `lua_toclose()` 后直接 `lua_close()` 会以 `nil` 错误对象调用 `__close`。
  - 实现约束：C-return close continuation 的原 C 返回计数保存在隐藏栈槽，而不是只放在线程全局字段；否则嵌套的 `pcall(__close, ...)` C-return 会在外层 C frame 仍有 close slot 时经过同一 `vm_returnc` 路径，覆盖外层返回窗口并导致多 close-yield 恢复崩溃。
  - 已排除方案：把 `lj_close_unwind()` 中的 C `lua_pcall` 直接换成 C `lua_call` 仍然会在第三个 close-yield 处报 `attempt to yield across C-call boundary`；问题不是 protected call 本身，而是 close 调度必须能从 VM continuation 恢复。
  - 当前进展：PC x64 close-active 动态 `return` 已改为 VM continuation：`RET` / `RET0` / `RET1` / `RETM` 在关闭前保存返回窗口、返回数量和 `MULTRES`，`__close` 内 yield / error / 动态启用 return hook 后都能恢复继续关闭或传播替换错误；`debug.getinfo()` 会隐藏内部 close pcall 并把 return hook 中的匿名 close C frame 命名为 `metamethod close`；generic-for closing value 只在运行期 `closelist` 非空时阻止热计数/JIT 记录，不再把所有泛型 for 函数标成 no-JIT。
  - 当前进展：yieldable `__close` 的 VM continuation 会使用 base library 初始化时保存的 raw `pcall`，不再受用户改写 `_G.pcall` 或篡改 legacy registry 字符串键影响；parser/helper close 路径也会使用隐藏 raw `xpcall`，不再受用户改写 `_G.xpcall` 或篡改 legacy registry 字符串键影响；动态 return 的 close-yield 路径已按官方 Lua 5.4 保持内部 protected call 隔离。
  - 需要补测试/实现：DynASM 模板生成已自动化，仍需在可用 32 位或交叉工具链时补 ARM/x86/MIPS/MIPS64/PPC artifact/runtime smoke；继续在 Android/iOS 实际平台批次验证 C API `lua_toclose` 正常 C 返回 close-yield 路径的 artifact 和 smoke。
  - 实现重点：普通 fall-through、固定返回值 return、动态多返回 return、break、goto、generic for 普通控制流退出、error unwind、coroutine reset/close 和 C API 弹栈关闭已先走 parser/helper/close-list/VM continuation 混合实现；完整实现仍需要在平台批次补齐真实 artifact/runtime 验证。

- [x] `_ENV` 的完整 upvalue 语义。
  - 当前状态：源码 main chunk 已在 Lua 5.4 兼容模式下创建隐藏但真实的首个 `_ENV` upvalue，普通隐式全局访问会降级为 `_ENV.name`，不再依赖只能保存 table 的 LuaJIT 函数环境伪 upvalue。
  - 当前进展：`debug.getupvalue` / `debug.getinfo(..., "u")` / `debug.setupvalue` / `debug.upvalueid` / `debug.upvaluejoin` 已能操作真实 `_ENV` upvalue；`debug.setupvalue(load("return x"), 1, 5)` 和把隐式 `_ENV` join 到非 table lexical `_ENV` 后，运行期会按 Lua 5.4 报 `_ENV` 非 table 索引错误。
  - 当前进展：Lua 5.4 兼容模式下 `rawget(_G, "_ENV")` 已对齐官方返回 `nil`，`next(_G)` / `pairs(_G)` 也会跳过内部兼容 `_ENV` 键，同时保留裸 `_ENV == _G` 的当前兼容表面。
  - 已覆盖：chunk 的 `_ENV` upvalue 名称/位置、`debug.setupvalue` 用 table 和非 table 替换环境、带真实上值且访问全局的闭包会把 `_ENV` 排在第一个 debug upvalue、闭包继承局部 `_ENV`、真实 lexical `_ENV` 的 `debug.upvalueid` / `debug.upvaluejoin`、非 table lexical `_ENV` 的 debug 枚举和运行期索引错误、隐式 `_ENV` join 到非 table lexical `_ENV`、C API `lua_getupvalue()` / `lua_setupvalue()` / `lua_upvalueid()` / `lua_upvaluejoin()` 的基础名称/值/共享槽位行为、dumped chunk 的真实首个 upvalue可由 `load(..., env)` 初始化为 table / number / false / nil、`rawget(_G, "_ENV") == nil`、`next(_G)` / `pairs(_G)` 不枚举 `_ENV`。

- [x] `<const>` 的 debug API 行为核对。
  - 当前状态：编译期赋值检查已覆盖；经本机 Lua 5.4.8 对照，`debug.setlocal`、`debug.setupvalue`、`debug.upvaluejoin` 可以绕过 `<const>` 的源码级赋值限制，当前实现保留该行为。
  - 已覆盖：`debug.setlocal` 修改 const local、`debug.setupvalue` 修改 const upvalue、`debug.upvaluejoin` 替换 const upvalue。
  - 说明：数值字面量 const 是否被官方实现优化到无 upvalue 属于优化/debug 形态差异，不再作为“debug API 必须拒绝修改”的缺口处理。

- [ ] 完整 Lua 5.4 64 位整数语义。
  - 当前状态：PC x64 Lua 5.4 compat dual-number 构建已把公开 `math.mininteger` / `math.maxinteger`、TValue boxed integer、算术、bitwise、字符串扫描/格式化、C API 常用路径和 JIT IR 主要子集推进到 64 位 integer 表面；剩余风险主要是跨平台 artifact/runtime、C API 深边界和 ABI 验证。
  - 当前进展：解释器、常量折叠和 `math.abs` 的 int32 最小值边界已改走 Lua 5.4 integer 环绕语义，`-(-2147483648)` / `math.abs(-2147483648)` 会生成 boxed `2147483648`，而 `-0` / `local z=0; -z` 保持 integer `0`，不再退回旧 LuaJIT float `-0.0` / 32-bit 环绕表面；所有 VM 后端的 `BC_UNM` / `math.abs` 分支同步改为在溢出时交给 64-bit helper。
  - 当前进展：numeric `for` 的解释器/VM 路径已能在 init/limit/step 任一控制槽为 boxed int64 时保持 integer 模式，跨 `2147483647` / `2147483648`、反向跨界、`math.mininteger` / `math.maxinteger` 和 `math.huge` / `-math.huge` limit 饱和边界均按 Lua 5.4 运行；隐藏 FOR index 更新会分配新的 boxed integer，避免污染共享的 `GCint64` 值。
  - 当前进展：JIT recorder 已开始记录三控制槽均为 integer 子类型的 boxed int64 numeric `for` 正/负步长子集；控制槽在 trace 内转为 raw `IRT_I64`，用 64-bit add/compare guard 推进，再按结果范围恢复 int32 或 boxed int64 TValue 循环变量。可按步长方向取整的 float limit 会在 FORI helper 阶段归一为 integer limit，并已覆盖从 int32 控制变量跨入 boxed int64 的 trace 边界。x64/x86 VM 模板的 boxed int64 `BC_JFORI` 路径已在 `branchPC` 后重新载入 trace id，避免已编译 trace 重入时把分支目标误当 trace 编号。
  - 当前进展：`math.type()` 在非 dual-number 构建下会把当前 32 位范围内可精确表示为整数的 number 报告为 `integer`；`math.floor`、`math.ceil`、`math.modf` 的整数部分会尽量返回当前兼容整数表面。
  - 当前进展：PC x64 Lua 5.4 compat smoke 已启用 dual-number 构建；`1.0`、`1e0`、`0x1p0` 这类按拼写应为 float 的常量不会再因常量表把 `1` / `1.0` 统一为同一个 key 而丢失子类型，`1.0 + 2` / `4 / 2` / `2 ^ 3` 等常量折叠也会保留官方 integer/float 结果形态；字符串数字算术会保留 `"1" + 2` 的 integer 结果和 `"1.0" + 2` 的 float 结果，integer `%` / `lua_arith(LUA_OPMOD)` 保留 integer 子类型并在 `% 0` 时报 Lua 5.4 错误，C API `lua_arith()` / `lua_isinteger()` 也按同一子类型表面覆盖。
  - 当前进展：Lua 5.4 兼容构建的 raw number-to-string 路径已保留 integer/float 子类型，`tostring(1.0)`、`"x" .. 1.0`、`table.concat({1.0, 2, 3.5})` 和 C API `luaL_tolstring(lua_pushnumber(1.0))` 都会输出带 `.0` 的 float 文本，integer 仍输出完整 64-bit 十进制文本。
  - 当前进展：JIT recorder 的 number-to-string 路径已接入 boxed int64，`tostring(2^40)`、`"x" .. 2^40`、`string.sub(2^40, ...)`、`string.format("%s/%d/%x", 2^40)` 和 buffer/io write 类共用 `lj_ir_tostr()` 的热路径会通过 `lj_strfmt_i64` / 64-bit integer formatter 保留完整整数文本，不再因 boxed int64 不是旧 `tref_isnumber` 而退解释器。
  - 当前进展：lowered 位运算 helper 已按公开 64-bit `lua_Integer` / `lua_Unsigned` 表面保持自洽，`1 << 63 == math.mininteger`、`(1 << 63) >> 63 == 1`、`-1 >> 1 == math.maxinteger`、`-1 << 63 == math.mininteger` 和 `~0 == -1` 均已覆盖；跨出 `math.mininteger..math.maxinteger` 的 number 操作数会报无整数表示；hex 整数字符串/字面量和 `tonumber(str, base)` 显式 base 路径也按公开 `lua_Unsigned` 宽度回绕。
  - 当前进展：ARM64/ARM/MIPS/MIPS64/PPC 后端的 Lua 5.4 一元负号边界会按当前公开 integer 位宽保持整数回绕为 `math.mininteger`，不再沿旧 LuaJIT 路径转换成 float；Android ARM64 smoke 已覆盖该边界。
  - 当前进展：`tonumber(str, base)` 已区分“base 缺省”和“显式 base=10”，显式 base 路径会按 Lua 字符串长度扫描并拒绝内嵌 NUL；`tonumber("10000000000", 10)` 以及动态 base 下的 `24^10` 级别结果会保留完整 64-bit integer，JIT helper 返回 raw `IRT_I64` 后恢复 int32/boxed integer，JIT 回归、官方 `math.lua` 和 perf smoke 均覆盖该路径。
  - 当前进展：base library 的整数参数检查已拆出完整 `lua_Integer` 路径，`select(1099511627776, ...)` / `select(math.maxinteger, ...)` 按 Lua 5.4 返回 0 个结果，负向超大索引仍报 `index out of range`；`tonumber(str, wide_base)` 会在完整整数解析后报 `base out of range`，`error(message, wide_level)` 和 `collectgarbage("step", wide)` 也不再被旧 32-bit helper 误拒绝。
  - 当前进展：Windows MinGW/UCRT Lua 5.4 兼容构建的 `file:seek()` offset 已按官方 `l_seeknum` / C `long` 边界校验，`2147483648`、`1099511627776` 和对应整数字符串会报 `not an integer in proper range`，不再被 LuaJIT 的 `fseeko64` 路径错误接受。
  - 当前进展：`file:read(n)` 的 Lua 5.4 数字长度路径已避免完整 `lua_Integer` 到 32-bit `MSize` 的静默截断，`1099511627776` 和超大负数会报 `not enough memory`，不再变成 0 字节读并返回空串或 `nil`。
  - 当前进展：官方 `testes/math.lua` 涉及 64-bit integer 表面的 math 库边界已收口，覆盖大数 float `%` 精度、`math.abs(mininteger)` 回绕、`math.atan(y, x)`、`math.fmod` integer/float 结果类型、Lua 5.4 xoshiro256** `math.random` / `randomseed`、full integer/float random 分布和区间投影。
  - 当前进展：Lua 5.4 dual-number JIT recorder 已保留 runtime float TValue 常量子类型，`i + 0.0` 热循环不会被 KINT 常量窄化回 integer；int+int 的 `+` / `-` / `*` 在 trace 中已覆盖跨 `2147483647` 后保持 64-bit integer 结果；boxed int64 的 `+` / `*` / unary minus 已固定保留 integer 子类型，`^` 会先转成 number 并记录 `IR_POW`，保持 Lua 5.4 power 返回 float 子类型，且 boxed int64 与字符串数字混合的 power 热路径也会用 Lua 5.4 数字扫描 guard 后记录；Lua 5.4 `math.random()` / `math.random(0)` / 常量整数区间，以及动态 integer、可精确整数 number 和可转整数 string 区间已通过 xoshiro256** side-effect helper 记录 trace；`math.randomseed()` 已接受完整 64-bit integer/string integer seed，并把返回 seed 保持为 integer；`math.random(0)` 已用官方 5.4.8 对齐的固定 seed 序列覆盖超 53-bit 正/负样本和接近端点的全宽 integer 返回；Lua 5.4 `math.tointeger()` 的热路径已固定 `2^40`、`math.maxinteger` 和越界 nil 边界；Lua 5.4 `math.ult()` recorder 已覆盖 boxed int64、精确 number 和 string integer 的无符号比较 guard；Lua 5.4 `tonumber()` recorder 已覆盖 no-base number passthrough、动态 float 字符串 `IR_STRTO`、动态 integer 字符串、混合 KSTR 常量字符串中的 boxed int64 返回值、普通 invalid 字符串、逗号 locale 常量和稳定拒绝的 LuaJIT-only 数字扩展，`tonumber(str, base)` 已覆盖动态字符串 + 常量/动态显式 base 的 integer scanner 成功/失败分支，其中动态 base 支持当前可转整数的 number 和 string；其它更复杂字符串扫描仍保守退解释器。
  - 当前进展：Lua 5.4 JIT 回归已覆盖 boxed int64 table key 的热 load、store 和 `pairs()` / `next()` 迭代，固定 trace 后 table key 不会被截断成 32 位整数，迭代返回的 key 仍保持 integer 子类型；exact int64 float table key 和 boxed int64 等值 key 已改走 value-based `lj_tab_geti64` / `lj_tab_seti64` helper 记录，不再依赖 boxed 对象身份或直接 NYI 退解释器；missing int64 key store 现在也会记录 `__newindex` 缺失 guard，metatable 后续增加 `__newindex` 时会退出 trace，不会绕过 Lua 5.4 metamethod 语义。
  - 当前进展：Lua 5.4 `math.abs()` 的 JIT recorder 已补 boxed int64 参数子集；负数走 raw `IRT_I64` `NEG` 后按结果范围恢复 int32 或 boxed integer，`math.mininteger` 仍保持 Lua 5.4 integer 回绕语义。
  - 当前进展：Lua 5.4 `math.sqrt()` / `math.log()` / `math.sin()` / `math.atan()` 等返回 float 的 fast-function recorder 已能把 boxed int64 参数转换为 number 记录；字符串数字参数会先走 Lua 5.4 数字扫描 guard 再转 number，`math.log(x, nil)` 和 `math.atan(y, nil)` 的 nil base 会按官方默认路径记录，二参数 `math.atan(y, x)` 会记录为 `atan2`；`LUA_COMPAT_5_3` 恢复的 `math.atan2` / `math.pow` / `math.log10` / `math.ldexp` 等旧入口也保留 fast-function id，其中 `math.pow` 的 compat recorder 已按 Lua 5.4 number 转换处理 boxed int64/string 数字。
  - 当前进展：Lua 5.4 覆盖版 `math.floor()` / `math.ceil()` / `math.modf()` / `math.min()` / `math.max()` / `math.fmod()` 已保留 fast-function recorder id；JIT recorder 对 boxed int64 参数直通，对可表示为 64-bit integer 的 float floor/ceil/modf 整数结果走 `IR_FPMATH` + raw `IRT_I64` 转换并恢复 int32/boxed integer subtype，`math.min/max` 的纯 integer 参数子集会记录 raw `IRT_I64` `IR_MIN` / `IR_MAX` 并恢复 int32/boxed integer subtype，`math.fmod` 的双 integer 子集会记录 raw `IRT_I64` C remainder 并保留 integer 返回；其它 Lua 5.4 `<`/metamethod/NaN/float fmod 语义仍保守退解释器。
  - 当前进展：`os.time()` 在 Lua 5.4 兼容构建下会把 `time_t` 结果作为 integer 返回，超出旧 32 位 TValue integer 表面的 timestamp 会走 boxed int64；`os.date()` / `os.difftime()` 同步接受 boxed int64 和可转成 boxed int64 的字符串 timestamp，Lua 5.4 标准库中按 `luaL_checkstring` 风格转换 string 参数的路径也已接受 boxed int64，覆盖 `table.concat` separator、`package.searchpath` name/path、`io.open` filename 和 `os.date` format；`test/smoke.lua`、`test/lua54_jit_regress.lua` 和 `test/lua54_perf.lua` 已固定这些 64-bit integer 表面。
  - 官方验证：PC x64 Lua 5.4 compat 构建下，官方 Lua 5.4.8 `testes/math.lua`、`testes/bitwise.lua`、`testes/strings.lua` 和未修改的 `testes/tpack.lua` 当前已通过；这表示当前 64-bit boxed integer 表面的核心 Lua 5.4 行为已经自洽，不表示所有 C API runtime 边界和目标平台 ABI 都已完成。
  - 当前进展：外部 C 头文件的 `lua_Unsigned` 已改为由 `LUA_UNSIGNED` 派生，Lua 5.4 兼容构建下 `LUA_UNSIGNED` 使用与 `lua_Integer` 同宽的 `uintptr_t`，避免 64 位宿主上 `lua_Integer` 为 `ptrdiff_t` 但 `lua_Unsigned` 仍被硬编码成 32 位 `unsigned int`；外部 Lua 5.4 头的 `LUA_MAXINTEGER` / `LUA_MININTEGER` 也已按 `lua_Integer` 公开宽度展开，`lua_numbertointeger()` 宏可接受 32 位以上但仍在公开 `lua_Integer` 范围内的 double 值，并固定 `LUA_MININTEGER` 下界 inclusive / 上界 exclusive；`lua_tointegerx()` / `luaL_checkinteger()` / `luaL_optinteger()` / `luaL_len()` / `lua_len()` 已能从精确 number/string、`__len` 返回值或 `lua_pushinteger()` 推入的值取回公开 64 位 C integer 范围内的值；`lua_tonumberx()` / `luaL_checknumber()` / `luaL_optnumber()` 已固定 boxed 64-bit integer 和精确 64-bit string number 会按 Lua 5.4 number 转换返回；`lua_tointegerx()` / `lua_isinteger()` / `lua_integer2str()` 已固定 `LUA_MININTEGER` / `LUA_MAXINTEGER` 端点；`lua_stringtonumber()`、`lua_compare()` 和 `lua_arith()` 的加法、乘法、一元负号、整除、取模和 C API 位运算边界也固定了 `2^40` 级别精确结果可由 `lua_tointegerx()` 取回，其中 `band` / `bor` / `bxor` / shift / `bnot` 使用公开 `lua_Integer` / `lua_Unsigned` 宽度，`LUA_OPPOW` 继续固定返回 float 子类型，且 `lua_arith()` 已覆盖 `LUA_MININTEGER` / `LUA_MAXINTEGER` wrap、`min // -1`、`min % -1`、`-mininteger` 和 `~maxinteger` 端点；`math.ult()` 已改用公开 `lua_Integer` / `lua_Unsigned` 宽度，精确 number/string 形式的 `2^40` 级别值可参与无符号比较；`lua_geti()` / `lua_seti()` / `lua_rawgeti()` / `lua_rawseti()` 的 Lua 5.4 wrapper 不再把外部 `lua_Integer` key 截断成 `int`。C API smoke 已增加 `sizeof(lua_Unsigned) == sizeof(lua_Integer)`、`LUA_MAXUNSIGNED` 超 32 位、`LUA_MAXINTEGER` / `LUA_MININTEGER` 超 32 位、以及 `2147483648` 和正负 `2^40` 级别的转换往返、普通和 raw table key、`lua_next()` key 子类型、C API 比较/长度/算术/位运算检查。默认 LuaJIT 构建仍保留旧 `unsigned int` 表面，Lua 5.4 compat dual-number 构建的 `lua_isinteger()` 观察当前 TValue integer/boxed-int64 子类型。
  - 当前进展：`LUA_COMPAT_APIINTCASTS` 的 `lua_pushunsigned()` / `lua_tounsignedx()` / `luaL_checkunsigned()` / `luaL_optunsigned()` 已按公开 `lua_Unsigned` 宽度覆盖 64-bit max、十六进制 unsigned wrap、十进制下界舍入和上界拒绝边界。
  - 当前进展：C API `lua_newuserdatauv()` 的 `nuvalue` ABI 边界已按官方 Lua 5.4 `0 <= nuvalue < SHRT_MAX` 收紧；负数和 `SHRT_MAX` 级别过大 uservalue 数会稳定报 `invalid value`，不再进入大表分配路径或依赖 API assert 构建开关。
  - 当前进展：C API `lua_getiuservalue()` 的 full userdata 越界语义已对齐官方 Lua 5.4；`n <= 0` 或超过声明 uservalue 数时会压入 `nil` 并返回 `LUA_TNONE`，不再把“无该 uservalue”表现成无压栈结果；非 userdata 仍保留 LuaJIT 旧 ABI 的容错无压栈行为。
  - 当前进展：C API `lua_pushcclosure()` 的 upvalue 数已增加 release 构建下的运行期 ABI guard；`n < 0` 或超过 `GCfuncC.nupvalues` 可表示的 `UCHAR_MAX` 时会稳定报 `invalid value`，避免负数转成巨大 `MSize` 分配或超过 255 后被 `uint8_t` 静默截断。
  - 当前进展：C API `lua_xmove()` 的移动数量已增加 release 构建下的运行期 guard；负数或超过源线程当前栈元素数量的 `n` 会稳定报 `invalid value`，不再进入 unsigned stack-growth 或错误栈搬移路径。
  - 当前进展：C API 固定数量栈消费入口已开始统一走 release 运行期 `api_checknelems()`；`lua_replace()`、`lua_pushcclosure()`、`lua_settable()`、`lua_setfield()`、`lua_seti()`、`lua_rawset()`、`lua_rawseti()`、`lua_rawsetp()`、`lua_setmetatable()`、`lua_setiuservalue()`、`lua_setfenv()`、`lua_setupvalue()` 缺少待消费值时会稳定报 `invalid value`，避免 release 构建下继续访问错误栈槽或执行 wrapper 插入路径。
  - 当前进展：C API `lua_createtable()` 的负预分配 hint 已在运行期归零；`narray < 0` 或 `nrec < 0` 不再被传入表分配层并把负 hash hint 扩展成巨大 hsize，从而避免无意义的大表分配/溢出路径。
  - 当前进展：C API `lua_checkstack()` / `lua_settop()` 的栈计数边界已增加 release 构建下的运行期 guard；负 stack request 会返回失败，过大的正 top 或低于当前 frame 的负 top 会稳定报 `invalid value`，不再依赖 API assert 避免越界栈指针计算。
  - 当前进展：C API `lua_concat()` 的 count 边界已对齐 Lua 5.4；`n <= 0` 会压入空串，超过当前栈元素数量的 `n` 会稳定报 `invalid value`，不再在 release 构建下静默忽略负数或访问错误栈槽。
  - 当前进展：C API `lua_arith()` 的 operand/op 边界已增加 release 构建下的运行期 guard；缺少一元/二元操作数或传入无效 op 会稳定报 `invalid value`，不再依赖 API assert 避免越界栈槽读取或错误元方法枚举。
  - 当前进展：C API `lua_call()` / `lua_pcall()` / `lua_callk()` / `lua_pcallk()` / `lua_yield()` / `lua_yieldk()` 的参数数量和结果数量边界已增加 release 构建下的运行期 guard；负 `nargs`、缺 callee、非法 `nresults` 或 yield 结果数超过当前栈元素数量时会稳定报 `invalid value`，不再进入错误栈指针或结果窗口计算。
  - 当前进展：`lua_arith()` 的 C API 字符串运算边界已对齐当前 Lua 5.4 表面：显式 string metatable 的 `__add` / `__idiv` 优先于 fallback 字符串数字转换；`LUA_OPBAND/BOR/BXOR/BNOT/SHL/SHR` raw bitwise 路径使用 no-string integer conversion，字符串操作数默认报 bitwise string 错误，显式 string metatable 的 bitwise 元方法优先执行，不再沿旧 C API 路径把字符串整数强转为 bitwise operand。
  - 当前进展：C API smoke 已覆盖 `lua_next()` 遍历正负 `2^40` 级别 integer table key 时返回 key 的 integer 子类型和值，防止 C API 迭代边界退化成 32 位截断或 float key。
  - 当前进展：table key 写入路径已按 Lua 5.4 把可精确表示为公开 `lua_Integer` 的 float key 规范成 integer key；`1099511627776.0` / `-1099511627776.0`、int32 hash key `0` / `-1` / `2147483647` 和 `2^53` 级别 exact float key 经 `pairs()` / `next()` 返回 integer 子类型；`next(t, 1.0)` / `next(t, 1099511627776.0)` 不再匹配规范化后的 integer key，按 Lua 5.4 的 key variant 规则报 invalid key；JIT recorder 对尚未建模的 exact int64 float table key 退解释器，避免 trace 写入 float key。
  - 当前进展：`table.concat()` 默认终点已改用公开 `lua_Integer` 长度，`__len` 返回 `2^40` 级别长度时不会先被压回 32 位；smoke 覆盖 `table.concat(proxy, sep, 2^40)` 读取 64-bit integer key，以及 `table.concat(proxy, sep, 2^40 + 1)` 空范围不触发元素读取。
  - 当前进展：JIT recorder 的 `table.concat()` 显式起止下标已补 `lj_buf_puttab_i64` helper，`table.concat(t, sep, 2^40, 2^40 + 1)` 能在 trace 中按 64-bit integer key 读取并格式化 boxed int64 元素；显式宽起点但默认终点依赖 `__len` 的路径会退解释器，避免用 raw `ALEN` 绕过 Lua 5.4 `__len` 语义。
  - 当前进展：`table.remove()` 的 Lua 5.4 兼容路径已改用公开 `lua_Integer` 长度和位置，不再把 `__len` 返回的 `2^40` 级别 proxy table 长度或显式位置压回 32 位；smoke 覆盖 `table.remove(proxy, 2^40)` 和 `table.remove(proxy, 2^40 + 1)` 通过 `lua_geti` / `lua_seti` 保持 64-bit integer key。
  - 当前进展：`LUA_INTEGER_FRMLEN` / `LUA_INTEGER_FMT` / `LUAI_UACINT` 已按 `lua_Integer` 的 `ptrdiff_t` 头文件 ABI 调整，`lua_integer2str()` 在外部 C 侧格式化 `2147483648` 和 `2^40` 这类超 32 位但可由 `lua_Integer` 表示的值时不再截断；VM/TValue 侧也已能以 integer subtype 保存并格式化这些 64-bit 值。
  - 当前进展：`string.pack` / `string.unpack` 的显式 `i8` / `I8` 已桥接到公开 `lua_Integer` 宽度，当前 number 表面可精确表示的 64 位值（例如 `2^40` / `-2^40`）可 roundtrip，`I8` 也保留 unsigned modulo 输入（例如 `-1`）的 8 字节布局；`j` / `J` 默认宽度已跟随当前 `math.maxinteger` 推导出的 bitwise integer 位宽，Lua smoke 固定了 `math.mininteger` / `math.maxinteger` 的 `j` roundtrip 和 `J` 的 unsigned modulo 输入。
  - 当前进展：Lua smoke 已固定 64 位整数字面量边界、`math.tointeger()` 的 `math.mininteger` / `math.maxinteger` / 越界 nil、VM 位运算/整除/比较、`string.pack("j/J")` 默认宽度、`string.format` 的 `maxinteger`/`mininteger` 边界，以及 `string.byte` / `string.sub` / `string.find` / `string.gmatch` / `utf8.len` / `utf8.offset` 的超 32-bit position 参数边界。
  - 当前进展：C API smoke 已固定 `lua_numbertointeger()`、`lua_stringtonumber()`、`lua_isnumber()`、`lua_tonumberx()`、`lua_tointegerx()` 和 lauxlib number 入口对十进制越过 `LUA_MININTEGER` / `LUA_MAXINTEGER` 的合法数字按 float 接受，而 `lua_tointegerx()` / lauxlib integer-only 入口保持 Lua 5.4 的整数转换边界；C API conversion 层和 lauxlib number / integer 入口也固定拒绝 `inf` / `nan` 和 LuaJIT 扩展二进制前缀字符串。
  - 当前进展：C API smoke 已补 `lua_compare()` 在 64-bit integer 与非精确 double、上界 exclusive double、NaN 混合比较时的 equality / `<` / `<=` 边界，固定公开 C API 不能把 53-bit 以上整数误按 double 舍入比较。
  - 已知差异：剩余 C API runtime 边界仍没有完整 Lua 5.4 64 位整数语义。
  - 对照结论：官方 `testes/math.lua` / `testes/bitwise.lua` / `testes/strings.lua` / `testes/tpack.lua` 已能按当前 64-bit boxed integer 配置跑通；剩余风险集中在剩余 C API runtime 边界，以及各目标平台 ABI/artifact 验证，不能再用单个库函数补丁硬凑。
  - 当前进展：`luaL_Buffer` 的增长尺寸溢出已按官方 Lua 5.4 报运行期错误 `buffer too large`，buffer box allocator 失败会以普通运行期错误返回 `not enough memory`，并由 C API smoke 用 strict/custom allocator 固定巨大 buffer box 在错误展开时仍通过 `__close` 释放。
  - 需要补测试：剩余 C API 深边界、ABI/跨平台 runtime 验证；allocator shrink/grow 主路径已有 strict allocator 回归，parser 模板表精确数组重分配路径和运行期短字符串 intern / 字符串表增长失败也已进入 strict allocator 回归。
  - 实现重点：需要统一 TValue 表示、数值转换、字符串扫描、格式化、运算符和库函数的整数路径。

- [x] 数值 `for` 的 Lua 5.4 整数/浮点控制变量语义。
  - 当前状态：Lua 5.4 数值 for 的运行期控制变量语义已覆盖 64 位 boxed integer 边界；JIT 已能记录正/负步长、integer-only boxed int64 FORL 子集，并覆盖可取整 float limit 归一后的 boxed int64 边界；float init/step 形成的 2^40 级别 float-mode 热循环已进入 JIT 回归，防止被 boxed-int64 FORL 路径重新窄化；对向越界 float limit、完整 SCEV 和更多后端 artifact 验证仍归入“完整 Lua 5.4 64 位整数语义”大项。
  - 当前进展：Lua 5.4 兼容模式已把常量 `0` / `0.0` step 从编译期错误改为运行期 `FORI` 前 helper 错误，和动态变量 `0`、字符串 `"0"` 一样在执行时统一报 `'for' step is zero`，可被官方 `checkerror(function() for ... do end end)` 捕获。
  - 当前进展：init/step 为 float 的循环会保持 float 控制变量；init/step 为 integer 且 limit 为 float 时会按步长方向取整，超出 int64 但同向无限延伸的 limit 会饱和到 `math.mininteger` / `math.maxinteger`，反向越界则切回 float 比较以保持跳过语义；官方 `testes/nextvar.lua` 当前已跑到 `OK`。
  - 当前进展：init 或 step 原始表达式为字符串时，即使字符串内容可表示为当前 integer，也会按 Lua 5.4 保持 float 控制变量；只有 limit 为字符串且 init/step 已是 integer 时，仍沿 integer FORL 路径。解释器和热循环 trace 均已加入回归。
  - 当前进展：JIT recorder 对接近当前 32 位 integer 边界、可能溢出的 integer numeric for 不再改录为 float trace，避免 `math.type(i)` 在热循环后从 `integer` 变成 `float`；同时低 `jit.opt` hotloop 配置下也不会把 `1.0, 10` 或 `-1, -10, -1.0` 这类 float 模式循环重新窄化成 integer trace。
  - 已知差异：boxed int64 numeric `for` 的 JIT 记录当前只覆盖 integer-only 子集；float init/step 已有 2^40 级别 float-mode 热循环回归，对向越界 float limit、完整 SCEV 和跨后端快路径 artifact 仍归入 64 位 integer/TValue 批次。
  - 已覆盖：正/负步长边界、跨 32 位边界、`math.maxinteger` / `math.mininteger` 附近、`math.huge` / `-math.huge` limit 饱和、整数和浮点控制变量的类型、低 hotloop JIT trace 下 float 控制变量不被重新窄化、boxed int64 正/负步长热循环可记录并可重复重入 trace、可取整 float limit 归一后跨 32 位边界仍记录为 integer trace、boxed int64 hotloop 不崩溃，以及官方 Lua 5.4.8 `testes/nextvar.lua`。

- [x] Lua 5.4 运算符元方法。
  - 当前状态：`//`、`%`、`&`、`|`、`~`、`<<`、`>>` 在 Lua 5.4 兼容模式下通过内部 helper 降级实现；helper 已在原始数值路径失败时查找并调用 `__idiv`、`__mod`、`__band`、`__bor`、`__bxor`、`__bnot`、`__shl`、`__shr`。
  - 当前进展：`//` helper 的无元方法失败路径已按 Lua 5.4 报 `attempt to idiv a '<lhs>' with a '<rhs>'`，不再泄露私有 helper 名，并保留 `__name` 类型名。
  - 当前进展：`%` helper 已覆盖 integer `%` 子类型、integer `% 0` 错误和 float `%` 结果；字符串操作数会先尊重显式 `__mod`，并在无元方法失败时保留 Lua 5.4 的 `attempt to mod a '<lhs>' with a '<rhs>'` 文本；非字符串 table/boolean/nil 等失败路径按普通 arithmetic 错误报告，并保留 `__name` 与 source operand 名；`__idiv` 也同步修正为字符串显式元方法优先。
  - 当前进展：bitwise helper 的失败路径已按 Lua 5.4 报运算符错误，不再泄露私有 helper 名；无整数表示的 number 报 `number has no integer representation`，string/boolean/带 `__name` 的 table 报 `attempt to perform bitwise operation on ... value`，并会尽量追加 `(constant '3')` / `(local 'x')` 等源级操作数名字。
  - 当前进展：官方 `testes/bwcoercion.lua` 通过 string metatable 覆盖 bitwise/idiv 字符串边界；当前 lowered helper 已规整 metamethod 返回栈，只返回 metamethod 第一个结果，不再把原操作数漏成额外返回值。
  - 当前进展：lowered helper 的原始数值结果路径也已规整返回槽；`local a, b = "1.0" // "2"` 不再把左操作数字符串漏到 `a`，只返回单个 Lua 5.4 结果。
  - 已覆盖：左右操作数元方法、反向查找、无元方法时报错、元方法返回值透传；一元 `~` 按 Lua 5.4 传入两份同一操作数。
  - 当前进展：原始位运算数值路径已按公开 `lua_Integer` / `lua_Unsigned` 宽度运行，覆盖 `1 << 63 == math.mininteger`、`(1 << 63) >> 63 == 1`、`-1 >> 1 == math.maxinteger`、`-1 << 63 == math.mininteger` 和 `~0 == -1`；JIT recorder 对这些 full-width 64-bit wrap/zero-fill shift 边界也已进入回归和 perf smoke。更深的 64-bit TValue/JIT 剩余语义继续归入“完整 Lua 5.4 64 位整数语义”。

- [x] `__le` 元方法不应再由 `__lt` 模拟。
  - 当前状态：只定义 `__lt` 的 table 做 `a <= b` 时不再走旧 Lua 5.1 风格的 `not (b < a)` 模拟路径，缺少 `__le` 会直接报错。
  - 当前进展：默认 Lua 5.4 compat 继续禁用 `__le` 缺失时的 `__lt` fallback；使用 `LUA_COMPAT_5_3` / `LUA_COMPAT_LT_LE` 构建时会恢复官方兼容 fallback，解释器和 JIT recorder 均按该开关分流，并由 `test/lua54_compat53_runtime.lua` 的解释器/热循环探针覆盖。
  - 已覆盖：只定义 `__lt`、只定义 `__le`、二者都定义、左右元表差异和基础错误路径。

- [x] `__eq` 元方法按 Lua 5.4 接受单侧定义。
  - 当前状态：table/userdata 的 `==` / `~=` 在对象不同且任一侧有 `__eq` 时，会按官方 Lua 5.4.8 `lvm.c` 的顺序先查左操作数，左侧没有再查右操作数；不再要求两侧 metatable 暴露同一个 `__eq` 函数。
  - 当前进展：`__eq` 元方法存在但不是函数/不可调用时，错误文本已保留 Lua 5.4 的 `metamethod 'eq'` 来源标注。
  - 已覆盖：左侧元方法、右侧 fallback、左右两侧 `__eq` 不同函数时的左侧优先、`~=` 反向结果，以及开启 JIT 后的热循环 recorder 路径。
  - 官方验证：`testes/events.lua` 中 `Set{...} == rawSet{...}` 和 `rawSet{...} == Set{...}` 已通过；`testC` userdata 块仍因当前未启用官方 testC harness 跳过。

- [x] `string.pack` / `string.unpack` / `string.packsize` 的完整格式布局。
  - 当前状态：已覆盖基础整数、浮点、字符串、`j` / `J`、`T`、`l` / `L`、`X` 和 `!n` 最大对齐，`packsize`、`pack`、`unpack` 三者共用当前位置对齐规则。
  - 当前进展：`i/I` 支持官方 1..16 字节尺寸；显式 `i8` / `I8` 已按公开 `lua_Integer` 宽度桥接，可处理当前 number 表面能精确表示的 64 位整数值，`I8` 对负输入按 unsigned modulo 写出 8 字节而 `I4` 仍会拒绝负数；超过公开 `lua_Integer` 宽度的部分按符号扩展或零扩展校验，错误路径会报 `N-byte integer does not fit into Lua Integer`；`j/J` 已按当前 64-bit VM integer 宽度读写默认整数格式，`L` 仍按宿主 C ABI 保留 Windows LLP64 的 32-bit long 表面，`I4` 仍可返回 `4000000000` 这类正 unsigned 结果。
  - 当前进展：`c` 格式必须显式带 size，格式数字读取已按 Lua 5.4 的 int 安全边界停止吞后续数字：`!` / `i` / `I` / `s` 使用 `integral size ... out of limits [1,16]`，`c9999999999` 这类固定字符串尺寸会把剩余数字当作下一格式项并报 `invalid format option '9'`；`packsize` 理论总长度按 Lua 5.4 `0x7fffffff` 上限报 `too large`；`X` 的 next option 必须紧跟，不会跳过中间空白；`unpack` 初始位置支持负索引，且 `0` 会按官方 `posrelatI` 语义映射到首字节，随后再参与 alignment。
  - 已覆盖：`X`、`!n` 最大对齐、padding 字节、`unpack` 位置推进、混合 endian 与 alignment、1..16 字节整数格式、超当前 integer 宽度的扩展校验、`J`、负初始位置、`0` 初始位置、非法格式基础错误，以及官方 Lua 5.4.8 `testes/tpack.lua`。
  - 剩余边界：`j/J` 的 64-bit 宽度已校准；后续仍需继续验证非 Windows C ABI 下 `l/L/T`、跨平台 artifact/runtime 和 C API 边界。

## P1：标准库和元语义缺口

- [x] table 的 `__gc` 元方法。
  - 当前状态：Lua 5.4 兼容构建会在 metatable 赋值时记录 table 是否已经具备 `__gc`，GC 回收时按 finalizer 队列调度。
  - 当前进展：赋值 RHS call 临时槽、table constructor 消费后的 call 临时槽、indexed assignment 的 LHS key/RHS 临时槽会在 Lua 5.4 模式下清空，避免 conservative stack scan 把已覆盖的 finalizable table、弱表临时 key 或 weak value 误保活；新登记 table/userdata finalizer 后会短期保持 allocation-driven GC 响应，避免刚登记后被推迟到很久之后才运行。
  - 当前进展：`collectgarbage(...)` 在 `__gc` finalizer 内会先完成参数校验，再对合法选项统一返回 `nil`，避免 collector 重入；非法选项仍按 Lua 5.4 报 `invalid option`。
  - 当前进展：关闭 Lua state 时已按 Lua 5.4 固定 finalizer 队列边界；`__gc` finalizer 中创建的新 `__gc` 对象不会在同一轮 `lua_close()` 中继续运行，finalizer 中递归 `os.exit(..., true)` 只会清空当前已经排队的 finalizer，不会重新分离新对象。
  - 当前进展：进入 finalizer 队列的 table 会在弱 value 表清理阶段按官方 Lua 5.4 从 value slot 清除；finalizer 取出对象时恢复普通状态，复活后重新放入弱 value 表且仍有强引用时不会被误清。
  - 已覆盖：带 `__gc` 的 table 被回收、多个 table finalizer 的 LIFO 顺序、晚加 `__gc` 不触发、替换 `__gc` 后调用新函数、删除 `__gc` 后不调用、finalizer 抛错进入 Lua 5.4 warning 通道、官方 `gc.lua` 中 `GC1`/`GC2` allocation loop 形态、嵌套 table constructor finalizer、finalizer 内 `collectgarbage` 非重入返回、普通 GC finalizer reentry 和 close-state finalizer reentry 不运行新创建的 finalizer。

- [x] 弱键表的 ephemeron 语义。
  - 当前状态：`__mode = "k"` 下 value 反向引用 key 时，value 不再反向保活 key；GC atomic 阶段会对弱键强值表做 ephemeron 固定点标记。
  - 当前进展：弱 `kv` 表清理时会先判断弱 value 是否应被清除，再判断弱 key，避免准备删除的 entry 因检查 string key 而把长字符串延后一轮回收；大临时字符串 buffer 在 full GC 后会收缩回小 buffer，以匹配官方 `gc.lua` 对长字符串弱表后的内存上界断言。
  - 当前进展：table lookup/set 会跳过 value 已为 nil 的 dead-key slot，避免后续字符串查找对 GC 留下的 dead long string key 做字节比较。
  - 当前进展：boxed 64-bit integer 在弱 key / weak value 表中按 Lua 5.4 数字语义处理为强值，不会因为内部 GCint64 表示而被弱表清理。
  - 已覆盖：只有 value 反向引用 key 的弱键表会在多次 GC 后清空，外部仍强引用 key 时对应 value 会保留；弱 `kv` 表中 dead value 对应的长字符串 key 会在同一轮 full GC 后释放，仍有 number value 的长字符串 key 会保留；table lookup/set 以及 `next()` / `lj_tab_keyindex()` 的 dead long-string key 保护路径在 JIT 默认和 `-joff` 下通过。
  - 当前进展：回归用例已覆盖多级链式 ephemeron 固定点、弱键弱值组合中 value 反向引用 key 不能保活该 key，以及 weak-key ephemeron 对 finalizable table value 的保活/释放边界。
  - 后续扩展：可继续按官方 `gc.lua` 压力形态补更细用例。

- [x] `__name` 元字段。
  - 当前状态：`tostring(setmetatable({}, {__name="Foo"}))` 已显示 `Foo: ...`；参数类型错误也会使用 `__name` 字符串。
  - 当前进展：`luaL_newmetatable()` 在 Lua 5.4 兼容模式下会把注册类型名写入 `__name`。
  - 当前进展：`luaL_tolstring()` 已使用 `__name` 作为默认对象前缀，C API smoke 已覆盖 lauxlib 路径。
  - 当前进展：`tostring()` 已按 Lua 5.4 校验 `__tostring` 返回值必须可作为 string，返回 string 或 number 会成功并保留 integer/float 文本，返回 table/boolean/nil 等其它值时会报 `'__tostring' must return a string`。
  - 当前进展：`coroutine.resume()` / `coroutine.close()` 的 Lua 5.4 兼容路径已改用通用 `thread expected, got <__name>` 类型错误；`coroutine.close()` 对 running / normal coroutine 的状态错误会按官方区分 direct `pcall(coroutine.close, ...)` 的裸错误文本和源码调用的用户调用点位置，不再泄露内部 Lua wrapper chunk；默认 LuaJIT 构建仍保留旧 `coroutine expected` 文本。
  - 当前进展：`coroutine.isyieldable([co])` 已支持 Lua 5.4 可选 thread 参数；挂起或死亡的非主 coroutine 返回 true，非 thread 参数使用 Lua 5.4 风格函数名和 `__name` 类型文本。
  - 当前进展：`coroutine.create()` / `resume()` / `status()` / `wrap()` / `close()` 的 direct `pcall(coroutine.xxx, ...)` 基础参数错误会带 `coroutine.xxx` fallback 函数名，并使用 Lua 5.4 的 `function/thread expected` 文本；普通源码调用和局部别名调用下，`status` / `create` / `wrap` / `isyieldable` 已能按 Lua 5.4 恢复调用点名，例如报到 `status` 或局部 `f`，且显式 `nil` 参数也已进入回归覆盖；`resume` / `close` 外层 wrapper 也已保留真实参数个数，缺参报 `got no value`、显式 `nil` 报 `got nil`，普通源码调用、局部别名调用以及 tail-position `return coroutine.xxx(nil)` / `return f()` 会报到 `status` / `isyieldable` / `create` / `wrap` / `resume` / `close` / `f`。
  - 当前进展：主线程直接 `coroutine.yield()` 会按 Lua 5.4 报 `attempt to yield from outside a coroutine`；其他不可 yield 的 C 边界仍保留 LuaJIT 现有错误路径。
  - 已覆盖：`tostring`、`type` 不受 `__name` 影响、非字符串 `__name` 被忽略、`__tostring` 返回 string/number 成功和返回 table 等其它值失败、`math.abs` 参数类型错误、`coroutine.resume` / `coroutine.close` / `coroutine.isyieldable` 线程类型错误、`coroutine.isyieldable([co])` 可选 thread 参数、`coroutine` 基础参数错误函数名、普通源码调用和局部别名调用下的 `status` / `create` / `wrap` / `isyieldable` / `resume` / `close` 调用点名、`status` / `create` / `wrap` / `isyieldable` / `resume` / `close` tail-position 源码调用和局部别名调用点名、`resume` / `close` wrapper 缺参 vs 显式 nil 区分、`coroutine.close()` 成功路径单返回值、running / normal coroutine 状态错误文本、`luaL_tolstring`。
  - 说明：其他函数名/逐字错误文本继续归入“标准库错误消息与边界参数完全对齐”。

- [x] `tonumber` 和 `string.format` 的 Lua 5.4 数值格式规则。
  - 当前状态：已补 `tonumber("0x10", 16) == nil`；`tonumber` 的显式 base 参数会拒绝无整数表示的 number，仍接受字符串数字；`tonumber()` 在 Lua 5.4 兼容模式下已拒绝 C 风格 `inf` / `infinity` / `nan` 字符串和 LuaJIT 扩展 `0b` / `0B` 二进制前缀字符串；整数格式 `%d`/`%i`/`%u`/`%x`/`%o` 和字符格式 `%c` 已拒绝无整数表示的 number/string number；`string.format("%q", number)` 已输出可读回文本，覆盖整数、十六进制浮点、负零、NaN 和正负无穷；`%q` 对 table 等没有 Lua 字面量形式的值会报错，不再走 `__tostring`；`string.format("%p", nil/boolean/number)` 已输出 `(null)`，GC 对象继续使用平台 C `%p` 文本，并用 C API smoke 固定无宽度、宽度和左对齐时都跟随宿主 C 指针 ABI。
  - 当前进展：`string.format` 已按 Lua 5.4 校验格式规格长度、各转换允许的 flag/precision、`%q` 禁止 modifier、缺少参数报 `no value`；格式项缺少实参时会先报 `bad argument ... (no value)`，即使该格式项本身是 invalid 或 too-long；带宽度/精度的 `%s` 遇到内嵌 NUL 会报 `string contains zeros`，裸 `%s` 仍保留 Lua 字符串字节；`%a` / `%A` 只接受裸转换，flag、width 或 precision modifier 会按官方报 `modifiers for format '%a'/'%A' not implemented`。
  - 当前进展：`string.format()` 的格式规格长度阈值已按 Lua 5.4 `MAX_FORMAT - 10` 对齐，21 位 width 或 19 位 precision 这类总长度达到 23 字节的格式项会报 `invalid format (too long)`，不再误报 `invalid conversion specification`。
  - 当前进展：`string.format("%s")` 已按 Lua 5.4 通过 `__tostring` 转换 table/userdata，缺少 `__tostring` 时使用 `__name` 作为类型名前缀；`__tostring` 返回 table 等非 string/number 值时会报 `'__tostring' must return a string`，不再把错误返回值继续格式化。
  - 当前进展：JIT recorder 已支持 `string.format("%s", number)` 和带宽度的 `%s` number 路径；记录时先把 integer/float TRef 转成字符串，避免旧 recorder 对非 string `%s` 直接 NYI，同时保留 object / `__tostring` 复杂表面在解释器里做运行期校验。
  - 当前进展：`string.format("%q", math.mininteger)` 会输出可重新读回的 64-bit integer 形态；`%d` / `%i`、`%u` / `%x` / `%X` / `%o` 已按公开 `lua_Integer` / `lua_Unsigned` 宽度覆盖 `math.mininteger`、`math.maxinteger` 和 `-1` 的十进制/无符号/十六进制边界。
  - 当前进展：`string.format()` 的 Lua 5.4 非法格式错误已区分未知转换和非法规格；`%I` / `%U` / `%F` 这类未知转换保持 `invalid conversion '...' to 'format'`，`%3.1p` / `%#p` 等合法转换的非法 flag/precision 组合会报 `invalid conversion specification: '...'`。
  - 当前进展：tail-position Lua wrapper 中的 `return string.format(...)` 会在 Lua 5.4 兼容模式下保留调用帧，因此 formatter 解析/规格错误会按官方带源码调用点位置，不再被 tail call 优化擦掉；格式串由 `string.rep()` 等嵌套调用动态拼接、或经 local/upvalue alias 调用 `string.format` 时也会保留同一源码前缀。
  - 当前进展：`string.format()` 参数错误会按 Lua 5.4 恢复源级调用名；direct `pcall(string.format, ...)` 保持 `string.format`，源码字段调用显示 `format`，局部/upvalue alias 显示 alias 名。
  - 当前进展：`tonumber(str, base)` 的显式 base 越界错误已按官方 Lua 5.4 报到 `tonumber`，覆盖 base 为 1 和 37 的 `base out of range`，不再从内部参数检查显示为 `?`。
  - 已覆盖：base16 的 `0x` 前缀、base34 下 `x` 仍作为有效数字、base 参数 fraction number 报错、base 字符串数字转换、`tonumber` 拒绝 `inf` / `infinity` / `nan` 及大小写/符号/空白变体、`0b` / `0B` 及符号/空白变体、locale 小数点为逗号时 `tonumber("3,4")` 成功、`%d` 严格整数检查、`%c` fraction number 报错和字符串数字转换、`%q` nil/boolean/integer/float/negative-zero/NaN/Inf/string 基础输出、`%q` table/`__tostring` table 报错、`%p` nil/boolean/number/string/长字符串对象身份/width/left-align、`%a` / `%A` modifier 错误、invalid/too-long 格式项缺实参的 `no value` 优先级、`%s` NUL modifier、`%s` number JIT trace、`__tostring`、`__name` 和非法 `__tostring` 返回值边界、未知转换与非法格式规格错误文本。
  - 验证状态：`cmd /c build.bat lua54` 已通过，覆盖官方 Lua 5.4.8 `strings.lua` / `math.lua`、Lua 5.4 smoke 中的扩展数字拒绝、locale 数字、严格整数格式、`%q` / `%p` / `%a` / `%s` / invalid format 文本，以及 64-bit integer 格式边界；该条没有已知剩余缺口。

- [x] 字符串到数字的运算转换细节。
  - 当前状态：普通算术路径已在 Lua 5.4 dual-number 兼容构建下保留字符串数字转换后的 integer/float 子类型；`//` / `%` helper 已支持字符串数字并尊重显式 string metatable；bitwise helper 已禁止 string-to-integer coercion，默认对 string 操作数报 Lua 5.4 bitwise 错误，并在显式 string metatable 提供 `__band` / `__bor` / `__bxor` / `__bnot` / `__shl` / `__shr` 时优先调用元方法；Lua 5.4 兼容模式下通用字符串数字转换已拒绝 `inf` / `nan` / `0b` 等 LuaJIT 扩展数字文本。
  - 当前进展：普通算术中字符串无法转换且无元方法时，错误文本已按 Lua 5.4 报具体操作名和左右操作数类型，例如 `attempt to add a 'string' with a 'number'`。
  - 当前进展：字符串 metatable 显式提供 `__add` / `__mul` / `__unm` 时，会优先于字符串数字转换执行，覆盖 `"1" + 2`、`2 + "1"`、`"1" * 2` 和 `-"1"`。
  - 当前进展：JIT recorder 已停止把 string bitwise / shift 样本记录成整数 `BAND` / `BSHL` trace；string 操作数会退回解释器，由运行期 helper 统一执行元方法查找或 Lua 5.4 错误路径。
  - 对照结论：Lua 5.4 的字符串数字算术可转换，但位运算不可转换；当前 PC x64 Lua 5.4 compat dual-number gate 已覆盖这一分流。
  - 已覆盖：`"1" + "2"` / `"1" + 2` 的 integer 结果、`"1.0" + 2` / `"1e0" + 2` 的 float 结果、`"5" % "2"`、变量 integer `%`、integer `% 0` 错误和 `-"1"` 的 integer 结果、字符串算术元方法优先级、普通算术字符串失败路径、`//` 字符串失败路径、`"3" & 1`、`1 & "3"`、`~"3"`、`"1" << 2`、`"8" >> 1` 的 bitwise string 错误、string metatable bitwise/idiv/mod 元方法优先级、JIT string bitwise/shift 不记录 `BAND` / `BSHL`、`math.abs()` / `math.tointeger()` / `math.type()` 以及 C API 数字转换拒绝扩展数字文本，同时保留 `"1e9999"` 溢出为无穷大的 Lua 5.4 行为。
  - 验证状态：`cmd /c build.bat lua54` 已通过，覆盖 Lua 5.4 smoke、JIT 回归、官方 Lua 5.4.8 `math.lua` / `bitwise.lua` / `bwcoercion.lua` / `strings.lua`，该条没有当前 PC x64 兼容构建下的已知剩余缺口。

- [x] `string.gmatch` 的 `init` 参数和空匹配推进语义。
  - 当前状态：第三个 `init` 参数已按 Lua 5.4 规则处理正数、负数和越界起点。
  - 当前进展：`string.gsub` / `string.gmatch` 已按官方 Lua 5.4 的 `lastmatch` 规则处理空匹配，避免在上一轮空匹配的同一结束位置立刻再次匹配；`"a b cd"` + `" *"` 和 `()%s*()` 用例已进入 smoke。
  - 当前进展：replacement 字符串中的非法 capture 会报具体 `%0/%1/%2`，孤立结尾 `%` 和 `%x` 这类字母 escape 会报 `invalid use of '%' in replacement string`；`%b` 缺少两个参数时报 `malformed pattern (missing arguments to '%b')`。
  - 已覆盖：正数 init、负数 init、越界 init、带捕获和无捕获模式、空匹配推进、replacement capture 错误文本、`%b` 缺参数错误文本；本机 Lua 5.4.8 对照的负数起点探针和官方 `testes/pm.lua` 非长字符串身份部分已确认一致。

- [x] `warn()` 参数转换规则。
  - 当前状态：`warn()` 已按 Lua 5.4 兼容转换 string / number；`nil`、boolean、table 以及带 `__tostring` 的 table 仍按官方行为报 `string expected`，不会走 `__tostring`。
  - 当前进展：默认 warning 输出已补 `Lua warning: ` 前缀，`warn("@on")` 和 standalone `-W` 路径共用 `lua_warning()` 输出逻辑；`warn()` 的所有参数都会进入 `lua_warning()`，因此多段 warning 中的 `@on` / `@off` 会按普通文本输出，只有非 continuation 的新消息头才按控制消息处理。
  - 当前进展：`warn()` 的参数错误已复用 Lua 5.4 base 库调用点名规则，direct `pcall(warn, ...)` 会报 `bad argument #1 to 'warn'`，普通源码调用和局部别名仍按调用点报 `warn` / `f`。
  - 已覆盖：number 转换、boolean/nil/table/带 `__tostring` table 的类型错误、direct `pcall(warn)` / `pcall(warn, nil)` / `pcall(warn, true)` / `pcall(warn, table)` 的函数名、`@on`/`@off` 控制消息、未知 `@xxx` 控制消息、默认 warning 前缀、多段 warning 中的 `@off` 文本拼接。

- [x] `math.randomseed()` 无参和 PRNG 细节。
  - 当前状态：`math.randomseed(x, y)` 已接受两个种子并返回种子对；无参数调用会返回两个可转整数的非零占位种子，不再固定返回 `0, 0`。
  - 已覆盖：无参返回两个可用整数种子且不同时为零、带两个参数返回实际种子、`math.random(0)` 和整数区间随机的基础 Lua 5.4 表面。
  - 剩余边界：`math.random(0)` 和整数区间已覆盖 64-bit integer 返回；完整随机算法和统计均匀性仍需继续扩大长跑/跨平台验证。

- [x] `utf8` lax 模式。
  - 当前状态：基础 `utf8` 函数可用，`utf8.char()` 和源码 `\u{...}` 字面量都支持 Lua 5.4 的 `0..0x7fffffff` 扩展码点范围；`utf8.codes(s, true)` / `utf8.len(s, i, j, true)` 的宽松解码也已接受扩展码点和 surrogate 字节序列。
  - 当前进展：`utf8.codes` 迭代器、`utf8.codepoint` / `utf8.len` / `utf8.offset` 的边界错误文本和越界归属已按官方 `lutf8lib.c` 收紧；`utf8.offset` 按官方只扫描 continuation byte，不用 strict decode 阻断 lax 5/6 字节序列。
  - 当前进展：`utf8.charpattern` 保持官方内嵌 NUL 常量；string pattern 引擎已改为按 pattern end 指针解析，`find` / `match` / `gmatch` / `gsub` 可处理模式串中间的 `\0`。
  - 已覆盖：超过 `0x10ffff` 的扩展码点、surrogate 字节序列、严格模式报错、lax 模式成功返回码点、内嵌 NUL `charpattern`、官方 Lua 5.4.8 `testes/utf8.lua`；本机 Lua 5.4.8 对照探针已确认一致。

- [x] 字符串对象身份和 locale 语义。
  - 当前状态：PC x64 Lua 5.4 compat 构建已按官方短/长字符串边界分流，`LJ_STR_MAXSHORT=40` 以下继续内化，运行期长字符串会保留独立 `GCstr` 对象；源码常量和 chunk 内长字符串 literal 仍通过 `lj_str_new_intern()` 共享，满足官方 literal reuse 规则。
  - 当前进展：新增 `lj_str_equal()` 作为非内化长字符串的字节相等接口，`lj_obj_equal()`、table 字符串 key、x64/ARM64 `ISEQV` / `ISEQS` 字符串比较以及 x64/ARM64 长字符串 table 访问慢路径已接入；同内容长字符串的 `==` 和 table key 命中按字节一致，`string.format("%p")` 可观察到不同对象身份。
  - 当前进展：`string.gsub` 已区分“实际替换生成新字符串”和“所有匹配都保留原文/没有匹配”的对象身份；实际生成同内容长字符串时会得到新对象，未替换时复用原对象，符合官方 `pm.lua` 对 `%p` 的断言。
  - 当前进展：scanner 已支持 locale decimal point 为逗号时的 Lua 5.4 数字转换；字符串 `<` / `<=` 在 Lua 5.4 兼容模式下使用 `strcoll` 并按官方逻辑处理内嵌 NUL 分段；pattern 字符类 `%a/%l/%u/...` 和 `string.upper/lower` 已切到当前 C `ctype` locale；JIT recorder 已通过 side-effect `lj_str_cmp_locale()` helper 记录 locale-sensitive 字符串有序比较，避免旧 bytewise trace，并保持运行期读取当前 C collate locale。
  - 官方验证：PC x64 Lua 5.4 compat 构建下，官方 Lua 5.4.8 `testes/literals.lua`、`testes/strings.lua`、`testes/pm.lua` 当前已通过，且 `strings.lua` 已覆盖可用 `collate` / `ctype` locale 分支。
  - 当前进展：x86/ARM/MIPS/MIPS64/PPC 的 table string-key fast path 已在 Lua 5.4 模式下对运行期长字符串 key 和长字符串常量 key 退回通用 lookup/set 路径，不再用只适合短字符串 intern 的 `sid` 快路径；这些后端的字符串 equality fast path 也会在 Lua 5.4 模式下退到 `lj_meta_equal_lstr()`，由共用 helper 调用 `lj_str_equal()` 做字节比较，避免把非内化长字符串误判为不相等；新增 `test/lua54_vm_backend_static.lua` 并纳入 `smoketest-lua54compat`，防止非 x64/ARM64 后端漏掉这类退避。
  - 当前进展：JIT recorder 在 Lua 5.4 模式下遇到运行期长字符串 equality 时，已通过 `lj_str_equal` helper 在 trace 内做 bytewise compare，不再对第一次看到的两个长字符串 GC 对象做 identity guard；同一个热循环轮换多组不同对象但同内容的长字符串时，`==` / `~=` 结果保持 Lua 5.4 字节相等语义。运行期长字符串 table key load 已能记录 trace：recorder 通过 side-effect `lj_tab_getstr()` helper 做 bytewise lookup，命中后 guard value slot 和 raw TValue bits，避免旧 `HREF` / `HREFK` 录入非内化长字符串的对象身份假设。运行期长字符串 table key store 已改走普通 `IR_HSTORE` TValue 写入，因此 GC64 和非 GC64 TValue 布局都能记录 existing-slot 和 new-key 写入：已有 key 每轮重新 `lj_tab_getstr()`，缺失 key 通过 `lj_tab_setstr()` 统一处理 rehash 和 key barrier；GC 值写入会额外发出 `IR_TBAR` 保留 table barrier。`test/lua54_jit_regress.lua` 已覆盖不同 GC 对象但同内容的长字符串 equality、同内容长字符串 key 能在 trace 内命中同一 table slot、existing-slot store、new-key store、GC 值写入必须带 `IR_TBAR`；`test/lua54_perf.lua` 已纳入 JIT on/off 性能/内存 smoke，并覆盖 long-string equality、existing/new store 与 GC value store 负载。
  - 当前进展：x64 非 GC64 Lua 5.4 compat 构建已恢复：`lua_State` 的 Lua 5.4 close 标志不再破坏 `gclist` 与其它 GC 对象的固定偏移，新增正式 `build.bat lua54nogc64` / `smoketest-lua54compat-nogc64` 目标，覆盖非 GC64 下完整 `test/smoke.lua lua54compat`、`test/lua54_jit_regress.lua` 的长字符串 table-key store 记录路径和 Lua 5.4/JIT 标志探针；一槽 VM 已补齐 Lua 5.4 debug transfer / tailcall marker、`pcall` / `xpcall` 合成 C return hook、error unwind marker 清理，以及 yielding `__close` continuation。

- [x] `debug.getuservalue` / `debug.setuservalue` 的 indexed uservalue 语义。
  - 当前状态：Lua 5.4 兼容模式不再暴露 LuaJIT userdata 内部环境表；`debug.getuservalue(io.stdout, 1)` 返回 `nil`，`debug.setuservalue(io.stdout, {}, 1)` 返回 `nil`。
  - 当前进展：C API 已补 `lua_newuserdatauv` / `lua_getiuservalue` / `lua_setiuservalue`，用 LuaJIT userdata 环境表保存声明数量和值；超出声明范围返回 `LUA_TNONE` / `0`。
  - 当前进展：Lua debug 库已改为通过 `lua_getiuservalue` / `lua_setiuservalue` 访问 declared uservalue；对未声明槽位返回 `nil`，`setuservalue` 返回 `nil`。
  - 当前进展：full userdata 带 `__close` metatable 时已可作为 Lua `<close>` local 自动作用域退出，也可通过 C API `lua_toclose()` 标记后在 C 函数正常返回时关闭。
  - 已覆盖：无 declared uservalue 的内置 userdata、indexed 参数表面、`setuservalue` 返回值、C API 声明两个 user values 后的 get/set/out-of-range 行为，C 创建 userdata 后通过 Lua `debug.getuservalue` / `debug.setuservalue` 读写 declared slot，full userdata 的 `<close>` 自动作用域退出，以及 `lua_toclose()` 的显式/自动关闭路径。

- [x] 真实 Lua 5.4 GC 模式。
  - 当前状态：PC x64 Lua 5.4 compat 构建已从 mode 返回值 shim 收敛到真实 incremental/generational 双模式；`collectgarbage("generational")` / `"incremental"` 会保存 Lua 5.4 mode 参数，从 incremental 切回 generational 时按官方行为执行完整收集建立模式基线，并保留 stopped-GC 状态；generational 路径已具备 age/list 分代边界、minor/major 调度、barrier aging、weak/finalizer 处理和字符串表收缩回归。
  - 当前进展：Lua 5.4 兼容构建已为 GC 对象头增加独立 `age` 字段和 `new/survival/old0/old1/old/touched1/touched2` 宏，避免挤占 LuaJIT `marked` 位和 cdata 高位，为真正分代链表与 barrier 演进做结构准备。
  - 当前进展：generational 模式下 full cycle 完成后会扫描 root、finalizer 队列和字符串表，把存活对象 age 归为 `old`，建立后续 young/survival/old1 演进所需的 major baseline。
  - 当前进展：generational major baseline 已开始维护颜色语义：存活对象进入 `old` 后保持 black，线程留在 `grayagain` 监视列表，后续调度使用 minor multiplier；切回 incremental 或开始下一轮 full-major 近似收集前会 whitelist 全量对象并清理 generational gray/weak 列表。
  - 当前进展：generational 自动触发和手动 `step` 已从“每次 full-major 近似”推进到 young/minor 收集闭环：JIT idle 时，minor 会重访 `old1` / `touched1` / `touched2` 对象和被监视线程，字符串 intern 表不会再参与无效重访扫描；保存 trace 会通过 trace barrier 标记并提升到 `old0`，因此即使 JIT 已开启或关闭但仍存在 saved trace，手动 generational `step` 也能继续走 minor，不会因 trace 生命周期退化成 full-major；minor 只释放未标记 young 对象，并按 `new -> survival -> old1 -> old`、`touched1 -> touched2 -> old` 推进 age；manual `step(0)` 不再退化成 full-major，返回值按 Lua 5.4 generational 表面保持 false，且不会因为刚登记 table finalizer 的 `fin_check` 窗口而跳过 minor finalizer/weak-value 清理；显式 full collection 建立新的 generational baseline 后会清除旧的 table-finalizer `fin_check` 窗口，避免后续手动 minor 把新登记的 finalizable table 推迟到 state close；`touched2` 对象再次 barrier 时只改回 gray/`touched1`，不重复挂入 `grayagain`，但会挂入普通 `gray` 链以便下一轮 minor 开头传播新对象图，all-weak old table 的 minor 清理已对齐官方 `gengc.lua`；minor 调度保留上次 major 后的 estimate，并在内存超过 `genmajormul` 增长阈值时回到 major 路径；正在 trace 上执行或 trace compiler 非 idle 时仍保守推迟/走 major，避免在机器码/recording 边界执行 atomic/finalizer。
  - 当前进展：generational major 已补 Lua 5.4 `lastatomic` 对应的 bad-major continuation；当一次 major 回收效果不足时，后续 generational `step` 会继续走 full 路径，直到完整 atomic 阶段的遍历工作量按官方 9/8 规则回落，同时 `collectgarbage("generational", ...)` 会按官方模式切换语义清除该状态。
  - 当前进展：bad-major continuation 会在留在 full 路径时同步刷新 major baseline estimate，再按 Lua 5.4 的长 pause 逻辑调度下一次 major step。
  - 当前进展：weak table atomic 阶段已从单一 `weak` 链表拆成 Lua 5.4 对应的 weak-value / ephemeron / all-weak 三类处理路径；atomic 现在先清 weak values，再分离 pending finalizer 并传播 resurrection，最后清 ephemeron/all-weak keys 和复活弱表 value，覆盖 all-weak finalizer 回调可观察顺序。
  - 当前进展：JIT idle 且仍存在 saved trace 时，手动 generational `step` 已可执行真实 minor collection：young weak value 会被清理，old weak value 保留，已有 trace 之后继续启用/重新开启 JIT 仍可使用；当前正在 trace 上执行或 trace compiler 非 idle 时仍保守避免直接跑 minor atomic/finalizer。
  - 当前进展：generational minor 开始前会先传播写屏障已经挂入 `gray` 的新对象图；closed upvalue 写入 young 对象时会把目标提升为 `old0`，修复 old closed upvalue 持有 young table graph 后下一轮 minor 释放子对象的问题；C API smoke 已覆盖 old userdata 设置 young uservalue/metatable graph 后 minor 仍保留子对象，finalizable userdata 在 `__gc` 内触发 young collection 时 metatable 图仍存活，以及 userdata finalizer 从 weak table 中清理 weak key/value alias 的官方 `gc.lua` testC 路径。
  - 当前进展：open upvalue 在进入 generational baseline、切回 incremental whitelist 和 minor survivor aging 时都会随所属线程 open-upvalue 链处理；minor 周期保持 open upvalue 为 gray，使 atomic 的 upvalue remark 每轮继续标记栈槽对象图，避免运行中 Lua 帧的 open upvalue 在分代 minor 后丢失。
  - 当前进展：LuaJIT 写屏障已开始维护 Lua 5.4 age：old 非 table 对象 forward barrier 指向 young 对象时会把目标推进到 `old0`，old table backward barrier 会把 table 推进到 `touched1`。
  - 当前进展：generational minor 现在记录上一轮 survivor/old0/touched1 aging 是否产生了下一轮必须重访的 `old1` / `touched2` 候选；没有 pending 候选时不再扫描整条 root/mmudata 链，只传播写屏障挂入的 gray 新对象图，为后续按 Lua 5.4 分段链表收敛做准备。
  - 当前进展：root/all-GC 链已开始按 Lua 5.4 分代边界维护 `survival` / `old1` / `reallyold` / `firstold1`，minor collection 只 sweep new/survival 段并把 OLD1 段按边界重标记；Lua 5.4 构建下 userdata 也进入同一 root 链，避免年轻 userdata 绕过 minor 分段。touched table 通过 `grayagain`/weak-list correction 在 `touched1 -> touched2 -> old` 间收敛，不再依赖每轮全 root sweep 推进 age。
  - 当前进展：generational minor 收尾已补 Lua 5.4 `finishgencycle` / `checkSizes` 等价的短字符串表缩容；大量 young short string 被 minor 清理后不会一直保留过大的 intern table，回归用例固定 minor 与 full GC 后内存差距。
  - 当前进展：Lua 5.4 兼容构建的公开初始 `stepmul` 已对齐 Lua 5.4，`collectgarbage("setstepmul", n)` 首次返回 `100`；默认 LuaJIT 构建仍保留原 `LUAI_GCMUL`。
  - 当前进展：经本机 Lua 5.4.8 对照，`collectgarbage("minor")` / `"major"` 不是官方有效选项，当前 invalid option 行为已进入 smoke。
  - 当前进展：`setpause` / `setstepmul` 参数会按 Lua 5.4 公开表面压到 `0..1000`，并按 4 的粒度向下取整。
  - 当前进展：`step` / `setpause` / `setstepmul` 的第二参数已拒绝无整数表示的 number，仍接受字符串数字。
  - 当前进展：`generational(minormul, majormul)` / `incremental(pause, stepmul, stepsize)` 的可选整数参数已按官方 Lua 5.4.8 做类型和整数表示校验；`incremental` 会同步更新公开 `pause` / `stepmul` 配置，`generational` 会在进入模式前保存 minor/major 配置供后续分代调度使用；多余参数保持忽略，`__gc` finalizer 内合法 mode 调用仍返回 `nil`，非法可选参数仍会先报错。
  - 当前进展：Lua 5.4 C API 的 `lua_gc()` 在 `__gc` finalizer 内会按官方规则对所有 option 返回 `-1`，避免 C finalizer 重入驱动 collector。
  - 当前进展：已执行过 `__gc` 的 userdata 复活后，如果再次通过 `lua_setmetatable()` / `debug.setmetatable()` 赋带 `__gc` 的 metatable，会按 Lua 5.4 重新 armed 并可再次 finalization；新登记 userdata finalizer 会像 table finalizer 一样保持短期 allocation-driven GC 响应；不带 `__gc` 的 metatable 赋值仍不会重新 armed。
  - 当前进展：`incremental(..., stepsize)` 保存的 Lua 5.4 step-size 参数已接入 LuaJIT 兼容构建的实际 GC step 粒度；默认使用 Lua 5.4 的 `13`（8KB）调度基数，非 5.4 构建保留旧 LuaJIT `GCSTEPSIZE`；incremental 模式下 `collectgarbage("step", 0)` 已改为不消费旧 debt 的单次 basic step；generational 模式在 JIT 关闭且无 trace 对象时走 minor step，JIT 开启或存在 trace 对象时仍保守走 major。
  - 当前进展：Lua 5.4 compat 的 atomic 阶段会把完整 atomic 遍历工作量计入单步成本；incremental `step` 不再把 atomic 当作 0 成本后继续无界推进 sweep。
  - 当前进展：无调参 GC 命令的多余参数已按官方 Lua 5.4.8 忽略，覆盖默认 `collectgarbage(nil, extra)` 以及 `"count"` / `"collect"` / `"stop"` / `"restart"` / `"isrunning"`；`step` / `setpause` / `setstepmul` 仍只校验其第 2 个可选整数参数，mode 命令仍只校验官方定义的可选整数参数。
  - 当前进展：incremental 模式下 `collectgarbage("step", n)` 的完成标志继续按官方 Lua 5.4.8 收紧；负数 step 不会被无符号化成超大收集量，`step(0)` 在 pause/basic-step 完成边界返回 `true`，Lua 入口和 C API `lua_gc(L, LUA_GCSTEP, ...)` 均已覆盖。
  - 已覆盖：`generational`/`incremental` 参数和旧模式返回、mode 可选整数/字符串整数参数、mode 可选参数 fraction/boolean 错误、finalizer 内 mode 可选参数错误、`minor`/`major` invalid option、`setpause` 初始返回 `200`、`setstepmul` 初始返回 `100`，以及负数、非 4 对齐值、超过 1000、fraction number、string number 的参数边界。
  - 实现重点：PC x64 语义路径已收口；后续风险归入跨平台 artifact/runtime 验证和更长时间 profile 压力测试，不再作为本机 Lua 5.4 GC mode 功能缺口跟踪。

- [x] `debug.getinfo` 的 Lua 5.4 选项和 hook 字段。
  - 当前状态：`debug.getinfo(f, "u")` 已有 `nparams`/`isvararg`；`"t"` 选项已接受，普通 Lua tail call frame 会报告 `istailcall=true`。
  - 当前进展：`lua_Debug` 已按官方 Lua 5.4.8 外部布局补 `srclen`、`nparams`、`isvararg`、`istailcall`、`ftransfer`、`ntransfer` 字段，并把 `nups` / `nparams` 缩为 unsigned char、`isvararg` / `istailcall` 缩为 char、private `i_ci` 改为 pointer-sized slot；`lua_getinfo(..., "Sut")` 会填入 source length 和参数字段，普通 Lua tail call hook/return hook 会填入 `istailcall=true`，非 hook transfer 场景仍返回保守 `0/0`。
  - 当前进展：`debug.getinfo(..., "r")` 和 C API `lua_getinfo(..., "r")` 已接受 Lua 5.4 transfer-info 选项；非 hook 场景返回保守的 `ftransfer=0` / `ntransfer=0`，普通 Lua 函数（含 vararg）call/return hook已能报告参数和返回值 transfer 范围，普通 C 函数和 fast C 函数的 call/return hook 也能按实际参数/结果报告 transfer 范围。
  - 当前进展：C return hook 已新增统一 dispatch 入口并在 x64 / ARM64 VM 普通 C 返回路径接入；fast C 函数入口会保留原始参数 transfer 起点，fast return 路径会在结果槽仍可被 debug API 观察时发布 result transfer 窗口；C API smoke 已覆盖 `lua_gethook()` / `lua_gethookmask()` / `lua_gethookcount()` 在 set/clear hook 后的查询表面。
   - 当前进展：普通 Lua `BC_CALLT` 路径已在 x64 / ARM64 VM 中保存 side marker，`debug.getinfo(..., "t")`、Lua hook 和 C API hook 都能识别 `tail call` / `LUA_HOOKTAILCALL`，并在该 frame 返回时清理 marker，避免后续普通调用误判；vararg pseudo-frame 已会回溯到真实 Lua frame 判断 `istailcall`，因此 vararg tailcall return hook 也能报告 `istailcall=true`。
  - 当前进展：tail-call 到已知 C 标准库字段的局部/upvalue alias 已能恢复 Lua 5.4 调用点函数名；parser 会给 `local f = math.abs` / `local f = table.concat` 这类由标准库字段初始化的 alias 打标记，并在 `return f(...)` 时保留 caller frame，闭包 upvalue 也会传播该标记，因此 `local f=math.abs; return f(true)` 和 `local f=table.concat; return f(nil)` 均按官方报局部名 `f`。同一 no-tail 标记也已覆盖当前解析路径中可静态证明的 global alias，例如 `global_it = io.lines(...); return global_it()` 会保留全局名；如果 setup 子函数先写入 `global_abs = math.abs`，同一 chunk 内后续编译的 sibling 函数 `return global_abs(true)` 也会按官方报全局名。当前解析路径可静态定位的 table 字段 alias 也已覆盖，例如 `alias_table.f = math.abs` 后 `return alias_table.f(true)` 会按官方报字段名 `f`，且未标记的普通 `t.f()` 仍保持 tailcall；静态 global table alias 也已覆盖，例如 `g = alias_table; g.h = math.abs; return alias_table.h(true)` 和 `alias_table.j = math.abs; g = alias_table; return g.j(true)` 会按官方报字段名 `h/j`。字段名解析已覆盖大 chunk 常量表超过 `TGETS/TSETS` 8 位范围后的 `KSTR + TGETV/TSETV` 形态，并继续覆盖构造器字段、普通字段赋值里的简单局部字符串 key、匿名嵌套构造器和同一 chunk 内直接赋给全局的嵌套构造器，例如 `{inner={f=math.abs}}` 后 `return t.inner.f(true)`、`local k="f"; local t={[k]=math.abs}; return t.f(true)`、`local t={}; t[k]=math.abs; return t.f(true)` 和 `g={inner={f=math.abs}}; return g.inner.f(true)` 均按官方报字段名。更通用的 `BC_CALLT` 调用点元数据仍未建立，后续如果要在不牺牲 tail-call 的情况下覆盖复杂 table 重绑定或跨 chunk 动态 alias，还需要 VM frame 级 callsite 元数据。
  - 当前进展：Lua 5.4 兼容模式下，debug 库的 level/index/count 参数已改用严格整数检查；`debug.getinfo`、`getlocal`、`setlocal`、`getupvalue`、`setupvalue`、`upvalueid`、`upvaluejoin`、`sethook`、`traceback`、`getuservalue`、`setuservalue` 和 `setcstacklimit` 都会拒绝无整数表示的 number，同时保留字符串数字转换；完整 64-bit `lua_Integer` 会先被接受，再按官方 C API 的 int 参数边界传给底层 debug level/count/index 入口。
  - 当前进展：`debug.traceback()` / `luaL_traceback()` 的 Lua 5.4 traceback 文本已按官方处理无名 C frame，输出 `[C]: in ?`，不再泄露 LuaJIT 旧格式的原始 C 函数指针 `[C]: at 0x...`；默认 LuaJIT 5.1 traceback 形态不变。
  - 当前进展：debug 名字反推已覆盖 Lua 5.4 operator metamethod、table finalizer `__gc`、`for iterator` 和 hook 回调帧；stripped chunk 的 `currentline=-1`、upvalue `(no name)` 和 line hook `nil` 行号也已对齐。
  - 当前进展：标准库 C 闭包的内部 helper upvalue 已按 Lua 5.4 debug 表面隐藏，覆盖 `print` / `pairs` / `ipairs` 的 `debug.getupvalue` / `setupvalue` / `upvalueid`；`string.gmatch()` 返回的 runtime C iterator 仍保留官方可见 unnamed upvalue，`ipairs` auxiliary 改存 registry 后仍保持多次调用稳定相等。
  - 当前进展：stripped chunk 的错误定位已区分 Lua/C API 显式 `error()` 与 VM runtime 错误：`error("x")` / `luaL_error()` 不拼 `?:-1:`，但算术等 VM 运行期错误仍按官方显示 `?:-1:`。
  - 已覆盖：C API smoke 读取新增 `lua_Debug` 字段，并通过 `lua_sethook` / `lua_getinfo(..., "r")` 覆盖普通 Lua 函数固定参数和 vararg 的 hook transfer、Lua tail call hook transfer / `istailcall`、vararg tailcall return hook 的 `istailcall`、tail-position C return 不误报 Lua tail hook、普通 C 函数 call/return hook transfer，以及 `lua_gethook` / `lua_gethookmask` / `lua_gethookcount` 查询；Lua smoke 覆盖非 hook 场景和 `debug.getinfo(2, "r")` 的固定参数 / vararg call-return hook transfer、C 函数 call/return hook transfer、fast C 函数 return hook transfer、Lua tail call hook/return transfer、vararg tailcall return transfer、tail-position C return transfer、`pcall` / `xpcall` protected C return hook transfer、tailcall error unwind 后普通调用不误报 `istailcall`，以及直接调用清 marker。
  - 官方验证：Lua 5.4.8 `testes/db.lua` 已通过；后续若扩展 debug 元数据，只作为压力/平台矩阵补充，不再作为当前已知功能缺口。

- [ ] Lua 5.4 C API / 头文件兼容。
  - 当前状态：`lua.h` / `lauxlib.h` 已开始按官方 Lua 5.4.8 源码收紧外部表面，但仍有部分 C API 语义缺失或保持 LuaJIT 内部 ABI wrapper，例如真实 continuation、完整 64 位 integer ABI、逐字错误/ABI 边界；allocator shrink/grow、parser 模板表精确数组重分配、JIT trace buffer grow/record failure 主路径已进入 strict allocator 回归，剩余主要是跨平台/profile 验证。
  - 当前进展：已补 `lua_Unsigned`、`LUA_MAXINTEGER`、`LUA_MININTEGER`、`LUA_NUMTAGS`、`LUA_RIDX_LAST`、`LUA_EXTRASPACE`、`LUA_GNAME`、`LUA_FILEHANDLE`、`LUA_VERSUFFIX`、`LUAMOD_API`、`luaopen_coroutine`、Lua 5.4 外部 `luaopen_base()` 官方单返回 ABI、`lua_absindex`、`lua_isinteger`、`lua_isyieldable`、`lua_rawlen`、`lua_geti`、`lua_seti`、`lua_rawgetp`、`lua_rawsetp`、`lua_pushglobaltable`、`lua_arith`、`lua_compare`、`lua_len`、`lua_numbertointeger`、`lua_rotate`、`lua_stringtonumber`、`lua_getextraspace`、`lua_getallocf`、`lua_setallocf`、`lua_newuserdatauv`、`lua_newuserdata`、`lua_getiuservalue`、`lua_setiuservalue`、`lua_getuservalue`、`lua_setuservalue`、`lua_getmetatable`、`lua_setmetatable`、`lua_setwarnf`、`lua_warning`、`lua_version` Lua 5.4 value-returning wrapper、`lua_toclose`、`lua_closeslot`、`lua_resetthread`、`lua_closethread`；外部兼容头已把 `lua_call` / `lua_pcall` / `lua_yield`、`lua_upvalueindex`、`lua_getextraspace`、`lua_tonumber` / `lua_tointeger`、`lua_numbertointeger`、`lua_pushglobaltable`、`lua_insert` / `lua_remove` / `lua_replace`、`lua_newuserdata`、`lua_getuservalue` / `lua_setuservalue`、`lua_pushliteral` 以及常用栈/类型便利入口收紧为官方 macro-only 入口，并用负向编译 gate 防止取到旧 LuaJIT 函数地址。
  - 当前进展：`lua_setwarnf(L, NULL, NULL)` 已按 Lua 5.4 作为显式禁用 warning callback 处理，后续 `lua_warning("@on")` 和普通 warning 都不会回退默认 stderr writer；C API smoke 通过独立小程序和 stderr 空输出固定该语义。
  - 当前进展：`lua_stringtonumber()`、`lua_isnumber()`、`lua_tonumberx()`、`lua_tointegerx()` 和 `luaL_checknumber()` 已和 Lua 5.4 `tonumber()` 对齐，拒绝 `inf` / `nan` / `0b` 等 LuaJIT 扩展数字字符串。
  - 当前进展：Lua 5.4 兼容模式下 `lua_tointegerx()` 已对无整数表示的 number 返回失败状态；公开头宏 `lua_numbertointeger()` 则按官方 Lua 5.4 `luaconf.h` 语义只做范围检查并用 C cast 截断 in-range fraction，两者不能混用。`table.unpack` 等运行时长度检查已改回 `lua_tointegerx()` 精确整数路径。
  - 当前进展：已补 `LUA_VERSION_MAJOR`、`LUA_VERSION_MINOR`、`LUA_VERSION_RELEASE`、`LUA_VERSION_RELEASE_NUM`、`LUA_NUMTYPES`、`LUA_NUMTAGS` 头文件宏；兼容构建已按官方 Lua 5.4.8 暴露 `LUA_VERSION_RELEASE "8"`、`LUA_RELEASE "Lua 5.4.8"`、`LUA_VERSION_RELEASE_NUM 50408`、`LUA_COPYRIGHT`、`LUA_AUTHORS` 和外部 `lua_ident` 符号。
  - 当前进展：`test/lua54_lua_header_smoke.c` 已把 `LUA_SIGNATURE`、`LUA_VERSION*` / `LUA_RELEASE`、`LUA_VERSION_NUM` / `LUA_VERSION_RELEASE_NUM`、`LUA_MINSTACK`、`LUA_IDSIZE`、`LUA_RIDX_*`、`LUA_NUMTYPES` / `LUA_NUMTAGS`、`LUA_EXTRASPACE`、`LUA_MULTRET`、状态码、类型码、算术/比较 op、GC op 和 hook/mask 常量纳入 `lua.h` 单独包含的编译期静态门禁，防止核心官方常量只在 C API smoke 运行期被覆盖。
  - 当前进展：新增 `test/lua54_lua_guard_smoke.c` 并接入 `smoketest-capi-lua54compat`，固定预定义 `lua_h` 时必须像官方 Lua 5.4.8 一样跳过整份 `lua.h` 内容，不能泄露版本宏、签名常量、伪索引或类型 tag；本轮也用官方 5.4.8 `lua.h` 预处理宏表做名字级对照，当前外部 `LUA_` / `lua_` / `luai_` 公开宏名无缺失。
  - 当前进展：`test/lua54_lua_header_smoke.c` 已增加官方函数指针签名矩阵，覆盖 `lua_newstate` / `lua_close` / `lua_newthread` / `lua_closethread` / `lua_resetthread` / `lua_version`、栈操作、取值/设值、`lua_load` / `lua_dump`、`lua_callk` / `lua_pcallk` / `lua_yieldk` / `lua_resume`、warning、GC、debug hook/upvalue 和 `lua_setcstacklimit` 等外部 5.4 头表面，固定这些宏 wrapper 不会泄露 LuaJIT 旧 ABI 签名。
  - 当前进展：已补 `lua_KContext`、`lua_KFunction`、`lua_WarnFunction`、`LUA_KCONTEXT`、`luai_likely`、`luai_unlikely`、`LUA_RIDX_MAINTHREAD`、`LUA_RIDX_GLOBALS`、`LUA_LOADED_TABLE`、`LUA_PRELOAD_TABLE`、`LUA_HOOKTAILCALL`、`LUA_GCGEN`、`LUA_GCINC`；Lua 5.4 外部兼容头已隐藏旧 `LUA_HOOKTAILRET`；registry 中会写入主线程和全局表；`lua_gc()` 在兼容构建下已改为 Lua 5.4 变参签名；`lua_sethook()` 已按官方 Lua 5.4 外部头暴露为 `void` 返回表面；`lua_callk` / `lua_pcallk` / `lua_yieldk` 已按官方 Lua 5.4 头文件暴露为可取函数指针的真实导出函数，`lua_call` / `lua_pcall` / `lua_yield` 继续作为官方宏映射到 `*k(..., 0, NULL)`；`lua_yieldk()` 已覆盖 non-NULL continuation 的基础恢复路径，resume 参数会作为 continuation 栈参数，continuation 返回值会成为协程最终结果；continuation 内再次 `lua_yieldk()` 的 re-yield 路径也已覆盖，下一次 resume 会进入新保存的 continuation。
  - 当前进展：Lua 5.4 外部 `lualib.h` 已隐藏 LuaJIT 专有 `LUA_BITLIBNAME` / `LUA_JITLIBNAME` / `LUA_FFILIBNAME`、`luaopen_bit` / `luaopen_jit` / `luaopen_ffi` / `luaopen_string_buffer` 以及 wrapper 内部名 `luaopen_base54`，并在单独包含 `lualib.h` 时不再暴露 lauxlib-only 的 `LUA_GNAME` / `LUA_FILEHANDLE` / `lua_assert`；标准库 `luaopen_*` 已按官方 Lua 5.4 头文件使用 `LUAMOD_API` 声明，`LUAMOD_API` 直接定义为 `LUA_API`，`luaL_openlibs` 继续使用 `LUALIB_API`；默认构建和内部 ABI 继续保留 LuaJIT 扩展。
  - 当前进展：`test/lua54_lualib_header_smoke.c` 已按官方 Lua 5.4.8 `lualib.h` 固定 `LUAMOD_API`、`LUA_COLIBNAME` / `LUA_TABLIBNAME` / `LUA_IOLIBNAME` / `LUA_OSLIBNAME` / `LUA_STRLIBNAME` / `LUA_UTF8LIBNAME` / `LUA_MATHLIBNAME` / `LUA_DBLIBNAME` / `LUA_LOADLIBNAME`、`luaopen_base` / `luaopen_coroutine` / `luaopen_table` / `luaopen_io` / `luaopen_os` / `luaopen_string` / `luaopen_utf8` / `luaopen_math` / `luaopen_debug` / `luaopen_package` 和 `luaL_openlibs` 的单独包含编译表面；`test/lua54_lualib_guard_smoke.c` 进一步固定预定义 `lualib_h` 时必须跳过整份 `lualib.h` 声明，避免只暴露保护宏但没有真正保护头内容。
  - 当前进展：已按官方 Lua 5.4.8 `luaconf.h` / `lua.h` 对齐外部 `LUAI_IS32INT`、`LUAI_MAXSTACK`、`LUA_REGISTRYINDEX (-LUAI_MAXSTACK - 1000)` 和 `lua_upvalueindex(i) (LUA_REGISTRYINDEX - (i))` 公式；运行时 `index2adr` 会识别官方 registry/upvalue 伪索引并映射回 LuaJIT 内部旧 ABI，默认构建不变。
  - 当前进展：已补 `LUA_COMPAT_APIINTCASTS` 下的 `lua_pushunsigned` / `lua_tounsignedx` / `lua_tounsigned` / `luaL_checkunsigned` / `luaL_optunsigned` 兼容宏，并用单独 C smoke 覆盖编译、基础运行、负整数/负整数字符串按官方 cast 转成 `LUA_MAXUNSIGNED`、fraction number 的 `lua_tounsignedx()` status 和 `luaL_checkunsigned()` / `luaL_optunsigned()` 错误、`lua_pushunsigned()` 的 64-bit integer subtype/signed storage、`2^32` 级别的 64 位 `lua_Unsigned` 往返/string check/default，以及 `LUA_MAXUNSIGNED` 通过官方 cast 宏回绕的边界；普通外部 Lua 5.4 头不定义 `LUA_COMPAT_APIINTCASTS` 时会隐藏这些 unsigned cast 宏，负向编译 gate 已同步覆盖；定义 `LUA_COMPAT_APIINTCASTS` 后，unsigned/int/long cast 入口也已纳入 macro-only 负向编译 gate，防止外部模块取到旧 LuaJIT 函数地址。
  - 当前进展：已补 Lua 5.4 `luaconf.h` 数字输出、路径、平台、API 标记和整数配置宏：`LUA_INT_TYPE` / `LUA_FLOAT_TYPE` 选项族、`LUA_32BITS`、`LUA_C89_NUMBERS`、`LUA_NUMBER_FRMLEN`、`LUA_INTEGER_FRMLEN`、`LUA_INTEGER_FMT`、`LUAI_UACINT`、`LUA_UNSIGNED`、`LUA_MAXUNSIGNED`、`LUA_PATH_SEP`、`LUA_EXEC_DIR`、Windows 下官方 `LUA_USE_WINDOWS` / `LUA_USE_C89` / `LUA_DL_DLL`、`l_sprintf` / `l_mathop` / `l_floor` / `l_floatatt`、`lua_str2number()` / `lua_strx2number()` / `lua_pointer2str()` / `lua_number2strx()` / `lua_getlocaledecpoint()`、三参数 `lua_number2str()` 和 `lua_integer2str()`，以及 `LUAMOD_API` 直接定义为 `LUA_API`；Windows/C89 下 `l_mathop(floor)`、`lua_str2number()` 和 `lua_strx2number` 暴露形态已按官方 Lua 5.4.8 头文件固定；外部 Lua 5.4 头不再泄露 LuaJIT 旧 `LUA_PATHSEP` / `LUA_EXECDIR`、standalone/package 私有的 `LUA_PATH` / `LUA_CPATH` / `LUA_INIT` 环境变量名宏，以及 `LUA_PATH_CONFIG`、`LUA_QL` / `LUA_QS`、`LUA_MAXCAPTURES`、`LUAI_MAXCSTACK`、`LUAI_GCPAUSE` / `LUAI_GCMUL`、`LUA_NUMBER_DOUBLE` / `LUA_NUMBER_SCAN`、`LUAI_MAXNUMBER2STR`、`LUA_INTFRMLEN` / `LUA_INTFRM_T`、默认 `luai_apicheck` 等 LuaJIT/Lua 5.1 私有配置宏；显式定义 `LUA_USE_APICHECK` 时仍会按官方 Lua 5.4 暴露 `luai_apicheck`。`lua_KContext` 在 Windows 兼容宏生效时保持官方 `ptrdiff_t` 形状，`lua_Unsigned` / `LUA_UNSIGNED` 头文件宽度已与 `lua_Integer` 对齐，`lua_integer2str()` 也不再截断外部 C 侧 64 位 `lua_Integer` 值。PC x64 compat runtime 的 `LUA_MAXINTEGER`、TValue integer subtype、标准库格式化和运算已按 64-bit integer 表面运行；剩余 64 位 ABI 风险集中在 C API 深边界和目标平台 artifact/runtime 验证。
  - 当前进展：新增 `test/lua54_luaconf_guard_smoke.c` 并接入 `smoketest-capi-lua54compat`，固定预定义 `luaconf_h` 时必须像官方 Lua 5.4.8 一样跳过整份 `luaconf.h` 内容，不能泄露数值配置、路径配置、平台配置或内部限制宏。
  - 当前进展：`src/Makefile` 已为 LuaJIT host/target 内部对象统一定义 `LUAJIT_INTERNAL_USE`，把内部源码和外部 C 模块的 Lua 5.4 头文件边界分开；新增 `test/lua54_luaconf_extra_reject.c` 和 `test/lua54_luaconf_apicheck_smoke.c` 并接入 `smoketest-capi-lua54compat`，固定外部 Lua 5.4 模块默认不能看到上述私有配置宏，同时保留 `LUA_USE_APICHECK` 的官方条件宏形态。`luajit.c` 的本地 internal marker 也已改成条件定义，避免和构建系统 marker 重复产生 warning。
  - 当前进展：新增 `test/lua54_header_macro_audit.lua` 并接入 `smoketest-capi-lua54compat`，运行时用 gcc 预处理官方 Lua 5.4.8 `lua.h` / `lauxlib.h` / `lualib.h` 和当前兼容头，固定当前外部 Lua 5.4 头没有缺失官方公开宏名；额外宏名只允许 `lua_load` / `lua_resume` / `lua_gettable` / `lua_version` 等已知 ABI wrapper 宏和构建开关宏，防止后续头文件改动重新泄露未记录的私有表面。该审计脚本已修正 Lua pattern 不支持 `|` alternation 导致空集合假绿的问题，并把临时输入移出 `src/`，避免 quoted include 搜索顺序把官方侧误导到本仓库头文件；`LUAJIT_EXTERNAL_LUA54` 已改为 `lua.h` / `lauxlib.h` / `lualib.h` 内部临时 selector，用完即 `#undef`，并由三个头文件 smoke 和宏名审计固定不能泄露到外部模块。
  - 当前进展：`lua.hpp` 已按 Lua 5.4 外部头边界收紧；定义 `LUAJIT_ENABLE_LUA54COMPAT` 时只包装官方三件套 `lua.h` / `lauxlib.h` / `lualib.h`，不会再经 `luajit.h` 暴露 `LUAJIT_VERSION` 等 LuaJIT 专有 C++ 头表面；默认 LuaJIT 模式仍包含 `luajit.h`。新增 `test/lua54_lu.hpp_header_smoke.cpp` 和 `test/lua51_lu.hpp_header_smoke.cpp` 分别固定 compat/default 两种 C++ wrapper 表面。
  - 当前进展：Lua 5.4 外部兼容头只有在显式定义 `LUA_COMPAT_APIINTCASTS` 时才暴露 deprecated `luaL_checkint` / `luaL_optint` / `luaL_checklong` / `luaL_optlong`；默认 LuaJIT 头仍保留这些旧宏，负向编译 gate 已覆盖普通 5.4 头不泄露它们。
  - 当前进展：Lua 5.4 外部兼容头已支持官方 `LUA_COMPAT_5_3` 总开关，会同步打开 `LUA_COMPAT_MATHLIB` / `LUA_COMPAT_APIINTCASTS` / `LUA_COMPAT_LT_LE`，并按官方兼容层暴露 `lua_strlen` / `lua_objlen` / `lua_equal` / `lua_lessthan` 以及旧整数 cast 宏；新增 `test/lua54_capi_compat53_smoke.c` 编译/运行 gate 覆盖该表面。
  - 当前进展：使用 `LUA_COMPAT_5_3` / `LUA_COMPAT_MATHLIB` 构建 Lua 5.4 compat runtime 时，`math.atan2` / `pow` / `log10` / `sinh` / `cosh` / `tanh` / `frexp` / `ldexp` 会按官方兼容数学库保留；使用 `LUA_COMPAT_5_3` / `LUA_COMPAT_LT_LE` 构建时会恢复 `__le` 缺失时的 `__lt` fallback；默认 Lua 5.4 compat 构建仍隐藏这些 deprecated 入口并禁用旧比较 fallback。新增 `test/lua54_compat53_runtime.lua` 和 `build.bat lua54compat53` 专门验证该构建面。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_newuserdata()`、`lua_getuservalue()`、`lua_setuservalue()` 暴露为官方 slot 1 alias 宏，默认构建仍保留 LuaJIT 旧函数 ABI。
  - 当前进展：Lua 5.4 兼容头已把外部 `lua_resume(L, from, nargs, nresults)` 映射到 `lua_resume54()` 包装入口，并支持按官方 `lua_resume` 名取函数指针；内部仍保留 LuaJIT 旧 2 参数 ABI，包装入口会填写 yield/return 的结果数量，且执行错误、`lua_yieldk()` continuation 错误、`lua_callk()` yield 后错误和 C return close-yield 错误都会按栈上错误对象回填 `nresults`，dead coroutine 这类早期 resume 错误保持当前兼容行为。
  - 当前进展：Lua 5.4 默认外部兼容头已隐藏 `LUA_ENVIRONINDEX`、`LUA_GLOBALSINDEX`、`LUA_PATHSEP`、`LUA_EXECDIR`、`LUA_PATH`、`LUA_CPATH`、`LUA_INIT`、`LUA_PATH_5_4`、`LUA_CPATH_5_4`、`LUA_INIT_5_4`、`lua_strlen`、`lua_open`、`lua_getregistry`、`lua_getgccount`、`lua_Chunkreader`、`lua_Chunkwriter`、`lua_setlevel`、LuaJIT-only `lua_loadx` 以及旧 `lua_equal`、`lua_lessthan`、`lua_objlen`、`lua_cpcall`、`lua_getfenv`、`lua_setfenv` 声明；`lua_Debug` 外部布局已补 `srclen` 和 Lua 5.4 字段尺寸/顺序；LuaJIT 内部和默认构建通过内部标记继续使用旧 ABI，负向编译 gate 已覆盖这些旧 C API 不从外部 5.4 头泄露。
  - 当前进展：Lua 5.4 外部兼容头已隐藏旧 lauxlib 注册入口 `luaL_openlib` / `luaL_register` / `luaL_pushmodule`、旧 `luaL_typerror`、旧 `luaL_putchar` 以及 LuaJIT helper `luaL_findtable`；默认构建仍通过 C API smoke 覆盖这些 LuaJIT/Lua 5.1 API。
  - 当前进展：外部兼容头下的 `lua_pushglobaltable()` / `lua_getglobal()` / `lua_setglobal()` 已改走 registry globals 表，不再依赖 `LUA_GLOBALSINDEX`；`lua_pushglobaltable()` 宏已按官方 Lua 5.4 暴露为 `void` 返回表面。
  - 当前进展：外部兼容头下的 `lua_gettable()`、`lua_getfield()`、`lua_geti()`、`lua_rawget()`、`lua_rawgeti()`、`lua_rawgetp()` 已通过 `*54` 包装入口返回取到值的 Lua 类型，并用官方函数名的源级别别名支持外部代码按 Lua 5.4 签名取函数指针；内部仍保留 LuaJIT 旧 `void` ABI。
  - 当前进展：外部兼容头下的 `lua_geti()` / `lua_seti()` / `lua_rawgeti()` / `lua_rawseti()` 索引参数已通过 Lua 5.4 表面暴露为 `lua_Integer`，并支持按官方函数名取函数指针；非 32 位 key 会走 generic/raw key 路径，避免截断回 LuaJIT 当前 32 位整数表槽。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_load(L, reader, data, chunkname, mode)` 映射到五参数 `lua_load54()` wrapper，并支持按官方 `lua_load` 名取函数指针；内部旧四参数 `lua_load()` 和 LuaJIT 扩展 `lua_loadx()` ABI 保持不变，但外部 5.4 头不再暴露 `lua_loadx`。C API smoke 覆盖 text reader 模式加载、binary-only 模式拒绝 text chunk、LuaJIT binary reader 模式加载以及 text-only 模式拒绝 binary chunk，负向编译 gate 覆盖 `lua_loadx` 不泄露。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_dump(L, writer, data, strip)` 映射到 `lua_dump54()`，并支持按官方 `lua_dump` 名取函数指针；内部旧 3 参数 `lua_dump()` ABI 保持不变，`strip` 会转发到 LuaJIT bytecode writer，writer 回调返回的非零失败码会按 C API 返回值传播；非 Lua 函数返回 `1` 且不会调用 writer。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_insert()` / `lua_remove()` / `lua_replace()` 暴露为官方宏，映射到 `lua_rotate()` / `lua_copy()`，默认构建仍保留 LuaJIT 旧函数 ABI。
  - 当前进展：Lua 5.4 外部兼容头已通过 `lua_pushlstring54()` / `lua_pushstring54()` wrapper 暴露 `lua_pushlstring()` / `lua_pushstring()` 的官方返回值表面，并支持按官方函数名取函数指针；默认构建仍保留 LuaJIT 5.1 的 `void` 头文件 ABI。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_tonumber()` / `lua_tointeger()` 暴露为官方宏，映射到 `lua_tonumberx(..., NULL)` / `lua_tointegerx(..., NULL)`；默认构建仍保留 LuaJIT 旧函数 ABI。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_getextraspace()` 暴露为宏，并映射到当前 pointer-sized `L->exdata` 兼容存储；`lua_newstate()` / `lua_getallocf()` / `lua_setallocf()` 自定义 allocator、`lua_getallocf(L, NULL)`、`lua_atpanic()`、`lua_pushthread()`、`lua_checkstack()`、`lua_settop()`、`lua_pushvalue()`、`lua_concat()`、`lua_next()`、`lua_error()`、`lua_type()`、`lua_typename()`、`lua_rawequal()`、`lua_iscfunction()`、`lua_tocfunction()`、`lua_touserdata()`、`lua_topointer()`、`lua_toboolean()` 和 light/full userdata 判定表面已进入 C API smoke；默认构建仍保留 LuaJIT 旧函数 ABI。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_rawlen()` 暴露为 `lua_Unsigned` 返回值表面，且 `lua_Unsigned` 头文件宽度已与 `lua_Integer` 对齐；内部和默认构建继续保留 LuaJIT 旧 `size_t` ABI。完整 64 位 table/string 长度、integer subtype 和 C API runtime 边界仍归入整数语义大项。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_version` 暴露为官方 value-returning 函数 wrapper，支持按 `lua_Number (*)(lua_State *)` 取函数指针，并覆盖 `lua_version(NULL)`；内部和默认构建仍保留 LuaJIT 旧指针 ABI。
  - 当前进展：Lua 5.4 外部兼容头已把 `lua_pushliteral()` 改为官方 `lua_pushstring(L, "" s)` 宏语义，包含内嵌 NUL 的 literal 会按 C 字符串截断；默认构建保留 LuaJIT 旧 `lua_pushlstring(sizeof)` 行为。
  - 当前进展：Lua 5.4 兼容头已声明现有 `lua_copy()`，C API smoke 覆盖把一个栈槽复制到另一个栈槽。
  - 当前进展：`lua_pushfstring()` / `lua_pushvfstring()` 已支持 Lua 5.4 C API 专用 `%I` / `%U` / `%f` / `%p` 表面；C API smoke 覆盖 `%I` 按 `lua_Integer` 输出并固定 `2^40`、`LUA_MAXINTEGER` 和 `LUA_MININTEGER` 不截断，`luaL_error()` 通过同一 `lua_pushvfstring()` 路径格式化 `2^40` 时也保持公开整数宽度，`%U` 输出 UTF-8 codepoint、`%f` 复用 raw number-to-string 并保留 `1.0`，`%p` 按宿主 C `%p` 指针 ABI 输出而不是 LuaJIT 内部 pointer formatter，同时固定非法 `%Z`、带 modifier 的 `%04d`、以及旧 LuaJIT 扩展 `%u` / `%g` 都会抛出 `invalid option ... to 'lua_pushfstring'`；Lua smoke 反向固定 `string.format("%I")` / `"%U"` 仍为非法转换。
  - 当前进展：`lua_arith()` 的 Lua 5.4 C API 字符串运算表面已补齐元方法优先级：显式 string metatable 的 `__add` / `__idiv` 会先于字符串数字转换执行；`LUA_OPBOR` / `LUA_OPBNOT` 不再把字符串整数强转为位运算 operand，无 string metatable 时保持 bitwise string 错误，有显式 `__bor` 时调用 string metatable 元方法。
  - 当前进展：已补 `lua_setcstacklimit()` C API shim，和 Lua 层 `debug.setcstacklimit()` 一样返回稳定兼容上限。
  - 当前进展：`LUA_GCCOUNTB` 已进入 C API smoke，覆盖返回 0..1023 byte remainder 的基础契约；兼容构建下 `lua_gc(L, what, ...)` 可用官方变参调用形态，并已覆盖 `LUA_GCSTOP` / `LUA_GCRESTART` / `LUA_GCCOLLECT` / `LUA_GCCOUNT` / `LUA_GCCOUNTB` / `LUA_GCSTEP` / `LUA_GCSETPAUSE` / `LUA_GCSETSTEPMUL` / `LUA_GCISRUNNING` / `LUA_GCGEN` / `LUA_GCINC` 的基础返回和参数表面。
  - 当前进展：GC 机会性 shrink 已按 Lua 5.4 allocator 语义改成可失败路径；自定义 allocator 拒绝缩小线程栈或 `string.rep` 撑大的 GC 临时 buffer 时，运行时会保留原块并让 `collectgarbage()` 继续成功。C API smoke 已明确断言 tmpbuf shrink 失败的旧块超过 1MB，避免该路径只被栈 shrink 间接覆盖；同一 smoke 还覆盖 `lua_setallocf()` 切换 allocator 后 `lua_getallocf()` 的函数/ud 表面，以及后续 openlibs、chunk allocation 和 `lua_close()` 释放会经过新 allocator。table rehash 中的数组部分缩小已改为先分配目标数组/hash、全部成功后再切换 table 状态，避免 allocator 拒绝原地 shrink 时留下半迁移 table；C API smoke 用严格 allocator 固定 block size/free size 一致。parser 模板表精确数组重分配和 JIT buffer 等其它逻辑尺寸必须改变的路径也已进入 strict allocator 回归。
  - 当前进展：C API smoke 的严格 allocator 已覆盖 `lua_newstate()` 部分初始化失败路径，会在初始化不同分配点强制失败并检查返回 `NULL` 后没有遗留 block、没有错误 `osize`、也没有未知指针释放；运行期短字符串 intern 分配失败会以 `LUA_ERRMEM` 收敛，字符串表机会性 resize 失败会保持非致命成功路径，二者的 `lua_close()` 后账本清零也已纳入覆盖；正常创建后 `lua_close()` 的 allocator 释放表面仍同步覆盖。
  - 当前进展：严格 allocator 继续覆盖 `lua_newthread()` 部分初始化失败路径，会在新线程对象和初始栈分配之间强制失败，确认 `LUA_ERRMEM` 展开、后续 full GC 和最终 `lua_close()` 都不会留下 partial thread block、错误 `osize` 或未知指针释放。
  - 当前进展：严格 allocator 已覆盖 `lua_newuserdatauv()` 的 uservalue table / `__nuv` key / userdata 分配链路中途失败，确认 `LUA_ERRMEM` 展开、栈恢复和 full GC 后不会留下 partial userdata/uservalue block、错误 `osize` 或未知指针释放。
  - 当前进展：严格 allocator 已覆盖 parser/load 路径的 table constructor chunk 编译中途失败，`luaL_loadbufferx()` 返回或抛出 `LUA_ERRMEM` 后会恢复栈并在 full GC 后清空 partial Proto/KGC/常量表分配，allocator 账本不出现错误 `osize` 或未知指针释放；同一测试现在会继续扫到无隐藏 allocator 失败的成功加载边界，确认当前 parser 分配窗口内的失败点都被注入过，而不是只覆盖早期分配；本轮进一步覆盖先创建 hash-only 模板表、末尾再精确分配数组部分的 table constructor 路径，固定 parser 末尾 `lj_tab_reasize()` 分配失败时也会释放临时块并保持 allocator 账本一致；同一测试修正了 partial Lua closure 在 upvalue 尚未初始化完成时按 `nupvalues=0` 释放、导致释放尺寸小于实际分配尺寸的问题，GC 计量和释放都改为以 prototype 的 `sizeuv` 计算 Lua closure 分配尺寸。严格 allocator 也已覆盖运行期 table 数组/hash 增长中途一次性分配失败，确认 `LUA_ERRMEM` 展开和 full GC 后不会留下 partial table block、错误 `osize` 或未知指针释放；table array shrink 重分配路径也已补失败回归，强制新 array 或新 hash 分配失败时会保持旧表内容且释放临时块。严格 allocator 还覆盖 JIT trace 记录、GCtrace 保存、`jit.flush()` 和 full GC 释放路径，并继续覆盖 JIT 记录/保存期间一次性 allocator 失败会只 abort trace、保留解释器结果且不破坏 IR/snapshot/mcode 临时 buffer 账本；失败后 full GC 还会断言 live block 账本不增长且至少触发过 partial cleanup，确保 JIT trace 相关分配不会用错误 `osize`、未知指针释放或悄悄泄漏。
  - 当前进展：`luaL_Buffer` 大缓冲已改为 Lua 5.4 同类的栈上 to-be-closed userdata box；`luaL_buffinit()` 压 placeholder，扩容时替换成带 `__gc` / `__close` 的 box，正常 `luaL_pushresult()` 显式关闭，错误 unwind 也会释放侧缓冲。C API smoke 用自定义 allocator 统计 live bytes，固定扩容后在 `luaL_pushresult()` 前抛错时，`lua_close()` 后不残留大缓冲分配；严格 allocator 也覆盖了 box 侧缓冲 resize/free 的 `osize` 必须匹配真实块大小、二次扩容失败时旧侧缓冲必须随 error unwind 关闭，以及 `luaL_addvalue()` 消费大字符串触发侧缓冲后 `luaL_pushresult()` 会关闭临时 box 并在 GC 后恢复 allocator 账本。
  - 当前进展：已补 lauxlib 常用 Lua 5.4 表面：`luaL_pushfail`、`luaL_len`、`luaL_getsubtable`、`luaL_requiref`、`luaL_tolstring`、`luaL_typeerror`、`luaL_argexpected`、`luaL_argcheck`、`luaL_checkstring`、`luaL_optstring`、`luaL_checkany`、`luaL_checktype`、`luaL_checkstack`、`luaL_checkoption`、`luaL_opt`、`luaL_optnumber`、`luaL_checkversion` 以及 buffer API；`luaL_checkversion_()` 现在会实际校验版本号和 numeric ABI 尺寸。
  - 已覆盖：新增 `test/lua54_capi_smoke.c` 和 `make smoketest-capi-lua54compat`，包含 `lua_isyieldable()` 主 C frame / resumed coroutine C frame 表面；`lua_resume()` 的 Lua 5.4 wrapper 会在 `from == NULL` 时继续返回 yield/return 结果数量并保留栈结果；`lua_yieldk()` non-NULL continuation 的 yield 结果、resume 参数、`LUA_YIELD` status、`lua_KContext`、最终返回值、continuation 抛错返回错误对象、continuation 内再次 `lua_yieldk()` 后继续 resume，以及 `lua_closethread()` 清理 pending continuation；`lua_callk()` / `lua_pcallk()` 在被调函数未 yield 时不会调用 non-NULL continuation，且 `lua_pcallk()` 的普通错误返回同样不会触发 continuation；`lua_callk()` / `lua_pcallk()` 的 yielding callee 会在第一次 resume 返回 `LUA_YIELD` 和 yielded values，第二次 resume 会进入 C continuation 并接收 callee 返回值；`lua_callk()` / `lua_pcallk()` 的 `LUA_MULTRET` yielding callee 会把完整多返回和 nil 洞交给 continuation；`lua_callk()` / `lua_pcallk()` 的 continuation 内再次 `lua_yieldk()` 的三段 resume 流程已覆盖；`lua_callk()` 的 callee 在 yield 后抛错时会按未保护调用传播错误且不调用 continuation；`lua_pcallk()` 的 callee 在 yield 后抛错时，continuation 会收到 `LUA_ERRRUN` 和错误对象，且带 message handler 的 `lua_pcallk()` 会先转换错误对象再进入 continuation；message handler 自身出错时会按 `LUA_ERRERR` 和 `error in error handling` 进入 continuation；`lua_getmetatable` / `lua_setmetatable` 的 Lua 5.4 返回值和栈效果；`lua_getiuservalue` / `lua_setiuservalue` 对非 userdata 的 `LUA_TNONE` / `0` 行为和栈效果；`lua_getupvalue()` / `lua_setupvalue()` / `lua_upvalueid()` / `lua_upvaluejoin()` 的 upvalue 名称、值、无效索引不压栈、id 查询和 join 后共享槽位行为。
  - 当前进展：`lua_upvalueid()` / `lua_upvaluejoin()` 的非法函数栈索引、非函数值、非 Lua 函数 join 和无效 upvalue 编号已从 debug-only `lj_checkapi` 收敛为 release 可见 `invalid value`；C API smoke 同时覆盖真实 Lua upvalue 从 Lua 5.4 `_ENV` 源 join 时会取得当前全局表值。
  - 当前进展：`lua_gettable()` 缺 key 以及 `lua_gettable()` / `lua_getfield()` / `lua_settable()` / `lua_setfield()` / `lua_setmetatable()` / `lua_getiuservalue()` / `lua_setiuservalue()` 的非法对象索引已补 release 可见 `invalid value` 边界，避免继续依赖 debug-only stack slot 断言；`lua_getmetatable()` 继续保持 `luaL_typeerror()` 缺参诊断所需的 `LUA_TNONE` 探测语义。
  - 当前进展：`lua_xmove()` 已补跨独立 `lua_State` 的 release 可见 `invalid value` 边界，C API smoke 覆盖 `n == 0` 也不能跨 global state 转移，避免 release 构建继续只靠 `lj_checkapi`。
  - 当前进展：已补 `lua_closethread()` / `lua_resetthread()` 的 `<close>` 基础表面，覆盖 yielded/fresh coroutine 返回 `LUA_OK`、清空栈并恢复 OK 状态，且会关闭 suspended coroutine 中的 active close locals；`lua_resume()` 已按 Lua 5.4 处理空栈 dead coroutine，不再把已关闭/reset 的线程当作 nil 函数调用；已补 `lua_toclose()` / `lua_closeslot()` C API 表面，覆盖 closable 校验、false/nil `lua_toclose` 跳过、`lua_closeslot` 只接受最后一个 active marked slot、显式 `__close(value, nil)` 后置空槽位、`lua_settop` / `lua_pop` 弹栈关闭、C 函数错误展开关闭，以及 C 函数正常返回自动关闭。
  - 当前进展：`lua_callk` / `lua_pcallk` 的 yielding callee 基础恢复已补；非 NULL continuation 且当前 C frame 可 yield 时会使用 resume-style VM 调用，避免普通 `vm_call` 的不可 yield C frame，首次 yield 重新抛给外层 coroutine，后续 `lua_resume54()` 先恢复 Lua callee，完成后再调用保存的 C continuation。
  - 当前进展：Lua 5.4 C continuation 恢复路径已改为用 stack offset 跨 `lua_pcallk()` message handler 和 continuation protected call 保存结果基址，避免 message handler/continuation 中 `lua_checkstack()` 触发 coroutine stack 重分配后继续写旧 `TValue *`；`lua_resume54()` 返回前也会把当前 VM 线程恢复到 `from`，C API smoke 用 message handler 错误路径后的 full GC 和新 coroutine 分配固定该边界。
  - 剩余边界：跨非 x64/arm64 平台的同批验证仍需继续补强；当前实现仍是 C API wrapper 配合 resume-style VM 调用，不是最终统一的 VM continuation frame 设计。
  - 需要补常量/类型/宏：继续核对完整 ABI 细节。
  - 需要清理/兼容旧 API：默认构建保留 LuaJIT/Lua 5.1 API；Lua 5.4 外部兼容头已隐藏一批旧 5.1/LuaJIT 表面、旧 lauxlib 注册入口、`luaL_typerror`、`luaL_putchar` 和 `luaL_findtable`，并补了常见 getter/number 转换/字符串 push 返回值签名和栈操作宏表面；负向编译门禁也覆盖 `lua_strlen`、`lua_open`、`lua_getregistry`、`lua_getgccount`、`lua_Chunkreader` 和 `lua_Chunkwriter` 不会泄露到外部 Lua 5.4 头。仍需继续核对更多旧兼容宏和完整 ABI 细节。
  - 需要补内存分配语义：table resize 的 grow 与 array shrink 分配失败、parser 模板表精确数组重分配、JIT trace buffer grow/record failure 已有严格 allocator 回归；后续主要补更多平台和更大 profile。
  - 当前进展：已补默认构建 C API smoke，覆盖旧 LuaJIT 5.1 头文件宏和 ABI 入口仍可编译、链接、运行。
  - 需要补测试：更完整 ABI 兼容测试，以及更多旧 LuaJIT API 在默认构建下不受影响的覆盖。
  - 当前进展：默认 LuaJIT 5.1 C API smoke 已继续补旧 ABI 覆盖：`lua_cpcall`、`lua_setfenv`、`lua_setlevel`、`lua_equal`、`lua_lessthan`、Lua 函数 environment、`luaL_ref` / `luaL_unref` 的 key 0 freelist 行为、`luaL_loadstring`、`luaL_loadfile`、`luaL_loadbuffer`、`luaL_dofile` / `luaL_dostring`、`luaL_checkint` / `luaL_checklong` / `luaL_optint` / `luaL_optlong`、`luaL_getmetafield` / `luaL_callmeta`、`luaL_newmetatable` / `luaL_checkudata`、`luaopen_string_buffer` 和 LuaJIT-only `lua_loadx` 均在默认构建下编译/运行验证，防止 Lua 5.4 外部头收紧时误伤默认 ABI。
  - 当前进展：默认 LuaJIT 5.1 `lua_getfenv()` / `lua_setfenv()` 的非法目标索引已从 debug-only stack slot 检查收敛为 release 可见 `invalid value`，同时保留合法非 env 对象返回 nil/0 的旧表面。
  - 当前进展：默认 LuaJIT 5.1 `lua_upvalueid()` 已补 Lua/C closure 的合法 id 查询和非法 upvalue 编号返回 `NULL` 的 smoke，避免 release 构建下越界取 upvalue 指针或依赖 debug-only `lj_checkapi`。

- [ ] Lua 5.4 auxiliary library / lauxlib 兼容。
  - 当前状态：`lauxlib.h` 仍以 Lua 5.1/LuaJIT 接口为主，只补了部分 5.2+ 辅助函数。
  - 当前进展：`luaL_addgsub`、`luaL_argexpected`、`luaL_buffaddr`、`luaL_bufflen`、`luaL_buffsub`、`luaL_checkversion`、`luaL_getsubtable`、`luaL_intop`、`luaL_len`、`luaL_newmetatable` 设置 `__name`、`luaL_pushfail`、`luaL_pushresultsize`、`luaL_requiref`、`luaL_tolstring`、`luaL_typeerror` 已补；外部兼容头中的 `luaL_argexpected` / `luaL_pushfail` 已按官方 Lua 5.4 暴露为宏。
  - 当前进展：Lua 5.4 兼容模式下 `luaL_prepbuffer()` / `luaL_prepbuffsize()` / `luaL_buffinitsize()` 会按请求尺寸增长 buffer；外部兼容头中的 `luaL_prepbuffer` 已按官方 Lua 5.4 暴露为宏，默认构建仍保留旧 LuaJIT buffer 结构。
  - 当前进展：`lauxlib.h` 已补 Lua 5.4 的 `luaL_Stream` 类型定义，以及官方放在 lauxlib 的 `LUA_GNAME` / `LUA_FILEHANDLE` / `LUA_LOADED_TABLE` / `LUA_PRELOAD_TABLE` / `lua_writestring` / `lua_writeline` / `lua_writestringerror` / `lua_assert` 宏，并进入 C API smoke 编译覆盖；Lua 5.4 兼容构建的 `LUAL_BUFFERSIZE` 已按官方 `16 * sizeof(void*) * sizeof(lua_Number)` 公式暴露，`luaL_Buffer` 已使用官方 `LUAI_MAXALIGN` / `init.b` 初始缓冲布局，并按官方头文件先 forward typedef 再定义 tagged `struct luaL_Buffer`；默认构建继续保留 LuaJIT/BUFSIZ 旧 ABI；`lualib.h` 已补 `LUA_VERSUFFIX`；外部 Lua 5.4 `lua.h` 单独包含时不会暴露 lauxlib-only 的 loaded/preload 宏；外部 `lauxlib.h` 下 `luaL_argexpected` / `luaL_pushfail` / `luaL_loadfile` / `luaL_loadbuffer` / `luaL_prepbuffer` 只暴露官方宏，不再泄露可取地址的旧函数声明。
  - 当前进展：`luaL_loadfilex` / `luaL_loadbufferx` 的 `mode` 参数已进入 C API smoke，覆盖 text 模式加载以及 binary-only 模式拒绝 text chunk。
  - 当前进展：`luaL_loadfilex` / `luaL_loadbufferx` 的 mode 不匹配错误文本已按 Lua 5.4 收紧，C API smoke 覆盖 `attempt to load a text chunk (mode is 'b')` 和文件/内存 buffer 路径下的 `attempt to load a binary chunk (mode is 't')`；`luaL_loadfilex()` 缺失文件路径已固定返回 `LUA_ERRFILE` 并压入 `cannot open ...` 错误对象；首行 `#` 注释后接 LuaJIT binary chunk 的文件加载也已进入 C API smoke。
  - 当前进展：外部兼容头中的 `luaL_loadfile()` / `luaL_loadbuffer()` 已按官方 Lua 5.4 暴露为映射到 `luaL_loadfilex(..., NULL)` / `luaL_loadbufferx(..., NULL)` 的宏。
  - 当前进展：新增 `test/lua54_lauxlib_header_smoke.c` 固定 `lauxlib.h` 单独包含表面，覆盖 `LUA_GNAME` / `LUA_FILEHANDLE` / `LUA_LOADED_TABLE` / `LUA_PRELOAD_TABLE`、输出宏、`lua_assert`、`LUA_ERRFILE` / ref 常量、`LUAL_NUMSIZES` / `LUAL_BUFFERSIZE` 公式，以及 `luaL_checkversion_`、metafield/callmeta、参数检查、userdata、load、traceback、require、buffer 等真实函数的官方函数指针签名；同一 smoke 现在还固定 `luaL_Buffer` 的 `b` / `size` / `n` / `L` / `init` 字段顺序、字段类型、`LUAI_MAXALIGN` union 成员和 `luaL_Stream` 的 `FILE *f` / `lua_CFunction closef` 字段类型；`test/lua54_header_static.lua` 额外固定 `luaL_Buffer` forward typedef / tagged struct 形态。
  - 当前进展：新增 `test/lua54_lauxlib_guard_smoke.c` 并接入 `smoketest-capi-lua54compat`，固定预定义 `lauxlib_h` 时必须像官方 Lua 5.4.8 一样跳过整份 `lauxlib.h` 内容，不能泄露 `LUA_GNAME` / `LUA_FILEHANDLE` / loaded/preload registry 宏。
  - 当前进展：外部兼容头中的 `luaL_newlib()` 已按 Lua 5.4 宏形态先执行 `luaL_checkversion()`，默认构建仍保留 LuaJIT 旧宏。
  - 当前进展：外部兼容头已隐藏旧 `luaL_typerror`，改用 Lua 5.4 的 `luaL_typeerror`；同时隐藏 LuaJIT helper `luaL_findtable` 和旧 `luaL_putchar`；默认构建继续保留并覆盖这些旧表面。
  - 当前进展：`luaL_argcheck()` / `luaL_argexpected()` 宏形态已继续对齐官方 Lua 5.4，条件分支通过 `luai_likely()` 包裹；`test/lua54_header_static.lua` 已加入静态门禁，防止后续回退到旧 LuaJIT 直接条件展开。
  - 当前进展：`lua_writestring()` / `lua_writeline()` / `lua_writestringerror()` / `lua_assert()` 已纳入 lauxlib macro-only 负向编译门禁，固定这些官方 function-like 宏不能被外部 Lua 5.4 模块按函数地址获取。
  - 当前进展：Lua 5.4 兼容模式下 `luaL_checkinteger()` / `luaL_optinteger()` 已拒绝无整数表示的 number，并进入 C API smoke。
- 当前进展：`luaL_pushresultsize()` / `luaL_buffinitsize()` 已按官方 Lua 5.4 暴露为可取函数指针的真实 lauxlib 函数；默认构建仍保留 LuaJIT 旧宏表面。
- 当前进展：`luaL_setfuncs()` 已补官方 Lua 5.4 `{name, NULL}` 占位项语义，会把目标字段设为 `false`，且不会为 placeholder 复制或消耗共享 upvalue。
- 当前进展：Lua 5.4 兼容构建下 `luaL_getmetafield()` 已按官方返回被压入 metafield 的实际类型码，例如 `__name` 字符串返回 `LUA_TSTRING`；同时确认该 helper 对 metatable 字段使用 raw lookup，metatable 自己的 `__index` 不会参与缺失字段查找；默认 LuaJIT 构建继续保留旧 1/0 表面。
- 当前进展：Lua 5.4 兼容构建下 `luaL_ref()` / `luaL_unref()` 的 freelist 已从旧 Lua 5.1 key `0` 移到 `LUA_RIDX_LAST + 1`，并在首次使用时初始化为 `0`；这保留官方 freed ref slot 链到整数 `0` 的行为，同时允许用户 ref table 自己安全使用 key `0`。
- 当前进展：`luaL_checkversion_()` 的 version mismatch 错误已按 Lua 5.4 把版本号格式化为 Lua number 文本，例如 `503.0` / `504.0`，不再用旧整数 `%d` 文本。
- 当前进展：`luaL_typeerror()` 已按 Lua 5.4 在没有字符串 `__name` 覆盖时把 light userdata 报为 `light userdata`；C API smoke 同时覆盖直接 `luaL_checktype()` 路径和 `luaL_argexpected()` 宏路径。
- 当前进展：`luaL_fileresult()` 已按 Lua 5.4 在失败且 `errno == 0` 时返回 `"(no extra info)"`；`luaL_execresult(nonzero)` 在 `errno` 有值时会优先返回 system-error tuple，而不是普通 `"exit"` tuple。
- 当前进展：`luaL_checkstack()` 已按 Lua 5.4 收紧 NULL auxiliary message 边界，栈溢出错误只报告 `stack overflow`，不再把旧格式化路径的 `(null)` 拼进错误文本。
- 当前进展：`luaL_checkoption()` 已按 Lua 5.4 收紧默认参数语义，只有缺参/`nil` 会使用 `def`；显式 table/boolean 等不可转字符串参数不再被默认值吞掉，而会按 `luaL_checkstring` 路径报类型错误，数字参数仍可转成字符串后匹配 option。
- 当前进展：Lua 5.4 `luaL_Buffer` 大缓冲现在通过 to-be-closed userdata box 绑定到栈，避免 C API 使用者在 `luaL_pushresult()` 前抛错时泄漏侧缓冲；新增 C API smoke 用 allocator live-bytes 断言该错误展开路径会清理到零。
- 当前进展：C API smoke 已补充 `luaL_fileresult()` 成功、errno 失败和 errno-zero 失败、`luaL_execresult()` exit 成功和 errno 失败、`luaL_newlib()`、`luaL_newlibtable()`、`luaL_setfuncs()` upvalue 和 NULL placeholder、`luaL_newmetatable()` / `luaL_getmetatable()`、`luaL_setmetatable()`、`luaL_testudata()`、`luaL_checkudata()`、`luaL_traceback()`、`luaL_dostring()`、`luaL_dofile()` 以及 `luaL_requiref()` 的全局注册、false-loaded 重载和 nil-returning module 继续保持 unloaded / 可重载语义覆盖。
- 已覆盖：最小 C 程序覆盖 buffer API、`LUAL_BUFFERSIZE` 官方公式、`luaL_prepbuffer` / `luaL_addchar` / `luaL_addsize` / `luaL_buffaddr` / `luaL_bufflen` / `luaL_buffsub` / `luaL_argexpected` / `luaL_argcheck` / `luaL_pushfail` / `luaL_checkversion` / `luaL_intop` / `luaL_loadfile` / `luaL_loadbuffer` 宏可见性和宏-only 负向编译 gate、`lua.h` / `lauxlib.h` / `lualib.h` 单独包含的官方函数指针签名、`luaL_newlib` 版本检查宏形态、`lualib.h` 官方库名宏和 `luaopen_*` 函数指针签名、`luaL_prepbuffsize()` / `luaL_buffinitsize()` 大于 `LUAL_BUFFERSIZE` 的写入、`luaL_Buffer` 大缓冲错误 unwind 清理、`luaL_addgsub`、`luaL_tolstring` 的 boolean、number subtype、字符串 `__name` 和非字符串 `__name` fallback 文本、`luaL_pushfail`、`luaL_getsubtable` 的普通创建/复用和 `__index` / `__newindex` metamethod 路径、`luaL_requiref` 的首次加载/复用和 `_LOADED[mod] == false` 重载入路径、`luaL_loadbufferx`、`luaL_loadfilex`。
- 已覆盖：`luaL_checkinteger()` / `luaL_optinteger()` 的 fraction number 错误，`luaL_checkinteger()` 的公开 `lua_Integer` 宽度 `2^40` number/string 输入，`luaL_checknumber()` / `luaL_optnumber()` 的 boxed 64-bit integer 和精确 64-bit string number 转换，`luaL_optnumber()` 的 default/string/error 路径，`lua_tointegerx()` 的 fraction status，`luaL_intop()` 的外部公开 `lua_Integer` 范围 wraparound，`luaL_checkoption()` 的缺参/`nil` 默认值、显式 option、非法 option 和显式 table 类型错误，`luaL_checkstring()`、`luaL_optstring()`、`luaL_argexpected()`、`luaL_checkany()`、`luaL_checktype()`、`luaL_checkstack()` 的成功和 NULL message 栈溢出错误文本、`luaL_len()` 的 string/metatable/error 路径，`luaL_argerror()` 的源码全局名、method bad self 和负索引参数归一化，`luaL_typeerror()` 的 numeric `__name` fallback 和 light userdata 名称、`luaL_opt()`、`luaL_gsub()`、`luaL_ref()` / `luaL_unref()` 的 Lua 5.4 freelist key 和 table key `0` 保留、`luaL_getmetafield()`、`luaL_callmeta()` 的调用和缺失栈不变路径、`luaL_where()`、`luaL_error()`、`luaL_typename()`，buffer API 的 `luaL_addchar()` / `luaL_addlstring()` / `luaL_addstring()` / `luaL_addvalue()` 组合，`lauxlib.h` 单独暴露 `LUA_GNAME` / `LUA_FILEHANDLE` / `LUA_LOADED_TABLE` / `LUA_PRELOAD_TABLE` 以及输出宏，`lua.h` 单独包含时隐藏 loaded/preload 宏，外部 5.4 头隐藏旧 `luaL_typerror` / `luaL_putchar` / `luaL_findtable`，以及上述常用 lauxlib 5.4 辅助入口的编译/链接/运行表面。
  - 剩余：lauxlib 已知表面主要转入完整 ABI/错误文本对齐；仍需随 C API 收尾批次继续核对逐字错误和旧兼容宏边界。

- [x] Lua 5.4 binary chunk / `string.dump` 兼容性。
  - 当前状态：仍使用 LuaJIT 自身 bytecode 格式，不兼容官方 Lua 5.4 binary chunk；这是已文档化的兼容边界，而不是继续伪装支持官方 bytecode。
  - 当前进展：C API `lua_dump(..., strip)` 已支持 Lua 5.4 外部调用表面，并可写出带 strip 标志的 LuaJIT bytecode；这不是官方 Lua 5.4 binary chunk 格式兼容。
  - 当前进展：Lua 层 `string.dump(f, strip)` 已进入 smoke，覆盖 full/stripped LuaJIT bytecode 写出、`mode="b"` 回读执行，以及 binary chunk 被 `mode="t"` 拒绝。
  - 当前进展：已将官方 Lua 5.4.8 dump 的加载失败固化为 smoke；兼容构建会明确拒绝官方 Lua 5.4 binary chunk，并保留 `mode="t"` 的 binary chunk 拒绝错误。
  - 当前进展：LuaJIT dump 回读时，带真实 upvalue 的函数会按 Lua 5.4 `load` 规则把第一个 upvalue 初始化为当前全局环境；带第 4 个 env 参数时会初始化为指定 env，且真实 upvalue 槽支持 table / number / false / nil 这类非 table 值。
  - 当前进展：LuaJIT stripped dump 回读后的 debug upvalue 枚举已避免把无真实 upvalue 的 plain dump 误暴露为 `_ENV`，带真实 upvalue 的 stripped dump 也不再额外插入伪 `_ENV`；使用全局名的 stripped dump 会保留真实首个 env upvalue，且 stripped upvalue 名称已按官方 Lua 5.4 表面显示为 `(no name)`。
  - 已覆盖/文档：mode=`"b"`/`"t"` 基础错误消息、LuaJIT bytecode 回读、官方 Lua 5.4 binary chunk 明确拒绝，以及 README 中的“compat 构建仍只支持 LuaJIT bytecode”边界说明。

## P2：继续做一致性回归的边缘面

- [ ] Android / iOS / PC / Emscripten 64 位构建矩阵。
  - 当前状态：当前环境已反复覆盖 Windows PC 64 位默认构建和 `LUAJIT_ENABLE_LUA54COMPAT` 构建；本轮 `cmd /c build.bat test` 已通过，包含默认构建 smoke/C API、Lua 5.4 compat smoke/C API，以及固定 `jit.opt` profile 的 JIT on/off perf/memory smoke。
  - 当前进展：已新增 `tools/lua54_platform_matrix.ps1`，可一键在当前 Windows/MSYS2 环境构建并运行 PC x64 default 与 Lua 5.4 compat smoke，并会直接读取 `src/luajit.exe` 的 PE header 确认 machine 为 AMD64 `0x8664`，避免只凭宿主环境推断 64 位产物；检测到 Android NDK 时会实际构建 Android ARM64 Lua 5.4 compat 静态 artifact，并用 `llvm-readelf` 确认 AArch64；检测到在线 Android 设备时会自动 push `luajit`、`test/smoke.lua` 和 `src/jit` LuaJIT 工具模块到 `/data/local/tmp` 并运行 Lua 5.4 smoke；当前已在 Android 设备 `R5CN30J05BT` 取得 `test/smoke.lua lua54compat` PASS；iOS/Emscripten 目标已拆成可单独调用的 `-Target ios` / `-Target emscripten`，并通过 `build.bat platformios` / `build.bat platformemscripten` 暴露。
  - 当前进展：iOS ARM64 目标在 macOS/Xcode 主机上已接入实际构建入口：使用 `xcrun --sdk iphoneos` 查找 SDK、clang、ar、strip，执行 `make TARGET_SYS=iOS BUILDMODE=static XCFLAGS='-DLUAJIT_ENABLE_LUA54COMPAT -DLUAJIT_NUMMODE=2' TARGET_FLAGS='-arch arm64 ...'`，并在产出 `src/libluajit.a` 后用 `lipo -info` 校验包含 `arm64`；当前 Windows 主机仍按真实环境限制输出 SKIP。
  - 当前进展：Emscripten probe 现在会从 `EMCC` / `EMSCRIPTEN` / `EMSDK`、本机 `H:\p4\gl_home_u4\pristine\emsdk-5.0.6` 以及 UE HTML5 engine-local emsdk 路径探测 `emcc` / `emcc.bat`；探测到工具后会分别执行 `emcc -target wasm32-unknown-emscripten ... -E src/lj_arch.h -dM` 和 `emcc -target wasm64-unknown-emscripten ... -E src/lj_arch.h -dM` 的轻量预处理探针，只有两个目标都命中 LuaJIT 自身 `Architecture not supported` / `No target architecture defined` / `No support for this number mode on this architecture` 这类 target 后端缺失错误时才输出 SKIP，其他探针失败会报 FAIL；本机已能探测到 emsdk-5.0.6，Emscripten 剩余边界收敛为 LuaJIT 缺 wasm/wasm64 target/VM 后端，不能产出 artifact。
  - 当前进展：Android ARM64 交叉编译暴露的 `floor` 隐式声明警告已通过补 `<math.h>` 清理；本轮 `cmd /c build.bat platformandroid` 已在更新 `vm_arm64.dasc` 的 C-return close continuation 后再次通过 Android ARM64 Lua 5.4 compat 静态 artifact 构建，确认 AArch64 后端可真实汇编。
  - 当前进展：`tools/lua54_platform_matrix.ps1` 现在会在未设置 `ANDROID_NDK_ROOT` / `ANDROID_NDK_HOME` / `NDK_ROOT` 时自动探测本机 `H:\p4\gl_home_u4\pristine\android-ndk-r25b`、`android-ndk-r21b` 和 `ndk`，避免 Windows 工作区已有 NDK 但平台矩阵直接 SKIP。
  - 当前进展：`tools/lua54_platform_matrix.ps1` 的独立 MSYS2 探测已对齐当前 Windows 工作区，优先使用 `H:\p4\gl_home_u4\pristine\ruby\msys64`，并一次性加入 `usr\bin` / `ucrt64\bin` / `mingw64\bin`，因此直接运行平台脚本和通过 `build.bat platform*` 都能找到同一套 `make` / gcc 工具链；平台矩阵内部调用 `make` 时也会使用当前物理核心数/2 的 `-jN`，并允许 `BUILD_JOBS` 只向下收紧并发，不再绕过 `build.bat` 的并发策略。
  - 已知缺口：本轮 `cmd /c build.bat platformpc` 已通过 PC x64 default 与 Lua 5.4 compat smoke，并确认两个产物都是 AMD64 PE；`cmd /c build.bat platformandroid` 已通过 Android ARM64 Lua 5.4 compat 静态 artifact 构建和 AArch64 检查，但当前 Windows 主机无在线 adb 设备，因此设备 smoke 输出 SKIP，仍需在有设备/模拟器时补最新运行验证。iOS ARM64 已有 macOS/Xcode 构建入口和静态库 artifact 架构检查，但当前 Windows 环境无法实际验证该分支，也还缺 iOS device/app smoke harness；Emscripten wasm/wasm64 已有工具探测和 LuaJIT target-selection 级别的失败探针，但当前主机未找到 `emcc`，且 LuaJIT 仍缺 wasm/wasm64 JIT 或解释器 VM 后端，不能产出 Emscripten artifact。
  - 需要补测试/脚本：继续在 macOS/Xcode 主机实际跑通 iOS ARM64 artifact 构建并补 device/app smoke；继续设计 Emscripten 的 interpreter/wasm 可行路径；Android 继续补设备/模拟器 smoke；每个目标至少验证编译完成、`LUAJIT_ENABLE_LUA54COMPAT` 可打开、目标可运行时执行 smoke，不可直接运行时产出可检查 artifact。
  - 实现重点：Emscripten 通常不能使用传统本机 JIT，需要明确解释器/wasm 可行路径；Android/iOS 需要分别确认 JIT 权限、mcode 分配和平台 ABI。

- [ ] 标准库错误消息与边界参数完全对齐。
  - 范围：`math`、`utf8`、`string.pack`、`table.move`、`require`、`load`/`loadfile`。
  - 当前进展：`math.type()` 已按 Lua 5.4 对齐“无参数报错，非 number 参数返回 nil”的边界。
  - 当前进展：Lua 5.4 兼容模式下的 `math.min` / `math.max` 已改用普通 `<` 比较，支持 string 和带 `__lt` 的对象，且无参数时报 value error。
  - 当前进展：`math.type()` / `math.tointeger()` / `math.ult()` / `math.min()` / `math.max()` 的缺参参数错误会带 `math.xxx` 函数名；`math.ult()` 的整数表示错误也会带实际函数名。
  - 当前进展：`math.atan(y, x)` 已按 Lua 5.4 支持第二参数并使用 `atan2`；`math.fmod` 已区分双 integer 返回 integer、含 float 返回 float，并处理 integer 除数 `-1/0` 特例；整数除零错误归属已对齐官方 `math.fmod`，含 float 零除数继续按 C `fmod` 返回 NaN；`math.fmod()` / `math.fmod(nil)` 的缺参顺序也按官方先检查第 2 个除数，`math.fmod(nil, 1)` 仍报第 1 参数为 nil。
  - 当前进展：Lua 5.4 compat 的 `math.random` / `math.randomseed` 已切到官方 xoshiro256** 状态和无偏区间投影；`math.random(0)` 返回当前 integer 位宽的全范围随机整数，`randomseed()` 返回的两个种子可复现状态。
  - 当前进展：`dofile()` 和 `collectgarbage()` 的 Lua 5.4 参数错误已带实际函数名，覆盖 option、字符串参数和整数参数边界。
  - 当前进展：`table.concat()` / `insert()` / `remove()` / `sort()` / `move()` / `unpack()` 的 Lua 5.4 入口参数错误已带 `table.xxx` fallback，非 tail-call 源码调用可恢复 `concat` / 局部 `f` 这类调用点函数名；静态字段 tail-call 形态 `return table.concat(nil)` / `return table.sort(nil)` 会保留 caller frame 并报告 `concat` / `sort`；已覆盖表、分隔符、整数位置、comparator 和 `table.sort` 内部 comparator 错误边界，其中 `table.sort` 只在长度大于 1、真正会比较元素时才校验 comparator，空表/单元素表允许任意第 2 参数。
  - 当前进展：table constructor 中直接保存的标准库函数 alias 已能参与 tail-position 调用点名保留，`local t = { f = math.abs }` / `{ m = table.move }` / `{ g = local_alias }` 后 `return t.f(...)` 会按官方报字段名，不再回退到 `math.abs` / `table.move`。
  - 当前进展：Lua 5.4 `luaL_traceback()` 会优先从 registry `_LOADED` 反查 C 标准库函数全名，因此 standalone 错误 traceback 中 `math.abs(true)` / `table.concat(nil)` / `os.exit({})` 的 C frame 已显示为 `math.abs` / `table.concat` / `os.exit`，同时参数错误正文仍保留官方短调用名 `abs` / `concat` / `exit`。
  - 当前进展：`os.date()` / `difftime()` / `execute()` / `getenv()` / `remove()` / `rename()` / `setlocale()` / `time()` 的 Lua 5.4 基础参数错误已带 `os.xxx` 函数名；`os.execute()` 现在会在 `system()` 前清 `errno`，所以前一次文件操作失败留下的 stale errno 不会把不存在命令误报成系统错误，而是按官方返回 `nil, "exit", status`；`os.date()` 第二参数和 `os.difftime()` 两个时间参数已按官方 Lua 5.4 要求严格检查整数表示，fraction number / fraction string 会报 `number has no integer representation`，`os.difftime()` / `os.difftime(nil)` 会先报第 1 参数，`os.difftime(1)` 会报第 2 参数缺失，字符串整数仍接受；`os.time(date)` 已按官方 `year` / `month` / `day` / `hour` / `min` / `sec` 顺序报告缺字段和非整数 date-table 字段；`os.date()` / `os.time()` 在目标 C runtime 无法表示结果时也已按官方抛 `date/time result cannot be represented in this installation`，不再返回 `nil`。
  - 当前进展：`os.exit()` 的 status 参数错误已带 direct `pcall(os.exit, ...)` 的 `os.exit` fallback，普通源码字段调用和 tail-position `return os.exit(...)` 会恢复官方短名 `exit`，覆盖非法字符串/table 和 fraction number；status 参数现在先按完整 Lua 5.4 `lua_Integer`（含 64-bit boxed integer 和字符串整数）解析，再按官方转换为 C `int` 退出码；boolean status 与第二个 close 参数仍保持官方 Lua 5.4 语义。
  - 当前进展：`debug.getinfo()` / `getlocal()` / `setlocal()` / `getupvalue()` / `setupvalue()` / `upvalueid()` / `upvaluejoin()` / `sethook()` / `gethook()` / `getmetatable()` / `setmetatable()` / `setuservalue()` / `setcstacklimit()` 的 Lua 5.4 基础参数错误和返回边界已带 direct `pcall(debug.xxx, ...)` 的 `debug.xxx` fallback；普通源码字段调用、局部/upvalue alias 和 tail-position 调用会恢复官方调用点名，例如 `return debug.getinfo(true)` 报 `getinfo`、`local f = debug.getinfo; return f(true)` 报 `f`；`debug.getlocal()` / `debug.setlocal()` 的 stack level 越界也会按官方保留公开函数名和 thread 偏移后的参数编号；`debug.gethook()` 无 hook 时返回单个 `nil`，`debug.sethook(function, "", 0)` 会真正关闭 hook，count-only hook 仍保留；`debug.upvalueid()` 对 0、负数和越界 upvalue index 会按官方作为查询返回单个 `nil`，`debug.upvaluejoin()` 仍作为 mutating API 拒绝越界 index，并按官方报 `invalid upvalue index`，无可见 upvalue 的 C 函数会优先报 index 参数而不是函数参数；`debug.setmetatable()` 第二参数会按官方保留 `nil or table expected, got <type>` 细节；`debug.getuservalue()` 已按官方对非 userdata 返回 `nil`，并在 bad slot index 时先报第 2 参数。
  - 当前进展：`utf8.char()` / `codepoint()` / `codes()` / `len()` / `offset()` 的 Lua 5.4 基础参数错误已带 direct `pcall(utf8.xxx, ...)` 的 `utf8.xxx` fallback；普通源码字段调用、局部/upvalue alias 和 tail-position 调用会恢复官方调用点名，例如 `return utf8.len(true)` 报 `len`。
  - 当前进展：`string.byte()` / `char()` / `dump()` / `find()` / `format()` / `gmatch()` / `gsub()` / `len()` / `lower()` / `match()` / `rep()` / `reverse()` / `sub()` / `upper()` / `pack()` / `unpack()` / `packsize()` 的 Lua 5.4 direct `pcall(string.xxx, ...)` 基础参数错误已带 `string.xxx` fallback；源码字段调用、局部/upvalue alias 和 tail-position wrapper 会按官方恢复调用点名，例如 `string.dump(true)` 报 `dump`、`local f = string.pack; return f(nil)` 报 `f`。
  - 当前进展：`string.gmatch()` 返回的 iterator 已按官方 Lua 5.4 debug 形态收紧为 3 个 upvalue：source string、pattern 和 userdata 状态；第 4 个内部 pos/last upvalue 不再暴露，空匹配推进和 `init` 参数语义不变。
  - 当前进展：`error(message, level)` 的 level 参数已拒绝无整数表示的 number；Lua 5.4 兼容模式下非 string 错误对象会保留原值，不再被当成 5.1 风格 string 加位置信息。
  - 当前进展：`select(index, ...)` 的 index 参数已拒绝无整数表示的 number，仍接受字符串数字；`select(0, ...)` / 越过负边界的 index range 错误会按官方报到 `select`。
  - 当前进展：Lua 5.4 兼容模式下 `getmetatable()` 无参数已报 value error，并保留 `__metatable` 保护返回值。
  - 当前进展：debug 库整数边界已按 Lua 5.4 收紧，覆盖 stack level、local/upvalue index、hook count、traceback level、uservalue slot、`setcstacklimit`，以及超出 32-bit 但仍可表示为 `lua_Integer` 的 level 参数。
  - 当前进展：string / utf8 库的常见整数参数已按 Lua 5.4 收紧，覆盖 `string.byte`、`char`、`sub`、`rep`、`find`、`match`、`gmatch`、`gsub`、`pack`、`unpack` 以及 `utf8.char`、`codepoint`、`len`、`offset`，都会拒绝无整数表示的 number；超出 32-bit 但仍是合法 `lua_Integer` 的位置参数已按官方 Lua 5.4 对齐，string API 走 position clipping，utf8 API 保持 out-of-bounds 错误。
  - 当前进展：`string.unpack()` 的初始位置已按官方 `posrelatI` 处理完整 `lua_Integer`，超大负位置会 clip 到字符串开头，例如 `string.unpack("b", "abc", -1099511627776)` 返回第一个字节和下一个位置。
  - 当前进展：pattern/replacement 的常见 Lua 5.4 错误文本已继续收紧，覆盖 `string.gsub` replacement 缺参/显式 nil/非法 boolean 的 `got no value` / `got nil` / `got boolean` 细节、invalid capture index `%0/%1/%2`、replacement 中孤立结尾 `%` 或非法 `%x` 用法，以及 `%b` 缺参数。
  - 当前进展：`string.char()` 的越界错误文本已收紧为 Lua 5.4 风格的 `value out of range`。
  - 当前进展：`utf8.char()` 对整数可表示但超出 0..0x7fffffff 扩展码点范围的输入，也已按 Lua 5.4 报 `value out of range`。
  - 当前进展：`utf8.codes()` 在首字节就是 continuation byte 时已按 Lua 5.4 包装为第 1 参数错误，并保留源码字段调用和局部 alias 的调用点名。
  - 当前进展：`string.format()` 的整数格式转换错误、整数格式的 number 类型错误以及 `%q` 无 Lua 字面量形式的错误，均已带可恢复的官方调用名，不再在内部格式化 helper 中显示为 `?`；direct `pcall(string.format, ...)` 报 `string.format`，源码字段调用报 `format`，局部 alias 报 alias 名；非法格式文本已区分未知转换和非法规格，`%3.1p` / `%#p` 等合法转换的非法规格会报 `invalid conversion specification: '...'`，并且 tail-position Lua wrapper 会保留调用点位置。
  - 当前进展：`string.pack()` / `string.unpack()` / `string.packsize()` 的基础缺参和格式参数错误已继续对齐官方完整函数名 `string.pack` / `string.unpack` / `string.packsize`；`string.pack("i1")` / `("f")` 缺数值会报 `number expected, got nil`，`("c1")` / `("z")` / `("s1")` 缺字符串会报 `string expected, got nil`，不再被 `luaL_buffinit()` 的内部 buffer 占位对象污染成 `got light userdata`；`string.pack("c", ...)`、`string.unpack("c", ...)` 与 `string.packsize("c")` 缺 size 现在按官方格式解析错误报 `missing size for format option 'c'`，不再包成 `bad argument #1`；`z` 含 NUL、`X` next option、alignment 非 2 的幂、`packsize("z")`、`unpack("z")` unfinished string、`unpack("s1", "")` / 短数据和 `pack("b", 1.2)` 的错误归属也已进入 smoke，不再从内部 helper 显示为短名或 `?`。
  - 当前进展：`string.pack` 格式数字解析已继续按 Lua 5.4 `getnum()` / `getnumlimit()` 收紧：`!0` / `!17`、`s0` / `s17` / 超长 `s999...`、`i2147483647` 这类边界会报官方 `integral size ... out of limits [1,16]`，`c9999999999` 会在安全 int 边界停止吞数字并把剩余 `9` 当作下一格式项报 `invalid format option '9'`。
  - 当前进展：`load()` / `loadfile()` 底层 mode 不匹配错误文本已按 Lua 5.4 收紧，会说明被拒绝的是 text 还是 binary chunk，并回显传入 mode；`loadfile()` 缺失文件固定返回 `nil,error` 且不额外返回 errno code，`dofile()` 缺失文件会抛出同源 `cannot open ...` 错误；`load()` reader 返回非 string/number/nil 时，即使处于 `return load(reader)` tail-position，或 reader 已经先返回过合法文本片段，后续错误也会返回带源码调用点前缀的 `reader function must return a string`，不会泄露内部 reader 拼接错误；`dofile()` 的 Lua 5.4 yieldable wrapper 参数错误会跳回用户调用点，不再泄露内部 wrapper chunk。
  - 当前进展：IO 库已继续按官方 `testes/files.lua` 收紧：文件对象 method 用点号调用漏掉 self 时，`close/read/write/flush/seek/setvbuf/lines` 会按 Lua 5.4 报公开 method 名和 `FILE* expected` 类型细节；`return io.stdin/stdout/stderr.<method>()` 这类标准文件句柄 tail-position 调用也会保留源码位置。`io.open` / `io.popen` 会先拒绝非法 mode，`io.input(path)` / `io.output(path)` / `io.lines(path)` 自动打开失败会按官方报 `cannot open file '...' (...)`，文件句柄支持 GC-style `__close`，`file:read("n")` 使用 Lua 5.4 数值扫描路径，`file:read(n)` length、`file:seek()` offset 和 `file:setvbuf()` size 在 Lua 5.4 分支下必须有精确整数表示，`file:write()` / `io.write()` 的非 string/number 参数按官方函数名和参数编号报错，`dofile` 可在 coroutine 中 yield，`io.lines` 支持 250 个 read options，普通 table read option 不会再被误识别为内部打包表；`io.lines(fname,{})` / `f:lines({})` 的 iterator read option 错误也已按官方报公开参数 #2，局部变量直接接收 iterator、再经过简单 local alias、闭包 upvalue alias 或本解析路径可静态证明的 global alias 后，`return it()` / `return it2()` / `return it3()` / `return lua54_lines_global_it()` 都会保留源码位置和 iterator 调用名，并按第一个返回值判断 EOF，因此带 `"L"` / 定长等 read option 的自动关闭迭代器不会泄漏文件句柄；IO smoke 中的有效 mode 临时文件现在会在 GC 后断言清理，`make clean` 也会兜底清理历史残留。
  - 当前进展：`io.type()` 缺参、`io.input()` / `io.output()` / `io.close()` 的错误类型参数、`io.open()` / `io.popen()` / `io.lines()` 的字符串参数和 mode 参数错误，以及 `io.read()` / `io.write()` 的函数入口参数错误已按官方 Lua 5.4 报到 direct `pcall(io.xxx, ...)` 的 `io.xxx` fallback；普通源码字段调用、局部/upvalue alias 和 tail-position 调用会恢复官方调用点名，例如 `return io.open(true)` 报 `open`、`local f = io.open; return f(true)` 报 `f`；`io.input` / `io.output` 仍支持路径字符串/数字字符串转换和文件句柄两种官方入口。
  - 当前进展：file method 被 direct `pcall(file.method)` 调用且漏掉 self 时，`close` / `seek` 等错误名已按官方收敛为 `?`，同时保留 `io.type` 等 io 模块函数 direct `pcall(io.xxx, ...)` 的 `io.xxx` fallback；`seek` / `setvbuf` 的首个 option 参数错误已能在非 tail 源码调用中区分 colon call、点号调用、局部 alias 和 direct `pcall(file.method, file, ...)`，按官方调整隐藏 self 后的参数编号和 `?` fallback。显式 `pcall(file.method, file, ...)` 与 tail-position 源码调用在 offset/size 等后续参数上的 #2/#3 细分仍需归入更通用的 VM frame callsite metadata 批次。
  - 当前进展：官方 `testes/files.lua` 相关收尾已继续补齐：`debug.getlocal` 会按 Lua 5.4 顺序暴露 generic-for 的 4 个隐藏 `(for state)` 值并把第 4 个映射到 closing value；`luaL_loadfilex` 支持首行 `#` 注释后接 LuaJIT binary chunk；`os.date` 保留格式串内嵌 NUL，同时按官方 C 字符串判断让 `*t\0...` / `!*t\0...` 仍进入 table 返回路径；`os.time(table)` 严格校验整数字段并回写 `mktime` 规范化后的日期表字段。
  - 官方验证：PC x64 Lua 5.4 compat 构建下，官方 Lua 5.4.8 `testes/files.lua` 当前已通过。
  - 当前进展：`package.searchpath()` / `package.searchers` 返回的错误片段已按 Lua 5.4 去掉前导换行缩进，`require()` 组装最终 module-not-found 错误时再补 `\n\t`；`package.searchpath()` 会保留 path 中前导、重复、尾部 `;` 以及空 path 本身产生的空模板，错误中可见 `no file ''`；`require()` 的 nil-returning loader 已按 Lua 5.4 写入并返回 `true`，loader 抛错时不会再把内部 sentinel 残留到 `package.loaded`，后续 `require()` 会重新执行 loader；`require()`、`package.searchpath()` 和 `package.loadlib()` 的基础 string 参数错误已带 direct `pcall` fallback 函数名 `require` / `package.searchpath` / `package.loadlib`，普通源码字段调用、局部/upvalue alias 和 tail-position 调用会恢复官方调用点名，例如 `return package.searchpath({}, "?.lua")` 报 `searchpath`、`local f = package.searchpath; return f({}, "?.lua")` 报 `f`；`return require(...)` 的 module-not-found 和 `package.searchers` 非 table 错误已保留 Lua 调用点位置前缀，parser 会在 Lua 5.4 compat 下避免该已知入口被尾调用擦掉调用帧。
  - 当前进展：`os.rename()` 失败时已按 Lua 5.4 返回原始系统错误文本，不再把源文件名拼进错误字符串；`os.remove()` 失败返回保留文件名前缀并已进入 smoke 覆盖。
  - 当前进展：Windows/UCRT 构建下 `os.tmpname()` 已避开会崩溃的 C `tmpnam()` 路径，改用 Win32 临时文件 API 生成并删除占位文件，返回可写且当前不存在的唯一路径；smoke 覆盖写入/删除，官方 Lua 5.4.8 `testes/files.lua` 已通过。
  - 当前进展：`pairs()` 已按 Lua 5.4 延迟 table 检查；没有 `__pairs` 时会返回原始 `next, value, nil`，由后续 `next()` 调用决定是否报错；`pairs(nil)` 得到的 iterator 经过直接 local、`pairs` 的 local/upvalue alias、简单 local alias、同 chunk global alias 或 table-field alias 后再 tail-position 调用时，会保留源码调用点名；有 `__pairs` 时走 fast-function tailcall，支持在 coroutine 中 yield 后恢复。
  - 当前进展：generic for 的 iterator 不是函数时，错误文本会按官方 Lua 5.4 标注 `(for iterator 'for iterator')`，覆盖裸 `for ... in nil` 和 `__pairs` 返回非函数 iterator 的路径。
  - 当前进展：`ipairs()` 已按官方 Lua 5.4 复用稳定的辅助迭代函数，`ipairs{} == ipairs{}` 成立；辅助迭代器在当前 64-bit integer 表面下会把 `math.maxinteger + 1` 回绕到 `math.mininteger`，并在迭代耗尽时返回单个 `nil`。
  - 当前进展：`assert()`、`type()`、`tostring()`、`pcall()`、`xpcall()`、`select()`、`error()`、`tonumber()`、`load()`、`loadfile()`、`next()`、`pairs()`、`ipairs()`、`getmetatable()`、`setmetatable()`、`rawget()` / `rawset()` / `rawequal()` / `rawlen()` 的基础参数错误会带实际函数名；其中 base/global 公开入口的 direct `pcall(cfunc, ...)` 会保留公开 fallback，普通源码全局调用、tail-position `return rawget()` / `return tonumber(...)` 等，以及 `local f = rawget/next/tonumber/select/assert; return f(...)` 这类局部/upvalue alias 会保留调用点位置并恢复 source-level 名字；`select(0, ...)` 的 `index out of range` 也不再退回内部 `?`；`next({})` 这类迭代耗尽路径会按 Lua 5.4 返回单个 `nil`；`rawlen()` 非 table/string 的期望类型文本已收紧为 `table or string`；`assert(false, value)` 在 Lua 5.4 兼容模式下会保留 number/table 等非 string 错误对象。
  - 当前进展：`setmetatable({})` 已按 Lua 5.4 报第 2 参数 `nil or table expected, got no value`；`setmetatable({}, nil)` 仍作为显式清除 metatable 成功。direct `pcall(setmetatable, {})`、源码 `return setmetatable({})` 和局部 alias `return f({})` 均已覆盖错误名。
  - 当前进展：Lua 5.4 `xpcall()` wrapper 已保留真实实参数量，缺少 message handler 时 direct `pcall(xpcall, f)`、源码 `return xpcall(f)` 和局部 alias `return g(f)` 都会报 `got no value`，显式传入 `nil` 仍报 `got nil`；原有 `xpcall(f, f)` 的 `error in error handling` 保护逻辑不变。
  - 当前进展：`print()` 已按 Lua 5.4 改用内部 `luaL_tolstring()` 转换参数，不再调用可变全局 `tostring`；重绑定或清空 `_G.tostring` 不影响 `print` 输出，但 `__tostring` 元方法仍会生效。
  - 当前进展：`math.deg()` / `math.rad()` 在 Lua 5.4 兼容模式下已从 LuaJIT 内置 Lua 片段改为带参数检查的 C helper，缺参和错误类型会报标准参数错误并保留数值字符串转换。
  - 当前进展：`math.abs()` / `acos()` / `asin()` / `ceil()` / `cos()` / `exp()` / `floor()` / `log()` / `modf()` / `sin()` / `sqrt()` / `tan()` 的 direct `pcall(math.xxx, bad)` 参数错误已对齐官方 `math.xxx` 回退名；源码普通字段调用和静态字段 tail-call `return math.xxx(bad)` / `return (math.xxx)(bad)` 会按 Lua 5.4 显示短字段名 `xxx`；局部/upvalue alias 形态 `local f=math.abs; return f(true)` 已恢复官方调用点局部名 `f`；`math.log(x, nil)` 已按 Lua 5.4 视作缺省 base。
  - 当前进展：`math.random()` / `math.randomseed()` 的整数参数错误归属已继续对齐官方：direct `pcall(math.random, ...)` / `pcall(math.randomseed, ...)` 保留完整 `math.random` / `math.randomseed` fallback，普通源码字段调用和 tail-position `return math.random(...)` / `return math.randomseed(...)` 报短名 `random` / `randomseed`，局部/upvalue alias 报 alias；覆盖空区间、fraction number 和 boolean 参数，不再从共享 PRNG helper 泄露为 `?`。
  - 当前进展：`math.fmod(整数, 0)` 的 Lua 5.4 错误归属已继续对齐官方 `math.fmod`，并保留 `math.fmod(整数, 0.0)` / `math.fmod(float, 0)` 返回 NaN 的边界。
  - 当前进展：普通 `__call` 链最终落到非函数值时，会按 Lua 5.4 报最终非可调用值的类型，同时保留原始调用点名，例如 `local f = setmetatable({}, {__call=true}); f()` 报 `boolean value (local 'f')`；`pcall(f)` / `xpcall(f, handler)` 也会在内层 protected call 中捕获同一错误。
  - 当前进展：动态 key 调用点名已按 Lua 5.4 收紧为未知字段名，`local k="abs"; return math[k](true)`、`return table[k](nil)`、`return string[k](nil)` 等标准库参数错误会报 `to '?'`；`local k="f"; return t[k]()` 报 `field '?'`，`_ENV[k]()` 报 `global '?'`；同类动态 `__call` 链错误也会在 `pcall` 后保留调用者活动栈槽，不会把错误对象覆盖到局部参数上；但 `pcall(math[k], true)` 这类没有源码 call slot 的 direct protected call 仍保留 `math.abs` fallback。
  - 当前进展：`//` helper 的无元方法失败路径已从私有 C helper 参数错误收紧为 Lua 5.4 `idiv` 运算符错误，覆盖左右操作数类型和 `__name`。
  - 当前进展：位运算 helper 的错误文本已从私有 C helper 参数错误收紧为 Lua 5.4 运算符错误，覆盖 fraction number、string、boolean 和带 `__name` 的 table；无整数表示的 number 会通过 debug frame/slot 反推源级操作数名字，例如 `math.huge << 1` 报 `number (field 'huge') has no integer representation`；非 number 位运算错误也会尽量保留常量/局部变量等 operand source，例如 `return "3" & 1` 报 `(constant '3')`、`local x="3"; return x & 1` 报 `(local 'x')`。
  - 当前进展：Windows 下 `os.date()` 已按本机官方 Lua 5.4.8 对齐 `%c` / `!%c` 的短日期短时间输出，支持官方允许的 `%#c` / `%#x` / `%#d` 等两字符扩展转换，并让 invalid conversion 的 direct `pcall(os.date, ...)` 报 `os.date` 而不是 `?`。
  - 当前进展：`__add` / `__unm` / `__len` / `__lt` / `__le` / `__concat` 元方法如果是带非可调用 `__call` 链的 table，会按 Lua 5.4 报最终非可调用值，并保留原始 `metamethod '...'` 来源标注。
  - 当前进展：lowered `//` / `%` / 位运算 helper 调用 `__idiv` / `__mod` / `__band` / `__bor` / `__bxor` / `__bnot` / `__shl` / `__shr` 时，同样会为带非可调用 `__call` 链的元方法保留原始 `metamethod '...'` 来源标注。
  - 当前进展：新增 `test/lua54_stdlib_edges.lua` 并接入 `smoketest-lua54compat`，把本机官方 Lua 5.4.8 对照过的一组 base/coroutine/debug/math/os/package/string/table/utf8 边界固化为正式门禁；覆盖 direct 字段调用的逐字错误文本、`pairs(nil)` / `ipairs(nil)` 的 nil 洞返回计数、`table.sort({}, true)` 的延迟 comparator 校验、`table.pack(...).n`、`utf8.char()` 空串返回以及 `debug.gethook(true)` / `debug.getuservalue()` 等单 nil 返回。
  - 剩余边界：继续用本机 `lua5.4.8` 扩展更多标准库逐字错误文本对照；标准库公开入口的 direct `pcall` fallback、普通源码字段/全局调用、局部/upvalue alias、当前解析路径可静态证明的 global alias / table-field alias 和 tail-position 调用点名已扩展覆盖 base/global、string、base loader、math、os、io、debug、utf8、package 以及 `coroutine.resume()` / `coroutine.close()` 代表路径；parser 只对真实来自 `_ENV`、标准库表或已静态证明来源的 table 字段保留调用帧，并按“字段名 + 实际库表/静态来源”组合识别，普通未标记的 `t.pack()` / `t.resume()` / `t.f()` 这类非标准库表调用仍保持 tail call。
  - 当前进展：字符串库方法语法的 tail-position 错误名/参数编号已按 Lua 5.4 收紧；`s:byte({})` / `s:find({})` / `s:format(true)` 会保留源码方法帧并按隐藏 self 之后的公开实参报 `#1`，不再被 tail call 擦成 `string.byte` / `string.find` / `string.format` 的 `#2` fallback。
  - 做法：将本机 `lua5.4.8` 的边界行为固化为对照测试，先覆盖返回值和是否报错，再逐步收紧错误文本。

- [x] table 库的 Lua 5.4 边界语义。
  - 当前状态：当前清单中的 table 库 Lua 5.4 边界已进入 smoke 覆盖。
  - 当前进展：`table.concat` 默认终点已改为尊重 Lua 5.4 的长度操作，因此带 `__len` 的表会按元方法返回的终点检查 nil 洞。
  - 当前进展：`table.concat({1,nil,3}, ",")` 已按 Lua 5.4 参考行为检查到 index 2 的 nil 并报错；实现只在 `table.concat` 默认终点补数组构造洞的兼容，不改变全局 `#table` 行为。
  - 当前进展：`table.concat` / `table.insert` / `table.remove` 的默认长度路径共用 Lua 5.4 表库长度兼容逻辑，`__len` 返回无整数表示的值时会报 `object length is not an integer`。
  - 当前进展：`table.insert` / `table.remove` / `table.move` 在 Lua 5.4 兼容模式下已改用严格整数参数检查；`1.2` 等无整数表示的位置参数会报错，数字字符串仍按官方 Lua 5.4 接受。
  - 当前进展：`table.insert` / `table.remove` 在 Lua 5.4 兼容模式下移动元素时会通过 `__index` 读取、通过 `__newindex` 写入代理表。
  - 当前进展：`table.remove(t)` 在长度为 0 时已按官方 `ltablib.c` 读取并清除 key `0`，因此 `{[0]="ban"}` 会返回 `"ban"` 并把 `t[0]` 置空。
  - 当前进展：`table.move` 在 Lua 5.4 兼容模式下已改用 API get/set 路径移动元素，因此会通过 `__index` 读取源值、通过 `__newindex` 写入目标值。
  - 当前进展：`table.move` 的 Lua 5.4 参数检查顺序已对齐，先检查 `f/e/t` 整数参数，再检查源表和目标表；缺参时会优先报第 2 个参数。
  - 当前进展：`table.insert` / `table.remove` 的默认长度已对齐当前兼容层的 Lua 5.4 表库长度语义，支持带 `__len` 的表以及 `{1,nil,3}` 这类新建 list table 中间 nil 洞的默认尾部操作。
  - 当前进展：`table.concat` 的显式 `i` / `j` 已改用严格整数参数检查。
  - 当前进展：`table.unpack` 的默认终点已共用 Lua 5.4 表库长度兼容逻辑，并对显式 `i` / `j` 做严格整数检查；读取元素时会通过 `__index` 访问代理表。
  - 当前进展：`table.concat` 在 Lua 5.4 兼容模式下读取元素时会通过 `__index` 访问代理表，并保留 nil/非 string/number 元素的错误检查。
  - 当前进展：`table.concat` 显式终点等于 `math.maxinteger` 时不会在循环自增后回绕到 `math.mininteger` 继续读取；`{[maxi]="alo"}` 和 `{[maxi-1]="y",[maxi]="alo"}` 边界已进入 smoke。
  - 当前进展：`table.sort` 的排序范围已共用 Lua 5.4 表库长度兼容逻辑，因此会尊重 `__len`，并在 `__len` 返回无整数表示的值时报 `object length is not an integer`。
  - 当前进展：`table.sort` 的超大 `__len` 错误已按官方收敛为参数错误，direct `pcall(table.sort, t)` 保留 `table.sort` fallback，普通源码字段调用和局部 alias 分别报 `sort` / 局部名。
  - 当前进展：`table.sort` 的 comparator 校验时机已对齐官方 Lua 5.4；空表和单元素表即使传入非函数第 2 参数也直接成功，两元素及以上才按官方报第 2 参数 `function expected`。
  - 当前进展：`table.sort` 在 Lua 5.4 兼容模式下会在分区扫描到 pivot 哨兵时拒绝非严格 comparator，例如 `a <= b` / `a >= b`，并报 `invalid order function for sorting`。
  - 当前进展：`table.sort` 在 Lua 5.4 兼容模式下已改用 API get/set 路径读写元素，因此代理表排序会通过 `__index` 读取、通过 `__newindex` 写入。
  - 当前进展：`table.concat` / `insert` / `remove` / `sort` 现在也接受带所需 `__len` / `__index` / `__newindex` 的非 table 代理对象；`table.move` 对源对象只要求 `__index`、对目标对象只要求 `__newindex`，并按官方 `lua_compare(..., LUA_OPEQ)` 判断源/目标是否同一对象。
  - 当前进展：`table.unpack` 不再入口强制 table；默认终点会先触发 length 语义，显式空范围可对 nil/number 直接返回空结果，实际读取时再由普通索引路径报错。
  - 已覆盖：显式 `i/j` 空范围、`table.sort` comparator 错误传播、非严格 comparator 报错，以及官方 5.4.8 对照中允许的 always-true / always-false comparator 结果。
  - 说明：逐字错误文本继续归入“标准库错误消息与边界参数完全对齐”。

- [x] 严格 Lua 5.4 语法表面。
  - 当前状态：Lua 5.4 兼容模式会拒绝 LuaJIT-only 数字字面量扩展；FFI 库本身仍作为 LuaJIT 扩展保留，但不再开放这些非官方 numeric literal 语法。
  - 当前进展：`load("return 0b1010")`、`load("return 1L")`、`load("return 1LL")`、`load("return 1UL")`、`load("return 1ULL")`、`load("return 1uLL")`、`load("return 1i")` 在 Lua 5.4 兼容模式下均报 malformed number。
  - 已覆盖：二进制数字字面量、FFI integer suffix 和 imaginary suffix。

- [x] standalone / 环境变量兼容边界。
  - 当前状态：已完成当前清单中列出的 Lua 5.4 standalone 行为核对和 smoke 覆盖。
  - 当前进展：Lua 5.4 兼容构建的 standalone 已优先读取 `LUA_INIT_5_4`，找不到时再回退 `LUA_INIT`；package 初始化会优先读取 `LUA_PATH_5_4` / `LUA_CPATH_5_4`，再回退旧 `LUA_PATH` / `LUA_CPATH`。
  - 当前进展：Lua 5.4 兼容构建无脚本、仅执行 `-e` 时，`arg[0]` 现在是程序名，`arg[1]` 是 `-e`，`arg[2]` 是命令字符串；有脚本时仍保持 `arg[0]` 为脚本名。
  - 当前进展：Lua 5.4 兼容构建的 `-l g=mod` 已按官方 standalone 行为把 `require(mod)` 的返回值写入 `_G[g]`。
  - 当前进展：Lua 5.4 兼容构建的 `-l mod` 已按官方 standalone 行为把 `require(mod)` 的返回值写入 `_G[mod]`；连续多个 `-l` 的后续模块可以看到前一个模块写入的全局。带版本后缀的 `-l mod-v2` 会按 `LUA_IGMARK` 只写入截断后的 `_G.mod`，不会额外写 `_G["mod-v2"]`。
  - 当前进展：Lua 5.4 兼容构建的 `-i` 交互模式已按官方先尝试把首行作为表达式 `return <line>;` 编译，裸表达式 `10` 会输出结果；如果全局 `print` 被破坏，结果输出阶段会报告 `error calling 'print'`，而不是把裸表达式误报为普通 chunk 语法错误。
  - 当前进展：Lua 5.4 兼容构建的 `-i` 多行 continuation 已按官方保留 incomplete `<eof>` 错误到拼接阶段再移除准确栈位；多行 assignment、注释后的表达式、以及跨行 long string 现在能正确继续读取，不再误输出 `nil` 或报告 `unexpected symbol near <eof>`。
  - 当前进展：Lua 5.4 兼容构建的交互提示符已按官方对非 `nil` `_PROMPT` / `_PROMPT2` 调用 `luaL_tolstring()`；非字符串 prompt 可通过 `__tostring` 生成提示符，不再退回默认 `> ` / `>> `。
  - 当前进展：Lua 5.4 兼容构建的交互 continuation 在 EOF 打断时已按官方输出 `<name> expected near <eof>`，`TK_name` expected 文本不再额外包一层引号；默认 LuaJIT 构建保留旧错误表面。
  - 当前进展：Lua 5.4 兼容构建已支持 standalone `-W`，会在执行 `-e` chunk 或脚本前开启 warning 输出。
  - 当前进展：`-E` 已覆盖忽略 `LUA_INIT_5_4` 和 versioned package path/cpath 环境变量。
  - 当前进展：Lua 5.4 兼容构建的 `-i` 交互启动不再额外打印 LuaJIT 的 `JIT:` 状态行，保留默认 LuaJIT 构建的原有交互输出。
  - 当前进展：`LUA_PATH` / `LUA_CPATH` 中 `;;` 替换默认路径的规则已继续按官方 `main.lua` 收紧；没有 `;;` 的 `";"` 原样保留，`";;"` / `";;b"` / `"a;;"` 替换后不会多出首尾空模板，`"a;b;;c"` 只在默认路径两侧保留用户前后缀需要的分隔符。
  - 当前进展：脚本文件、`-- script` 和 stdin `-` 的 `arg` 表组合已按官方 Lua 5.4.8 对照进入 smoke。
  - 当前进展：新增 `test/lua54_standalone_regress.lua` 并接入 `smoketest-lua54compat`，把官方 `testes/main.lua` 中 Windows 可移植的 standalone 子集从 Makefile 零散命令收拢成正式回归：stdin `-`、UTF-8 BOM 和坏 BOM、紧凑 `-eprint(1)` / `-ea=3`、`-v -e` 继续执行 chunk、`LUA_INIT_5_4` / `LUA_PATH_5_4` / `LUA_CPATH_5_4` 优先级、`-E` 忽略环境、`LUA_PATH` 的 `;;` 默认替换边界、`-l mod` / `-l g=mod` / `-l mod-v2` 全局写入语义、invalid option / missing option argument / `-e a` syntax-error 诊断、`debug.debug()`、交互裸表达式、多行 continuation、long-string continuation、非字符串 prompt 的 `__tostring`、EOF 打断 continuation 的错误文本、30 个 argv、脚本执行前 `arg` 非 table 的错误、非字符串 error object standalone 消息、`warn()` 参数错误后的 warning recovery、`print()` 不依赖全局 `tostring`、首行 `#` 注释、首行 `#` 后接 LuaJIT binary chunk、进程退出时关闭打开的输出文件、close-state finalizer reentry，以及 `os.exit(nil/0/true/1/false, true)` 的退出状态和无输出边界。
  - 当前进展：兼容构建默认 `package.path` / `package.cpath` 已改用 Lua 5.4 风格搜索顺序；Windows 覆盖 executable-local `lua` 目录、`..\\share\\lua\\5.4`、`..\\lib\\lua\\5.4` 和当前目录 fallback，非 Windows 覆盖 `/usr/local/share/lua/5.4`、`/usr/local/lib/lua/5.4` 和当前目录 fallback。
  - 当前进展：Lua 5.4 兼容构建的 standalone `-v` 已按官方输出 `LUA_COPYRIGHT`，版本行包含 `Lua 5.4.8` 和 `PUC-Rio`；`-v -e "print'hello'"` 保持版本行后正常继续执行 chunk。
  - 当前进展：从官方 `testes/main.lua` / `lua.c` 抽出的 invalid option 负向用例已进入 smoke；Lua 5.4 兼容构建会拒绝 `-h`、`---`、`-Ex`、`-vv`、`-iv`，并按官方表面报告 `unrecognized option ...`，缺少参数的 `-e` / `-l` 以及 `-e -v` / `-l -e` 这类后续 argv 仍是选项的场景都会报告 `needs argument`。
  - 当前进展：从官方 `testes/main.lua` 抽出的 `lua -e a` 语法错误边界已进入 smoke；Lua 5.4 兼容构建下，`load("a")` 和 standalone `-e a` 都按官方表面报告 `syntax error near <eof>`，不再泄露旧 assignment parser 的 `'=' expected` 诊断。
  - 当前进展：从官方 `testes/main.lua` 抽出的 `arg` 表破坏用例已进入 smoke；`-e 'arg = 1' -` 这类在执行脚本前把全局 `arg` 改成非 table 的路径会按官方 Lua 5.4 报 `arg is not a table`，而不是静默不给脚本传参。
  - 当前进展：standalone 顶层错误处理已按官方 Lua 5.4 对齐非字符串错误对象；脚本中 `error({})` / `error(false)` / `error(nil)` 会分别报告 `(error object is a table/boolean/nil value)`，带 `__tostring` 且返回字符串的错误对象直接使用该字符串作为消息；官方 `main.lua` 中通过 `debug.getinfo(4).currentline` 检查 message handler 栈形态的 `__tostring` 用例也已进入 smoke。
  - 当前进展：从官方 `testes/main.lua` 抽出的 `warn()` 参数错误边界已进入 smoke；`warn("@on"); pcall(warn, "SHOULD NOT APPEAR", {}); warn("VISIBLE")` 只会输出下一条完整 warning，不会把参数错误前的未完成 warning 片段残留到 stderr。
  - 当前进展：standalone `-W` 已按官方 Lua 5.4 命令行顺序执行；`-e warn -W` 不会输出，`-W -e warn` 会输出，夹在两个 `-e` 中间时只影响后续 chunk。
  - 已覆盖：`LUA_INIT_5_4` 覆盖旧 `LUA_INIT`，`LUA_PATH_5_4` / `LUA_CPATH_5_4` 覆盖旧 path/cpath 环境变量，`-E` 忽略环境变量，`LUA_PATH` / `LUA_CPATH` 的 `;;` 默认路径替换和首尾分隔符边界，无脚本 `-e`、脚本文件、`-- script` 和 stdin `-` 的 `arg` 表形态，BOM / 首行 `#` / binary chunk 入口，`-l mod` / `-l g=mod` / `-l mod-v2` 的全局写入语义，`-W` 按参数顺序开启 warning，`warn()` 参数错误不会残留未完成 warning，`-i` 不输出额外 `JIT:` 状态行且裸表达式按结果打印，交互多行 continuation、long string continuation、EOF 打断 continuation 的 `<name>` 错误文本和非字符串 prompt 的 `__tostring`，`debug.debug()` 提示符，`-v` 官方版权版本行，默认 `package.path` / `package.cpath` 的 Lua 5.4 搜索目录形态，官方 standalone invalid option / missing-argument（含 `-e -v` / `-l -e`）/ `-e a` syntax-error 诊断，脚本执行前 `arg` 非 table 的错误，standalone 非字符串 error object 的类型化消息，以及 `os.exit(..., true)` 关闭状态和退出码边界。

- [ ] JIT/trace 对 Lua 5.4 新语义的记录。
  - 当前状态：多个新语法通过 helper 调用实现，语义优先于 JIT 性能。
  - 当前进展：已补 JIT smoke，在当前 PC 兼容构建中开启 JIT、降低 hotloop 后运行包含 `//`、位运算和局部 `_ENV` 的热循环，并用 `jit.util.traceinfo()` 确认产生 trace。
  - 当前进展：Lua 5.4 dual-number recorder 会保留 runtime float TValue 常量子类型，避免 `0.0` 等 integral float 常量在热循环中被折成 KINT；`i + 0.0` 的结果类型已进入 smoke。
  - 当前进展：Lua 5.4 `math.random` 已新增 xoshiro256** 专用 recorder 子集：无参、`0`、常量整数区间，以及当前运行值可精确转为当前 `lua_Integer` 的动态 integer/number/string 区间，会在 trace 内记录 side-effect helper call，不再退回 LuaJIT 旧 PRNG recorder；`test/lua54_jit_regress.lua` 已固定同一 seed 下 JIT on 热循环序列必须等于 JIT off 解释器序列，并覆盖 `math.random(10, 5)` 热错误路径仍报 `interval is empty`。更复杂动态转换仍退回解释器，后续随通用 Lua 5.4 string-to-number recorder 再扩展。
  - 当前进展：已补 JIT regression，覆盖 `math.min` 热循环下仍走 Lua 5.4 普通 `<` 比较和 `__lt` 元方法，元方法替换后会失效/重录；`math.abs()` 的 boxed int64 参数热路径会记录 `IR_NEG` 并保持 integer 结果；`math.tointeger()` 热循环保持字符串整数转换和 fraction number 返回 `nil`；`math.ult()` 热循环已覆盖 boxed int64 和 string integer 的无符号比较 recorder。
  - 当前进展：Lua 5.4 `tonumber()` 已恢复 recorder 子集：no-base number 输入直接保留 integer/float subtype，动态 float 字符串通过 `IR_STRTO` 记录，动态 integer 字符串通过 guarded int helper 记录，混合 KSTR 常量字符串中的 64-bit integer 会生成 boxed int64 结果而不被误读为 number 常量，普通 invalid 字符串通过分类 guard 返回 `nil`，逗号常量通过 locale-sensitive side-effect scanner guard 返回当前 numeric locale 下的 `nil` 或 number，`inf` / `nan` / `0b` 等 LuaJIT 扫描器扩展数字字符串通过分类 guard 返回 `nil`；`tonumber(str, base)` 的动态字符串已通过 explicit-base scanner helper 记录成功/失败分支，动态 base 会按 Lua 5.4 `luaL_checkinteger()` 表面 guard number/string 的整数表示并 guard `2..36` 范围。
  - 当前进展：已补 JIT smoke，覆盖字符串 metatable 的 `__add` 优先于字符串数字转换，并在热循环中产生 trace。
  - 当前进展：已补 JIT smoke，覆盖 Lua 5.4 generic for 第 4 个 closing value 采用 `false` 时，`next` 热循环仍可产生 trace。
  - 当前进展：已补 JIT regression，覆盖 Lua 5.4 `ipairs()` 在热循环记录 trace 后仍通过普通索引访问触发 `__index`，避免 recorder 回退成旧 LuaJIT 原始数组迭代语义。
  - 当前进展：已补 JIT smoke，覆盖真实 `_ENV` upvalue 经 `debug.setupvalue` 替换为自定义 table 后，隐式全局访问仍可在热循环中执行并产生 trace。
  - 当前进展：Lua 5.4 的高索引 upvalue 已可记录 trace；`IR_UREFO` / `IR_UREFC` 的 literal 编码现在支持 0..199 upvalue index，并保留 7 bit alias hash 给跨函数 upvalue disambiguation。JIT 回归覆盖 150 upvalue 闭包读写第 150 个 mutable upvalue，性能 smoke 同步覆盖 JIT on/off。
  - 当前进展：Lua 5.4 locale 字符串有序比较已可记录 trace；recorder 通过 side-effect `IRCALL_lj_str_cmp_locale` 在运行期调用 `lj_str_cmp_locale()`，用当前 C collate locale 和内嵌 NUL 分段语义执行 `<` / `<=` / `>` / `>=`，并避免旧 bytewise `lj_str_cmp()` trace。JIT 回归覆盖包含内嵌 NUL 的字符串有序比较，性能 smoke 同步覆盖 JIT on/off。
  - 当前进展：Lua 5.4 `math.random()` / `math.random(0)` / 常量整数区间 `math.random(m, n)` / 动态 integer、可精确整数 number 或可转整数 string 区间已可记录 trace；recorder 通过 side-effect `IRCALL_lj_prng_*_random54` 调用 xoshiro256** helper，并在 helper 前 guard `up == 0`、`up >= 1`、`low <= up`，保持 deterministic seed、full integer sample、字符串整数参数和无偏区间投影语义。JIT 回归要求 trace 内出现 `IR_CALLS`，性能 smoke 同步覆盖 JIT on/off。
  - 当前进展：Lua 5.4 `tonumber()` no-base recorder 已可记录动态 float/integer 字符串、普通 invalid 字符串、逗号 locale 常量和稳定拒绝的 LuaJIT-only 数字扩展；`tonumber(str, base)` recorder 已可记录动态字符串 + 常量/动态显式 base 的 integer scanner 成功/失败路径，动态 base 覆盖 number、decimal string 和 hex string 可转整数路径；JIT 回归分别要求 no-base float 字符串路径出现 `IR_STRTO`、no-base invalid 路径单条 trace 内出现 `IR_CALLN`、逗号 locale 常量路径出现 `IR_CALLS`、explicit-base helper 路径出现 `IR_CALLN`，性能 smoke 同步覆盖 JIT on/off。
  - 当前进展：已补 JIT smoke，覆盖 Lua 5.4 integer numeric for 在接近当前 32 位边界时不能被错误记录成 float 控制变量；这类可能溢出的边界循环会回退解释器以保持语义。
  - 当前进展：已补低 `hotloop` JIT smoke，覆盖 `jit.opt.start("hotloop=1", "hotexit=1")` 下 init/step 为 float 的 numeric for 仍保持 float 控制变量；性能 smoke 另行固定多档显式 `jit.opt` profile，包含 opt level 3 和 opt level 0，避免 optimizer 配置变化掩盖真实性能回退。
  - 当前进展：已补 boxed int64 numeric for recorder 子集，覆盖正/负步长 integer-only 热循环会记录 raw `IRT_I64` add/compare 并恢复 integer TValue，且 x64/x86 `BC_JFORI` trace 重入会重新读取 trace id；JIT 回归要求 trace 内出现 boxed int64 分配路径，并覆盖 int32 控制变量跨入 boxed int64、可取整 float limit 归一后的 trace 边界，性能 smoke 同步覆盖 JIT on/off。
  - 当前进展：`jit._lua54_*` helper 字段访问已处理大 chunk 常量表超过 255 时的 `TGETS` 索引截断问题，超出 8 位范围时改用 `KSTR + TGETV`。
  - 当前进展：Lua 5.4 兼容模式仍隐藏启动全局 `bit`，但已把 `bit` 作为显式 preload 模块保留，避免 `jit.dump` / `-jdump` 因内部 `require("bit")` 失败。
  - 当前进展：`test/lua54_jit_regress.lua` 已继续覆盖 Lua 5.4 helper 热路径：`getmetatable()` 保留 `__metatable` 保护值并在保护值变化后失效/重录，未受保护 `setmetatable()` 可记录 trace，受保护 `setmetatable()` 错误路径不生成错误 trace，`rawlen()` / `rawget()` / `rawset()` / `rawequal()` 热循环会绕过 `__len` / `__index` / `__newindex` / `__eq`；运行期长字符串 equality 会按 bytewise 语义记录 trace，table key load、existing-slot store 和 new-key store 也会按 bytewise key 语义记录 trace，并覆盖不同 GC 对象但同内容的长字符串比较、命中/写入同一 table slot，GC 值写入必须保留 `IR_TBAR`；`tostring()` 热循环保留 primitive、`__tostring` 字符串返回、非字符串返回错误、默认对象名 `__name`，Lua 5.4 recorder 已避免把 `__tostring` 当 LuaJIT 5.1 tailcall 跳过返回值类型检查；`type()` / `select("#", ...)` / `select("2", ...)` / `select(1.2, ...)` 错误路径 / `assert(true, ...)` 可记录 trace并保持 Lua 5.4 语义，`assert(false, table)` 错误路径保持非字符串错误对象且不生成错误 trace；`pcall()` / `xpcall()` 热循环覆盖成功调用、多参数传递、字符串错误、非字符串错误对象和 message handler 返回对象，`error(table, level)` 保留非字符串错误对象；`coroutine.running()` / `status()` / `isyieldable(co)`、`resume()` 多返回和 nil 洞、`coroutine.close()` 成功关闭 suspended coroutine 且只返回单个 `true`、关闭 `<close>` local 抛出的非字符串错误对象，以及 `coroutine.wrap()` 错误路径先关闭 active close local 都已进入热循环 trace 回归；`collectgarbage()` 的 `generational` / `incremental` mode shim、`isrunning`、`step` 字符串整数参数和 fraction step 错误也已进入热循环 trace 回归；`require()` 的 loaded fast path、nil-returning preload loader、loader error 不污染 `package.loaded`，以及 `package.searchpath()` 空模板错误片段也已进入热循环 trace 回归；`string.format("%p")` / `%q` / `%a` / `%A` / `%s` number 可记录 trace 并保持 `(null)`、`1e9999`、`(0/0)`、hex-float、integer/float subtype 文本和宽度格式语义；带宽度的 `%s` 遇到内嵌 NUL、`%#a` modifier 错误和 invalid 格式项缺实参的 no-value 优先级会退出回解释器并保持 Lua 5.4 错误文本；`string.byte()` / `string.sub()` 热循环接受字符串整数参数并产生 trace，`string.sub(..., 1.2)` 和 `string.char(256)` 热错误路径仍保留 Lua 5.4 整数表示和值域错误；`string.find(..., init, true)` 热循环接受字符串整数 init 并产生 trace，pattern capture 热循环记录 trace 后仍保留捕获结果，fraction init 热错误仍保留整数表示错误；`string.gmatch(..., init)` 热循环接受字符串整数 init 并保持空匹配推进，`string.gsub("a b cd", " *", "-")` 热循环保持 Lua 5.4 空匹配 replacement 计数；`string.match(..., init)` 热循环接受字符串整数 init，`string.len` / `string.reverse` / `string.lower` / `string.upper` 热循环保留当前 Lua 5.4 helper 结果；`string.rep(s, n, sep)` 可记录 trace 并保留 separator 语义，超大 repeat 热错误路径保持 `resulting string too large` 且不生成旧 helper trace；`string.pack(">i2")` / `string.unpack(">i2")` 的整数读写热循环会保持格式语义，`pack("z", "a\0b")` 和 `unpack("s1", "")` 的热错误路径仍保留 Lua 5.4 错误文本；`utf8.char()` / `codepoint()` / `codes()` / `len()` / `offset()` 在扩展码点和 lax 模式热循环中保持当前 Lua 5.4 结果，`utf8.charpattern` 的 `match` / `gmatch` 热循环会保留扩展码点分段，`utf8.codes()` 严格模式非法字节迭代热路径保留 `invalid UTF-8 code` 错误，`utf8.char(0x80000000)`、`utf8.offset(..., 1.2)` 和非法 UTF-8 `utf8.len()` 热路径仍保留值域、整数表示和 bad-position 语义；`table.concat` / `table.move` / `table.unpack` / `table.insert` / `table.remove` / `table.sort` 在 trace 后仍会走 Lua 5.4 的 `__index` / `__newindex` / `__len` / `__lt` 元方法和 API get/set 语义，并在元方法替换后失效/重录，避免退回旧 raw fast path；`table.sort` 还覆盖了非法比较函数的热错误路径。`test/lua54_perf.lua` 也已把 base/raw helper、base value helper、protected-call helper、coroutine helper、GC mode helper、package helper、`string.find` / `string.sub` / `string.rep` / `string.format("%q")` / `string.format("%a")` / `string.format("%s", number)` / `string.gsub` / `string.gmatch` / `string.match` / `string.len` / `string.reverse` / `string.lower` / `string.upper` 混合 helper 负载、运行期长字符串 bytewise equality、table lookup、existing/new store 和 GC value store、`utf8` 扩展码点/lax helper、严格错误和 `utf8.charpattern` 负载，以及小表 `table.sort` / `__lt` / proxy get/set 排序负载，纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
  - 当前进展：`test/lua54_jit_regress.lua` 已继续覆盖 number/pack helper 热路径：`math.type()` 区分 integer/float/非 number，`tonumber(str, base)` 在显式 base 下保持 Lua 5.4 integer-base scanner、拒绝 `1.0` 和内嵌 NUL并保留 base 16/34 差异，`string.packsize()` 固定格式和 `z` / `c` 错误格式在热循环下保持当前 Lua 5.4 语义，`string.pack("c", ...)` 缺 size 的热错误路径也固定为格式解析错误而非 `bad argument` 包装，`string.unpack(..., 0)` 的成功读数与空串短数据错误也进入热循环；同一 JIT 回归还覆盖 `tostring(1.0)`、数字拼接和 `table.concat` 数字元素在 trace 下保留 integer/float subtype 文本；`test/lua54_perf.lua` 同步新增 `number_pack_helpers` 和 `number_string_helpers`，纳入 JIT on 三档 profile 和 JIT off 性能/内存 smoke。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 `package.searchpath(name, "")` 空 path 错误片段纳入 JIT on/off 覆盖，固定兼容实现不能因为 path 为空就返回空错误串；同批新增 `debug.upvalueid(fn, missing)`、`debug.setupvalue(fn, missing, v)` / 标准 C closure hidden upvalue 和 `debug.gethook()` 无 hook 热路径，固定缺失 upvalue / hidden C upvalue / 无 hook 在 trace 下仍保持 Lua 5.4 的单 `nil` 或 0 返回值表面，不能退化成错误数量或额外 `mask/count`。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 `load(text, ..., "b")` 和 `load(binary, ..., "t")` 的 mode 拒绝错误纳入 JIT on/off 覆盖，固定热路径仍保留 Lua 5.4 的 text/binary chunk 分类和传入 mode 文本。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把标准库 C 闭包内部 upvalue 隐藏纳入 JIT on/off 覆盖，固定 `debug.getupvalue(print/pairs/ipairs, 1)` 在热路径下仍为 0 个返回值，`debug.upvalueid(print, 1)` 仍为单个 `nil`。
  - 当前进展：`test/lua54_jit_regress.lua` 已继续覆盖 os helper 热路径：`os.date()` 保留格式串内嵌 NUL 和稳定本地日期输出，`os.difftime()`、`os.time(table)` 字段规范化回写、`os.getenv()`、`os.setlocale(nil, "time")` 以及 `os.time()` / `os.date()` 参数错误均进入热循环 trace 回归；`test/lua54_perf.lua` 同步新增 `os_helpers`，纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
  - 当前进展：`test/lua54_jit_regress.lua` 已继续覆盖 IO helper 热路径：二进制临时文件下的 `file:seek()`、`file:read("n")`、定长 read、`file:setvbuf("no")`、字符串整数 seek offset，以及 fraction number / fraction string / 非数字字符串 read/write/seek/setvbuf 参数错误均进入热循环 trace 回归；`io.lines(fname,{})` iterator 的 `bad argument #2 to 'it'` 错误边界、局部 iterator alias 的 tail-position 调用点保留、简单 local alias / upvalue alias 后 `bad argument #2 to 'it2'` / `'it3'` 的调用点名传播，以及静态 global alias 后 `bad argument #2 to 'lua54_jit_io_global_it'` 也已进入同一热路径；`test/lua54_perf.lua` 同步新增 `io_helpers`，纳入固定 `jit.opt` profile 和 JIT off 的时间/内存 smoke。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把静态 table-field table alias 链纳入 JIT on/off 覆盖，固定 `holder.inner = alias_table; holder.inner.k = math.abs` 后，`return alias_table.k(true)` 和 `return holder.inner.k(true)` 都保留字段名 `k`，且同一字段重写成非原 table 后会清除旧 alias。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 table 构造器字段 alias 纳入 JIT on/off 覆盖，固定 `local holder = { inner = alias_table }; holder.inner.l = math.abs` 后，`return alias_table.l(true)` 和 `return holder.inner.l(true)` 都保留字段名 `l`。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 table-field alias 作为 RHS 的静态来源传播纳入 JIT on/off 覆盖，固定 `base.inner = alias_table` 后，`local holder = { alias = base.inner }` 和 `holder.alias = base.inner` 两种写法继续让 `holder.alias.m/n = math.abs` 保留字段名。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 `coroutine.status(nil)` / `coroutine.isyieldable(nil)` / `coroutine.create(nil)` / `coroutine.wrap(nil)` 的 tail-position 源码调用点名纳入 JIT on/off 覆盖，固定这类 wrapper 错误不能退回 `coroutine.xxx` fallback 名。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把字符串库方法语法 tail-position 诊断纳入 JIT on/off 覆盖，固定 `s:byte({})` / `s:find({})` 必须保留短方法名 `byte` / `find` 和公开参数 `#1`。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 `pairs(nil)` 返回的 iterator 在 direct/local/upvalue/global/table-field alias 后的 tail-position 调用点名纳入 JIT on/off 覆盖，固定这类后续 `next()` 参数错误不能退回 `next` fallback 名。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 table 库非 table 代理对象纳入热路径覆盖，固定 `table.concat` / `insert` / `remove` / `sort` / `unpack` 和无 `__len` 的 `table.move` 不能退回旧 raw table 假设。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 `table.pack()` 的 `n` 字段和 nil 洞纳入 JIT on/off 覆盖，固定热路径不能丢失 Lua 5.4 多返回打包语义。
  - 当前进展：`test/lua54_jit_regress.lua` / `test/lua54_perf.lua` 已把 `debug.setcstacklimit()` 纳入 JIT on/off 覆盖，固定 Lua 5.4 debug 库公开 C stack limit shim 的热路径返回旧 limit 语义。
  - 剩余边界：`io.lines` iterator 或标准库 C 函数经过当前解析路径可静态定位的 table 字段 alias、global table alias、table-field table alias 链、静态 table 构造器字段 alias 和 RHS table-field alias 后再 tail-position 调用时已可保留字段名；复杂 table 重赋值、构造器内动态 key、跨 chunk/module 的运行期全局赋值或其它动态传播后再 tail-position 调用时，调用点名传播仍需归入更通用的 debug frame metadata / notail 标记传播批次处理。
  - 需要补测试：继续扩展到更多 Lua 5.4 helper 路径，并在 unsupported trace 路径上补退出或 recorder。

## 当前验证结果

- `cmd /c build.bat lua54` 和 `cmd /c build.bat default` 已通过，覆盖本轮新增 `lua_setmetatable()` 非 table metatable、默认 ABI `lua_setfenv()` 非 table env、`lua_replace(LUA_GLOBALSINDEX/LUA_ENVIRONINDEX)` 非 table value 的 release C API 边界。
- `cmd /c build.bat lua54` 和 `cmd /c build.bat default` 已通过，覆盖本轮新增 `lua_yieldk()` continuation 返回负数/超过当前栈结果数量的 release C API 边界；Lua 5.4 continuation smoke 和默认 C API smoke 均保持通过。
- `cmd /c build.bat lua54` 和 `cmd /c build.bat default` 已通过，覆盖本轮新增 `lua_pushvalue()` / `lua_copy()` / `lua_remove()` / `lua_insert()` / `lua_rotate()` 和 `lua_pcall()` message handler 无效栈索引 C API 边界；同时确认 `table.unpack()` 缺参仍保留 Lua 5.4 的 nil 运行期错误文本。
- `cmd /c build.bat lua54` 和 `cmd /c build.bat default` 已通过，覆盖本轮新增 `lua_compare()` 非法 op、`lua_next()` 缺 key/非 table、`lua_rawget*()` 缺 key/非 table 和 `lua_rawset*()` 非 table C API 边界；Lua 5.4 兼容构建、官方矩阵、C API smoke 和默认 ABI smoke 均保持通过。
- `cmd /c build.bat test` 已通过，覆盖默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、x64 非 GC64 Lua 5.4 JIT smoke、官方 Lua 5.4.8 可执行矩阵、VM 后端静态/DynASM 门禁，以及 Lua 5.4 perf/memory smoke；本轮同时固定 Lua 5.4 专用 bitwise/idiv metamethod bad-call smoke 必须通过 `load()` 字符串承载，避免默认构建在执行开头兼容性检查前解析失败。
- `cmd /c build.bat default` 已通过，覆盖默认 LuaJIT smoke、默认 C++ `lua.hpp` gate，以及新增默认 C API 旧 ABI 覆盖。
- `cmd /c build.bat smoke54` 已通过，确认 Lua 5.4 IO mode smoke 不再留下 `lua54_valid_open_mode.tmp`，且 `make clean` 会兜底清理旧版本残留的 Lua54 IO 临时文件。
- `cmd /c build.bat lua54compat53` 已通过，覆盖 `LUA_COMPAT_5_3` runtime 构建下的 deprecated math library 兼容入口和 `LUA_COMPAT_LT_LE` 的 `__le`/`__lt` fallback；随后 `cmd /c build.bat lua54` 已通过，确认默认 Lua 5.4 compat 仍隐藏这些旧入口、禁用旧比较 fallback，并通过 C API/官方矩阵。
- `cmd /c build.bat lua54` 已通过，覆盖新增 `LUA_COMPAT_5_3` 外部头兼容 smoke、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 矩阵和 C API 运行 smoke。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过；`build.bat` 默认识别 `H:\p4\gl_home_u4\pristine\ruby\msys64`，`make` 并发保持当前物理核心数/2（本机 `-j8`），并默认填充 `LUA54_SRC_DIR` / `LUA54_TESTES_DIR`；覆盖 Windows/UCRT `os.tmpname()` 回归、官方 Lua 5.4.8 矩阵和 Lua 5.4 perf/memory smoke。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 Lua 5.4 `tonumber()` no-base、no-base 普通 invalid 动态字符串、逗号 locale 常量、`tonumber(str, const_base)` 动态字符串以及 `tonumber(str, dynamic_base)` JIT recorder 回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵、VM 后端静态/DynASM 门禁，以及 Lua 5.4 perf/memory smoke；`.\src\luajit.exe test\lua54_perf.lua jit_on` / `jit_off` 也已单独通过，`number_pack_helpers` 负载覆盖 no-base `tonumber()` float/integer/reject/普通 invalid/逗号常量路径、explicit-base 动态字符串成功/失败路径、动态 base 的 number/string 整数转换路径和 JIT on 三档显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 以及 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 Lua 5.4 `math.random()` / `math.random(0)` / 常量整数区间 / 动态整数区间 / 字符串整数区间 JIT recorder 回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵、VM 后端静态/DynASM 门禁，以及 Lua 5.4 perf/memory smoke；`.\src\luajit.exe test\lua54_perf.lua jit_on` / `jit_off` 也已单独通过，`random_helpers` 负载覆盖 JIT on 三档显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 Lua 5.4 locale 字符串 `<` / `<=` / `>` / `>=` JIT 记录回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵、VM 后端静态/DynASM 门禁，以及 Lua 5.4 perf/memory smoke；`.\src\luajit.exe test\lua54_perf.lua jit_on` / `jit_off` 也已单独通过，新增 `string_order_helpers` 负载覆盖 JIT on 三档显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 Lua 5.4 150 upvalue 闭包高索引 mutable upvalue JIT 记录回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵、VM 后端静态/DynASM 门禁，以及 Lua 5.4 perf/memory smoke；perf 段新增 `many_upvalue_helpers`，覆盖 JIT on 三档显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 已通过，覆盖本轮新增 generic long-string equality JIT 语义回归、Lua 5.4 compat smoke、官方 Lua 5.4.8 可执行矩阵和 VM 后端静态/DynASM 门禁；`.\src\luajit.exe test\lua54_perf.lua jit_on` / `jit_off` 已通过，新增 `long_string_equality` 负载覆盖 JIT on 三档显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增字符串库方法语法 `s:byte({})` / `s:find({})` tail-position 参数错误函数名/公开参数编号回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 coroutine `status` / `isyieldable` / `create` / `wrap` tail-position 参数错误函数名回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 table-field alias 作为 RHS 时的静态来源传播回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增标准库函数经静态 table 构造器字段 alias 后的 tail-position 调用点名回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增标准库函数经静态 table-field table alias 链后的 tail-position 调用点名回归、字段重写后旧 alias 清除探针、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增标准库函数经静态 global table alias 后的 tail-position 调用点名回归、`KSTR + TGETV/TSETV` 大常量表字段解析边界、动态 key 不被本轮静态规则吸收的探针、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增标准库函数经静态 table 字段 alias 后的 tail-position 调用点名回归、普通未标记 table 字段继续保持 `CALLT` 的探针、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 Windows `os.date()` `%c` / `%#` 格式和 invalid conversion 错误名回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 `setmetatable({})` 缺第 2 参数和显式 nil 清除 metatable 的区分、direct/source/alias 错误名回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 `xpcall()` 缺 message handler 的 no-value / nil 区分、direct/source/alias 错误名回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 和 `cmd /c build.bat test` 已通过，覆盖本轮新增 base/global 公开入口 tail-position 调用点与局部 alias 错误名回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off。
- `cmd /c build.bat smoke54` 已通过，覆盖新增 `test/lua54_standalone_regress.lua`，固定官方 `main.lua` 中 Windows 可移植的 standalone env/path/module/stdin/BOM/debug/interactive/argv/error/warn/print/exit/open-file-close 边界，以及 `os.exit(true, true)` 会按 LIFO 关闭 main chunk `<close>` locals；并继续通过 Lua 5.4 compat smoke、官方 Lua 5.4.8 可执行矩阵和 VM 后端静态/DynASM 门禁。
- `cmd /c build.bat test` 已通过，覆盖本轮新增 standalone `-e -v` / `-l -e` missing-argument 诊断，以及既有 standalone `-W` 参数顺序语义、`-l mod` / `-l g=mod` / `-l mod-v2` 全局写入、`-v` / invalid option / missing-argument / `-e a` syntax-error / `LUA_PATH` 的 `;;` 默认路径替换 / 交互裸表达式与 `print` 错误 / `arg` 非 table / 非字符串 error object / `warn()` 参数错误不残留 warning continuation 回归、默认构建 smoke/C API/C++ `lua.hpp` gate、Lua 5.4 compat smoke/C API/C++ `lua.hpp` gate、官方 Lua 5.4.8 可执行矩阵，以及 Lua 5.4 perf/memory smoke；perf 段包含显式 `jit.opt` profile `3,hotloop=3,hotexit=2,instunroll=4,loopunroll=4`、`3,hotloop=56,hotexit=10`、`0,hotloop=3,hotexit=2` 和 JIT off，并覆盖 `pairs` / `__pairs` / base/raw helper / base value helper / protected-call helper / coroutine helper / GC mode helper / package helper / os helper / number/pack helper / `//` / `%` / string helper / utf8 helper / table.sort helper / hook churn。
- `cmd /c build.bat lua54nogc64` 已通过，覆盖 x64 非 GC64 Lua 5.4 compat 构建、完整 Lua 5.4 smoke、Lua 5.4/JIT 标志探针，以及 `test/lua54_jit_regress.lua` 的长字符串 table-key load/store/equality 记录路径；该目标固定 `make` 同样走 `build.bat` 的半核并发策略。
- 本轮 VM 后端 DynASM 验证：`test/lua54_vm_backend_dynasm.lua` 已纳入 `smoketest-lua54compat`，自动生成并清理 x86 dualnum、ARM hard/soft、MIPS32 LE hard-float、MIPS32 BE soft-float、MIPS64 LE hard-float、MIPS64 BE soft-float、PPC hard-float、PPC soft-float、PPC GPR64/TOC 的临时 `buildvm_arch`，确认这些非当前主机模板的 `<close>` / Lua 5.4 VM 接线语法不过度依赖手工命令；该门禁不替代真实 artifact/runtime smoke。
- `cmd /c build.bat platformpc` 已通过，覆盖 PC x64 default 与 Lua 5.4 compat 双构建、AMD64 PE header 检查和 smoke，并确认平台矩阵内部 `make` 调用使用本机半核并发 `-j8`；本轮 `cmd /c build.bat platformprobe` 已通过当前主机探针，iOS 因 Windows 主机限制 SKIP，Emscripten 因当前主机未找到 `emcc` 输出 SKIP。
- 官方 Lua 5.4.8 `testes/gc.lua` 在 JIT 默认开启和 `-joff` 下均已通过；仓库新增 `test/lua54_gc_regress.lua` 固化本轮 table finalizer、弱 `kv` 长字符串、finalizer 内 `collectgarbage`、弱 metatable `__gc` 和 finalizer metatable 弱表清理回归。
- 官方 Lua 5.4.8 未修改 `testes/tpack.lua` 已通过；仓库新增 `test/lua54_tpack_regress.lua` 固化 1..16 字节整数格式、`J`、`X` next option、`c` size、格式 size 溢出、`packsize` 总长度和 `unpack` 负初始位置。
- 官方 Lua 5.4.8 `testes/files.lua` 已通过。
- 官方 Lua 5.4.8 当前可执行矩阵已接入 `test/lua54_official_matrix.lua` / `build.bat official54` 并通过：`literals.lua`、`strings.lua`、`pm.lua`、`math.lua`、`bitwise.lua`、`bwcoercion.lua`、`gc.lua`、`gengc.lua`、`tracegc.lua`、`tpack.lua`、`closure.lua`、`cstack.lua`、`constructs.lua`、`goto.lua`、`db.lua`、`nextvar.lua`、`utf8.lua`、`vararg.lua`、`sort.lua`、`errors.lua`、`coroutine.lua`、`code.lua`、`api.lua`、`calls.lua pre-binary`、`files.lua`、`attrib.lua`、`locals.lua`、`events.lua`、`verybig.lua`、`big.lua`。Windows shell 非移植 `main.lua` 和已文档化的 LuaJIT bytecode / 官方 Lua 5.4 binary chunk header 差异中仅 `calls.lua` 的 binary chunk header block 不计入绿色矩阵。
- 官方 Lua 5.4.8 `testes/cstack.lua` 已完整通过；仓库新增 `test/lua54_cstack_regress.lua` 固化 recursive `string.gsub` replacement、table replacement 和链式 `coroutine.close` 的 `C stack overflow` 行为。
- 当前仍需单独归因的官方核心矩阵观察项：`closure.lua` 已在 JIT 默认和 `-joff` 下通过；`calls.lua` 的 function/call、tail call、`load`/`string.dump`、upvalue 和 200 upvalue dump/load 段已进入官方矩阵，剩余边界只保留官方 Lua 5.4 binary chunk header 断言；该断言与本 TODO 中已文档化的“compat 构建只支持 LuaJIT bytecode，不支持官方 Lua 5.4 binary chunk 格式”边界一致。iOS/Emscripten 构建矩阵仍未完成。

## 已确认不列入当前 TODO 的已实现项

- `_VERSION == "Lua 5.4"`、`jit.lua54compat == true`。
- Lua 5.4 模式隐藏旧 Lua 5.1/LuaJIT API：`getfenv`、`setfenv`、`module`、`newproxy`、`loadstring`、全局 `unpack`、`bit` 等。
- `rawlen`、`table.pack`、`table.unpack`、`table.move`、`coroutine.isyieldable([co])` 已可见并覆盖可选 thread 参数。
- `load(..., env)` 和 `loadfile(..., env)` 已能让 source / LuaJIT binary chunk 使用传入环境。
- `pairs` 已支持 `__pairs`，包括 `__pairs` 在 coroutine 中 yield；`ipairs` 已按 Lua 5.4 使用普通索引访问，不走旧 `__ipairs`。
- `package.searchers`、`require` loader data、nil-returning loader、loader error 不污染 `package.loaded`、`utf8` 基础库、`warn`、`math.randomseed(x, y)`、`math.random` 的基础 Lua 5.4 行为已进入 smoke 覆盖。
